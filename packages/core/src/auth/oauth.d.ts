import type { AppAction, AutomationAction, BaseAction, FieldAction, RecordAction, TableAction, UserAction, ViewAction } from './actions';
export declare const OAUTH_ACTIONS: (AppAction | BaseAction | TableAction | ViewAction | FieldAction | RecordAction | UserAction | AutomationAction)[];
