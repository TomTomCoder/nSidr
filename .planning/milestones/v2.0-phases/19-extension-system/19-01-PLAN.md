---
plan_id: 19-01
phase: 19-extension-system
wave: 1
type: spike
depends_on: []
requirements: [EXT-01]
autonomous: true
files_modified:
  - .planning/phases/19-extension-system/19-SPIKE.md
must_haves:
  truths:
    - "19-SPIKE.md exists with an AGPL go/no-go for OpenClaw + ClawHub"
    - "19-SPIKE.md contains a port-vs-reimplement recommendation (default: native per D-01/D-02)"
    - "19-SPIKE.md identifies which pieces to adopt: MCP contract yes; OpenClaw plugin-SDK ref-only unless AGPL clears; ClawHub bridge deferred"
  artifacts:
    - path: .planning/phases/19-extension-system/19-SPIKE.md
      provides: "EXT-01 decision document"
      contains: "AGPL compatibility verdict + adopt-list"
  key_links: []
---

<objective>
EXT-01 license/reuse spike. Assess OpenClaw (github.com/openclaw/openclaw) + ClawHub (clawhub.ai)
for AGPL compatibility. Produce `19-SPIKE.md` with: (a) AGPL go/no-go, (b) port-vs-reimplement
recommendation (default = native MCP manifest per D-01/D-02), (c) adopt-list (MCP standard = yes;
OpenClaw plugin-SDK = ref only unless cleared; ClawHub marketplace protocol = assess for later bridge).

This spike GATES the ClawHub bridge only — native build (19-02+) proceeds regardless.
Output: .planning/phases/19-extension-system/19-SPIKE.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-extension-system/19-CONTEXT.md
@.planning/research/SUMMARY.md
</context>

<tasks>

## Task 1 — Write 19-SPIKE.md

Produce `.planning/phases/19-extension-system/19-SPIKE.md` with:

### Section 1 — AGPL Compatibility Analysis
- **OpenClaw**: github.com/openclaw/openclaw. Determine: is it AGPL-3.0? If so, does linking/using its
  SDK in a commercial SaaS trigger the "network use = distribution" clause? Verdict: CLEARED or BLOCKED.
- **ClawHub** (clawhub.ai): their marketplace/install protocol is proprietary. Assess whether integrating
  their client SDK (if any) creates license coupling. Verdict: ASSESS / BRIDGE-OK / BLOCKED.

### Section 2 — Port-vs-Reimplement Recommendation
Based on D-01/D-02 (extension = MCP-capable Plugin; native MCP manifest as contract):
- **Recommendation:** REIMPLEMENT NATIVE (default unless spike clears AGPL)
- Rationale: MCP standard already in place (Phase 17). No OpenClaw SDK needed. No AGPL risk.
- If AGPL clears: which OpenClaw pieces could be ported? (Manifest format? Registry protocol?)

### Section 3 — Adopt List
| Item | Decision | Rationale |
|------|----------|-----------|
| MCP protocol as tool contract | YES | Already in place (Phase 17) |
| OpenClaw plugin-SDK | REF ONLY | AGPL risk; native manifest is simpler |
| ClawHub marketplace browse/install | DEFERRED | Assess for later bridge; not blocking Wave 2+ |
| MCP manifest structure (field names) | ADOPT | Standard MCP tool declaration; safe |

### Section 4 — ClawHub Bridge Gate
State clearly: "ClawHub marketplace bridge is DEFERRED. Phase 19 Wave 2+ proceeds with native
registry + install-by-URL. Bridge re-evaluated after ClawHub publishes a stable API + license."

### Section 5 — Verdict Summary
Single-sentence go/no-go for each: OpenClaw SDK use, ClawHub bridge, native MCP manifest direction.

Commit message: `docs(19-01): EXT-01 license/reuse spike — 19-SPIKE.md`

</tasks>

<acceptance_criteria>
- [ ] `19-SPIKE.md` file exists at the path above
- [ ] Contains all 5 sections
- [ ] AGPL verdict is explicit (BLOCKED / CLEARED) for both OpenClaw and ClawHub
- [ ] Adopt-list table present
- [ ] ClawHub bridge explicitly DEFERRED
- [ ] Native MCP manifest direction CONFIRMED
</acceptance_criteria>
