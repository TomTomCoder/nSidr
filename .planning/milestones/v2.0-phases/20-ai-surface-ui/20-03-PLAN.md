---
plan_id: 20-03
phase: 20-ai-surface-ui
wave: 2
type: execute
depends_on: [20-01]
requirements: [UI-01]
autonomous: false
files_modified:
  - apps/nextjs-app/src/features/app/blocks/admin/setting/components/ai-config/InlineProviderKeyList.tsx
must_haves:
  truths:
    - "InlineProviderKeyList renders a list of providers with masked key, test button, status, remove"
    - "Add-provider inline (no wizard)"
    - "Reuses LlmproviderManage or LlmProviderForm internals"
    - "Typechecks without errors"
  artifacts:
    - path: apps/nextjs-app/src/features/app/blocks/admin/setting/components/ai-config/InlineProviderKeyList.tsx
      provides: "Inline per-provider key management list"
  key_links:
    - from: InlineProviderKeyList.tsx
      to: LlmproviderManage.tsx
      via: reuse or wrap
      pattern: "LlmproviderManage"
---

<objective>
Build InlineProviderKeyList: replaces the AISetupWizard multi-step flow with an inline list of
provider keys (provider name, masked key, test button, status badge, remove button) plus an
add-provider inline row. Reuses LlmproviderManage / LlmProviderForm as building blocks.

This is primarily an assembly/wrapper — read LlmproviderManage.tsx first to see if it already
provides most of this. If it does, InlineProviderKeyList may simply wrap or extend it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-ai-surface-ui/20-CONTEXT.md
@apps/nextjs-app/src/features/app/blocks/admin/setting/components/ai-config/LlmproviderManage.tsx
@apps/nextjs-app/src/features/app/blocks/admin/setting/components/ai-config/LlmProviderForm.tsx
@apps/nextjs-app/src/features/app/blocks/admin/setting/components/ai-config/TestButton.tsx
</context>

<tasks>

## Task 1 — Read LlmproviderManage and LlmProviderForm

Read both files. Determine:
- Does LlmproviderManage already render the list we need?
- What props does it need?
- What does it lack for the inline-add flow?

## Task 2 — Create InlineProviderKeyList (or enhance LlmproviderManage)

If LlmproviderManage already provides the inline list UX, create InlineProviderKeyList as a
thin wrapper that:
- Uses the same data source (admin settings provider list from API)
- Adds the "Add Provider" inline row if it doesn't exist yet
- Shows status badge (active/error) per provider

If LlmproviderManage already has this, just export it under the new name or re-export.

The key UX improvement over the wizard: everything on one screen — no multi-step flow.

## Task 3 — Wire into AiSettingsHub Providers & Keys tab

In `AiSettingsHub.tsx`, the "Providers & Keys" tab should show `<InlineProviderKeyList />`.
If LlmproviderManage was already there, this is a no-op or light wrapper.

## Task 4 — Typecheck
```bash
cd apps/nextjs-app && yarn typecheck 2>&1 | grep -E "error|Error" | head -20
```

Commit: `feat(20-03): InlineProviderKeyList — inline provider key management (no wizard)`
Write `20-03-SUMMARY.md` and commit: `docs(20-03): plan summary`

</tasks>

<acceptance_criteria>
- [ ] InlineProviderKeyList shows all providers with key + test + remove
- [ ] Add-provider flow is inline (no multi-step wizard)
- [ ] Typechecks without errors
</acceptance_criteria>
