---
plan_id: 20-02
phase: 20-ai-surface-ui
wave: 2
type: execute
depends_on: [20-01]
requirements: [UI-01]
autonomous: false
files_modified:
  - apps/nextjs-app/src/features/app/blocks/admin/setting/components/ai-config/UnifiedModelPicker.tsx
must_haves:
  truths:
    - "UnifiedModelPicker renders a searchable list of models with modality filter chips"
    - "Filter chips: All / Text / Image / Audio / Embedding"
    - "Reuses GatewayModelPickerDialog internals or AiModelSelect as a base"
    - "Typechecks without errors"
  artifacts:
    - path: apps/nextjs-app/src/features/app/blocks/admin/setting/components/ai-config/UnifiedModelPicker.tsx
      provides: "Single searchable model picker with modality filter"
  key_links: []
---

<objective>
Build UnifiedModelPicker: a single searchable model list with modality filter chips (All / Text /
Image / Audio / Embedding) and a recommended badge. Replaces the multi-step GatewayModelPickerDialog
wizard flow. Internally reuses the model data from existing GatewayModelPickerDialog or AiModelSelect.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-ai-surface-ui/20-CONTEXT.md
@apps/nextjs-app/src/features/app/blocks/admin/setting/components/ai-config/GatewayModelPickerDialog.tsx
@apps/nextjs-app/src/features/app/blocks/admin/setting/components/ai-config/AiModelSelect.tsx
@apps/nextjs-app/src/features/app/blocks/admin/setting/components/ai-config/constant.ts
</context>

<tasks>

## Task 1 — Read existing picker components

Read `GatewayModelPickerDialog.tsx` and `AiModelSelect.tsx` to understand:
- How they fetch/receive model data
- What props they accept
- What filtering they already do

## Task 2 — Create UnifiedModelPicker

Create `apps/nextjs-app/src/features/app/blocks/admin/setting/components/ai-config/UnifiedModelPicker.tsx`:

```tsx
// Single searchable picker with modality filter chips.
// Props:
//   models: IGetGatewayModelsVo (from useQuery or parent)
//   value?: string (currently selected model id)
//   onSelect: (modelId: string) => void
//
// Layout:
//   - Search input at top
//   - Filter chips: All | Text | Image | Audio | Embedding
//   - Scrollable list of model cards (provider name, model name, recommended badge)
//   - Selected item highlighted

type Modality = 'all' | 'text' | 'image' | 'audio' | 'embedding';
```

Reuse the model data shape from GatewayModelPickerDialog. Use modality flags
(visionEnable, audioEnable, etc.) to filter. Keep the logic simple — a filter + search
on a flat list of models received from the parent.

## Task 3 — Wire into AiSettingsHub Models tab

In `AiSettingsHub.tsx`, in the Models tab, include a `<UnifiedModelPicker />` alongside
the existing `<GatewayModelsStep />` OR replace GatewayModelsStep's picker with this one.

## Task 4 — Typecheck
```bash
cd apps/nextjs-app && yarn typecheck 2>&1 | grep -E "error|Error" | head -20
```

Commit: `feat(20-02): UnifiedModelPicker — searchable model picker with modality filter`
Write `20-02-SUMMARY.md` and commit: `docs(20-02): plan summary`

</tasks>

<acceptance_criteria>
- [ ] UnifiedModelPicker renders with filter chips
- [ ] Modality filter works (All/Text/Image/Audio/Embedding)
- [ ] Search input filters model list
- [ ] Typechecks without errors
</acceptance_criteria>
