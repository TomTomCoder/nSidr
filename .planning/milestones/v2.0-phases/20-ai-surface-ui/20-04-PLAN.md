---
plan_id: 20-04
phase: 20-ai-surface-ui
wave: 3
type: execute
depends_on: [20-01]
requirements: [UI-02]
autonomous: false
files_modified:
  - apps/nextjs-app/src/features/app/blocks/admin/setting/components/ai-config/CompactAiFieldConfig.tsx
must_haves:
  truths:
    - "CompactAiFieldConfig renders: prompt input + source columns + model picker + output typology selector + live preview slot"
    - "Output typology options: free text / enum / number / JSON shape"
    - "Live preview slot shows generated output for one row"
    - "Typechecks without errors"
  artifacts:
    - path: apps/nextjs-app/src/features/app/blocks/admin/setting/components/ai-config/CompactAiFieldConfig.tsx
      provides: "Compact one-panel AI field config with live preview"
  key_links: []
---

<objective>
Build CompactAiFieldConfig: a one-panel config UI for AI fields (FieldType.Ai). Shows prompt,
source columns, model picker (reuse UnifiedModelPicker), output typology (free text / enum /
number / JSON shape), and a live preview of one row's generated output.

First: audit the existing AI-field config UI to understand what already exists.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-ai-surface-ui/20-CONTEXT.md
</context>

<tasks>

## Task 1 — Audit existing AI-field config UI

Search for the existing AI field config component:
```bash
grep -rn "FieldType.Ai\|aiConfig\|AiField\|ai-field" apps/nextjs-app/src --include="*.tsx" -l
```

Read the main AI field setting component to understand:
- What prompt/source/model config it currently shows
- What output typology it currently supports (if any)
- Where live preview might plug in

## Task 2 — Create CompactAiFieldConfig

Create `apps/nextjs-app/src/features/app/blocks/admin/setting/components/ai-config/CompactAiFieldConfig.tsx`:

```tsx
// One-panel AI field configuration.
// Sections (vertically stacked, no wizard):
//   1. Prompt textarea
//   2. Source columns multi-select
//   3. Model picker (reuse UnifiedModelPicker or AiModelSelect)
//   4. Output typology selector (radio/select): Free text | Enum | Number | JSON shape
//   5. Live preview panel: shows generated text for one sample row
//      (placeholder if no row data available)
//
// Props: aiFieldConfig (current field config), onChange, spaceId, tableId
```

Keep the data contract unchanged — this is a layout/UX change, not an API change.
The live preview section can show a placeholder "Preview will appear here" if a real
generation requires a live API call (document as DEFERRED).

## Task 3 — Wire into the AI field settings entry point

Find where AI field configuration is currently opened (from the audit in Task 1).
Replace the existing multi-step or scattered layout with `<CompactAiFieldConfig />`.

If the existing component is complex, just wrap it — don't rewrite the data layer.

## Task 4 — Typecheck
```bash
cd apps/nextjs-app && yarn typecheck 2>&1 | grep -E "error|Error" | head -20
```

Commit: `feat(20-04): CompactAiFieldConfig — one-panel AI field config + output typology + preview slot`
Write `20-04-SUMMARY.md` and commit: `docs(20-04): plan summary`

</tasks>

<acceptance_criteria>
- [ ] CompactAiFieldConfig renders with all 5 sections
- [ ] Output typology selector has 4 options
- [ ] Live preview slot present (may be placeholder)
- [ ] Typechecks without errors
</acceptance_criteria>
