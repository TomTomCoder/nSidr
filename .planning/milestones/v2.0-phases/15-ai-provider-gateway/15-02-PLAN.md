---
phase: 15-ai-provider-gateway
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
autonomous: true
requirements: [GW-04]
files_modified:
  - apps/nestjs-backend/src/features/ai/unified-ai.service.ts
  - apps/nestjs-backend/src/features/ai/unified-ai.service.spec.ts

must_haves:
  truths:
    - "generateEmbeddings resolves the space-configured embedding provider and routes through aiService.embed"
    - "When no embeddingProvider is configured, generateEmbeddings throws a typed error (no OpenAI/env fallback)"
    - "No process.env.OPENAI_API_KEY read and no direct fetch to api.openai.com remains in unified-ai.service.ts"
  artifacts:
    - path: "apps/nestjs-backend/src/features/ai/unified-ai.service.ts"
      provides: "Gateway-routed embeddings with spaceId-resolved config"
      contains: "aiService.embed"
  key_links:
    - from: "unified-ai.service.generateEmbeddings"
      to: "aiService.embed"
      via: "gateway call"
      pattern: "\\.embed\\("
    - from: "generateEmbeddings"
      to: "aiConfig.embeddingProvider"
      via: "space config resolution"
      pattern: "embeddingProvider"
---

<objective>
Retrofit embeddings (GW-04, D-15-04): replace the hardcoded `process.env.OPENAI_API_KEY` + direct `fetch('https://api.openai.com/v1/embeddings')` in `unified-ai.service.ts` (lines 598-631) with a space-scoped resolution of the configured `embeddingProvider` that routes through `aiService.embed()` built in Plan 01. Remove the silent OpenAI fallback entirely — a missing embedding provider surfaces as a typed error.

Purpose: Close the one confirmed runtime gap from the audit (embeddings bypass the gateway) and enforce fail-loud config semantics (D-15-03/04).
Output: Gateway-routed `generateEmbeddings(texts, spaceId)` + unit tests proving the typed throw and the routing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-ai-provider-gateway/15-CONTEXT.md
@.planning/phases/15-ai-provider-gateway/15-PATTERNS.md
@.planning/phases/15-ai-provider-gateway/15-01-SUMMARY.md

<interfaces>
From Plan 01 (ai.service.ts):
  embed(texts: string[], embeddingModelKey: string, llmProviders: LLMProvider[]): Promise<number[][]>
  getEmbeddingModelInstance(embeddingModelKey, llmProviders)
embeddingProvider config shape (aiConfigSchema, Plan 01): { providerType, providerName, modelId }
Model key format: `{providerType}@{modelId}@{providerName}`
Error pattern: CustomHttpException(message, HttpErrorCode.VALIDATION_ERROR, { localization: { i18nKey } }).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Retrofit generateEmbeddings to route through gateway with fail-loud config</name>
  <read_first>
    - apps/nestjs-backend/src/features/ai/unified-ai.service.ts (generateEmbeddings 598-631, AI config resolution ~142, AiService injection point + constructor)
    - apps/nestjs-backend/src/features/ai/ai.service.ts (embed/getEmbeddingModelInstance from Plan 01, getAIConfig/getSetting resolution)
    - .planning/phases/15-ai-provider-gateway/15-PATTERNS.md (unified-ai replacement pattern 197-228, CustomHttpException + AI_SERVICE shared patterns 381-411)
  </read_first>
  <behavior>
    - Test "no silent fallback": aiConfig has no embeddingProvider → generateEmbeddings throws CustomHttpException(VALIDATION_ERROR, i18nKey httpErrors.ai.embeddingProviderNotSet). No fetch issued, no env read.
    - Test "routes through gateway": aiConfig.embeddingProvider set → generateEmbeddings builds modelKey `${providerType}@${modelId}@${providerName}` and calls aiService.embed once with that key + aiConfig.llmProviders; returns its result.
  </behavior>
  <action>
    Delete the entire body of `generateEmbeddings` (lines 598-631) including the `process.env.OPENAI_API_KEY` read and the direct `fetch` to `api.openai.com` (D-15-04 removes both). Change the signature to `generateEmbeddings(texts: string[], spaceId: string)`. Resolve the space AI config (use the existing config-resolution call already present in this service — same one referenced at ~line 142), read `aiConfig.embeddingProvider`; if absent throw `CustomHttpException('No embedding provider configured. Set an embedding provider in AI settings.', HttpErrorCode.VALIDATION_ERROR, { localization: { i18nKey: 'httpErrors.ai.embeddingProviderNotSet' } })`. Otherwise construct `embeddingModelKey = ${providerType}@${modelId}@${providerName}` and return `this.aiService.embed(texts, embeddingModelKey, aiConfig.llmProviders)`.
    Ensure `AiService` is injected via the `AI_SERVICE` token with a VALUE import (not `import type` — Pitfall 1). If it is already injected for chat, reuse it.
    Update every in-process caller of `generateEmbeddings` to pass `spaceId`. The doc-ingest worker already carries `spaceId` per AUDIT.md — thread it through the job payload to the call site (read the worker/processor to confirm the payload field name; if `spaceId` is absent from the payload schema, add it — RESEARCH Open Question 1). Add the i18n key `httpErrors.ai.embeddingProviderNotSet` to the locale source if required for typecheck.
  </action>
  <verify>
    <automated>cd apps/nestjs-backend && npx vitest run src/features/ai/unified-ai.service.spec.ts</automated>
    <automated>cd apps/nestjs-backend && grep -c "OPENAI_API_KEY\|api.openai.com/v1/embeddings" src/features/ai/unified-ai.service.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both behavior tests pass GREEN.
    - `grep` for `OPENAI_API_KEY` and `api.openai.com/v1/embeddings` in `unified-ai.service.ts` returns 0.
    - All `generateEmbeddings` call sites compile with the new `(texts, spaceId)` signature; worker payload carries `spaceId`.
    - `npx vitest run src/features/ai` full ai-feature suite green.
  </acceptance_criteria>
  <done>Embeddings route through the gateway; missing config fails loud; no direct OpenAI fetch/env read remains.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker job → embedding provider | spaceId-scoped config crosses from job payload into key resolution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03 | Information Disclosure | leftover `process.env.OPENAI_API_KEY` read leaking a dev key into prod embeddings | mitigate | Task 1 deletes the env read entirely; grep gate asserts 0 occurrences. |
| T-15-05 | Spoofing/Config | silent OpenAI fallback hiding misconfiguration | mitigate | Task 1 replaces fallback with typed throw; unit test asserts the throw + no fetch. |
| T-15-SC | Tampering | npm installs | accept | No new packages this plan. |
</threat_model>

<verification>
- `npx vitest run src/features/ai` green.
- grep proves no OPENAI_API_KEY / direct openai fetch in unified-ai.service.ts.
</verification>

<success_criteria>
GW-04 embeddings routed through gateway with separate embedding-provider config; silent fallback removed; fail-loud verified by test.
</success_criteria>

<output>
Create `.planning/phases/15-ai-provider-gateway/15-02-SUMMARY.md` when done. Note whether the worker payload already carried spaceId or needed extension. Add a changelog note that OPENAI_API_KEY is no longer used for embedding fallback (RESEARCH V14).
</output>
