---
phase: 15-ai-provider-gateway
plan: 03
type: execute
wave: 2
depends_on: ["15-01"]
autonomous: false
requirements: [GW-01, GW-02, GW-03, GW-04, GW-05]
files_modified:
  - apps/nextjs-app/src/features/app/blocks/admin/setting/components/ai-config/LlmProviderForm.tsx
  - apps/nestjs-backend/src/features/setting/setting.service.ts
  - apps/nestjs-backend/src/features/setting/setting.service.spec.ts

must_haves:
  truths:
    - "An Ollama provider form shows an 'Install Ollama →' link opening ollama.com/download in a new tab"
    - "The backend proxies GET {ollamaUrl}/api/tags and returns the parsed model list"
    - "Each model row exposes an inline 'Override' affordance that saves modelConfigs[modelId].apiKey"
    - "An embedding-provider picker lets the user pick a separate embedding provider+model"
  artifacts:
    - path: "apps/nextjs-app/.../ai-config/LlmProviderForm.tsx"
      provides: "Ollama link, per-model override UI, embedding picker"
      contains: "ollama.com/download"
    - path: "apps/nestjs-backend/src/features/setting/setting.service.ts"
      provides: "Ollama /api/tags proxy"
      contains: "api/tags"
  key_links:
    - from: "LlmProviderForm override form"
      to: "modelConfigs[modelId].apiKey"
      via: "settings update PATCH"
      pattern: "modelConfigs"
    - from: "setting.service ollama proxy"
      to: "{ollamaUrl}/api/tags"
      via: "server-side fetch"
      pattern: "api/tags"
---

<objective>
Extend the admin AI-config UI (GW-03/05) and add the Ollama model-list proxy (GW-02/03): the "Install Ollama →" link, the inline per-model API-key override affordance (D-15-02), the separate embedding-provider picker (D-15-04 surface), and a backend proxy for `GET {ollamaUrl}/api/tags` (server-side to avoid CORS and keep config server-side). Closes the human-facing surface for the schema fields added in Plan 01.

Purpose: Make the gateway's per-model override, embedding-provider config, and Ollama support reachable and editable by an admin.
Output: Extended `LlmProviderForm.tsx`, an Ollama tags proxy on the setting service, and a verification checkpoint for the visual/interactive UI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-ai-provider-gateway/15-CONTEXT.md
@.planning/phases/15-ai-provider-gateway/15-PATTERNS.md
@.planning/phases/15-ai-provider-gateway/15-01-SUMMARY.md

<interfaces>
Schema (Plan 01): modelConfigSchema now has optional `apiKey`; aiConfigSchema has optional `embeddingProvider { providerType, providerName, modelId }`.
Public schema strips per-model apiKey — the form must read the override state from the admin (non-public) GET, not the public one.
Existing form imports: zodResolver, llmProviderSchema + LLMProviderType from @teable/openapi, useForm, toast from @teable/ui-lib/shadcn/ui/sonner.
Existing model catalog: setting.service.getGatewayModels() returns modality flags (language/embedding/image).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend Ollama /api/tags proxy + GW-02 modality-filter verification</name>
  <read_first>
    - apps/nestjs-backend/src/features/setting/setting.service.ts (getGatewayModels + existing fetch/proxy patterns + admin guard usage)
    - apps/nestjs-backend/src/features/setting/setting.service.spec.ts (existing spec structure)
    - .planning/phases/15-ai-provider-gateway/15-RESEARCH.md (Architectural Responsibility Map — Ollama proxy is API tier; Security SSRF note)
  </read_first>
  <action>
    Add a setting-service method (e.g. `listOllamaModels(ollamaUrl: string)`) that does a server-side `fetch(`${ollamaUrl}/api/tags`)` and returns the parsed `{ models: [{ name, ... }] }` list (Ollama API per Assumptions A5). Default the URL to `http://localhost:11434` when not supplied. SSRF mitigation (T-15-04): validate the URL is http(s) and reject obvious internal-metadata targets unless the operator opted in — keep the codebase's existing baseUrl stance consistent; do not allow arbitrary internal IPs silently. Expose it via the existing admin-guarded setting controller surface (reuse the guard pattern already on getGatewayModels). Add a unit test mocking `global.fetch` for `/api/tags` asserting the proxy returns the parsed list, and one asserting an SSRF-suspect URL is rejected.
    Verify GW-02: add/confirm a test that `getGatewayModels()` returns entries carrying modality flags (language/embedding/image) so the UI can filter — extend the existing spec if a `getGatewayModels` test is absent (RESEARCH GW-02 row "Wave 0 verify").
  </action>
  <verify>
    <automated>cd apps/nestjs-backend && npx vitest run src/features/setting/setting.service.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `listOllamaModels` proxies `GET {url}/api/tags` server-side and returns the parsed model array (test GREEN, fetch mocked).
    - SSRF-suspect URL rejected (test GREEN).
    - A `getGatewayModels` modality-flag test passes (GW-02 verified).
  </acceptance_criteria>
  <done>Ollama tags proxy + modality-catalog verification land with passing tests.</done>
</task>

<task type="auto">
  <name>Task 2: LlmProviderForm — Install Ollama link, inline per-model override, embedding picker</name>
  <read_first>
    - apps/nextjs-app/.../ai-config/LlmProviderForm.tsx (imports 1-51, baseUrl input region, model picker rows ~122/276-300/486-660, existing target="_blank" anchors)
    - .planning/phases/15-ai-provider-gateway/15-PATTERNS.md (LlmProviderForm patterns 314-352: Ollama link 326-339, per-model override 341-346, embedding picker 348-352)
    - .planning/phases/15-ai-provider-gateway/15-CONTEXT.md (D-15-02 inline override UX, D-15-05 Ollama link)
  </read_first>
  <action>
    GW-03: when `providerType === LLMProviderType.OLLAMA`, render an "Install Ollama →" anchor next to the baseUrl input pointing at `https://ollama.com/download` with `target="_blank" rel="noopener noreferrer"` (copy the existing external-link anchor style in this file). Wire a "Load models" action that calls the Plan-03 Task-1 Ollama tags proxy to populate the model list (no model-pull UI, no spawn — D-15-05).
    GW-05: in each model row render a status badge ("Using provider default key" vs "Override set") and an "Override" button that opens a small inline form (`{ apiKey: string }`, validated via `modelConfigSchema.pick({ apiKey: true })` + zodResolver) which on save writes `modelConfigs[modelId].apiKey` through the existing settings update endpoint (D-15-02 inline-at-point-of-use). Read override state from the admin (non-public) setting GET — the public schema strips the key (Plan 01 Task 2).
    GW-04: add an embedding-provider section below the chat-model pickers: a provider+model `<Select>` filtered to embedding-capable models (use `getGatewayModels` modality flags / `modelType === 'embedding'`); on change persist `aiConfig.embeddingProvider` ({ providerType, providerName, modelId }) via the settings update API. Surface "no embedding provider configured" per Claude's discretion (toast+link to settings is acceptable).
    Use value imports for any DI-relevant code; reuse existing `useForm`/`zodResolver`/`toast` patterns. Run the UI typecheck.
  </action>
  <verify>
    <automated>yarn workspace @teable/nextjs-app typecheck</automated>
    <automated>cd apps/nextjs-app && grep -c "ollama.com/download" src/features/app/blocks/admin/setting/components/ai-config/LlmProviderForm.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Ollama install anchor present (grep returns ≥1) with new-tab attrs.
    - Per-model override form writes `modelConfigs[modelId].apiKey`; embedding-provider picker persists `aiConfig.embeddingProvider`.
    - `@teable/nextjs-app typecheck` passes.
  </acceptance_criteria>
  <done>Form exposes Ollama link, inline per-model override, and embedding-provider picker; typecheck green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of AI-config UI extensions</name>
  <action>Pause for the user to manually verify the admin AI-config UI per the steps below. This is a verification checkpoint after the automated UI work in Tasks 1-2 — no further code is written here unless the user reports issues.</action>
  <what-built>Admin AI-config UI extensions: Ollama install link + model-load, inline per-model API-key override, and a separate embedding-provider picker — plus the backend Ollama /api/tags proxy.</what-built>
  <how-to-verify>
    1. Boot the app (web :3000 + API). Open Admin → Settings → AI config.
    2. Add/edit an Ollama provider: confirm the "Install Ollama →" link opens https://ollama.com/download in a new tab. If a local Ollama is running, click "Load models" and confirm models appear; if not, confirm it fails gracefully (no crash).
    3. On any provider's model row, click "Override", enter a test key, save; reload and confirm the badge reads "Override set" (and the network GET for public settings does NOT contain the key).
    4. In the embedding section, pick an embedding provider+model and save; reload and confirm it persists.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → setting GET | per-model override key must not appear in the public payload |
| admin form → Ollama proxy | user-controlled ollamaUrl crosses into a server-side fetch (SSRF surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Information Disclosure | per-model apiKey shown in UI sourced from public GET | mitigate | Task 2 reads override state from admin (non-public) GET; public schema already strips key (Plan 01). |
| T-15-04 | Tampering (SSRF) | user-controlled ollamaUrl in `/api/tags` proxy | mitigate | Task 1 validates scheme + rejects internal-metadata targets without operator opt-in; default localhost:11434. |
| T-15-SC | Tampering | npm installs | accept | No new packages this plan. |
</threat_model>

<verification>
- `npx vitest run src/features/setting/setting.service.spec.ts` green.
- `yarn workspace @teable/nextjs-app typecheck` green.
- Human checkpoint approved.
</verification>

<success_criteria>
GW-03 Ollama UX (link + model list), GW-05 inline override UI, GW-04 embedding-picker UI, GW-02 modality catalog verified — all reachable from the admin form.
</success_criteria>

<output>
Create `.planning/phases/15-ai-provider-gateway/15-03-SUMMARY.md` when done.
</output>
