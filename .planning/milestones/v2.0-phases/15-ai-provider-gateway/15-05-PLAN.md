---
phase: 15-ai-provider-gateway
plan: 05
type: execute
wave: 4
depends_on: ["15-01", "15-02", "15-04"]
autonomous: true
requirements: [GW-01, GW-04]
files_modified:
  - apps/nestjs-backend/src/features/agent/agent-execution.service.ts
  - apps/nestjs-backend/src/features/agent/agent-execution.service.spec.ts

must_haves:
  truths:
    - "agent-execution.service builds its model via aiService.getModelInstance, not a direct provider factory"
    - "agent-execution.service contains no @ai-sdk/* import and no inline createOpenAI/createGateway"
    - "The phase-exit grep guard passes: no provider SDK construction outside features/ai/"
  artifacts:
    - path: "apps/nestjs-backend/src/features/agent/agent-execution.service.ts"
      provides: "Gateway-routed agent model construction"
      contains: "getModelInstance"
  key_links:
    - from: "agent-execution.service"
      to: "aiService.getModelInstance"
      via: "model build before generateText"
      pattern: "getModelInstance"
    - from: "scripts/check-no-direct-ai-sdk.sh"
      to: "apps/nestjs-backend/src (excluding ai/)"
      via: "phase-exit CI guard"
      pattern: "@ai-sdk"
---

<objective>
Final retrofit + phase-exit gate: route `agent-execution.service.ts` (the last direct `generateText`-with-inline-model consumer, Phase 17 territory, single call site ~line 317) through `aiService.getModelInstance()`, then run the [BLOCKING] grep guard proving success criterion 5 — no provider SDK imports/construction outside the gateway module. This plan runs last so every other consumer is already retrofitted when the guard executes.

Purpose: Close the retrofit set and lock the phase-exit invariant. Coordinated with Phase 17 — must not re-introduce the 3 open Phase 17 bugs.
Output: Gateway-routed agent execution + a GREEN phase-exit grep guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-ai-provider-gateway/15-CONTEXT.md
@.planning/phases/15-ai-provider-gateway/15-PATTERNS.md
@.planning/phases/15-ai-provider-gateway/15-01-SUMMARY.md
@.planning/phases/15-ai-provider-gateway/15-02-SUMMARY.md
@.planning/phases/15-ai-provider-gateway/15-04-SUMMARY.md
@.planning/phases/17-agent-mcp-enhancement/17-VERIFICATION.md

<interfaces>
From ai.service.ts: getModelInstance(modelKey, llmProviders, isImageGeneration=false).
Existing correct analog in this same file: streamLlmIteration (lines ~300-390) already uses aiService.getModelInstance — copy its pattern.
DI rule: value import of AiService (Pitfall 1). Phase 17 bug-1 was exactly an import type on a DI service — do not repeat.
Phase-exit guard: scripts/check-no-direct-ai-sdk.sh (created Plan 01).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retrofit agent-execution.service generateText call site through gateway</name>
  <read_first>
    - apps/nestjs-backend/src/features/agent/agent-execution.service.ts (import line 3, generateText call ~317, streamLlmIteration analog ~300-390, AiService injection point)
    - apps/nestjs-backend/src/features/ai/ai.service.ts (getModelInstance 171-253)
    - .planning/phases/15-ai-provider-gateway/15-PATTERNS.md (agent-execution retrofit 232-258)
    - .planning/phases/17-agent-mcp-enhancement/17-VERIFICATION.md (phase_17_bugs_found_by_live_test — do not re-introduce)
  </read_first>
  <action>
    At the direct `generateText` call (~line 317), replace any inline provider/model construction with `const modelInstance = await this.aiService.getModelInstance(resolvedModelKey, aiConfig.llmProviders)` then `generateText({ model: modelInstance, ... })`, mirroring `streamLlmIteration` which already does this correctly in the same file. Keep the `generateText` import from `ai` (only the model construction moves to the gateway). Remove any `@ai-sdk/*` import and any inline `createOpenAI`/`createGateway`. Confirm `AiService` is injected via the `AI_SERVICE` token as a VALUE import (Phase 17 bug-1: never `import type` a DI service — re-check this exact file since it is Phase 17 territory).
    Do NOT touch the 3 open Phase 17 bugs (interface-tools MCP wiring, MCP RBAC, knowledge-scope) — this change is scoped strictly to model construction routing.
    Add/extend a unit test asserting the agent path calls `aiService.getModelInstance(modelKey, providers)` (mock the gateway + SDK generateText helper) — proves routing without API credits.
  </action>
  <verify>
    <automated>cd apps/nestjs-backend && npx vitest run src/features/agent/agent-execution.service.spec.ts</automated>
    <automated>cd apps/nestjs-backend && grep -c "@ai-sdk/\|createOpenAI\|createGateway" src/features/agent/agent-execution.service.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "@ai-sdk/\|createOpenAI\|createGateway"` on the file returns 0.
    - Agent path builds the model via `aiService.getModelInstance`; routing test GREEN.
    - No `import type` on `AiService`; backend boots (DI resolves).
  </acceptance_criteria>
  <done>Agent execution routed through gateway; no direct provider SDK construction; Phase 17 bugs untouched.</done>
</task>

<task type="auto" gate="blocking">
  <name>Task 2: [BLOCKING] Phase-exit grep guard — no provider SDK outside the gateway</name>
  <read_first>
    - scripts/check-no-direct-ai-sdk.sh (created Plan 01)
    - .planning/phases/15-ai-provider-gateway/15-RESEARCH.md (D-15-06 exit criterion, Risk-ranked ordering)
  </read_first>
  <action>
    Run the phase-exit guard `bash scripts/check-no-direct-ai-sdk.sh` (success criterion 5 / D-15-06). It must exit 0 — meaning every `@ai-sdk/`, `createOpenAI`, `createGateway` token in `apps/nestjs-backend/src` (excluding `*spec*` and `features/ai/`) is gone. If it exits non-zero, the printed file list is the remaining offenders: fix each by routing through `aiService.getModelInstance` (these should only be files already retrofitted in Plans 02/04/05 — investigate any unexpected file). Then run the full backend suite to confirm no regression from the retrofit set.
    This is a BLOCKING gate: do not mark the phase complete until the guard is GREEN.
  </action>
  <verify>
    <automated>bash scripts/check-no-direct-ai-sdk.sh && echo GUARD_PASS</automated>
    <automated>cd apps/nestjs-backend && npx vitest run src/features/ai src/features/setting src/features/agent</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/check-no-direct-ai-sdk.sh` exits 0 and prints the OK message (no offenders outside features/ai/).
    - Full ai+setting+agent suite GREEN (no retrofit regression).
  </acceptance_criteria>
  <done>Phase-exit invariant locked: provider SDK construction exists only inside features/ai/; guard GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent runtime → gateway | model + key construction confined to the gateway module |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-06 | Elevation/Config | scattered provider SDK construction = multiple uncontrolled key paths | mitigate | Task 1 removes the last direct path; Task 2 BLOCKING guard enforces single-module invariant. |
| T-15-07 | DI / Availability | `import type` on AiService crashing backend boot (Phase 17 bug-1) | mitigate | Task 1 enforces value import; boot/DI resolution verified. |
| T-15-SC | Tampering | npm installs | accept | No new packages this plan. |
</threat_model>

<verification>
- `bash scripts/check-no-direct-ai-sdk.sh` exits 0.
- `npx vitest run src/features/ai src/features/setting src/features/agent` green.
</verification>

<success_criteria>
Success criterion 5 met: no provider SDK imports outside the gateway module. Agent execution routed through gateway; full retrofit set verified.
</success_criteria>

<output>
Create `.planning/phases/15-ai-provider-gateway/15-05-SUMMARY.md` when done. Confirm guard GREEN and note any unexpected offenders found.
</output>
