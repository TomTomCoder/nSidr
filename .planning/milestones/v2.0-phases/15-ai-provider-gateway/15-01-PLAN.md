---
phase: 15-ai-provider-gateway
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [GW-01, GW-02, GW-05]
files_modified:
  - scripts/check-no-direct-ai-sdk.sh
  - packages/openapi/src/admin/setting/update.ts
  - packages/openapi/src/admin/setting/get-public.ts
  - apps/nestjs-backend/src/features/ai/ai.service.ts
  - apps/nestjs-backend/src/features/ai/ai.service.spec.ts

must_haves:
  truths:
    - "A model-level apiKey override resolves before the provider-default key"
    - "When neither a model override nor a provider key exists, resolution throws an error naming the model"
    - "Per-model apiKey is never serialized into the public (browser-facing) setting schema"
    - "The grep guard script fails when an @ai-sdk import exists outside features/ai/"
  artifacts:
    - path: "scripts/check-no-direct-ai-sdk.sh"
      provides: "Phase-exit CI guard for D-15-06"
      contains: "grep -r"
    - path: "apps/nestjs-backend/src/features/ai/ai.service.ts"
      provides: "resolveApiKey cascade + getEmbeddingModelInstance + embed"
      contains: "resolveApiKey"
    - path: "packages/openapi/src/admin/setting/update.ts"
      provides: "modelConfigSchema.apiKey + aiConfigSchema.embeddingProvider"
      contains: "embeddingProvider"
  key_links:
    - from: "ai.service.ts resolveApiKey"
      to: "provider.modelConfigs[model].apiKey"
      via: "cascade lookup"
      pattern: "modelConfigs"
    - from: "get-public.ts simpleLLMProviderSchema"
      to: "modelConfigSchema.omit({ apiKey })"
      via: "key stripping"
      pattern: "omit"
---

<objective>
Lay the Phase 15 foundation: Wave 0 scaffolding (grep guard, `ai` export verification, cascade test stubs), the additive schema extensions (per-model `apiKey` override for GW-05, separate `embeddingProvider` config for GW-04), public-schema key stripping, and the gateway resolver/embed methods on `AiService`. No callers change in this plan — this is pure additive groundwork that unblocks every downstream slice.

Purpose: De-risk the phase by landing schema + resolver + tests first (lowest-risk per RESEARCH risk-ranked ordering) so retrofit slices build against stable contracts.
Output: Extended schemas, `resolveApiKey()`/`embed()`/`getEmbeddingModelInstance()` on the gateway, the CI guard script, and passing cascade unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-ai-provider-gateway/15-CONTEXT.md
@.planning/phases/15-ai-provider-gateway/15-RESEARCH.md
@.planning/phases/15-ai-provider-gateway/15-PATTERNS.md

<interfaces>
Model key format (ai.service.ts:84-86): `{type}@{model}@{name}` parsed by `parseModelKey(modelKey)` → `{ type, model, name }`.
Error pattern: `CustomHttpException(message, HttpErrorCode.VALIDATION_ERROR, { localization: { i18nKey, context } })` (custom.exception.ts:7-19).
Provider factory map: `modelProviders` + `getAdaptedProviderOptions(type, { name, baseURL, apiKey })` in features/ai/util.ts.
`ai` package version: 6.0.169 (confirmed in apps/nestjs-backend/package.json).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 scaffolding — grep guard, ai-export verify, cascade test stubs</name>
  <read_first>
    - apps/nestjs-backend/src/features/ai/ai.service.spec.ts (existing spec structure + helpers)
    - apps/nestjs-backend/src/features/ai/ai.service.ts (lines 84-86 parseModelKey, 116-168 getModelConfig)
    - .planning/phases/15-ai-provider-gateway/15-PATTERNS.md (scripts/check-no-direct-ai-sdk.sh pattern, lines 355-375)
    - .planning/phases/15-ai-provider-gateway/15-RESEARCH.md (Validation Architecture + Assumptions A1)
  </read_first>
  <action>
    Create `scripts/check-no-direct-ai-sdk.sh` (executable, `chmod +x`) implementing the D-15-06 exit criterion: grep `apps/nestjs-backend/src` for tokens `@ai-sdk/`, `createOpenAI`, `createGateway` with `--include="*.ts" --exclude="*spec*" --exclude-dir="ai"`; exit 1 with the offending file list if any hit, else echo OK. Filter comment lines with `grep -v '^[[:space:]]*//'` before counting so header prose cannot self-invalidate the gate. Do NOT add `generateText`/`generateImage` to the forbidden token list — those imports legitimately remain in retrofitted callers (only the inline provider construction moves to the gateway, per PATTERNS retrofit notes).
    Verify the `ai` package export surface (Assumptions A1): run the export-check command from the backend workspace and confirm `embedMany` is exported; record the result in the SUMMARY. If `embedMany` is absent, fall back to `provider.textEmbeddingModel(model)` + per-instance embed and note the deviation.
    Add failing/pending test stubs to `ai.service.spec.ts` named exactly: `"cascade: model override wins"`, `"cascade: provider default used when no override"`, `"cascade: fail loud names model"`. Build a fixture `llmProviders[]` with one provider that has `apiKey` and one model carrying a `modelConfigs[model].apiKey` override. Stubs reference `aiService.resolveApiKey(modelKey, providers)` (implemented in Task 3) — they will be RED until then.
  </action>
  <verify>
    <automated>bash scripts/check-no-direct-ai-sdk.sh; echo "exit=$?"</automated>
    <automated>cd apps/nestjs-backend && node -e "console.log(Object.keys(require('ai')).includes('embedMany'))"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/check-no-direct-ai-sdk.sh` exists, is executable, and exits 0 on the current tree (no callers retrofitted yet means callers still hold createGateway, so the guard MAY exit 1 at this point — if so, record the offending files in SUMMARY as the retrofit worklist for Plans 04/05; the guard itself must be correct, not necessarily green yet).
    - The `ai` export check prints `true` for `embedMany`, OR the SUMMARY records the documented fallback.
    - Three named test stubs exist in `ai.service.spec.ts` and run (RED is acceptable here).
  </acceptance_criteria>
  <done>CI guard script present + correct; ai-export assumption resolved; three cascade test names present.</done>
</task>

<task type="auto">
  <name>Task 2: Schema extension — per-model apiKey (GW-05) + embeddingProvider (GW-04) + public-schema strip</name>
  <read_first>
    - packages/openapi/src/admin/setting/update.ts (modelConfigSchema lines 46-78, aiConfigSchema lines 168-197, LLMProviderType import)
    - packages/openapi/src/admin/setting/get-public.ts (simpleLLMProviderSchema lines 9-14)
    - .planning/phases/15-ai-provider-gateway/15-PATTERNS.md (schema + key-stripping patterns, lines 136-193, 439-449)
  </read_first>
  <action>
    In `update.ts`: add optional `apiKey: z.string().optional()` to `modelConfigSchema` (per-model override, GW-05, per D-15-03 cascade). Add optional `embeddingProvider` object to `aiConfigSchema` with fields `providerType: z.nativeEnum(LLMProviderType)`, `providerName: z.string()`, `modelId: z.string()` (GW-04, D-15-04 separate embedding provider config). Both additions are additive optional fields — no Prisma migration (aiConfig is JSON-on-Setting per RESEARCH Runtime State Inventory A6).
    In `get-public.ts`: change `simpleLLMProviderSchema` so `modelConfigs` values strip the new per-model `apiKey` — define `safeModelConfigSchema = modelConfigSchema.omit({ apiKey: true })` and rebuild `simpleLLMProviderSchema` to `.pick({ type, name, models, isInstance })` + `.extend({ modelConfigs: z.record(z.string(), safeModelConfigSchema).optional() })`. This closes the Information-Disclosure threat (T-15-01). Provider-level `apiKey` is already not picked — leave that alone.
    Rebuild the openapi package so backend typecheck sees the new shape: `yarn workspace @teable/openapi build` (Pitfall 2 — schema→package→backend order).
  </action>
  <verify>
    <automated>yarn workspace @teable/openapi build</automated>
    <automated>cd packages/openapi && grep -c "embeddingProvider" src/admin/setting/update.ts</automated>
    <automated>cd packages/openapi && grep -c "omit" src/admin/setting/get-public.ts</automated>
  </verify>
  <acceptance_criteria>
    - `modelConfigSchema` includes `apiKey: z.string().optional()`; existing JSON blobs still validate (additive).
    - `aiConfigSchema` includes `embeddingProvider` optional object with `providerType`/`providerName`/`modelId`.
    - `simpleLLMProviderSchema` no longer exposes per-model `apiKey` (verified: omit-based safe model-config used).
    - `@teable/openapi` builds clean.
  </acceptance_criteria>
  <done>Both additive fields present; public schema strips per-model key; openapi package builds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Gateway resolver + embed methods (resolveApiKey, getEmbeddingModelInstance, embed)</name>
  <read_first>
    - apps/nestjs-backend/src/features/ai/ai.service.ts (getModelConfig 116-168, getModelInstance 171-253, parseModelKey 84-86, imports 1-46)
    - apps/nestjs-backend/src/features/ai/util.ts (modelProviders map 113-133, getAdaptedProviderOptions)
    - apps/nestjs-backend/src/features/ai/ai.service.spec.ts (the three stubs from Task 1)
    - .planning/phases/15-ai-provider-gateway/15-PATTERNS.md (resolveApiKey 69-95, getEmbeddingModelInstance/embed 98-118, Provider Factory Lookup 425-437)
  </read_first>
  <behavior>
    - Test "cascade: model override wins": provider has apiKey="prov", modelConfigs[m].apiKey="model" → resolveApiKey returns "model".
    - Test "cascade: provider default used when no override": no model override, provider.apiKey="prov" → returns "prov".
    - Test "cascade: fail loud names model": no override, no provider apiKey → throws CustomHttpException whose message contains the modelKey (no env-var fallback, D-15-03).
    - Test "embed routes through configured provider": embed(texts, embeddingModelKey, providers) calls embedMany with a model instance built from getEmbeddingModelInstance (assert via mock that the model arg derives from the configured provider, not a hardcoded OpenAI instance).
  </behavior>
  <action>
    Add `embedMany` to the existing `import { ... } from 'ai'` line in `ai.service.ts` (value import). Implement `resolveApiKey(modelKey, providers): string` exactly per PATTERNS lines 69-95 — strict 2-level cascade (model `modelConfigs[model].apiKey` → provider `apiKey` → throw `CustomHttpException` with `HttpErrorCode.VALIDATION_ERROR`, i18nKey `httpErrors.ai.apiKeyNotConfigured`, and the message naming the model per D-15-03). Add the i18n key to the relevant locale source if the project requires it for typecheck.
    Implement `getEmbeddingModelInstance(embeddingModelKey, llmProviders)` mirroring `getModelInstance` but calling `provider(...).textEmbeddingModel(model)` (PATTERNS 98-111; use the Provider Factory Lookup shared pattern). Implement `embed(texts, embeddingModelKey, llmProviders): Promise<number[][]>` that resolves the instance and calls `embedMany({ model, values: texts })`, returning `embeddings`. If the Task 1 export check found no `embedMany`, use the documented `textEmbeddingModel` fallback path instead.
    Make the three Task 1 stubs pass. Use the SDK's own mock language/embedding model helper (confirm exact export in this `ai` version) rather than hand-rolled mocks (Pitfall 6).
    Do NOT use `import type` for any DI-injected dependency (Pitfall 1 / Phase 17 bug-1).
  </action>
  <verify>
    <automated>cd apps/nestjs-backend && npx vitest run src/features/ai/ai.service.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - All three cascade tests + the embed-routing test pass GREEN.
    - `resolveApiKey` throws with the model named on full cascade miss; no env-var read anywhere in the method.
    - `embed`/`getEmbeddingModelInstance` build the provider instance from `modelProviders` + `getAdaptedProviderOptions` (no inline `createOpenAI`).
    - `grep -c "import type.*AiService\|import type.*Service" ai.service.ts` does not introduce new type-only DI imports.
  </acceptance_criteria>
  <done>Resolver + embed methods land with passing tests; cascade fails loud naming the model.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → setting GET endpoint | Public schema must never serialize secrets |
| admin form → setting update endpoint | Untrusted key/baseUrl values cross here (admin-guarded) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Information Disclosure | per-model `apiKey` leaking via `simpleLLMProviderSchema` (GET public) | mitigate | Task 2: `safeModelConfigSchema = modelConfigSchema.omit({ apiKey })`; public schema rebuilt to exclude per-model key. Verified by grep + (Plan 03) schema test. |
| T-15-02 | Information Disclosure | resolver returning a key in an error/log | mitigate | Task 3: error message names only the model, never the key value. |
| T-15-05 | Spoofing/Config | silent fallback masking a config gap | mitigate | Task 3: strict fail-loud cascade, no env-var fallback (D-15-03). Unit test asserts the throw. |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed this plan (RESEARCH Package Legitimacy Audit: all libs present). |
</threat_model>

<verification>
- `yarn workspace @teable/openapi build` clean.
- `npx vitest run src/features/ai/ai.service.spec.ts` green.
- `bash scripts/check-no-direct-ai-sdk.sh` runs (records retrofit worklist if not yet green).
</verification>

<success_criteria>
GW-05 cascade implemented + tested; GW-04 embed plumbing present; schemas extended additively; public schema strips per-model key; CI guard authored.
</success_criteria>

<output>
Create `.planning/phases/15-ai-provider-gateway/15-01-SUMMARY.md` when done. Record the `ai` export-check result and the grep-guard offending-file worklist.
</output>
