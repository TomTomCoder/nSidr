---
phase: 24-agent-runtime-hardening
plan: 06
type: execute
wave: 4
depends_on: [24-05]
files_modified:
  - apps/nextjs-app/src/features/app/blocks/agent-chat/ApprovalCard.tsx
  - apps/nextjs-app/src/features/app/blocks/agent-chat/AgentChat.tsx
autonomous: false
requirements: [ARH-03]
must_haves:
  truths:
    - "An approval card is rendered in the agent chat UI when conversation.status === 'waiting_for_approval'"
    - "Clicking Approve POSTs {decision:'accept'} to the backend approve endpoint and the chat resumes streaming the new run"
    - "Clicking Reject prompts for a reason and POSTs {decision:'reject', reason} and the conversation closes"
    - "LLM-controlled question/context is rendered as plain React text children (no raw HTML)"
  artifacts:
    - path: "apps/nextjs-app/src/features/app/blocks/agent-chat/ApprovalCard.tsx"
      provides: "Approval card React component"
      contains: "ApprovalCard"
    - path: "apps/nextjs-app/src/features/app/blocks/agent-chat/AgentChat.tsx"
      provides: "Renders ApprovalCard when conversation.status === 'waiting_for_approval'"
      contains: "ApprovalCard"
  key_links:
    - from: "AgentChat conversation query"
      to: "ApprovalCard"
      via: "status === 'waiting_for_approval' branch renders <ApprovalCard>"
      pattern: "waiting_for_approval"
    - from: "ApprovalCard mutation"
      to: "POST /api/agent/:agentId/conversation/:conversationId/approve"
      via: "react-query useMutation"
      pattern: "/approve"
---

<objective>
Implement the frontend half of ARH-03 — render an `<ApprovalCard>` in the agent chat when the backend (plan 24-05) has suspended the conversation with `status='waiting_for_approval'`. Clicking Approve or Reject hits the approve endpoint from plan 24-05; the chat refreshes on resolution and resumes streaming the new run. Closes ARH-03 with an end-to-end UAT gate.

Purpose: Surface the HITL gate to the user so destructive/sensitive operations can be confirmed before they execute.
Output: New `ApprovalCard.tsx` component; `AgentChat.tsx` wiring to render the card when the conversation is waiting; UAT checkpoint verifying the full backend + frontend loop against the running app.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-agent-runtime-hardening/24-RESEARCH.md
@.planning/phases/24-agent-runtime-hardening/24-PATTERNS.md
@.planning/phases/24-agent-runtime-hardening/24-05-PLAN.md
@apps/nextjs-app/src/features/app/blocks/agent-chat/AgentChat.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Approval card React component + wire into AgentChat</name>
  <files>apps/nextjs-app/src/features/app/blocks/agent-chat/ApprovalCard.tsx, apps/nextjs-app/src/features/app/blocks/agent-chat/AgentChat.tsx</files>
  <read_first>
    - apps/nextjs-app/src/features/app/blocks/agent-chat/AgentChat.tsx (current message-rendering loop and conversation status handling; locate where streamed events are processed — specifically the handler for the `type: 'hitl'` event yielded by the backend in plan 24-05 Task 2; also locate the existing react-query hooks for sending messages so we can mirror the mutation pattern for approve)
    - apps/nextjs-app/src/features/app/blocks/agent-chat/*.tsx (any existing small card / inline-action components to mirror styling and Radix primitives)
  </read_first>
  <action>
    Create `ApprovalCard.tsx` exporting a `<ApprovalCard>` component with props `{ conversationId: string; agentId: string; payload: { question: string; context?: string }; onResolved: () => void }`. The card renders the `question` prominently, the `context` (if present) below as muted text, and two buttons "Approve" (primary) and "Reject" (destructive). Wire a `useMutation` (matching the existing react-query usage in `AgentChat.tsx`) that POSTs to `/api/agent/:agentId/conversation/:conversationId/approve` with `{ decision:'accept' }` on Approve or `{ decision:'reject', reason }` on Reject (Reject opens a small inline textarea for the reason before submission). On mutation success call `onResolved` so `AgentChat.tsx` can refetch the conversation. Render the LLM-controlled `question` and `context` as plain React text children only — never as raw HTML — so they are escaped by React's default text rendering. In `AgentChat.tsx`, when the conversation query returns `status === 'waiting_for_approval'` (or when the streamed event `type:'hitl'` is observed), render `<ApprovalCard>` inline at the end of the message list using `approvalPayload` from the conversation record. After resolution refetch the conversation and resume streaming the new run started by the backend.
  </action>
  <verify>
    <automated>cd apps/nextjs-app && pnpm exec tsc --noEmit 2>&1 | grep -E "ApprovalCard|AgentChat" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/nextjs-app/src/features/app/blocks/agent-chat/ApprovalCard.tsx` exists and exports `ApprovalCard` as a default or named React component.
    - `grep -v '^#' apps/nextjs-app/src/features/app/blocks/agent-chat/ApprovalCard.tsx | grep -cE "'accept'|'reject'"` returns at least `2`.
    - `grep -v '^#' apps/nextjs-app/src/features/app/blocks/agent-chat/AgentChat.tsx | grep -c "ApprovalCard"` returns at least `1` (component imported and rendered).
    - `grep -v '^#' apps/nextjs-app/src/features/app/blocks/agent-chat/AgentChat.tsx | grep -c "waiting_for_approval"` returns at least `1` (status branch).
    - Typecheck output contains no NEW errors in these two files versus the baseline.
  </acceptance_criteria>
  <done>Approval card renders in the chat when status is `waiting_for_approval`; clicking Approve/Reject hits the backend endpoint and the chat refreshes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: UAT — End-to-end HITL flow against the running app</name>
  <what-built>
    Backend (plan 24-05): `request_human_approval` tool, terminate-on-HITL run loop, `POST /approve` endpoint with ownership + status guards, schema migration applied.
    Frontend (this plan): `<ApprovalCard>` rendering when `conversation.status === 'waiting_for_approval'`, react-query mutation hitting the approve endpoint, conversation refresh on resolution.
  </what-built>
  <how-to-verify>
    1. Launch the stack: `pnpm start:local` (web :3000, API :3002). Confirm both `/tmp/teable-backend.log` and `/tmp/teable-frontend.log` show no boot errors.
    2. Configure an agent (or use an existing one) with a prompt instructing it to call `request_human_approval` before performing a destructive action — e.g. "Before deleting any row, call `request_human_approval` with the question 'Confirm deletion of row X?'".
    3. In the agent chat UI, send a message that triggers the approval path. Expected: the message stream pauses, an approval card appears inline with the question text; backend log shows `status=waiting_for_approval` persisted.
    4. Verify via SQL (or Prisma Studio): `SELECT status, approval_payload FROM agent_conversation WHERE id = '<conversationId>'` — `status` is `waiting_for_approval`, `approval_payload` is non-null JSON containing the question.
    5. Click Approve. Expected: card disappears, conversation resumes streaming, agent proceeds with the previously-pending action; final `status` becomes `completed` (or `in_progress` then `completed`).
    6. Repeat from step 3 in a NEW conversation but click Reject with a reason. Expected: conversation closes with `status=failed` and the rejection reason appears as the final assistant message.
    7. Ownership check: with a second user session, try to POST `/approve` against the first user's `waiting_for_approval` conversation (e.g. via browser devtools fetch). Expected: HTTP 403; no state change visible in the DB.
  </how-to-verify>
  <resume-signal>Type "approved" if all 7 steps pass, or describe which step failed and the observed vs expected behavior.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM → ApprovalCard render | `question` and `context` are LLM-controlled strings persisted by the backend; the UI must render them as text only, never as markup. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-06-01 | Cross-site scripting | LLM-controlled `question`/`context` rendered in ApprovalCard | mitigate | React renders strings as text by default and escapes them; the component MUST render these values only as React text children — no raw-HTML injection APIs are used. Verified by static review of `ApprovalCard.tsx`. |
| T-24-06-02 | Tampering | Forged approve request from another user's browser session | mitigate | Inherits backend ownership check from plan 24-05 (T-24-05-01); UAT step 7 exercises the negative path. |
</threat_model>

<verification>
- Frontend typecheck clean for the two touched files.
- UAT checkpoint passes all 7 steps including the negative ownership case.
</verification>

<success_criteria>
ARH-03 fully satisfied end-to-end: UI surfaces approval card on suspended conversations, accept resumes the run, reject terminates the run, ownership check blocks cross-user approvals during live UAT.
</success_criteria>

<output>
Create `.planning/phases/24-agent-runtime-hardening/24-06-SUMMARY.md` when done.
</output>
</content>
</invoke>