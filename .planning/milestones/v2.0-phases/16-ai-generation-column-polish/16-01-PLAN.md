---
phase: 16-ai-generation-column-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/models/field/ai-config/text.ts
  - packages/core/src/models/field/ai-config/single-select.ts
  - packages/core/src/models/field/ai-config/multiple-select.ts
  - packages/core/src/models/field/ai-config/rating.ts
  - packages/core/src/models/field/ai-config/date.ts
  - packages/core/src/models/field/ai-config/attachment.ts
  - packages/core/src/models/field/ai-config/index.ts
  - packages/core/src/models/field/ai-config/__tests__/output-schema.spec.ts
autonomous: true
requirements:
  - AICOL-02-OUTPUT

must_haves:
  truths:
    - "Each of the 6 ai-config files exports an OUTPUT Zod schema distinct from its existing INPUT schema"
    - "Single-select OUTPUT schema rejects values not in configured options"
    - "Multi-select OUTPUT schema rejects arrays containing values not in configured options"
    - "Rating OUTPUT schema rejects non-integer or out-of-range values"
    - "Date OUTPUT schema accepts ISO-8601 and epoch-ms; rejects other strings"
    - "Attachment OUTPUT schema validates the attachment-meta shape"
    - "A factory `getAiOutputSchema(field)` returns the correct schema by FieldType + options/max"
  artifacts:
    - path: "packages/core/src/models/field/ai-config/text.ts"
      provides: "textFieldOutputSchema"
      contains: "textFieldOutputSchema"
    - path: "packages/core/src/models/field/ai-config/single-select.ts"
      provides: "buildSingleSelectFieldOutputSchema(choices)"
      contains: "buildSingleSelectFieldOutputSchema"
    - path: "packages/core/src/models/field/ai-config/multiple-select.ts"
      provides: "buildMultipleSelectFieldOutputSchema(choices)"
    - path: "packages/core/src/models/field/ai-config/rating.ts"
      provides: "buildRatingFieldOutputSchema(max)"
    - path: "packages/core/src/models/field/ai-config/date.ts"
      provides: "dateFieldOutputSchema"
    - path: "packages/core/src/models/field/ai-config/attachment.ts"
      provides: "attachmentFieldOutputSchema"
    - path: "packages/core/src/models/field/ai-config/index.ts"
      provides: "getAiOutputSchema factory + re-exports"
      contains: "getAiOutputSchema"
    - path: "packages/core/src/models/field/ai-config/__tests__/output-schema.spec.ts"
      provides: "vitest suite covering valid + invalid + edge for all 6 types"
      min_lines: 120
  key_links:
    - from: "packages/core/src/models/field/ai-config/index.ts"
      to: "each ai-config/*.ts OUTPUT schema"
      via: "named re-export + getAiOutputSchema dispatch"
      pattern: "export.*OutputSchema"
---

<objective>
Add per-output-type Zod OUTPUT schemas (distinct from existing INPUT schemas) in
`packages/core/src/models/field/ai-config/*.ts`. Pure type-level + schema work,
zero runtime behavior change. Implements D-16-01 schema half (server-side
validation pipeline is plan 16-02).

Purpose: Give the validation pipeline (16-02) and the regen handler (16-03) a
single source of truth for "what shape must the LLM output be?"
Output: 6 OUTPUT schema exports + 1 factory + 1 vitest suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-ai-generation-column-polish/16-CONTEXT.md
@.planning/phases/17.1-agent-mcp-hardening-live-test-follow-ups/17.1-SUMMARY.md

<interfaces>
Existing input-schema pattern (mirror it for OUTPUT, do not modify it):

From packages/core/src/models/field/ai-config/text.ts:
- export const textFieldAIConfigSchema = z.discriminatedUnion('type', [...])  (INPUT — leave alone)
- export const commonFieldAIConfig = z.object({ modelKey, isAutoFill?, attachPrompt? })  (shared)

From packages/core/src/models/field/ai-config/single-select.ts:
- export const singleSelectFieldAIConfigSchema = z.discriminatedUnion('type', [...])  (INPUT — leave alone)

Existing call site (do NOT change here — 16-02 will):
- apps/nestjs-backend/src/features/field/field-calculate/field-supplement.service.ts:1722 — uses getAiConfigSchema(type) for input validation.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OUTPUT schemas in 6 ai-config files</name>
  <files>packages/core/src/models/field/ai-config/text.ts, packages/core/src/models/field/ai-config/single-select.ts, packages/core/src/models/field/ai-config/multiple-select.ts, packages/core/src/models/field/ai-config/rating.ts, packages/core/src/models/field/ai-config/date.ts, packages/core/src/models/field/ai-config/attachment.ts</files>
  <read_first>
    - packages/core/src/models/field/ai-config/text.ts (full)
    - packages/core/src/models/field/ai-config/single-select.ts (full)
    - packages/core/src/models/field/ai-config/multiple-select.ts
    - packages/core/src/models/field/ai-config/rating.ts
    - packages/core/src/models/field/ai-config/date.ts
    - packages/core/src/models/field/ai-config/attachment.ts
    - packages/core/src/models/field/derivate/ai.field.ts (for FieldType.Ai shape)
    - packages/core/src/models/field/derivate/single-select.field.ts (for IChoice / IChoice.name shape — needed by build* factories)
    - packages/core/src/models/field/derivate/attachment.field.ts (for attachment-meta shape — re-use the existing item schema, do not re-derive)
    - packages/core/src/models/field/derivate/rating.field.ts (for default max)
  </read_first>
  <behavior>
    - text: textFieldOutputSchema = z.string() (passthrough; trim not enforced)
    - single-select: buildSingleSelectFieldOutputSchema(choices: {name:string}[]) returns z.enum([names]) when choices non-empty, else z.string() (degrade gracefully when onlyAllowConfiguredOptions=false / no choices yet)
    - multiple-select: buildMultipleSelectFieldOutputSchema(choices) returns z.array(z.enum([names])).min(0); empty array valid
    - rating: buildRatingFieldOutputSchema(max: number) returns z.number().int().min(1).max(max)
    - date: dateFieldOutputSchema = z.union([z.string().datetime({offset:true}), z.number().int().nonnegative()]) — ISO-8601 (with offset) OR epoch-ms
    - attachment: attachmentFieldOutputSchema = z.array(<re-export existing attachment-item schema>) (NEVER hand-roll the item shape)
    - All exports are pure values (no DI). All schemas exported as named exports.
  </behavior>
  <action>
    Add an OUTPUT schema (or factory function for the choice/max-parametric ones)
    to each of the 6 ai-config files. Place each new export ABOVE the existing
    discriminated union but BELOW the existing input schemas, separated by a
    `// === OUTPUT SCHEMA (per D-16-01) ===` comment banner. Re-use existing
    attachment-item schema by importing from the attachment field derivate module —
    do not redefine the item shape. For single-select / multi-select / rating the
    export is a factory function because options/max are runtime-resolved from the
    field instance. For text / date / attachment a plain schema export is enough.
    Each new export gets a matching `export type I<Name>Output = z.infer<...>`.
    Do NOT add fenced code blocks. Do NOT touch the existing INPUT schemas or
    the existing discriminated unions. Do NOT add any DI/service imports — these
    files are pure core. (per D-16-01)
  </action>
  <verify>
    <automated>pnpm --filter @teable/core build 2>&1 | tail -20; grep -l "OutputSchema\|attachmentFieldOutputSchema" packages/core/src/models/field/ai-config/*.ts | grep -v -c "^$"</automated>
  </verify>
  <done>
    All 6 files contain a named OUTPUT export, @teable/core builds, no INPUT
    schema diffed except for the new lines added.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: getAiOutputSchema factory + vitest suite</name>
  <files>packages/core/src/models/field/ai-config/index.ts, packages/core/src/models/field/ai-config/__tests__/output-schema.spec.ts</files>
  <read_first>
    - packages/core/src/models/field/ai-config/index.ts (full)
    - packages/core/src/models/field/ai-config/text.ts (after task 1)
    - packages/core/src/models/field/ai-config/single-select.ts (after task 1)
    - packages/core/src/models/field/derivate/ai.field.ts
    - packages/core/src/models/field/constant.ts (FieldType enum)
  </read_first>
  <behavior>
    - getAiOutputSchema(field: { type: FieldType, options?: any }) returns a Zod schema
    - For FieldType.SingleSelect: dispatches to buildSingleSelectFieldOutputSchema(field.options.choices)
    - For FieldType.MultipleSelect: dispatches to buildMultipleSelectFieldOutputSchema(field.options.choices)
    - For FieldType.Rating: dispatches to buildRatingFieldOutputSchema(field.options?.max ?? 5)
    - For FieldType.Date: returns dateFieldOutputSchema
    - For FieldType.Attachment: returns attachmentFieldOutputSchema
    - For everything else (text-like): returns textFieldOutputSchema
    - Test suite covers (minimum): 1 valid + 1 invalid + 1 edge for each of the 6 output types
    - Tests bind to the REAL Zod schema via schema.safeParse — never use stubbed/mocked schemas (mock-shape-drift mitigation per 17.1)
  </behavior>
  <action>
    Re-export all 6 OUTPUT schemas/factories from index.ts. Add getAiOutputSchema
    factory in index.ts dispatching by FieldType (default branch = textFieldOutputSchema).
    Create the vitest spec under __tests__/output-schema.spec.ts with one describe
    block per output type. For each: a valid example .safeParse(...).success===true,
    an invalid example .safeParse(...).success===false, and one edge case
    (single-select with empty choices array → degrades to z.string(); rating with
    max=10; date as epoch-ms 1700000000000; attachment with 0 items). The spec
    MUST call the real schema imports (no jest.mock of the schema module). (per D-16-01)
  </action>
  <verify>
    <automated>pnpm --filter @teable/core vitest run packages/core/src/models/field/ai-config/__tests__/output-schema.spec.ts</automated>
  </verify>
  <done>
    Vitest suite green; getAiOutputSchema exported; all 6 OUTPUT schemas
    re-exported from index; no test uses jest.mock on the schema module.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM → server | LLM-generated text crosses into typed cell write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering | LLM output → typed cell | mitigate | OUTPUT Zod schemas (this plan) + pipeline parse (16-02) reject malformed values |
| T-16-02 | Information disclosure | Zod error messages | accept | Schema parse errors echoed only to authenticated caller (no PII in schema) |
</threat_model>

<verification>
- `pnpm --filter @teable/core build` clean
- `pnpm --filter @teable/core vitest run packages/core/src/models/field/ai-config/__tests__/output-schema.spec.ts` green
- `grep -c "OutputSchema\|attachmentFieldOutputSchema\|dateFieldOutputSchema\|textFieldOutputSchema" packages/core/src/models/field/ai-config/index.ts` ≥ 6
- No diff against existing input schemas (existing exports unchanged)
</verification>

<success_criteria>
6 OUTPUT schema exports + 1 factory + 1 passing vitest suite. Zero runtime
behavior changes elsewhere. Tests bind to real Zod schemas (no mock shape drift).
</success_criteria>

<output>
Create `.planning/phases/16-ai-generation-column-polish/16-01-SUMMARY.md` when done.
</output>
