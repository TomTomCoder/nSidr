---
phase: 16-ai-generation-column-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/nestjs-backend/src/features/ai/ai.service.ts
  - apps/nestjs-backend/src/features/ai/ai-output-validation.service.ts
  - apps/nestjs-backend/src/features/ai/ai-output-validation.service.spec.ts
  - apps/nestjs-backend/src/features/ai/ai.module.ts
autonomous: true
requirements:
  - AICOL-02-OUTPUT

must_haves:
  truths:
    - "AiService exposes generateForField(baseId, field, prompt) that returns { value, validated, attempts, error? }"
    - "When the configured provider supports structured output, generateObject({schema}) is used and no retry path runs"
    - "When generateText is used, parsed output is validated against getAiOutputSchema(field); on fail, ONE retry with augmented prompt"
    - "On second validation failure, the call returns { value: null, validated: false, attempts: 2, error: <zod msg> } — never throws silently"
    - "All gateway calls go through createGateway in ai.service.ts — no new @ai-sdk/* import elsewhere"
  artifacts:
    - path: "apps/nestjs-backend/src/features/ai/ai-output-validation.service.ts"
      provides: "AiOutputValidationService.validateAndRepair(field, rawText) + AiOutputValidationService.buildRetryPrompt(originalPrompt, zodError, exampleHint)"
      min_lines: 80
    - path: "apps/nestjs-backend/src/features/ai/ai.service.ts"
      provides: "AiService.generateForField method"
      contains: "generateForField"
    - path: "apps/nestjs-backend/src/features/ai/ai-output-validation.service.spec.ts"
      provides: "vitest covering success / retry-then-success / retry-then-fail / structured-output paths"
      min_lines: 140
  key_links:
    - from: "apps/nestjs-backend/src/features/ai/ai.service.ts"
      to: "packages/core/src/models/field/ai-config/index.ts (getAiOutputSchema)"
      via: "import + per-call dispatch"
      pattern: "getAiOutputSchema"
    - from: "apps/nestjs-backend/src/features/ai/ai.module.ts"
      to: "AiOutputValidationService"
      via: "providers array"
      pattern: "AiOutputValidationService"
---

<objective>
Build the server-side validation + retry pipeline (D-16-01 runtime half). Uses
`generateObject({schema})` from the `ai` package when the provider supports
structured output (preferred — constrains LLM at sampling time, zero retry).
Falls back to `generateText` + post-validation + ONE prompt-augmented retry for
unsupported providers. All routed through the existing `AiService` gateway
(D-16-03). NO new direct `@ai-sdk/*` imports outside `apps/nestjs-backend/src/features/ai/`.

Purpose: Plan 16-03's regenerate endpoint and the existing bulk pipeline can
both call one method that guarantees "validated cell value or surfaced error".
Output: AiOutputValidationService + AiService.generateForField + vitest pipeline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-ai-generation-column-polish/16-CONTEXT.md
@.planning/phases/17.1-agent-mcp-hardening-live-test-follow-ups/17.1-SUMMARY.md
@.planning/phases/16-ai-generation-column-polish/16-01-PLAN.md

<interfaces>
From apps/nestjs-backend/src/features/ai/ai.service.ts (existing):
- import { createGateway, generateText, stepCountIs, streamText } from 'ai';
- async generateText(baseId: string, aiGenerateRo: IAiGenerateRo): Promise<string>
- async getGenerationModelInstance(baseId, aiGenerateRo)  // returns model instance for createGateway

For generateObject support, the 'ai' package also exports:
- generateObject({ model, schema, prompt }) — returns { object: T } where T is z.infer<schema>
  See Context7 docs (vercel-ai). Use mcp__plugin_context7_context7__query-docs if unsure of signature.

From packages/core/src/models/field/ai-config/index.ts (after 16-01):
- export function getAiOutputSchema(field): ZodSchema
- export const textFieldOutputSchema, dateFieldOutputSchema, attachmentFieldOutputSchema
- export function buildSingleSelectFieldOutputSchema(choices)
- export function buildMultipleSelectFieldOutputSchema(choices)
- export function buildRatingFieldOutputSchema(max)

Field type used by callers:
- IFieldInstance from apps/nestjs-backend/src/features/field/model/factory.ts (has .type, .options, .aiConfig)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AiOutputValidationService (pure, no IO)</name>
  <files>apps/nestjs-backend/src/features/ai/ai-output-validation.service.ts, apps/nestjs-backend/src/features/ai/ai-output-validation.service.spec.ts, apps/nestjs-backend/src/features/ai/ai.module.ts</files>
  <read_first>
    - apps/nestjs-backend/src/features/ai/ai.module.ts (full)
    - apps/nestjs-backend/src/features/ai/ai.service.ts:1-100 (imports + class header)
    - packages/core/src/models/field/ai-config/index.ts (after 16-01)
    - apps/nestjs-backend/src/features/agent/agent-execution.service.ts (reference: a recently-shipped service in same backend that uses DI properly — DO NOT `import type` for DI per 17.1)
  </read_first>
  <behavior>
    - validateAndRepair(field, rawText) returns { ok: true, value } when getAiOutputSchema(field).safeParse(coerce(rawText, field)) succeeds
    - coerce strips code-fence wrappers (```json ... ```), trims, and JSON.parses if the schema expects an object/array
    - On fail returns { ok: false, error: zodError.message }
    - buildRetryPrompt(originalPrompt, field, zodErrorMsg) returns string starting with "Previous output failed validation: ${zodErrorMsg}. Output must match the schema exactly. Example valid output: ${exampleFor(field)}." then the original prompt
    - exampleFor(field) returns a hand-coded valid example per FieldType (e.g., single-select → first choice name; rating → "3"; date → "2026-01-01T00:00:00Z"; attachment → "[]")
    - Test cases (bind to REAL schemas from @teable/core — no mocked schemas):
      1. text valid → ok:true
      2. single-select valid (member of choices) → ok:true
      3. single-select invalid (not a choice) → ok:false, error contains "enum"
      4. rating valid (3 in 1..5) → ok:true
      5. rating invalid (6 in 1..5) → ok:false
      6. date valid ISO → ok:true; date valid epoch → ok:true
      7. attachment empty array → ok:true
      8. coerce strips ```json wrapper before parsing
      9. buildRetryPrompt contains "Previous output failed validation" and the example
  </behavior>
  <action>
    Create AiOutputValidationService as an @Injectable() NestJS provider (no
    constructor deps — pure). Implement validateAndRepair and buildRetryPrompt
    per behavior block above. Use `import { Injectable } from '@nestjs/common'`
    (value import — never `import type` for DI per 17.1 bug-1). Import
    getAiOutputSchema from `@teable/core` (path same as other ai-config imports
    in this codebase). Add provider to AiModule's providers + exports arrays so
    AiService can inject it. Write the spec under the SAME directory using
    vitest; bind every test to the real schema by importing getAiOutputSchema
    directly — DO NOT vi.mock the schema module (17.1 mock-shape drift). Use a
    small inline IFieldInstance-shaped object literal as test fixture. (per D-16-01)
  </action>
  <verify>
    <automated>pnpm --filter @teable/backend vitest run apps/nestjs-backend/src/features/ai/ai-output-validation.service.spec.ts</automated>
  </verify>
  <done>
    Spec green; service registered in AiModule providers + exports; zero
    `import type` on injectable; zero vi.mock of the schema module (grep gate).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AiService.generateForField (gateway entry for typed cell gen)</name>
  <files>apps/nestjs-backend/src/features/ai/ai.service.ts, apps/nestjs-backend/src/features/ai/ai.service.spec.ts</files>
  <read_first>
    - apps/nestjs-backend/src/features/ai/ai.service.ts (full — at least lines 1-250 and 510-560)
    - apps/nestjs-backend/src/features/ai/ai.service.spec.ts (full — for existing mock harness)
    - apps/nestjs-backend/src/features/ai/ai-output-validation.service.ts (after task 1)
    - context7: vercel-ai package — query "generateObject schema usage" + "provider structured output support"
  </read_first>
  <behavior>
    - Signature: async generateForField(baseId: string, field: IFieldInstance, prompt: string): Promise<{ value: unknown; validated: boolean; attempts: 1|2; error?: string }>
    - Resolve the model instance via getGenerationModelInstance (existing)
    - If provider supports structured output (capability check via model metadata OR an allow-list of provider names: openai, anthropic, google for now), call generateObject({ model, schema: getAiOutputSchema(field), prompt }) → return { value: result.object, validated: true, attempts: 1 }
    - Else: generateText({ model, prompt }) → validationService.validateAndRepair(field, text)
      - on ok → { value, validated: true, attempts: 1 }
      - on fail → second call with validationService.buildRetryPrompt(prompt, field, err); validateAndRepair again
        - on ok → { value, validated: true, attempts: 2 }
        - on fail → { value: null, validated: false, attempts: 2, error: <msg> }
    - NEVER throws on validation failure — always returns the result object so callers can surface to UI
    - Logs via this.logger.warn on retry; this.logger.error on final fail
    - Tests cover: structured-output path (generateObject mocked at the `ai` module boundary), text-then-validate success, retry-then-success, retry-then-fail
  </behavior>
  <action>
    Inject AiOutputValidationService into AiService constructor (value import
    from './ai-output-validation.service' — NOT `import type`). Add the
    generateForField method on AiService below the existing generateText
    method (~line 528). Import generateObject from 'ai' alongside the existing
    `generateText`/`createGateway` imports (this is the ONLY file that may
    import from `ai` — per D-16-03). Provider-support check: implement
    `private isStructuredOutputProvider(modelInstance): boolean` — for now,
    read the model's provider id and return true for openai/anthropic/google,
    false otherwise (this is the discretion choice per CONTEXT line 99-100).
    Extend ai.service.spec.ts with a new describe('generateForField') block.
    Mock the `ai` module's `generateText` and `generateObject` exports via
    vi.mock at the top — but bind validation through the REAL
    AiOutputValidationService instance (not a mocked one) so output-shape
    drift breaks tests (17.1 lesson). Include all 4 path tests. NO direct
    `@ai-sdk/*` import — only the `ai` package. (per D-16-01, D-16-03)
  </action>
  <verify>
    <automated>pnpm --filter @teable/backend vitest run apps/nestjs-backend/src/features/ai/ai.service.spec.ts -t "generateForField" && grep -rn "from '@ai-sdk/" apps/nestjs-backend/src --include="*.ts" | grep -v "^Binary" | grep -v -c "^$"</automated>
  </verify>
  <done>
    All 4 generateForField tests pass; grep for `@ai-sdk/` outside
    apps/nestjs-backend/src/features/ai/ returns nothing; AiService method
    signature matches contract above so plan 16-03 can call it directly.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM → typed cell write | LLM-generated content crosses into typed storage |
| Caller → AiService | Internal backend boundary; callers trust gateway |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03 | Tampering | LLM output → cell value | mitigate | generateObject(schema) at sampling time OR post-validation + 1 retry + surface error (this plan) |
| T-16-04 | Denial of service | Infinite retry on malformed LLM | mitigate | Exactly 1 retry then surface; no recursion |
| T-16-05 | Repudiation | Silent fallback on validation fail | mitigate | Return { validated:false, error } — caller MUST surface; logger.error on final fail |
</threat_model>

<verification>
- `pnpm --filter @teable/backend vitest run apps/nestjs-backend/src/features/ai/ai-output-validation.service.spec.ts` green
- `pnpm --filter @teable/backend vitest run apps/nestjs-backend/src/features/ai/ai.service.spec.ts -t "generateForField"` green
- `grep -rn "from '@ai-sdk/" apps/nestjs-backend/src --include="*.ts" | grep -v "/features/ai/"` empty
- `grep -n "import type.*AiOutputValidationService\|import type.*AiService" apps/nestjs-backend/src/features/ai/*.ts` empty (DI value imports — 17.1)
- `grep -c "vi.mock.*ai-config" apps/nestjs-backend/src/features/ai/*.spec.ts` == 0 (real schemas only — 17.1 mock-shape drift)
</verification>

<success_criteria>
generateForField returns the contract object on all 4 paths (structured-success,
text-success, retry-success, retry-fail). Validation pipeline never throws on
schema fail — always surfaces. Zero new @ai-sdk imports anywhere.
</success_criteria>

<output>
Create `.planning/phases/16-ai-generation-column-polish/16-02-SUMMARY.md` when done.
</output>
