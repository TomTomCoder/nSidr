---
phase: 22-automations-rpa-agent-surface
plan: 05
type: execute
wave: 4
depends_on: [22-04]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/22-automations-rpa-agent-surface/22-SUMMARY.md
autonomous: false
requirements: [AUTO-01, AUTO-02]
user_setup:
  - service: dev-boot
    why: "Live MCP smoke against a running backend"
    dashboard_config:
      - task: "Start dev backend (pnpm dev:swc) and confirm port 3001 accepts MCP requests"
        location: "Local shell"
must_haves:
  truths:
    - "Live dev:swc boot succeeds with no DI errors after WorkflowModule injection into agent module"
    - "MCP tools/list against running server returns 23 tools incl. all 3 new workflow tools by name (live-UAT smoke per critical_constraint 6)"
    - "MCP tools/call list_workflows returns real workflow records for the agent's base"
    - "MCP tools/call run_workflow on a real workflow returns the executor's step result envelope"
    - "REQUIREMENTS.md AUTO-01 + AUTO-02 marked ✅ with citation to this phase"
    - "22-SUMMARY.md records: artifacts, cumulative lessons, any open gaps (e.g. run_workflow input pass-through if testRunWorkflow doesn't yet consume it)"
  artifacts:
    - path: .planning/phases/22-automations-rpa-agent-surface/22-SUMMARY.md
      provides: "Phase 22 summary with UAT evidence + lessons"
    - path: .planning/REQUIREMENTS.md
      provides: "AUTO-01 + AUTO-02 marked complete"
  key_links:
    - from: 22-SUMMARY.md
      to: REQUIREMENTS.md
      via: AUTO-01/AUTO-02 status update
      pattern: "AUTO-0[12].*✅"
---

<objective>
Run live MCP UAT against a running dev backend to catch any DI / boot / wiring bug invisible to unit + integration specs, then mark requirements complete and write the phase summary.

Purpose: Every prior phase has had ≥1 bug invisible to unit tests (CONTEXT anti-patterns list). Critical_constraint 6 mandates live-boot smoke as the closing gate.

Output: UAT evidence in SUMMARY, requirements updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-automations-rpa-agent-surface/22-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-knowledge-graph-write-linking/21-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Live MCP UAT — 9-step smoke script</name>
  <what-built>
    Phase 22 wires 3 workflow tools (list_workflows, get_workflow, run_workflow) into the agent MCP surface. This task runs them against a live backend.
  </what-built>
  <how-to-verify>
    1. Start backend: `cd apps/nestjs-backend && pnpm dev:swc` — confirm boot succeeds with NO Nest DI errors mentioning WorkflowService or WorkflowExecutorService
    2. Confirm at least one workflow exists in a base the test agent belongs to (create via UI if needed; record baseId + workflowId)
    3. MCP `tools/list` for the test agent — confirm response contains exactly 23 tools and includes `list_workflows`, `get_workflow`, `run_workflow`
    4. MCP `tools/call list_workflows` with empty args — confirm response is an array of workflows scoped to the agent's base; assert at least one entry returned
    5. MCP `tools/call list_workflows` with `{baseId: "<some-other-base>"}` — confirm response IGNORES the input baseId and still returns the agent's base (T-21-16 defense check)
    6. MCP `tools/call get_workflow` with `{workflowId: "<real-id-in-agent-base>"}` — confirm full record with `config` returned
    7. MCP `tools/call get_workflow` with `{workflowId: "<id-from-OTHER-base>"}` — confirm typed error envelope (not data)
    8. MCP `tools/call run_workflow` with `{workflowId: "<real-id>"}` — confirm response contains `status`, `trigger`, `steps` fields
    9. MCP `tools/call run_workflow` with `{workflowId: "<id-from-OTHER-base>"}` — confirm typed error envelope
    Record JSON-RPC request + response for each step into 22-SUMMARY.md.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any failed step.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md (AUTO-01, AUTO-02) + write 22-SUMMARY.md</name>
  <files>.planning/REQUIREMENTS.md, .planning/phases/22-automations-rpa-agent-surface/22-SUMMARY.md</files>
  <action>
    Open REQUIREMENTS.md. If no "Automations (AUTO)" section exists, add one. Add or update AUTO-01 (list+read workflows via MCP) and AUTO-02 (trigger workflows with args via MCP), both ✅ with reference to Phase 22 + UAT evidence file. Write 22-SUMMARY.md following the template: objective, artifacts list (5 plans + key files), UAT evidence captured in Task 1, cumulative lessons (extend the Phase 21 list — e.g. RBAC must hold at service layer when bypassing controller, run_workflow `input` pass-through gap if testRunWorkflow does not yet consume it). Note any deferred items (workflow CRUD via agent, streaming run output, per-agent allow-list) — these match D-22-CONTEXT deferred_ideas.
  </action>
  <verify>
    <automated>grep -cE "AUTO-0[12].*✅" .planning/REQUIREMENTS.md  # MUST be >= 2</automated>
    <automated>test -f .planning/phases/22-automations-rpa-agent-surface/22-SUMMARY.md && echo OK</automated>
  </verify>
  <done>Both grep gates pass; SUMMARY contains UAT evidence + lessons + deferred-items note.</done>
</task>

</tasks>

<verification>
- Live boot succeeds
- All 9 UAT steps pass
- Requirements + summary committed
</verification>

<success_criteria>
- AUTO-01 + AUTO-02 marked ✅
- 22-SUMMARY.md exists with UAT evidence
- Phase 22 ready to commit
</success_criteria>

<output>
Create `.planning/phases/22-automations-rpa-agent-surface/22-05-SUMMARY.md` when done (in addition to the phase-level 22-SUMMARY.md)
</output>
