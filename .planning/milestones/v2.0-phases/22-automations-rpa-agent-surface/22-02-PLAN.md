---
phase: 22-automations-rpa-agent-surface
plan: 02
type: execute
wave: 2
depends_on: [22-01]
files_modified:
  - apps/nestjs-backend/src/features/agent/mcp/workflow-tools.ts
  - apps/nestjs-backend/src/features/agent/agent-tool-registry.service.ts
  - apps/nestjs-backend/src/features/agent/agent-tool-registry.service.spec.ts
autonomous: true
requirements: [AUTO-01, AUTO-02]
must_haves:
  truths:
    - "WORKFLOW_TOOLS constant exists with 3 entries: list_workflows, get_workflow, run_workflow"
    - "Each entry uses AI-SDK wrapper shape {type:'function', function:{name, description, parameters}} — NEVER {name, description, inputSchema} (Phase 17.1 bug-3 mitigation)"
    - "agent-tool-registry.service merges WORKFLOW_TOOLS with built-ins + interface + KG, producing exactly 23 tools"
    - "list_workflows schema has no baseId param (resolved from agent.baseId per T-21-16); get_workflow accepts workflowId; run_workflow accepts workflowId + optional input object"
  artifacts:
    - path: apps/nestjs-backend/src/features/agent/mcp/workflow-tools.ts
      provides: "WORKFLOW_TOOLS constant with 3 AI-SDK-shape tool descriptors"
      contains: "WORKFLOW_TOOLS"
    - path: apps/nestjs-backend/src/features/agent/agent-tool-registry.service.ts
      provides: "Registry merges WORKFLOW_TOOLS into total tool list"
  key_links:
    - from: agent-tool-registry.service.ts
      to: workflow-tools.ts
      via: import WORKFLOW_TOOLS
      pattern: "import.*WORKFLOW_TOOLS"
---

<objective>
Define the 3 new MCP tool descriptors (list_workflows, get_workflow, run_workflow) per D-22-01 and register them in agent-tool-registry.service.ts following the Phase 21 KG_TOOLS pattern.

Purpose: Establish the contract surface agents see before wiring dispatch in plan 22-03. Interface-first ordering.

Output: WORKFLOW_TOOLS constant + registry merge + shape gates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-automations-rpa-agent-surface/22-CONTEXT.md
@.planning/phases/21-knowledge-graph-write-linking/21-SUMMARY.md
@apps/nestjs-backend/src/features/agent/agent-tool-registry.service.ts
@apps/nestjs-backend/src/features/agent/mcp/kg-tools.ts

<interfaces>
Required AI-SDK shape (Phase 17.1 lesson, critical_constraint 2):
```
{
  type: 'function',
  function: {
    name: 'tool_name',
    description: '...',
    parameters: { type: 'object', properties: {...}, required: [...] }
  }
}
```
NEVER `{name, description, inputSchema}` at top level.

Tool schemas per D-22-01:
- list_workflows: parameters {type:'object', properties:{}, required:[]} — NO baseId (resolved from agent.baseId; T-21-16)
- get_workflow: parameters with `workflowId: {type:'string'}`, required:['workflowId']
- run_workflow: parameters with `workflowId: {type:'string'}` required, `input: {type:'object', additionalProperties:true}` optional
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create WORKFLOW_TOOLS constant in mcp/workflow-tools.ts</name>
  <files>apps/nestjs-backend/src/features/agent/mcp/workflow-tools.ts</files>
  <behavior>
    - Exports `WORKFLOW_TOOLS` array of length 3
    - Each entry shape: {type:'function', function:{name, description, parameters}}
    - No entry contains top-level `inputSchema`
    - Tool names: list_workflows, get_workflow, run_workflow
  </behavior>
  <action>
    Mirror `kg-tools.ts` from Phase 21. Define `WORKFLOW_TOOLS` as a typed array with 3 entries using the AI-SDK shape (critical_constraint 2). Schemas per D-22-01: list_workflows has empty params (baseId comes from agent context per T-21-16, never from input — critical_constraint 3). get_workflow requires workflowId string. run_workflow requires workflowId string and accepts optional `input` as an object with additionalProperties:true.
  </action>
  <verify>
    <automated>grep -c "type: 'function'" apps/nestjs-backend/src/features/agent/mcp/workflow-tools.ts  # MUST be 3</automated>
    <automated>grep -v '^#' apps/nestjs-backend/src/features/agent/mcp/workflow-tools.ts | grep -c "inputSchema"  # MUST be 0</automated>
    <automated>grep -v '^#' apps/nestjs-backend/src/features/agent/mcp/workflow-tools.ts | grep -cE "baseId.*type.*string"  # MUST be 0 (list_workflows must NOT accept baseId)</automated>
  </verify>
  <done>3 entries, AI-SDK shape, no inputSchema, no baseId param.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Merge WORKFLOW_TOOLS into agent-tool-registry.service</name>
  <files>apps/nestjs-backend/src/features/agent/agent-tool-registry.service.ts, apps/nestjs-backend/src/features/agent/agent-tool-registry.service.spec.ts</files>
  <behavior>
    - Registry returns 23 tools total (20 prior + 3 new workflow)
    - All 3 workflow tool names present in the returned list
    - Tool descriptor shapes pass the same {type:'function', function:{...}} assertion used for KG tools
  </behavior>
  <action>
    Import `WORKFLOW_TOOLS` from `./mcp/workflow-tools` (value import). Concatenate into the existing merged tools list alongside KG_TOOLS / interface tools / built-ins. Update or add a spec that asserts: (a) total count is 23, (b) the 3 new tool names appear, (c) shape gate via JSON.stringify-includes for `"type":"function"`.
  </action>
  <verify>
    <automated>cd apps/nestjs-backend && pnpm vitest run src/features/agent/agent-tool-registry.service.spec.ts</automated>
    <automated>grep -E "from.*@ai-sdk" apps/nestjs-backend/src/features/agent/mcp/workflow-tools.ts apps/nestjs-backend/src/features/agent/agent-tool-registry.service.ts | grep -v '^#' | wc -l  # MUST be 0 — critical_constraint 5</automated>
  </verify>
  <done>Spec green; registry exposes 23 tools incl. all 3 new workflow tools by name.</done>
</task>

</tasks>

<verification>
- AI-SDK wrapper shape enforced
- No inputSchema regressions
- No new @ai-sdk imports outside features/ai/
</verification>

<success_criteria>
- 3 grep gates pass
- Registry spec green with 23-tool assertion
</success_criteria>

<output>
Create `.planning/phases/22-automations-rpa-agent-surface/22-02-SUMMARY.md` when done
</output>
