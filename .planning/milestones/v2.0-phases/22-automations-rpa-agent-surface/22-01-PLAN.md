---
phase: 22-automations-rpa-agent-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/nestjs-backend/src/features/workflow/workflow.service.ts
  - apps/nestjs-backend/src/features/workflow/workflow.module.ts
  - apps/nestjs-backend/src/features/workflow/workflow.service.spec.ts
autonomous: true
requirements: [AUTO-01, AUTO-02]
must_haves:
  truths:
    - "WorkflowModule exports WorkflowService and WorkflowExecutorService so the agent module can inject them"
    - "WorkflowService.findMany / findOne / testRunWorkflow each enforce base|read or base|update via PermissionService at the service entry-point (not only at the controller)"
    - "Cross-base reads (workflowId from baseId B passed under baseId A context) reject with a permission error before touching the executor"
  artifacts:
    - path: apps/nestjs-backend/src/features/workflow/workflow.service.ts
      provides: "Service-level RBAC checks for findMany/findOne/testRunWorkflow"
      contains: "PermissionService"
    - path: apps/nestjs-backend/src/features/workflow/workflow.module.ts
      provides: "Module exports for WorkflowService + WorkflowExecutorService"
      contains: "exports:"
    - path: apps/nestjs-backend/src/features/workflow/workflow.service.spec.ts
      provides: "RBAC enforcement specs"
  key_links:
    - from: workflow.service.ts
      to: PermissionService
      via: constructor injection
      pattern: "permissionService\\.(checkPermissionByBaseId|...)"
---

<objective>
Audit and harden WorkflowService so the agent path cannot bypass the RBAC guards the REST controller enforces (D-22-02). Verify WorkflowModule exports both services so the agent module can inject them (D-22-03).

Purpose: Today `WorkflowController` uses `@Permissions('base|read' | 'base|update')` decorators, but `WorkflowService` methods perform no permission checks. Phase 22 will inject WorkflowService into agent-execution.service.ts; if we don't add service-level checks, the agent path bypasses RBAC entirely.

Output: WorkflowService methods enforce permissions inline; module exports verified; tests confirm enforcement and cross-base rejection.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-automations-rpa-agent-surface/22-CONTEXT.md
@apps/nestjs-backend/src/features/workflow/workflow.service.ts
@apps/nestjs-backend/src/features/workflow/workflow.controller.ts
@apps/nestjs-backend/src/features/workflow/workflow.module.ts

<interfaces>
Existing WorkflowService signatures (verified):
- `findMany(baseId: string): Promise<IWorkflowVo[]>`
- `findOne(baseId: string, workflowId: string): Promise<IWorkflowVo | null>`
- `testRunWorkflow(baseId: string, workflowId: string): Promise<{status, trigger, steps}>`

Controller decorator mapping (must mirror at service level):
- `findMany` ← `@Permissions('base|read')`
- `findOne` ← `@Permissions('base|read')`
- `testRunWorkflow` ← `@Permissions('base|update')`

PermissionService is the canonical RBAC enforcer in this codebase. Locate it via:
`grep -rn "class PermissionService" apps/nestjs-backend/src` and use its base-scoped check method (e.g. `checkPermissionByBaseId(baseId, ['base|read'])`) per D-22-02.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add service-level RBAC to WorkflowService.findMany/findOne/testRunWorkflow per D-22-02</name>
  <files>apps/nestjs-backend/src/features/workflow/workflow.service.ts, apps/nestjs-backend/src/features/workflow/workflow.service.spec.ts</files>
  <behavior>
    - Test 1: findMany(baseId) calls permissionService with ['base|read'] on baseId before reading
    - Test 2: findOne(baseId, workflowId) calls permissionService with ['base|read'] on baseId; throws ForbiddenException when check rejects
    - Test 3: testRunWorkflow(baseId, workflowId) calls permissionService with ['base|update'] on baseId; throws when check rejects
    - Test 4: Cross-base attempt — findOne(baseA, workflowFromBaseB) returns null (existing where-clause) AND permission check still runs against baseA first
  </behavior>
  <action>
    Inject PermissionService into WorkflowService constructor (value import — NO `import type`, per critical_constraint 1). At the entry of each of findMany / findOne / testRunWorkflow, call `permissionService.checkPermissionByBaseId(baseId, [...])` with the matching action verb from the controller (mirror the existing `@Permissions(...)` decorators exactly). Do NOT change createWorkflow / updateWorkflow / deleteWorkflow in this plan — agents don't write workflows per D-22-CONTEXT phase_boundary. Keep the existing where-clause filtering on baseId intact (defense in depth).
  </action>
  <verify>
    <automated>cd apps/nestjs-backend && pnpm vitest run src/features/workflow/workflow.service.spec.ts</automated>
    <automated>grep -n "^import type.*PermissionService\|^import type.*WorkflowService" apps/nestjs-backend/src/features/workflow/workflow.service.ts | grep -v '^#' | wc -l  # MUST be 0</automated>
  </verify>
  <done>All 4 specs green; grep gate returns 0 type-only DI imports; controller decorators unchanged (defense in depth retained at both layers).</done>
</task>

<task type="auto">
  <name>Task 2: Verify WorkflowModule exports both services per D-22-03</name>
  <files>apps/nestjs-backend/src/features/workflow/workflow.module.ts</files>
  <action>
    Open workflow.module.ts. Verify the `exports:` array contains BOTH `WorkflowService` and `WorkflowExecutorService`. (Pre-audit confirms both are already exported — this task is the verification gate; if a future merge dropped one, restore.) Also verify PermissionService dependency is available — if PermissionService lives in another module, add that module to `imports:` here. Run a 1-line grep gate to assert the exports.
  </action>
  <verify>
    <automated>grep -E "exports:.*WorkflowService.*WorkflowExecutorService|exports:.*WorkflowExecutorService.*WorkflowService" apps/nestjs-backend/src/features/workflow/workflow.module.ts | grep -v '^#' | wc -l  # MUST be >= 1</automated>
    <automated>cd apps/nestjs-backend && pnpm vitest run src/features/workflow/workflow.service.spec.ts</automated>
  </verify>
  <done>Module exports both services; PermissionService is resolvable in WorkflowService (no Nest DI errors when specs run).</done>
</task>

</tasks>

<verification>
- WorkflowService methods enforce same RBAC as controller decorators
- Module exports unchanged or restored
- No new circular dependencies (workflow does not import from features/agent)
</verification>

<success_criteria>
- 4 RBAC specs green
- 2 grep gates pass (no type-only DI imports; both services exported)
- WorkflowService importable from agent module in Plan 22-03 without DI errors
</success_criteria>

<output>
Create `.planning/phases/22-automations-rpa-agent-surface/22-01-SUMMARY.md` when done
</output>
