---
phase: 22-automations-rpa-agent-surface
plan: 03
type: execute
wave: 2
depends_on: [22-01]
files_modified:
  - apps/nestjs-backend/src/features/agent/agent-execution.service.ts
  - apps/nestjs-backend/src/features/agent/agent.module.ts
  - apps/nestjs-backend/src/features/agent/agent-execution.service.spec.ts
autonomous: true
requirements: [AUTO-01, AUTO-02]
must_haves:
  truths:
    - "executeToolCall handles list_workflows, get_workflow, run_workflow"
    - "Every dispatcher resolves baseId from agent.baseId — never from toolCall.input (T-21-16)"
    - "Cross-base requests (workflowId not in agent's base) return a typed error envelope, not silent success"
    - "Any user-FK column write uses resolveAgentCallerUserId(agent, ctx) — not agent.id (Phase 21 lesson)"
  artifacts:
    - path: apps/nestjs-backend/src/features/agent/agent-execution.service.ts
      provides: "3 new dispatch cases + WorkflowService / WorkflowExecutorService injection"
      contains: "list_workflows"
  key_links:
    - from: agent-execution.service.ts
      to: WorkflowService
      via: constructor injection (value import)
      pattern: "private.*workflowService.*WorkflowService"
---

<objective>
Inject WorkflowService + WorkflowExecutorService into agent-execution.service.ts and add 3 dispatch cases in executeToolCall (D-22-03).

Purpose: Wire the tool contracts from 22-02 to the hardened service layer from 22-01. This is the agent-to-workflow bridge.

Output: 3 working dispatchers + unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-automations-rpa-agent-surface/22-CONTEXT.md
@.planning/phases/21-knowledge-graph-write-linking/21-SUMMARY.md
@apps/nestjs-backend/src/features/agent/agent-execution.service.ts
@apps/nestjs-backend/src/features/workflow/workflow.service.ts

<interfaces>
Service signatures the dispatchers consume (verified in source):
- `WorkflowService.findMany(baseId): Promise<IWorkflowVo[]>`
- `WorkflowService.findOne(baseId, workflowId): Promise<IWorkflowVo | null>`
- `WorkflowService.testRunWorkflow(baseId, workflowId): Promise<{status, trigger, steps}>`

Agent context helpers (already present per Phase 21):
- `resolveAgentSpaceId(agent, ctx)`
- `resolveAgentCallerUserId(agent, ctx)`
- `agent.baseId` — canonical baseId source (critical_constraint 3)

run_workflow `input` handling: today `testRunWorkflow` ignores per-run input (uses mock trigger data). For v1, accept `input` from toolCall but pass-through; if WorkflowExecutorService.executeSteps later gains an input arg, extend then. Document this gap in the SUMMARY for plan 22-05.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Inject WorkflowService + WorkflowExecutorService and add 3 executeToolCall dispatch cases</name>
  <files>apps/nestjs-backend/src/features/agent/agent-execution.service.ts, apps/nestjs-backend/src/features/agent/agent.module.ts, apps/nestjs-backend/src/features/agent/agent-execution.service.spec.ts</files>
  <behavior>
    - Test 1: list_workflows dispatcher calls workflowService.findMany(agent.baseId) — NEVER toolCall.input.baseId
    - Test 2: get_workflow dispatcher calls findOne(agent.baseId, workflowId); returns typed error envelope when result is null (cross-base or missing)
    - Test 3: run_workflow dispatcher calls testRunWorkflow(agent.baseId, workflowId); returns {status, trigger, steps} envelope
    - Test 4: run_workflow rejects with typed error envelope when workflow is not found in agent's base (cross-base attempt)
    - Test 5: list_workflows ignores any baseId field that sneaks into toolCall.input (defense-in-depth check)
    - Test 6: Dispatcher errors are wrapped as `{error, toolName}` typed result (consistent with KG pattern per D-22-CONTEXT discretion)
    - Test 7 (only if a user-FK write is added in the run path): asserts resolveAgentCallerUserId is used, not agent.id
  </behavior>
  <action>
    Add constructor params `private readonly workflowService: WorkflowService` and `private readonly workflowExecutorService: WorkflowExecutorService` using VALUE imports (critical_constraint 1 — NO `import type`). Update agent.module.ts to import WorkflowModule. In the `executeToolCall` switch, add 3 new cases mirroring the KG dispatcher pattern: each resolves baseId from `agent.baseId` (NEVER `toolCall.input` — critical_constraint 3), calls the corresponding service method, catches errors and returns `{error: String(err.message), toolName}`. For run_workflow, accept `input` from toolCall and pass through (currently testRunWorkflow does not consume it — note this in SUMMARY). If any user-FK column write is required by the run audit path, use `resolveAgentCallerUserId(agent, ctx)` per critical_constraint 7.
  </action>
  <verify>
    <automated>cd apps/nestjs-backend && pnpm vitest run src/features/agent/agent-execution.service.spec.ts</automated>
    <automated>grep -E "^import type.*WorkflowService|^import type.*WorkflowExecutorService" apps/nestjs-backend/src/features/agent/agent-execution.service.ts | grep -v '^#' | wc -l  # MUST be 0</automated>
    <automated>grep -E "toolCall\.input.*baseId|input\.baseId" apps/nestjs-backend/src/features/agent/agent-execution.service.ts | grep -v '^#' | wc -l  # MUST be 0 (T-21-16)</automated>
    <automated>grep -c "agent\.baseId" apps/nestjs-backend/src/features/agent/agent-execution.service.ts  # MUST be >= 3 (one per new dispatcher)</automated>
    <automated>grep -E "from '@ai-sdk" apps/nestjs-backend/src/features/agent/agent-execution.service.ts | grep -v '^#' | wc -l  # MUST be 0 — critical_constraint 5</automated>
  </verify>
  <done>5-7 specs green; all 4 grep gates pass; WorkflowModule wired in agent.module.ts.</done>
</task>

</tasks>

<verification>
- baseId resolved from agent context only
- Value-import DI maintained
- Typed error envelope consistent with KG pattern
- No new @ai-sdk imports outside features/ai/
</verification>

<success_criteria>
- All specs green
- 4 grep gates pass
- Boot does not throw DI errors (validated indirectly via spec; live boot is 22-05)
</success_criteria>

<output>
Create `.planning/phases/22-automations-rpa-agent-surface/22-03-SUMMARY.md` when done
</output>
