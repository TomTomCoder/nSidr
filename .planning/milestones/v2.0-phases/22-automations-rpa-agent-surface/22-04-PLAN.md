---
phase: 22-automations-rpa-agent-surface
plan: 04
type: execute
wave: 3
depends_on: [22-02, 22-03]
files_modified:
  - apps/nestjs-backend/src/features/agent/mcp/workflow-tools.integration.spec.ts
autonomous: true
requirements: [AUTO-01, AUTO-02]
must_haves:
  truths:
    - "End-to-end in-process tool/list returns all 3 workflow tools alongside prior 20"
    - "tool/call list_workflows returns array of workflows scoped to agent.baseId"
    - "tool/call get_workflow returns full workflow record incl. config"
    - "tool/call run_workflow returns {status, trigger, steps} envelope"
    - "Cross-base attempts (workflowId belonging to other base) return typed error, not data"
  artifacts:
    - path: apps/nestjs-backend/src/features/agent/mcp/workflow-tools.integration.spec.ts
      provides: "End-to-end MCP integration suite for the 3 workflow tools"
  key_links:
    - from: workflow-tools.integration.spec.ts
      to: agent-execution.service.ts dispatchers
      via: in-process tool/call invocations
      pattern: "tools\\/call|executeToolCall"
---

<objective>
Add an end-to-end in-process integration spec mirroring `kg-tools.integration.spec.ts` covering all 3 new workflow tools.

Purpose: Unit tests in 22-03 stub services; this spec wires the real dispatcher to a fake WorkflowService/Executor and asserts the full request/response shape that a real MCP client would observe.

Output: workflow-tools.integration.spec.ts with ~6-8 cases.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-automations-rpa-agent-surface/22-CONTEXT.md
@apps/nestjs-backend/src/features/agent/mcp/kg-tools.integration.spec.ts
@apps/nestjs-backend/src/features/agent/agent-execution.service.ts

<interfaces>
Mirror the kg-tools.integration.spec.ts harness: in-memory Prisma stub, fake WorkflowService returning canned workflows for baseId=agent.baseId and empty for other baseIds, fake WorkflowExecutorService.executeSteps returning a deterministic step list. Assert envelope shapes match the dispatcher contract from 22-03.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author workflow-tools.integration.spec.ts (6-8 cases)</name>
  <files>apps/nestjs-backend/src/features/agent/mcp/workflow-tools.integration.spec.ts</files>
  <behavior>
    - Case 1: tools/list returns 23 tools incl. list_workflows, get_workflow, run_workflow
    - Case 2: tools/call list_workflows returns canned workflows for agent.baseId
    - Case 3: tools/call list_workflows ignores `baseId` field in arguments (defense-in-depth; still returns agent's base)
    - Case 4: tools/call get_workflow returns full record for valid workflowId in agent's base
    - Case 5: tools/call get_workflow returns typed error envelope for cross-base workflowId
    - Case 6: tools/call run_workflow returns {status, trigger, steps} envelope
    - Case 7: tools/call run_workflow rejects cross-base workflowId with typed error
    - Case 8 (optional): tools/call run_workflow accepts `input` arg without crashing (even if currently unused downstream)
  </behavior>
  <action>
    Clone the structure of `kg-tools.integration.spec.ts`. Substitute KG fakes with `fakeWorkflowService` / `fakeWorkflowExecutorService`. Build a deterministic fixture: agent with `baseId='base-A'`, 2 workflows in base-A, 1 in base-B. Assert: list returns 2; get on base-B id returns error envelope; run on base-A id returns the executor's canned step result. Run via the same in-process dispatcher entry-point the KG spec uses.
  </action>
  <verify>
    <automated>cd apps/nestjs-backend && pnpm vitest run src/features/agent/mcp/workflow-tools.integration.spec.ts</automated>
  </verify>
  <done>6-8 cases green; cross-base rejection asserted; envelope shapes match dispatcher contract.</done>
</task>

</tasks>

<verification>
- All cases green
- Cross-base attempts rejected with typed error
- 23-tool count assertion holds
</verification>

<success_criteria>
- Integration spec green
- Coverage proves agent path cannot read/run workflows outside agent.baseId
</success_criteria>

<output>
Create `.planning/phases/22-automations-rpa-agent-surface/22-04-SUMMARY.md` when done
</output>
