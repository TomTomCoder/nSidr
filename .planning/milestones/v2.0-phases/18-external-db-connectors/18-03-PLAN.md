---
plan_id: 18-03
phase: 18-external-db-connectors
wave: 2
type: execute
depends_on: [18-02]
requirements: [EXTDB-01]
autonomous: true
files_modified:
  - apps/nestjs-backend/src/features/external-connection/sync/vector-sync.processor.ts
  - apps/nestjs-backend/src/features/external-connection/sync/vector-sync.service.ts
  - apps/nestjs-backend/src/features/external-connection/sync/vector-sync.service.spec.ts
  - apps/nestjs-backend/src/features/external-connection/sync/vector-sync.constants.ts
  - apps/nestjs-backend/src/worker/vector-sync-worker.bootstrap.ts
  - apps/nestjs-backend/src/worker/vector-sync-worker.module.ts
  - apps/nestjs-backend/src/index.ts
must_haves:
  truths:
    - "Importing/reindexing a doc upserts its chunk vectors into the space's Qdrant collection"
    - "Deleting a doc removes its vectors from Qdrant"
    - "A backfill command populates an empty Qdrant collection from existing doc_chunk embeddings"
    - "All sync work runs off the API hot path (separate worker role, BullMQ queue)"
  artifacts:
    - path: apps/nestjs-backend/src/features/external-connection/sync/vector-sync.processor.ts
      provides: "BullMQ processor that performs upsert/delete/backfill against Qdrant"
    - path: apps/nestjs-backend/src/worker/vector-sync-worker.bootstrap.ts
      provides: "TEABLE_ROLE=vector-sync-worker lean bootstrap (mirrors doc-worker)"
  key_links:
    - from: apps/nestjs-backend/src/index.ts
      to: vector-sync-worker.bootstrap.ts
      via: TEABLE_ROLE role-based entry
      pattern: "TEABLE_ROLE === 'vector-sync-worker'"
    - from: vector-sync.service.ts
      to: doc ingest/delete lifecycle
      via: enqueue on doc change
      pattern: "enqueue|add\\(VECTOR_SYNC"
---

<objective>
EXTDB-01 write path. Sync Teable doc embeddings INTO the external Qdrant store so it can back the
knowledge graph at scale (D-03), strictly off the API hot path. A dedicated BullMQ queue + worker
(mirroring the doc-worker role entry) handles initial backfill plus upsert/delete on doc change.
Internal pgvector remains the source of truth.

Purpose: External Qdrant stays current without ever blocking the API or risking the boot-OOM path.
Output: vector-sync queue + processor + worker role + lifecycle enqueue hooks + backfill.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/18-external-db-connectors/18-CONTEXT.md
@apps/nestjs-backend/src/worker/doc-worker.module.ts
@apps/nestjs-backend/src/worker/doc-worker.bootstrap.ts
@apps/nestjs-backend/src/features/doc-search/doc-ingest.processor.ts
@apps/nestjs-backend/src/index.ts

<interfaces>
Verified worker pattern:
  doc-ingest.processor.ts: export const DOC_INGEST_QUEUE='DOC_INGEST';
    @Processor(DOC_INGEST_QUEUE, DOC_INGEST_WORKER_OPTIONS) class DocIngestionProcessor extends WorkerHost
  index.ts: if (process.env.TEABLE_ROLE === 'doc-worker') { await bootstrapDocWorker(); return; }
  doc-worker.module.ts header: run via `TEABLE_ROLE=doc-worker node ... dist/index.js`,
    pair with DOC_INGEST_WORKER_EXTERNAL=true so API stops consuming inline.
  → Mirror ALL of this for a VECTOR_SYNC queue + vector-sync-worker role.
DocChunk (schema.prisma ~1126): embedding Unsupported("vector(1536)"); reads via prisma.$queryRaw.
QdrantConnectorService (18-02): reuse for upsert/delete against the collection.
OAuthIntegrationFieldSync + OAuthSyncDirection (schema ~1013/1056): the existing sync-direction
pattern to model after (EXPORT direction = Teable→external). Note: enum/model are OAuth-prefixed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: VECTOR_SYNC queue + processor (upsert / delete / backfill)</name>
  <files>apps/nestjs-backend/src/features/external-connection/sync/vector-sync.constants.ts, apps/nestjs-backend/src/features/external-connection/sync/vector-sync.processor.ts, apps/nestjs-backend/src/features/external-connection/sync/vector-sync.service.ts, apps/nestjs-backend/src/features/external-connection/sync/vector-sync.service.spec.ts</files>
  <read_first>apps/nestjs-backend/src/features/doc-search/doc-ingest.processor.ts (queue/processor shape, WorkerHost), apps/nestjs-backend/src/features/external-connection/qdrant/qdrant-connector.service.ts (18-02 connector)</read_first>
  <action>Define VECTOR_SYNC_QUEUE + job types (upsert-doc, delete-doc, backfill-space) in
  vector-sync.constants.ts. Build VectorSyncProcessor (extends WorkerHost, @Processor) that, per job,
  reads the relevant doc_chunk embeddings via prisma.$queryRaw and upserts them into the space's
  Qdrant collection (point id = chunkId, payload = docId/content/spaceId), deletes points for a
  removed doc, or backfills all of a space's chunks in batches (configurable batch size + retry).
  VectorSyncService exposes enqueue helpers (enqueueUpsert/enqueueDelete/enqueueBackfill). Model the
  EXPORT (Teable→external) direction on OAuthSyncDirection. Internal pgvector is never mutated here.</action>
  <verify>
    <automated>cd apps/nestjs-backend && pnpm vitest run src/features/external-connection/sync/vector-sync.service.spec.ts</automated>
  </verify>
  <done>Upsert/delete/backfill jobs run against the 18-00 Qdrant; pgvector untouched; batched backfill populates an empty collection.</done>
</task>

<task type="auto">
  <name>Task 2: vector-sync worker role + lifecycle enqueue hooks</name>
  <files>apps/nestjs-backend/src/worker/vector-sync-worker.bootstrap.ts, apps/nestjs-backend/src/worker/vector-sync-worker.module.ts, apps/nestjs-backend/src/index.ts</files>
  <read_first>apps/nestjs-backend/src/worker/doc-worker.bootstrap.ts, apps/nestjs-backend/src/worker/doc-worker.module.ts, apps/nestjs-backend/src/index.ts (TEABLE_ROLE branch)</read_first>
  <action>Add a lean vector-sync worker mirroring doc-worker: vector-sync-worker.module.ts boots only
  config+Prisma+BullMQ+the VectorSyncProcessor; vector-sync-worker.bootstrap.ts is its entry. Extend
  index.ts with `if (process.env.TEABLE_ROLE === 'vector-sync-worker') { ...bootstrap; return; }`.
  Document run command + an EXTERNAL gate flag (so the API enqueues but does not consume inline),
  matching the DOC_INGEST_WORKER_EXTERNAL convention. Wire enqueue calls into the existing doc
  ingest/reindex/delete lifecycle so any doc change fires a VECTOR_SYNC job (only when the space has an
  enabled qdrant connection). All heavy work stays in the worker — the API only enqueues.</action>
  <verify>
    <automated>cd apps/nestjs-backend && pnpm tsc --noEmit 2>&1 | grep -v '^#' | grep -c 'vector-sync' | grep -qx 0</automated>
  </verify>
  <done>`TEABLE_ROLE=vector-sync-worker` boots and consumes VECTOR_SYNC; doc import/delete enqueues jobs; API process does no sync work inline.</done>
</task>

</tasks>

<threat_model>
## Trust boundaries
| Boundary | Description |
|----------|-------------|
| worker → external Qdrant host | Outbound writes to user-configured host (SSRF-gated at 18-01 save) |
| API → worker | Enqueue only; no heavy work on hot path |

## STRIDE register
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-18-03-D | DoS / memory | sync on API process | mitigate | Dedicated worker role + BullMQ; API only enqueues (boot-OOM avoidance) |
| T-18-03-SSRF | Spoofing | Qdrant upsert host | mitigate | Stored config validated at 18-01 save |
| T-18-03-T | Tampering | pgvector corruption | mitigate | Sync is EXPORT-only; internal pgvector never written |
| T-18-03-R | Repudiation | failed syncs lost | accept | BullMQ retry/backoff; pgvector remains source of truth for re-backfill |
</threat_model>

<verification>
Sync spec green; live: doc import upserts to Qdrant, delete removes, backfill populates empty collection; worker isolated.
</verification>

<success_criteria>
Importing a doc upserts its vectors into Qdrant; deleting removes them; backfill populates an empty collection; all off the API hot path.
</success_criteria>

<output>
Create `.planning/phases/18-external-db-connectors/18-03-SUMMARY.md` when done.
</output>
