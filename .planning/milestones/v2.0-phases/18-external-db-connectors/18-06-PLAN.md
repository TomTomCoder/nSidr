---
plan_id: 18-06
phase: 18-external-db-connectors
wave: 4
type: execute
depends_on: [18-02, 18-03, 18-04, 18-05]
requirements: [EXTDB-01, EXTDB-02]
autonomous: false
files_modified:
  - apps/nestjs-backend/src/features/external-connection/__tests__/ssrf-guard.integration.spec.ts
  - apps/nestjs-backend/src/features/external-connection/__tests__/rrf-merge.spec.ts
  - apps/nestjs-backend/src/features/external-connection/__tests__/introspection-statement-filter.spec.ts
  - apps/nestjs-backend/src/features/external-connection/__tests__/vector-sync.spec.ts
  - apps/nestjs-backend/e2e/external-db-connectors.e2e-spec.ts
  - .planning/phases/18-external-db-connectors/18-06-LIVE-VERIFY.md
must_haves:
  truths:
    - "Unit suites cover SSRF guard, RRF merge with an external list, introspection + statement filter, and sync upsert/delete"
    - "An e2e/live run adds a Qdrant + Postgres connection and verifies merged RAG result + browsable external table"
  artifacts:
    - path: apps/nestjs-backend/e2e/external-db-connectors.e2e-spec.ts
      provides: "Live e2e of both success flows"
    - path: .planning/phases/18-external-db-connectors/18-06-LIVE-VERIFY.md
      provides: "Recorded live-verification of the two success flows"
  key_links:
    - from: external-db-connectors.e2e-spec.ts
      to: the 18-00 Qdrant + external Postgres fixtures
      via: real connections added through the 18-01 API
      pattern: "qdrant|external.*postgres"
---

<objective>
Wave 4 verification. Consolidate unit coverage for the four risk-bearing behaviors and run a
live e2e that exercises both EXTDB success flows end to end against the 18-00 fixtures.

Purpose: Prove the phase against the running app, with SSRF as the headline security test.
Output: unit specs + e2e spec + recorded live-verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/18-external-db-connectors/18-CONTEXT.md
@apps/nestjs-backend/src/features/external-connection/ssrf-guard.service.ts
@apps/nestjs-backend/src/features/doc-search/search.service.ts
@apps/nestjs-backend/src/features/external-connection/postgres/statement-filter.ts
@apps/nestjs-backend/src/features/external-connection/sync/vector-sync.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unit coverage — SSRF, RRF merge, introspection/statement-filter, sync</name>
  <files>apps/nestjs-backend/src/features/external-connection/__tests__/ssrf-guard.integration.spec.ts, apps/nestjs-backend/src/features/external-connection/__tests__/rrf-merge.spec.ts, apps/nestjs-backend/src/features/external-connection/__tests__/introspection-statement-filter.spec.ts, apps/nestjs-backend/src/features/external-connection/__tests__/vector-sync.spec.ts</files>
  <read_first>ssrf-guard.service.ts (18-01), search.service.ts hybridSearch fuse() (18-02), statement-filter.ts + pg-introspection.service.ts (18-04), vector-sync.service.ts (18-03)</read_first>
  <action>Add focused unit/integration specs that, beyond the per-plan specs, assert the cross-cutting
  behaviors: (a) SSRF guard rejects metadata/loopback/private/link-local + a DNS-rebinding host and
  passes a public/allowlisted host; (b) hybridSearch fuses an external list so an external-only hit
  appears and a shared hit's rank rises; (c) introspection lists tables and the statement filter
  rejects non-SELECT/multi-statement; (d) vector-sync upsert adds points and delete removes them. No
  fenced implementations — name the cases and assert outcomes.</action>
  <verify>
    <automated>cd apps/nestjs-backend && pnpm vitest run src/features/external-connection/__tests__</automated>
  </verify>
  <done>All four unit specs green.</done>
</task>

<task type="auto">
  <name>Task 2: Live e2e — both success flows</name>
  <files>apps/nestjs-backend/e2e/external-db-connectors.e2e-spec.ts</files>
  <read_first>an existing e2e spec under apps/nestjs-backend/e2e for the harness/auth fixture pattern; the 18-00 docker-compose fixtures</read_first>
  <action>With the app running and the 18-00 Qdrant + external Postgres fixtures up, write an e2e that:
  (1) adds a Qdrant and a Postgres ExternalConnection via the 18-01 API (asserting test-on-save and an
  SSRF rejection for 169.254.169.254/localhost); (2) verifies a query returns RRF-merged internal +
  external results; (3) verifies an external Postgres table is browsable read-only through the virtual
  table API and a write/non-SELECT is rejected. Skip gracefully (with a clear message) if the fixtures
  are not reachable so CI without docker does not hard-fail.</action>
  <verify>
    <automated>cd apps/nestjs-backend && pnpm vitest run e2e/external-db-connectors.e2e-spec.ts</automated>
  </verify>
  <done>e2e passes against the live fixtures: connection add/test + SSRF rejection, merged RAG result, read-only external table browse.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Full external-DB connector phase against the running app.</what-built>
  <how-to-verify>
    1. App up (web :3000 / API :3002); 18-00 Qdrant + external Postgres fixtures up.
    2. In a space's settings, add + test a Qdrant and a Postgres connection; confirm SSRF to 169.254.169.254/localhost is rejected.
    3. Run a doc search and confirm external Qdrant hits are merged in.
    4. Import/delete a doc; confirm vectors upsert/remove in Qdrant (off the API hot path).
    5. Browse an external Postgres table; confirm read-only (no edit/add/delete).
    6. Record outcomes in 18-06-LIVE-VERIFY.md.
  </how-to-verify>
  <resume-signal>Type "approved" or list the flows that failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust boundaries
| Boundary | Description |
|----------|-------------|
| test harness → live external hosts | Uses controlled 18-00 fixtures only |

## STRIDE register
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-18-06-SSRF | Spoofing | regression coverage | mitigate | SSRF rejection asserted in both unit + e2e (top-risk gate) |
| T-18-06-T | Tampering | read-only regressions | mitigate | e2e asserts external table write/non-SELECT rejected |
</threat_model>

<verification>
Unit suites green; live e2e green against 18-00 fixtures; live checklist recorded.
</verification>

<success_criteria>
Suites green; live flows verified: merged RAG result; browsable read-only external table; SSRF rejection.
</success_criteria>

<output>
Create `.planning/phases/18-external-db-connectors/18-06-SUMMARY.md` when done.
</output>
