---
plan_id: 18-00
phase: 18-external-db-connectors
wave: 0
type: execute
depends_on: []
requirements: [EXTDB-01, EXTDB-02]
autonomous: false
files_modified:
  - .planning/phases/18-external-db-connectors/18-00-VERIFY.md
  - docker-compose.qdrant.yml
  - docker-compose.ext-pg.yml
must_haves:
  truths:
    - "base-sql-executor.createConnection builds a working PrismaClient at runtime"
    - "Encryptor/getEncryptor encrypts+decrypts round-trip with INTEGRATION_SECRET_KEY"
    - "A local Qdrant (1536-dim collection) and throwaway external Postgres are reachable for the build"
  artifacts:
    - path: .planning/phases/18-external-db-connectors/18-00-VERIFY.md
      provides: "Recorded confirmation of each assumption against the running app"
  key_links:
    - from: apps/nestjs-backend/src/utils/encryptor.ts
      to: process.env.INTEGRATION_SECRET_KEY
      via: getEncryptor algorithm+key
      pattern: "getEncryptor|createCipheriv"
---

<objective>
Wave 0 process gate. Confirm the three reuse assumptions hold against the *running* app
before any build begins, and stand up the live test dependencies (Qdrant + throwaway external
Postgres). No production code ships in this plan.

Purpose: De-risk the whole phase. The outline's reuse claims (createConnection, Encryptor,
RRF) were verified statically during planning; this plan verifies them dynamically.
Output: 18-00-VERIFY.md (assumption log) + two docker-compose files for local deps.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-external-db-connectors/18-CONTEXT.md
@apps/nestjs-backend/src/features/base-sql-executor/base-sql-executor.service.ts
@apps/nestjs-backend/src/utils/encryptor.ts
@apps/nestjs-backend/src/worker/doc-worker.module.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stand up local Qdrant + throwaway external Postgres</name>
  <files>docker-compose.qdrant.yml, docker-compose.ext-pg.yml</files>
  <read_first>apps/nestjs-backend/src/worker/README.md (env/role conventions)</read_first>
  <action>Create docker-compose.qdrant.yml running qdrant/qdrant (REST :6333) and
  docker-compose.ext-pg.yml running a postgres image seeded with a small read sample
  (one schema, two tables, a few rows) for 18-04/18-05 introspection tests. Use distinct
  host ports that do not collide with Teable's own Postgres/Redis. Document the connection
  URLs and the Qdrant REST base URL in each file's header comment. Do NOT wire these into
  the app yet — they are build-time test fixtures only.</action>
  <verify>
    <automated>docker compose -f docker-compose.qdrant.yml config && docker compose -f docker-compose.ext-pg.yml config</automated>
  </verify>
  <done>Both compose files parse; bringing them up exposes a reachable Qdrant REST endpoint and an external Postgres with seed data.</done>
</task>

<task type="auto">
  <name>Task 2: Live-verify createConnection + Encryptor round-trip</name>
  <files>.planning/phases/18-external-db-connectors/18-00-VERIFY.md</files>
  <read_first>apps/nestjs-backend/src/features/base-sql-executor/base-sql-executor.service.ts (createConnection, getReadOnlyDatabaseConnectionConfig), apps/nestjs-backend/src/utils/encryptor.ts (Encryptor, getEncryptor)</read_first>
  <action>With the app booted (API :3002), write a throwaway script/spec that (a) instantiates
  Encryptor via getEncryptor with a 32+ char key and asserts encrypt→decrypt round-trips a
  sample credential object, and (b) confirms BaseSqlExecutorService.createConnection produces a
  PrismaClient that answers SELECT 1. Record results in 18-00-VERIFY.md. CRITICAL FINDING TO
  CONFIRM: createConnection currently derives its URL from getReadOnlyDatabaseConnectionConfig()
  (internal DSN) — note explicitly that 18-04 must parameterize the connection URL for a
  user-supplied external DSN, it does NOT already accept arbitrary hosts.</action>
  <verify>
    <automated>cd apps/nestjs-backend && pnpm vitest run src/utils/encryptor.spec.ts 2>/dev/null || echo "MISSING — create encryptor round-trip spec first"</automated>
  </verify>
  <done>18-00-VERIFY.md records: Encryptor round-trips; createConnection answers SELECT 1; the createConnection-takes-internal-DSN caveat is documented for 18-04.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Local Qdrant + external Postgres fixtures and the assumption log.</what-built>
  <how-to-verify>
    1. `docker compose -f docker-compose.qdrant.yml up -d` and confirm the Qdrant dashboard/REST responds.
    2. `docker compose -f docker-compose.ext-pg.yml up -d` and confirm the seed tables exist.
    3. Read 18-00-VERIFY.md and confirm all three assumptions are marked confirmed.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what failed to confirm.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust boundaries
| Boundary | Description |
|----------|-------------|
| dev machine → docker deps | Local-only fixtures; not exposed publicly |

## STRIDE register
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-18-00-I | Information disclosure | INTEGRATION_SECRET_KEY in test | accept | Use a throwaway key in the spec; never commit real key |
| T-18-00-SC | Tampering | docker images | mitigate | Pin qdrant/qdrant + postgres image tags; both are first-party official images |
</threat_model>

<verification>
All three reuse assumptions confirmed live; local deps reachable.
</verification>

<success_criteria>
Assumptions confirmed against the running app; Qdrant + external Postgres fixtures up; the
createConnection-internal-DSN caveat recorded for 18-04.
</success_criteria>

<output>
Create `.planning/phases/18-external-db-connectors/18-00-SUMMARY.md` when done.
</output>
