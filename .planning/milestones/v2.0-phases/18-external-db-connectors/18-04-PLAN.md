---
plan_id: 18-04
phase: 18-external-db-connectors
wave: 3
type: execute
depends_on: [18-01]
requirements: [EXTDB-02]
autonomous: true
files_modified:
  - apps/nestjs-backend/src/features/external-connection/postgres/external-pg-connector.service.ts
  - apps/nestjs-backend/src/features/external-connection/postgres/external-pg-connector.service.spec.ts
  - apps/nestjs-backend/src/features/external-connection/postgres/pg-introspection.service.ts
  - apps/nestjs-backend/src/features/external-connection/postgres/pg-introspection.service.spec.ts
  - apps/nestjs-backend/src/features/external-connection/postgres/statement-filter.ts
  - apps/nestjs-backend/src/features/external-connection/postgres/statement-filter.spec.ts
must_haves:
  truths:
    - "The connector opens a read-only connection to an external Postgres using stored config"
    - "Any non-SELECT statement is rejected before execution"
    - "Introspection lists external tables with their columns and types"
    - "Introspection results are cached and reused"
  artifacts:
    - path: apps/nestjs-backend/src/features/external-connection/postgres/external-pg-connector.service.ts
      provides: "Read-only external Postgres connection built on base-sql-executor's createConnection pattern"
      exports: ["ExternalPgConnectorService"]
    - path: apps/nestjs-backend/src/features/external-connection/postgres/statement-filter.ts
      provides: "SELECT-only statement guard"
    - path: apps/nestjs-backend/src/features/external-connection/postgres/pg-introspection.service.ts
      provides: "Schema/table/column introspection + cache"
  key_links:
    - from: external-pg-connector.service.ts
      to: apps/nestjs-backend/src/features/base-sql-executor/base-sql-executor.service.ts
      via: PrismaClient datasource-url connection pattern, parameterized for external DSN
      pattern: "new PrismaClient\\(\\{[^}]*datasources"
---

<objective>
EXTDB-02 connector half. Build a READ-ONLY external Postgres connector that reuses the
base-sql-executor `createConnection` pattern but PARAMETERIZES the datasource URL for a
user-supplied DSN (18-00 confirmed the existing method only uses the internal DSN). Enforce
read-only via a read-only role plus a SELECT-only statement filter, and introspect the external
schema (tables/columns/types) with caching.

Purpose: A safe, read-only query surface and schema map that 18-05 maps into virtual tables.
Output: ExternalPgConnectorService + statement filter + introspection service.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/18-external-db-connectors/18-CONTEXT.md
@apps/nestjs-backend/src/features/base-sql-executor/base-sql-executor.service.ts
@apps/nestjs-backend/src/features/external-connection/external-connection.service.ts

<interfaces>
From base-sql-executor.service.ts (verified):
  createConnection() does: const connection = new PrismaClient({ datasources: { db: { url } } });
    await connection.$connect(); await connection.$queryRawUnsafe('SELECT 1'); (validation)
  CAVEAT (from 18-00-VERIFY): `url` comes from getReadOnlyDatabaseConnectionConfig() = INTERNAL DSN.
    For external use you MUST supply the user DSN from the decrypted ExternalConnection config —
    reuse the PrismaClient-with-datasource-url shape, not the internal config getter.
  parseDsn from '@teable/core' is available to parse the external DSN.
  const.ts: BASE_READ_ONLY_ROLE_PREFIX / read-only role grants are the precedent for read-only enforcement.
ExternalConnectionService (18-01): getDecryptedConfig(connectionId) → { host, port, database, user, password } (SSRF-validated at save).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SELECT-only statement filter</name>
  <files>apps/nestjs-backend/src/features/external-connection/postgres/statement-filter.ts, apps/nestjs-backend/src/features/external-connection/postgres/statement-filter.spec.ts</files>
  <read_first>apps/nestjs-backend/src/features/base-sql-executor/const.ts (read-only role precedent)</read_first>
  <behavior>
    - assertSelectOnly('SELECT * FROM t') passes
    - INSERT/UPDATE/DELETE/DROP/ALTER/CREATE/TRUNCATE/GRANT/COPY/CALL reject
    - multi-statement (';' chaining a write) rejects
    - leading comments / whitespace / CTE (WITH ... SELECT) handled: CTE-to-SELECT passes, CTE-to-write rejects
  </behavior>
  <action>Implement assertSelectOnly(sql): normalize (strip comments/whitespace), reject anything that
  is not a single read statement (SELECT or read-only WITH…SELECT), and reject multi-statement input.
  This is defense-in-depth alongside the read-only DB role. No fenced implementation.</action>
  <verify>
    <automated>cd apps/nestjs-backend && pnpm vitest run src/features/external-connection/postgres/statement-filter.spec.ts</automated>
  </verify>
  <done>All write/DDL/multi-statement cases rejected; plain and CTE SELECTs pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Read-only external Postgres connector</name>
  <files>apps/nestjs-backend/src/features/external-connection/postgres/external-pg-connector.service.ts, apps/nestjs-backend/src/features/external-connection/postgres/external-pg-connector.service.spec.ts</files>
  <read_first>apps/nestjs-backend/src/features/base-sql-executor/base-sql-executor.service.ts (createConnection, PrismaClient datasource shape, SELECT 1 validation), apps/nestjs-backend/src/features/external-connection/external-connection.service.ts (decrypted config)</read_first>
  <behavior>
    - connect() builds a PrismaClient against the EXTERNAL user DSN (from decrypted config), validates with SELECT 1
    - every query passes through assertSelectOnly before execution
    - connections are pooled/reused per connectionId and disposable
    - a write attempt is rejected (by filter) even if the role somehow permitted it
  </behavior>
  <action>Build ExternalPgConnectorService that reuses base-sql-executor's
  new-PrismaClient-with-datasource-url pattern but supplies the external DSN from
  ExternalConnectionService decrypted config (parse via parseDsn). Prefer a read-only role in the DSN;
  enforce SELECT-only by routing all queries through assertSelectOnly (Task 1) before
  $queryRawUnsafe. Validate the connection with SELECT 1 on connect. Cache live clients per
  connectionId with disposal. Host already SSRF-validated at 18-01 save.</action>
  <verify>
    <automated>cd apps/nestjs-backend && pnpm vitest run src/features/external-connection/postgres/external-pg-connector.service.spec.ts</automated>
  </verify>
  <done>Connector opens a read-only connection to the 18-00 external Postgres, runs a SELECT, and rejects a non-SELECT.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Schema introspection + cache</name>
  <files>apps/nestjs-backend/src/features/external-connection/postgres/pg-introspection.service.ts, apps/nestjs-backend/src/features/external-connection/postgres/pg-introspection.service.spec.ts</files>
  <read_first>external-pg-connector.service.ts (Task 2 query surface)</read_first>
  <behavior>
    - introspect(connectionId) returns tables with columns (name, data type, nullable, pk) via information_schema
    - results are cached per connectionId; a second call within TTL hits cache (no re-query)
    - invalidate(connectionId) forces a refresh
    - introspection queries are themselves SELECT-only (go through the connector)
  </behavior>
  <action>Implement PgIntrospectionService that queries information_schema.tables/columns (and pk
  constraints) through ExternalPgConnectorService to produce a normalized schema map, with a
  per-connection TTL cache and explicit invalidate(). All queries go through the connector's
  SELECT-only path.</action>
  <verify>
    <automated>cd apps/nestjs-backend && pnpm vitest run src/features/external-connection/postgres/pg-introspection.service.spec.ts</automated>
  </verify>
  <done>Lists the 18-00 external tables + columns + types; second call served from cache.</done>
</task>

</tasks>

<threat_model>
## Trust boundaries
| Boundary | Description |
|----------|-------------|
| API → external Postgres host | Read-only outbound to user-configured host (SSRF-gated at 18-01) |
| query input → external DB | Must never permit writes/DDL |

## STRIDE register
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-18-04-T | Tampering | external DB writes | mitigate | Read-only role + assertSelectOnly statement filter (defense in depth) |
| T-18-04-SSRF | Spoofing | external connect | mitigate | Host validated at 18-01 save |
| T-18-04-I | Information disclosure | DSN/creds | mitigate | Decrypted only server-side from 18-01 store |
| T-18-04-E | Elevation | SQL injection via introspection | mitigate | Parameterized information_schema queries; SELECT-only gate |
</threat_model>

<verification>
Statement-filter, connector, introspection specs green; live read against 18-00 external Postgres; write rejected.
</verification>

<success_criteria>
Connector lists external tables + columns; a non-SELECT statement is rejected; introspection cached.
</success_criteria>

<output>
Create `.planning/phases/18-external-db-connectors/18-04-SUMMARY.md` when done.
</output>
