---
plan_id: 18-02
phase: 18-external-db-connectors
wave: 2
type: execute
depends_on: [18-01]
requirements: [EXTDB-01]
autonomous: true
files_modified:
  - apps/nestjs-backend/package.json
  - apps/nestjs-backend/src/features/external-connection/qdrant/qdrant-connector.service.ts
  - apps/nestjs-backend/src/features/external-connection/qdrant/qdrant-connector.service.spec.ts
  - apps/nestjs-backend/src/features/external-connection/qdrant/vector-connector.interface.ts
  - apps/nestjs-backend/src/features/doc-search/search.service.ts
  - apps/nestjs-backend/src/features/doc-search/search.service.spec.ts
must_haves:
  truths:
    - "A space with an enabled Qdrant connection returns merged (internal + external) results for a query"
    - "Qdrant query-by-embedding returns hits mapped into the DocSearchResult shape"
    - "Connecting to a Qdrant collection whose vector size != 1536 fails fast with a clear error"
  artifacts:
    - path: apps/nestjs-backend/src/features/external-connection/qdrant/qdrant-connector.service.ts
      provides: "Qdrant client wrapper: validate-dim, query-by-embedding"
      exports: ["QdrantConnectorService"]
    - path: apps/nestjs-backend/src/features/external-connection/qdrant/vector-connector.interface.ts
      provides: "IVectorConnector interface so Weaviate/pgvector can be added later (D-01)"
  key_links:
    - from: apps/nestjs-backend/src/features/doc-search/search.service.ts
      to: qdrant-connector.service.ts
      via: third ranked list fed into the existing RRF fuse()
      pattern: "fuse\\(external|QdrantConnector|vectorConnector"
---

<objective>
EXTDB-01 read path. Add the Qdrant client, build a connector that queries by embedding, and
merge its hits into `DocSearchService.hybridSearch` as a THIRD ranked list through the existing
RRF fusion (augment, not replace — D-02). Define an IVectorConnector interface (D-01) so other
backends slot in later.

Purpose: External vectors enrich RAG results without changing the internal pgvector source of truth.
Output: @qdrant/js-client-rest + QdrantConnectorService + RRF merge in hybridSearch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/18-external-db-connectors/18-CONTEXT.md
@apps/nestjs-backend/src/features/doc-search/search.service.ts
@apps/nestjs-backend/src/features/external-connection/external-connection.service.ts

<interfaces>
From doc-search/search.service.ts (verified):
  export interface DocSearchResult { chunkId: string; ...; score: number }
  async hybridSearch(spaceId, query, limit=10, scope?): Promise<DocSearchResult[]>
  Internals: const K=60; const fused=new Map<string,DocSearchResult>();
             const fuse=(list:DocSearchResult[])=>{...}; fuse(semantic); fuse(keyword);
  → Merge point: add `fuse(external)` BEFORE the final sort/slice. Same RRF math, no new fusion path.
  semanticSearch already turns query→1536-dim embedding via embedding.service.ts — reuse that embedding for the Qdrant query.
ExternalConnectionService (18-01): list enabled connections of type=qdrant for a spaceId, returns decrypted config.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>About to add @qdrant/js-client-rest (not yet installed — confirmed by planning).</what-built>
  <how-to-verify>
    1. Confirm legitimacy at https://www.npmjs.com/package/@qdrant/js-client-rest (official Qdrant org, maintained).
    2. Approve adding it to apps/nestjs-backend/package.json.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with install, or name an alternative client.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1: QdrantConnectorService + IVectorConnector interface + dim validation</name>
  <files>apps/nestjs-backend/package.json, apps/nestjs-backend/src/features/external-connection/qdrant/vector-connector.interface.ts, apps/nestjs-backend/src/features/external-connection/qdrant/qdrant-connector.service.ts, apps/nestjs-backend/src/features/external-connection/qdrant/qdrant-connector.service.spec.ts</files>
  <read_first>apps/nestjs-backend/src/features/external-connection/external-connection.service.ts (decrypted-config access), apps/nestjs-backend/src/features/doc-search/embedding.service.ts (1536-dim embedding shape)</read_first>
  <behavior>
    - validateCollection() rejects when the target collection's vector size != 1536
    - queryByEmbedding(embedding, limit) returns hits mapped to {chunkId-equivalent id, content/payload, score}
    - the connector resolves its host through the 18-01 SSRF-guarded config (no direct env host)
    - implements IVectorConnector (search(embedding, limit), validate())
  </behavior>
  <action>Install @qdrant/js-client-rest. Define IVectorConnector in vector-connector.interface.ts
  (validate(), search(embedding:number[], limit):Promise<DocSearchResult[]>). Implement
  QdrantConnectorService using the client to (a) validate the collection vector size == 1536 on
  connect and throw a clear error otherwise, and (b) query by embedding and map payload→DocSearchResult.
  Pull the Qdrant URL/apiKey from ExternalConnectionService decrypted config (already SSRF-gated at
  save). No fenced implementation.</action>
  <verify>
    <automated>cd apps/nestjs-backend && pnpm vitest run src/features/external-connection/qdrant/qdrant-connector.service.spec.ts</automated>
  </verify>
  <done>Connector validates 1536-dim, rejects mismatched collections, and returns mapped hits against the 18-00 local Qdrant.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Merge Qdrant hits into hybridSearch via existing RRF</name>
  <files>apps/nestjs-backend/src/features/doc-search/search.service.ts, apps/nestjs-backend/src/features/doc-search/search.service.spec.ts</files>
  <read_first>apps/nestjs-backend/src/features/doc-search/search.service.ts (hybridSearch: K=60, fuse(), fuse(semantic)/fuse(keyword))</read_first>
  <behavior>
    - when a space has no enabled qdrant connection, hybridSearch behaves exactly as before (two lists)
    - when a space has an enabled qdrant connection, external hits are fused as a THIRD list and can raise an item's rank
    - external connector errors are swallowed (degrade to internal-only), never fail the whole search
  </behavior>
  <action>In hybridSearch, after computing semantic+keyword, if ExternalConnectionService reports an
  enabled qdrant connection for the space, run QdrantConnectorService.search(queryEmbedding, limit)
  (reuse the embedding already produced for semanticSearch) and add `fuse(external)` before the final
  sort/slice — same K=60 RRF math, no new fusion code path. Wrap the external call in try/catch so a
  down external store degrades gracefully to internal-only results. Inject the two new services into
  DocSearchService's module.</action>
  <verify>
    <automated>cd apps/nestjs-backend && pnpm vitest run src/features/doc-search/search.service.spec.ts</automated>
  </verify>
  <done>Merged results returned when a qdrant connection exists; identical-to-before when none; external failure degrades to internal-only.</done>
</task>

</tasks>

<threat_model>
## Trust boundaries
| Boundary | Description |
|----------|-------------|
| API → external Qdrant host | Outbound query to user-configured host (already SSRF-gated at 18-01 save) |

## STRIDE register
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-18-02-SSRF | Spoofing | Qdrant connect | mitigate | Host validated at 18-01 save via SsrfGuard; connector uses stored config only |
| T-18-02-D | DoS | external store down | mitigate | try/catch degrades to internal-only; never blocks search |
| T-18-02-T | Tampering | dim mismatch poisons RAG | mitigate | validateCollection enforces 1536 on connect |
| T-18-02-SC | Tampering | @qdrant/js-client-rest install | mitigate | Blocking legitimacy checkpoint before install |
</threat_model>

<verification>
Connector spec + search spec green; live merge returns enriched results against 18-00 Qdrant.
</verification>

<success_criteria>
A space with a Qdrant connection returns RRF-merged internal+external results; 1536-dim enforced on connect.
</success_criteria>

<output>
Create `.planning/phases/18-external-db-connectors/18-02-SUMMARY.md` when done.
</output>
