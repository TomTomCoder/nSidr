---
plan_id: "17-06"
phase: 17-agent-mcp-enhancement
wave: 3
depends_on: ["17-02", "17-03", "17-04", "17-05"]
requirements: [AGENT-04]
autonomous: false
files_modified:
  - apps/nestjs-backend/src/features/agent/agent-memory.service.spec.ts
  - apps/nestjs-backend/src/features/agent/mcp/mcp-aggregator.integration.spec.ts
  - apps/nestjs-backend/src/features/agent/mcp/rbac-gating.spec.ts
  - apps/nestjs-backend/test/agent-mcp-flow.e2e-spec.ts
must_haves:
  truths:
    - "AgentMemoryService is confirmed wired into the execution loop and scoped per-agent"
    - "Unit tests cover the MCP aggregator, scoped search, and RBAC gating"
    - "One e2e agent flow passes"
    - "An agent recalls a prior-conversation fact"
  artifacts:
    - path: apps/nestjs-backend/test/agent-mcp-flow.e2e-spec.ts
      provides: "end-to-end agent flow exercising MCP tool + memory recall"
  key_links:
    - from: agent-memory.service.spec.ts
      to: agent-execution.service.ts (memoryService.getRecent/saveRecent)
      via: "assert memory load + save are invoked and scoped by agentId"
---

<objective>
Wave 3 (AGENT-04). Verify memory + lock down the phase with tests. Confirm `AgentMemoryService` is wired into the execution loop and scoped per-agent (it already is — see execution.service.ts:121,233 — enhance only on a real gap). Add unit tests for the MCP aggregator, scoped search, and RBAC gating; one e2e for an end-to-end agent flow including memory recall.

DISCREPANCY FLAGGED: 17-CONTEXT D-06 treated memory wiring as "verify it exists". Grounding confirms it IS wired (getRecent/getPreferences @ execution.service.ts:121-122, saveRecent @ 233). So this plan is verification + test coverage, not new memory code, unless a gap surfaces.

Output: memory verification spec, aggregator/scoped-search/RBAC unit specs, one e2e flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-agent-mcp-enhancement/17-CONTEXT.md
@.planning/phases/17-agent-mcp-enhancement/17-02-SUMMARY.md
@.planning/phases/17-agent-mcp-enhancement/17-03-SUMMARY.md
@.planning/phases/17-agent-mcp-enhancement/17-04-SUMMARY.md
@.planning/phases/17-agent-mcp-enhancement/17-05-SUMMARY.md

<interfaces>
- AgentMemoryService getRecent/getPreferences/saveRecent @ agent-memory.service.ts; called in execution.service.ts:121,122,233.
- McpClientAggregatorService (17-02), PluginMcpDiscoveryService (17-03), InterfaceToolsService (17-04), scoped hybridSearch + search_knowledge_base branch (17-05).
- vitest unit config + vitest-e2e.config.ts (apps/nestjs-backend/package.json: test-unit, test-e2e).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Memory verification spec + aggregator/scoped-search/RBAC unit specs</name>
  <files>apps/nestjs-backend/src/features/agent/agent-memory.service.spec.ts, apps/nestjs-backend/src/features/agent/mcp/mcp-aggregator.integration.spec.ts, apps/nestjs-backend/src/features/agent/mcp/rbac-gating.spec.ts</files>
  <read_first>
    apps/nestjs-backend/src/features/agent/agent-memory.service.ts
    apps/nestjs-backend/src/features/agent/agent-execution.service.ts (121-137, 230-233)
    apps/nestjs-backend/src/features/agent/mcp/mcp-client-aggregator.service.ts (17-02)
    apps/nestjs-backend/src/features/agent/mcp/interface-tools.service.ts (17-04)
  </read_first>
  <action>
    Memory spec: assert getRecent/getPreferences are loaded into the system prompt and saveRecent persists per agentId (memory scoped by agentId; another agent's memory not visible). Aggregator integration spec: built-in + MCP + plugin tools merged with correct namespacing; failing server skipped. RBAC spec: interface tools + MCP server tools reject unauthorized identity and separate read vs write permission. If memory is found NOT scoped/wired (gap), patch agent-memory or execution.service and note it.
  </action>
  <verify>
    <automated>cd apps/nestjs-backend && pnpm vitest run src/features/agent/agent-memory.service.spec.ts src/features/agent/mcp/</automated>
  </verify>
  <acceptance_criteria>
    - Memory spec proves per-agent scoping + loop wiring; aggregator + RBAC specs green.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: End-to-end agent MCP + memory flow</name>
  <files>apps/nestjs-backend/test/agent-mcp-flow.e2e-spec.ts</files>
  <read_first>
    apps/nestjs-backend/vitest-e2e.config.ts
    an existing *.e2e-spec.ts under apps/nestjs-backend/test for the harness/setup pattern
  </read_first>
  <action>
    One e2e: create an agent, configure an MCP-served tool (Teable's own MCP server), run a conversation that invokes that tool AND establishes a fact; run a second turn and assert the agent recalls the prior-conversation fact (memory). Keep it single-flow to respect the memory-constrained box; gate heavy setup behind the existing e2e harness.
  </action>
  <verify>
    <automated>cd apps/nestjs-backend && pnpm vitest run --config ./vitest-e2e.config.ts test/agent-mcp-flow.e2e-spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - e2e: agent invokes an MCP tool and recalls a prior-conversation fact; suite green.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Phase 17 test suite + memory recall verification.</what-built>
  <how-to-verify>
    1. Boot web :3000 + API :3002 (best-effort).
    2. In a real conversation, tell the agent a fact; in a later message, ask it back → recalled.
    3. Confirm the full Phase 17 test suite is green (`pnpm test-unit` + the new e2e).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| agent A memory → agent B | per-agent memory isolation must hold |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-14 | Info disclosure | cross-agent memory leak | mitigate | memory queries filtered by agentId; spec asserts isolation |
| T-17-15 | Repudiation | untested RBAC paths | mitigate | dedicated rbac-gating.spec covering interface + MCP tools |
</threat_model>

<verification>
All Phase 17 unit specs green; one e2e flow green; memory per-agent scoping proven.
</verification>

<success_criteria>
Agent recalls a prior-conversation fact; Phase 17 test suite green.
</success_criteria>

<output>
Create `.planning/phases/17-agent-mcp-enhancement/17-06-SUMMARY.md` when done.
</output>
