---
plan_id: "17-05"
phase: 17-agent-mcp-enhancement
wave: 3
depends_on: ["17-02"]
requirements: [AGENT-03]
autonomous: false
files_modified:
  - packages/db-main-prisma/prisma/postgres/schema.prisma
  - apps/nestjs-backend/src/features/doc-search/search.service.ts
  - apps/nestjs-backend/src/features/agent/agent-execution.service.ts
  - apps/nestjs-app/src/features/app/components/agent/tabs/KnowledgeTab.tsx
  - apps/nestjs-app/src/features/app/components/agent/steps/PersonalizationStep.tsx
must_haves:
  truths:
    - "A user can pick allowed docs/folders for an agent in the builder modal AND the config UI"
    - "search_knowledge_base is scoped to the agent's selected doc/folder set"
    - "An agent restricted to folder X only returns answers from X"
  artifacts:
    - path: packages/db-main-prisma/prisma/postgres/schema.prisma
      provides: "per-agent knowledge sources (Agent.knowledgeSources Json or AgentKnowledgeSource table)"
    - path: apps/nestjs-backend/src/features/doc-search/search.service.ts
      provides: "hybridSearch with optional doc/folder scope"
  key_links:
    - from: agent-execution.service.ts search_knowledge_base branch
      to: DocSearchService.hybridSearch(spaceId, query, limit, scope)
      via: "pass the agent's knowledgeSources as scope"
    - from: KnowledgeTab.tsx + PersonalizationStep.tsx
      to: agent update API
      via: "knowledge-source picker persists docIds/folderIds on the agent"
---

<objective>
Wave 3 (AGENT-03). Per-agent doc/folder knowledge scoping (D-05). Store a per-agent knowledge-source set, scope `DocSearchService.hybridSearch` to it, wire the agent's `search_knowledge_base` tool to pass that scope, and add a "Knowledge sources" picker in BOTH the agent builder (PersonalizationStep / wizard) AND the agent configuration UI (KnowledgeTab).

DISCREPANCY FLAGGED: `search_knowledge_base` is REGISTERED in BUILT_IN_TOOLS but has NO execution branch in agent-execution.service.ts (it currently falls to `default:`). This plan must ADD the execution branch that calls hybridSearch — not just scope an existing one.

Output: schema + migration, scoped hybridSearch, new execution branch, two UI pickers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-agent-mcp-enhancement/17-CONTEXT.md
@.planning/phases/17-agent-mcp-enhancement/17-02-SUMMARY.md

<interfaces>
- hybridSearch(spaceId, query, limit=10) @ doc-search/search.service.ts:77 (semanticSearch/keywordSearch use WHERE d."spaceId"=...).
- imported_doc table has docId/spaceId; chunks join imported_doc (search.service.ts:37,60).
- Agent model @ schema.prisma:1151 (add relation/field).
- executeToolCall switch @ agent-execution.service.ts:327; default branch @ 592 (no search_knowledge_base case today).
- AgentConfigModal tabs (KnowledgeTab) @ components/agent/AgentConfigModal.tsx; AgentWizard steps/PersonalizationStep.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Knowledge-source schema + scoped hybridSearch</name>
  <files>packages/db-main-prisma/prisma/postgres/schema.prisma, apps/nestjs-backend/src/features/doc-search/search.service.ts</files>
  <read_first>
    packages/db-main-prisma/prisma/postgres/schema.prisma (Agent 1151-1175)
    apps/nestjs-backend/src/features/doc-search/search.service.ts (semanticSearch 21, keywordSearch 53, hybridSearch 77)
  </read_first>
  <action>
    Add `knowledgeSources Json? @map("knowledge_sources")` to Agent (shape: `{ docIds: string[]; folderIds: string[] }`); generate migration. Extend hybridSearch (and the two underlying searches) to accept an optional `scope?: { docIds?: string[]; folderIds?: string[] }`; when present, add `AND d."docId" IN (...)` / folder filter to the WHERE clause (parameterized — do NOT interpolate ids). Empty/undefined scope = current behavior (unscoped).
  </action>
  <verify>
    <automated>cd packages/db-main-prisma && pnpm prisma validate && cd ../../apps/nestjs-backend && pnpm vitest run src/features/doc-search/</automated>
  </verify>
  <acceptance_criteria>
    - Agent.knowledgeSources added; prisma validate passes.
    - hybridSearch signature accepts optional scope; scoped query restricts results (spec/assert WHERE includes IN clause when scope present).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wire search_knowledge_base execution branch with scope</name>
  <files>apps/nestjs-backend/src/features/agent/agent-execution.service.ts</files>
  <read_first>
    apps/nestjs-backend/src/features/agent/agent-execution.service.ts (executeToolCall 327-592, default branch)
    apps/nestjs-backend/src/features/agent/agent-tool-registry.service.ts (search_knowledge_base def @ 152)
  </read_first>
  <action>
    Add a `case 'search_knowledge_base':` to executeToolCall that loads the agent's knowledgeSources, resolves the agent's spaceId (via baseId→space), and calls DocSearchService.hybridSearch(spaceId, query, limit, scope) passing the agent's knowledgeSources as scope. Inject DocSearchService into AgentExecutionService (and agent.module imports) if not present. Return the search results as the tool output.
  </action>
  <verify>
    <automated>cd apps/nestjs-backend && pnpm vitest run src/features/agent/agent-execution.service.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - executeToolCall has a search_knowledge_base case calling hybridSearch with the agent's scope.
    - Spec: an agent with knowledgeSources={folderIds:[X]} passes that scope into hybridSearch.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Knowledge-source picker in builder + config UI</name>
  <files>apps/nextjs-app/src/features/app/components/agent/tabs/KnowledgeTab.tsx, apps/nextjs-app/src/features/app/components/agent/steps/PersonalizationStep.tsx</files>
  <read_first>
    apps/nextjs-app/src/features/app/components/agent/tabs/KnowledgeTab.tsx (current content)
    apps/nextjs-app/src/features/app/components/agent/steps/PersonalizationStep.tsx
    apps/nextjs-app/src/features/app/components/agent/AgentConfigModal.tsx (tab wiring)
  </read_first>
  <action>
    Add a doc/folder multi-select picker that lists the space's docs/folders and persists the selected `{docIds, folderIds}` to the agent's knowledgeSources via the existing agent update endpoint. Place it in KnowledgeTab (config UI) AND in the wizard PersonalizationStep (builder) — per D-05 both surfaces are required. Reuse an existing doc-library list query if available; do not build a new backend list endpoint unless none exists.
  </action>
  <verify>
    <automated>cd apps/nextjs-app && pnpm tsc --noEmit -p tsconfig.json 2>&1 | grep -i 'KnowledgeTab\|PersonalizationStep' || echo "no type errors in target files"</automated>
  </verify>
  <acceptance_criteria>
    - Both KnowledgeTab and PersonalizationStep render the picker and persist knowledgeSources.
    - Selection round-trips on the agent record.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Per-agent knowledge scoping end-to-end (schema → search → both UIs).</what-built>
  <how-to-verify>
    1. Boot web :3000 + API :3002 (best-effort).
    2. In the agent builder AND config UI, restrict an agent to folder X.
    3. Ask the agent a question answerable only by a doc in folder X → answered.
    4. Ask a question answerable only by a doc OUTSIDE X → not surfaced (scope enforced).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| agent search → doc library | scope filter must not be bypassable by query input |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-12 | Info disclosure | scoped hybridSearch | mitigate | scope applied as parameterized WHERE IN at the SQL layer, not post-filtered in app code |
| T-17-13 | Tampering | docIds/folderIds injection | mitigate | parameterize id lists; never string-interpolate into raw SQL |
</threat_model>

<verification>
Schema validates+migrates; scoped search + execution-branch specs green; both UIs persist scope; live folder-restriction holds.
</verification>

<success_criteria>
An agent restricted to folder X only returns answers from X.
</success_criteria>

<output>
Create `.planning/phases/17-agent-mcp-enhancement/17-05-SUMMARY.md` when done — note the search_knowledge_base branch was newly added (was missing).
</output>
