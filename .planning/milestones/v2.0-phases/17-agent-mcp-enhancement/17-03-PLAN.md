---
plan_id: "17-03"
phase: 17-agent-mcp-enhancement
wave: 2
depends_on: ["17-02"]
requirements: [AGENT-02]
autonomous: true
files_modified:
  - packages/db-main-prisma/prisma/postgres/schema.prisma
  - apps/nestjs-backend/src/features/plugin/plugin.service.ts
  - apps/nestjs-backend/src/features/agent/mcp/plugin-mcp-discovery.service.ts
  - apps/nestjs-backend/src/features/agent/mcp/plugin-mcp-discovery.service.spec.ts
  - apps/nestjs-backend/src/features/agent/mcp/mcp-client-aggregator.service.ts
must_haves:
  truths:
    - "A plugin can declare an MCP tool capability in its manifest"
    - "Installed plugins that declare MCP tools surface those tools to the agent"
    - "UI-only plugins declare nothing and are a no-op"
  artifacts:
    - path: apps/nestjs-backend/src/features/agent/mcp/plugin-mcp-discovery.service.ts
      provides: "Discovers installed plugins' declared MCP tools and feeds the aggregator"
  key_links:
    - from: plugin-mcp-discovery.service.ts
      to: mcp-client-aggregator.service.ts
      via: "discovered plugin MCP endpoints are aggregated into the agent's tool set"
    - from: Plugin schema mcpUrl/toolManifest
      to: plugin.service.ts
      via: "manifest field surfaced through plugin reads"
---

<objective>
Wave 2 (AGENT-02). Let **plugins declare MCP tools** (D-03). Extend the `Plugin` model with an optional MCP capability (`mcpUrl` and/or inline `toolManifest`), surface it through `plugin.service`, and add a discovery service that feeds installed plugins' declared MCP tools into the 17-02 aggregator so they appear to the agent. UI-only plugins declare nothing → no-op. No generic `invoke_plugin` fallback (deferred).

Output: schema field(s) + migration, plugin.service surfacing, discovery service, aggregator hook.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-agent-mcp-enhancement/17-CONTEXT.md
@.planning/phases/17-agent-mcp-enhancement/17-02-SUMMARY.md

<interfaces>
- Plugin model @ schema.prisma:661; PluginInstall @ 686 (baseId-scoped installs).
- PluginService @ features/plugin/plugin.service.ts.
- McpClientAggregatorService.getAggregatedTools(agentId) @ agent/mcp/mcp-client-aggregator.service.ts (17-02).
- AgentMcpServer model (17-02) — plugin tools are aggregated alongside explicit MCP servers.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Plugin manifest with MCP capability</name>
  <files>packages/db-main-prisma/prisma/postgres/schema.prisma, apps/nestjs-backend/src/features/plugin/plugin.service.ts</files>
  <read_first>
    packages/db-main-prisma/prisma/postgres/schema.prisma (Plugin 661-685)
    apps/nestjs-backend/src/features/plugin/plugin.service.ts (read/create/update of plugin)
  </read_first>
  <action>
    Add optional `mcpUrl String? @map("mcp_url")` and `toolManifest Json? @map("tool_manifest")` to the Plugin model; generate migration. In plugin.service.ts, surface these fields on plugin read/create/update so they round-trip. Both nullable → existing UI-only plugins are unaffected (no-op).
  </action>
  <verify>
    <automated>cd packages/db-main-prisma && pnpm prisma validate</automated>
  </verify>
  <acceptance_criteria>
    - Plugin model has mcpUrl + toolManifest; prisma validate passes; plugin.service reads/writes them.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Plugin MCP discovery service + aggregator hook</name>
  <files>apps/nestjs-backend/src/features/agent/mcp/plugin-mcp-discovery.service.ts, apps/nestjs-backend/src/features/agent/mcp/plugin-mcp-discovery.service.spec.ts, apps/nestjs-backend/src/features/agent/mcp/mcp-client-aggregator.service.ts</files>
  <read_first>
    apps/nestjs-backend/src/features/agent/mcp/mcp-client-aggregator.service.ts (getAggregatedTools, executeMcpTool)
    apps/nestjs-backend/src/features/plugin/plugin.service.ts
  </read_first>
  <behavior>
    - discoverPluginMcpServers(agentId/baseId): returns the set of plugin-declared MCP endpoints (mcpUrl) and/or inline toolManifest tool defs for plugins installed in the agent's base.
    - Plugins with no mcpUrl AND no toolManifest contribute nothing.
    - Inline toolManifest tools are adapted to ToolDefinition[] via the 17-00 adapter; mcpUrl plugins are treated like an extra MCP server.
  </behavior>
  <action>
    Implement PluginMcpDiscoveryService that queries installed plugins (PluginInstall scoped to the agent's base) for declared MCP capability. In McpClientAggregatorService.getAggregatedTools, ALSO fold in plugin-discovered endpoints/tools (call discovery and merge), namespacing as `mcp__plugin_{pluginId}__{tool}`. Keep connections short-lived (BOOT-OOM). Register PluginMcpDiscoveryService in agent.module providers.
  </action>
  <verify>
    <automated>cd apps/nestjs-backend && pnpm vitest run src/features/agent/mcp/plugin-mcp-discovery.service.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Spec: a plugin declaring one MCP tool surfaces it in getAggregatedTools; a UI-only plugin surfaces nothing.
    - Spec: plugin tool name is namespaced and executable via the aggregator.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| installed plugin → agent tool set | plugin-declared MCP endpoint is third-party; output crosses into agent |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-07 | Elevation | plugin MCP tool call | mitigate | plugin tools only discovered for plugins installed in the agent's base (PluginInstall scope); aggregator runs under agent identity |
| T-17-08 | Spoofing | plugin-declared mcpUrl | mitigate | only honor mcpUrl from installed plugin records, never client-supplied URLs |
</threat_model>

<verification>
Schema validates+migrates; discovery + aggregator specs green; UI-only plugin remains no-op.
</verification>

<success_criteria>
Install a plugin that declares one MCP tool → agent can invoke it. UI-only plugins unaffected.
Live: best-effort with web :3000 + API :3002.
</success_criteria>

<output>
Create `.planning/phases/17-agent-mcp-enhancement/17-03-SUMMARY.md` when done.
</output>
