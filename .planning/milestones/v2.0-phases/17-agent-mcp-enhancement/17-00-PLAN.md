---
plan_id: "17-00"
phase: 17-agent-mcp-enhancement
wave: 0
depends_on: []
requirements: [AGENT-05]
autonomous: false
files_modified:
  - apps/nestjs-backend/package.json
  - apps/nestjs-backend/src/features/agent/mcp/SPIKE-NOTES.md
  - apps/nestjs-backend/src/features/agent/mcp/mcp-tool-adapter.ts
  - apps/nestjs-backend/src/features/agent/mcp/mcp-tool-adapter.spec.ts
must_haves:
  truths:
    - "Existing agent loop runs a built-in tool end-to-end against a booted app"
    - "@modelcontextprotocol/sdk is installed and version-pinned"
    - "A throwaway MCP server's tool is invoked by the agent via the adapter"
  artifacts:
    - path: apps/nestjs-backend/src/features/agent/mcp/mcp-tool-adapter.ts
      provides: "MCP tool -> AgentToolRegistry ToolDefinition adapter (the pinned shape Wave 1 builds on)"
    - path: apps/nestjs-backend/src/features/agent/mcp/SPIKE-NOTES.md
      provides: "Pinned SDK versions, transport decision, adapter contract"
  key_links:
    - from: mcp-tool-adapter.ts
      to: agent-tool-registry.service.ts ToolDefinition
      via: "adapter output matches { type:'function', function:{ name, description, parameters } }"
---

<objective>
Wave 0 process gate. Two outcomes, no production wiring:
1. LIVE-VERIFY the existing agent loop already invokes a built-in tool end-to-end (regression baseline before we touch it).
2. SPIKE the MCP layer: install + pin `@modelcontextprotocol/sdk`, choose transport (StreamableHTTP vs stdio), and write the canonical MCP-tool → AI-SDK-`ToolDefinition` adapter that Wave 1 (17-01/17-02) consumes verbatim.

Purpose: De-risk Wave 1's exact API. The adapter shape and pinned versions produced here are LOCKED inputs to every later MCP plan.
Output: Installed+pinned SDK, `mcp/mcp-tool-adapter.ts` (+ unit test), `mcp/SPIKE-NOTES.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-agent-mcp-enhancement/17-CONTEXT.md
@apps/nestjs-backend/BOOT-OOM-INVESTIGATION.md

<interfaces>
Existing ToolDefinition (apps/nestjs-backend/src/features/agent/agent-tool-registry.service.ts:4):
```ts
interface ToolDefinition {
  type: 'function';
  function: {
    name: string;
    description: string;
    parameters: { type: 'object'; properties: Record<string, unknown>; required: string[] };
  };
}
```
NOTE: 17-CONTEXT D-02 assumed a flat `{name,description,parameters}` shape and an AI-SDK-native MCP client. BOTH are wrong — the real shape is nested under `function`, and `ai@6.0.169` has no MCP client. The adapter MUST emit the nested shape above.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install + pin MCP SDK, capture spike notes</name>
  <files>apps/nestjs-backend/package.json, apps/nestjs-backend/src/features/agent/mcp/SPIKE-NOTES.md</files>
  <read_first>
    apps/nestjs-backend/package.json (confirm only ai@6.0.169 present, no modelcontextprotocol)
    apps/nestjs-backend/BOOT-OOM-INVESTIGATION.md
  </read_first>
  <action>
    Add `@modelcontextprotocol/sdk` to apps/nestjs-backend dependencies via `pnpm --filter @teable/nestjs-backend add @modelcontextprotocol/sdk`, then pin it to an exact version (no `^`). In SPIKE-NOTES.md record: the exact pinned version, the chosen transport (default to StreamableHTTP — it keeps the MCP server reachable over HTTP without a long-lived stdio child process, which matters for the memory-constrained box per BOOT-OOM), and the import paths for `Server`, `Client`, and `StreamableHTTPServerTransport`/`StreamableHTTPClientTransport` from the SDK's subpath exports. Explicitly note that `ai@6.0.169` has NO MCP client so the agent's MCP client uses the SDK `Client` directly.
  </action>
  <acceptance_criteria>
    - `grep -E '"@modelcontextprotocol/sdk": "[0-9]' apps/nestjs-backend/package.json` matches (exact pin, no caret).
    - SPIKE-NOTES.md states transport choice + rationale + exact SDK import subpaths.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write the MCP-tool -> ToolDefinition adapter (the pinned contract)</name>
  <files>apps/nestjs-backend/src/features/agent/mcp/mcp-tool-adapter.ts, apps/nestjs-backend/src/features/agent/mcp/mcp-tool-adapter.spec.ts</files>
  <read_first>
    apps/nestjs-backend/src/features/agent/agent-tool-registry.service.ts (lines 4-17 ToolDefinition; 217-244 getToolsForAgent)
  </read_first>
  <behavior>
    - Given an MCP tool descriptor `{ name, description, inputSchema (JSON Schema object) }`, adapter returns a ToolDefinition with `type:'function'` and `function.{name,description,parameters}` where parameters = inputSchema coerced to `{ type:'object', properties, required: [] | required[] }`.
    - Namespaces tool name to avoid collisions with built-ins: prefix `mcp__{serverId}__{toolName}` (record the chosen prefix scheme in SPIKE-NOTES).
    - Returns a paired executor closure `(input) => Promise<result>` that calls `client.callTool({ name, arguments })` and returns its content; this is what executeToolCall will dispatch to.
  </behavior>
  <action>
    Implement a pure adapter module (no NestJS DI) exporting `mcpToolToDefinition(serverId, mcpTool): ToolDefinition` and `makeMcpExecutor(client, serverId, originalName): (input)=>Promise<unknown>`. Keep it framework-free so it unit-tests without booting Nest. Do NOT wire it into the registry yet — that is 17-02.
  </action>
  <verify>
    <automated>cd apps/nestjs-backend && pnpm vitest run src/features/agent/mcp/mcp-tool-adapter.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Unit test proves a sample MCP tool descriptor maps to the exact nested ToolDefinition shape with namespaced name.
    - Test proves executor calls `client.callTool` with the de-namespaced original tool name.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Existing agent loop baseline + throwaway MCP round-trip via the adapter.</what-built>
  <how-to-verify>
    1. Boot separated: web :3000 + API :3002 (best-effort; if box OOMs, note it and rely on unit test only).
    2. Trigger an agent run that uses a built-in tool (e.g. search_records) and confirm it returns data end-to-end (regression baseline).
    3. Stand up a throwaway in-memory MCP server exposing one trivial tool; using SDK `Client` + the new adapter, confirm the agent invokes that tool and gets its result. This can be a scratch script under mcp/ deleted after.
  </how-to-verify>
  <resume-signal>Type "approved" (adapter shape + transport + versions pinned) or describe issues.</resume-signal>
</task>

</tasks>

<verification>
- SDK pinned; adapter unit test green; spike notes capture transport + adapter contract + name-prefix scheme.
- Live: existing tool round-trips; throwaway MCP tool round-trips via adapter (best-effort).
</verification>

<success_criteria>
A throwaway MCP server's tool is invoked by the agent via the adapter; SDK version + transport + adapter shape are pinned and documented. Wave 1 can build against a fixed API.
</success_criteria>

<output>
Create `.planning/phases/17-agent-mcp-enhancement/17-00-SUMMARY.md` when done — MUST record pinned SDK version, transport, and final adapter signature for downstream plans.
</output>
