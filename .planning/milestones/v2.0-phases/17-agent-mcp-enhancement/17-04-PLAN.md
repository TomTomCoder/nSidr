---
plan_id: "17-04"
phase: 17-agent-mcp-enhancement
wave: 2
depends_on: ["17-02", "17-03"]
requirements: [AGENT-01]
autonomous: false
files_modified:
  - apps/nestjs-backend/src/features/agent/mcp/interface-tools.ts
  - apps/nestjs-backend/src/features/agent/mcp/interface-tools.service.ts
  - apps/nestjs-backend/src/features/agent/mcp/interface-tools.service.spec.ts
must_haves:
  truths:
    - "An agent can read an app's/dashboard's state and data"
    - "An agent can perform a write action on an app/dashboard"
    - "Every interface tool call is RBAC-enforced via agent-permission/authority-matrix"
  artifacts:
    - path: apps/nestjs-backend/src/features/agent/mcp/interface-tools.service.ts
      provides: "get_app/get_dashboard (read) + run_app_action/update_dashboard (write) MCP tools, RBAC-gated"
  key_links:
    - from: interface-tools.service.ts
      to: AppBuilderService / DashboardService
      via: "read/write operations delegate to existing app-builder + dashboard services"
    - from: interface-tools.service.ts
      to: authority-matrix / agent-permission
      via: "each tool authorizes the scoped identity before acting"
---

<objective>
Wave 2 (AGENT-01). Add **interface (app/dashboard) read/write MCP tools** (D-04): `get_app`/`get_dashboard` (read state+data) and `run_app_action`/`update_dashboard` (write), each gated by `agent-permission.guard` + authority-matrix. Tools register through the existing Teable MCP server tool surface (17-01) and aggregate into the agent (17-02).

Output: interface tool definitions + service wiring to AppBuilderService/DashboardService, RBAC-enforced.
NOTE: depends_on 17-03 ONLY to serialize aggregator/registry edits within Wave 2 (no true logical dependency on plugin tools). Files here are net-new (interface-tools*) — no overlap with 17-03's files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-agent-mcp-enhancement/17-CONTEXT.md
@.planning/phases/17-agent-mcp-enhancement/17-01-SUMMARY.md
@.planning/phases/17-agent-mcp-enhancement/17-02-SUMMARY.md

<interfaces>
- AppBuilderService @ features/app-builder/app-builder.service.ts:16.
- DashboardService @ features/dashboard/dashboard.service.ts:33.
- AuthorityMatrixService @ features/authority-matrix/authority-matrix.service.ts:47.
- agent-permission.guard.ts (PermissionGuard extension, baseId resolution).
- teable-mcp-tools.ts registration pattern (17-01) — register interface tools the same way.
- App model @ schema.prisma (App, content Json).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define interface tool schemas</name>
  <files>apps/nestjs-backend/src/features/agent/mcp/interface-tools.ts</files>
  <read_first>
    apps/nestjs-backend/src/features/app-builder/app-builder.service.ts (read/write method signatures)
    apps/nestjs-backend/src/features/dashboard/dashboard.service.ts (read/write method signatures)
    apps/nestjs-backend/src/features/agent/agent-tool-registry.service.ts (ToolDefinition shape 4-17)
  </read_first>
  <action>
    Export ToolDefinition[] for: get_app(appId), get_dashboard(dashboardId) [reads], run_app_action(appId, action, args), update_dashboard(dashboardId, patch) [writes]. Start with the highest-value read+write ops per D-04 discretion — match parameter names to the actual AppBuilderService/DashboardService method args you read. Use the nested `{type:'function', function:{...}}` shape.
  </action>
  <acceptance_criteria>
    - 4 tool definitions exported with correct nested shape; parameters match real service args.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Interface tools service with RBAC + service delegation</name>
  <files>apps/nestjs-backend/src/features/agent/mcp/interface-tools.service.ts, apps/nestjs-backend/src/features/agent/mcp/interface-tools.service.spec.ts, apps/nestjs-backend/src/features/agent/agent.module.ts</files>
  <read_first>
    apps/nestjs-backend/src/features/authority-matrix/authority-matrix.service.ts
    apps/nestjs-backend/src/features/agent/agent-permission.guard.ts
    apps/nestjs-backend/src/features/agent/mcp/teable-mcp-tools.ts (17-01 registration)
  </read_first>
  <behavior>
    - executeInterfaceTool(name, input, identity): for each tool, FIRST authorize the scoped identity against the target resource's baseId via authority-matrix; deny → error, no side effect.
    - Reads (get_app/get_dashboard) return state+data; writes (run_app_action/update_dashboard) delegate to AppBuilderService/DashboardService and return the mutation result.
    - A read permission must NOT authorize a write (separate permission actions).
  </behavior>
  <action>
    Implement InterfaceToolsService injecting AppBuilderService, DashboardService, AuthorityMatrixService/PermissionService. Register its 4 tools into the Teable MCP server via teable-mcp-tools (extend the existing registration entry point — do not duplicate the server). Add InterfaceToolsService + required module imports (AppBuilderModule, DashboardModule, AuthorityMatrixModule) to agent.module. Keep off the hot path: services are invoked only per tool call.
  </action>
  <verify>
    <automated>cd apps/nestjs-backend && pnpm vitest run src/features/agent/mcp/interface-tools.service.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Spec: unauthorized identity → read AND write both rejected.
    - Spec: authorized read returns data; authorized write mutates via the real service; read-only identity cannot write.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Agent interface read/write tools (app + dashboard), RBAC-gated.</what-built>
  <how-to-verify>
    1. Boot web :3000 + API :3002 (best-effort).
    2. Have an agent read a dashboard's data (get_dashboard) → returns data.
    3. Have the agent perform a write (update_dashboard or run_app_action) → change persists.
    4. Confirm an agent identity without write permission is rejected on the write.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| agent → app/dashboard write tools | agent-initiated state mutation crosses into user data |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-09 | Elevation | write tools (run_app_action/update_dashboard) | mitigate | authority-matrix write-permission check per call; read perm never authorizes write |
| T-17-10 | Repudiation | agent-performed writes | mitigate | writes flow through existing services that audit/log under the scoped identity |
| T-17-11 | Tampering | run_app_action args | mitigate | delegate to AppBuilderService validation; no raw execution path |
</threat_model>

<verification>
Specs green (RBAC read/write separation); live dashboard read + write both enforced.
</verification>

<success_criteria>
An agent reads a dashboard's data AND performs a write action, both RBAC-enforced.
</success_criteria>

<output>
Create `.planning/phases/17-agent-mcp-enhancement/17-04-SUMMARY.md` when done.
</output>
