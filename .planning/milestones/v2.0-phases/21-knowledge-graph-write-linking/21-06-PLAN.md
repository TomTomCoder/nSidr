---
phase: 21-knowledge-graph-write-linking
plan: 06
type: execute
wave: 4
depends_on: [21-01]
files_modified:
  - apps/nestjs-backend/src/features/doc-search/doc-search.controller.ts
  - apps/nextjs-app/src/features/app/blocks/doc-search/LinkedDocsPanel.tsx
  - apps/nextjs-app/src/features/app/blocks/doc-search/DocEditorArea.tsx
  - apps/nextjs-app/src/features/app/blocks/doc-search/index.ts
autonomous: true
requirements: [KG-02]

must_haves:
  truths:
    - "Doc viewer (DocEditorArea) renders a 'Linked docs' sidebar listing both outgoing and incoming agent/user-authored links (label IS NOT NULL)"
    - "Sidebar fetches via a new GET /api/doc-search/docs/:docId/links endpoint that delegates to DocLinkService.getOutgoing + getIncoming"
    - "Sidebar is read-only (v1) — clicking a linked title navigates to that doc"
    - "Empty state when no links exist: 'No linked docs yet.'"
    - "Endpoint enforces RBAC via the same per-user space check the existing doc-search controller uses (no new auth pattern)"
  artifacts:
    - path: apps/nestjs-backend/src/features/doc-search/doc-search.controller.ts
      provides: "GET /api/doc-search/docs/:docId/links route"
      contains: "@Get\\([\"']docs/:docId/links[\"']\\)"
    - path: apps/nextjs-app/src/features/app/blocks/doc-search/LinkedDocsPanel.tsx
      provides: "React component — Linked docs sidebar"
  key_links:
    - from: LinkedDocsPanel.tsx
      to: GET /api/doc-search/docs/:docId/links
      via: "fetch / existing api client (axios)"
      pattern: "doc-search/docs/.+/links"
    - from: DocEditorArea.tsx
      to: LinkedDocsPanel
      via: "JSX render alongside doc content"
      pattern: "<LinkedDocsPanel"
---

<objective>
Add a minimal read-only "Linked docs" sidebar to the existing doc viewer
(DocEditorArea.tsx) plus the backing GET endpoint (KG-02 UI requirement).
Parallel to 21-05 (no file overlap — backend controller vs MCP tools).

Purpose: KG-02 explicitly requires "doc-doc links schema + UI". 21-01..21-05
covers schema + agent-facing API; this plan covers the user-facing UI.
Output: One new backend route + one new React component + viewer integration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-knowledge-graph-write-linking/21-CONTEXT.md
@.planning/phases/21-knowledge-graph-write-linking/21-03-PLAN.md
@apps/nestjs-backend/src/features/doc-search/doc-search.controller.ts
@apps/nextjs-app/src/features/app/blocks/doc-search/DocEditorArea.tsx

<interfaces>
DocLinkService.getOutgoing / getIncoming (defined in 21-03):
- input: { docId, callerSpaceId, limit? }
- output: [{ linkId, toDocId | fromDocId, toTitle | fromTitle, label, createdAt }]

doc-search.controller.ts pattern: existing routes use the same RBAC guard as the rest of the controller — derive callerSpaceId from req.user / req.session per the established pattern (verify by reading the file; do NOT invent a new auth pattern).

The route returns `{ outgoing: [...], incoming: [...] }` shaped exactly like the `get_doc_links` MCP tool returns — single canonical response shape.

UI: DocEditorArea.tsx already lays out an editor area. Inject `<LinkedDocsPanel docId={...} />` in a sidebar slot (right column or below the editor — see what fits; if no obvious slot, render at the bottom of the editor column with `mt-4`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GET /api/doc-search/docs/:docId/links endpoint + controller spec</name>
  <files>apps/nestjs-backend/src/features/doc-search/doc-search.controller.ts</files>
  <behavior>
    - GET /api/doc-search/docs/:docId/links → 200 with {outgoing:[...], incoming:[...]} when caller has access to the doc
    - 404 (or whatever the existing controller emits via NotFoundException) when doc is out-of-space — DocLinkService already enforces; just propagate
    - Authenticated route — uses the controller's existing auth guard (already in place)
    - Spec: hit endpoint with mock services, assert {outgoing, incoming} shape; assert 404 propagation
  </behavior>
  <action>
    Read the controller to find:
    - The existing auth guard decoration pattern
    - How callerSpaceId / userId is extracted (likely a `@Req()` or a custom decorator)
    - Existing route prefixes (e.g. `@Controller('doc-search')` or similar — match it)

    Inject `DocLinkService` into the controller (value import — critical_constraint #1).

    Add an `@Get('docs/:docId/links')` handler that extracts callerSpaceId per the existing pattern, calls both `getOutgoing` and `getIncoming` from DocLinkService in `Promise.all`, and returns `{ outgoing, incoming }`.

    Add a controller spec (or extend existing) covering happy path + cross-space NotFoundException propagation.
  </action>
  <verify>
    <automated>pnpm --filter @teable/nestjs-backend exec vitest run src/features/doc-search/doc-search.controller.spec.ts</automated>
  </verify>
  <done>
    Endpoint reachable; spec passes; no `import type { DocLinkService }`.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: LinkedDocsPanel React component + DocEditorArea integration</name>
  <files>apps/nextjs-app/src/features/app/blocks/doc-search/LinkedDocsPanel.tsx, apps/nextjs-app/src/features/app/blocks/doc-search/DocEditorArea.tsx, apps/nextjs-app/src/features/app/blocks/doc-search/index.ts</files>
  <action>
    Create `LinkedDocsPanel.tsx`:
    - Props: `{ docId: string }`
    - useQuery (react-query is already in the codebase — verify by checking neighbouring components) to fetch `/api/doc-search/docs/:docId/links`
    - Render two sections: "Outgoing links" and "Incoming links". Each section lists `{label}: {title}` rows as TEXT NODES only — never as raw HTML. Clicking a row navigates to that doc — reuse the existing doc-open pattern from the file tree (likely via `useDocEditorStore.openDoc(targetDocId)` — verify by reading the store).
    - Loading state: skeleton row
    - Empty state: "No linked docs yet."
    - Error state: "Couldn't load links — try again." (no silent fallback — show the failure per critical_constraint #3)

    Integrate into `DocEditorArea.tsx`:
    - Import LinkedDocsPanel
    - Render `<LinkedDocsPanel docId={currentDocId} />` in a sidebar slot. If the file already has a right rail, slot it there; otherwise render below the editor with a heading divider. Keep the diff small — this is v1 read-only, not a UI refactor.

    Export from `index.ts` for tree-shake-friendly imports.

    XSS safety: render label + title via standard JSX text interpolation (`<span>{label}</span>`). Do NOT use any raw-HTML injection prop. The grep gate in Task 2 verifies this is upheld (see verify command).

    Note: agent UI for editing the KG (creating docs via UI) is OUT OF SCOPE per 21-CONTEXT phase_boundary. The sidebar is read-only.
  </action>
  <verify>
    <automated>pnpm --filter @teable/nextjs-app exec tsc --noEmit && pnpm --filter @teable/nextjs-app exec eslint apps/nextjs-app/src/features/app/blocks/doc-search/LinkedDocsPanel.tsx apps/nextjs-app/src/features/app/blocks/doc-search/DocEditorArea.tsx && grep -c 'dangerouslySet' apps/nextjs-app/src/features/app/blocks/doc-search/LinkedDocsPanel.tsx | grep -q '^0$'</automated>
  </verify>
  <done>
    TS + lint clean; component renders both sections; clicking a row triggers doc navigation; empty/error/loading states present; grep confirms zero raw-HTML injection in the new component.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Browser → /api/doc-search/docs/:docId/links | Untrusted docId crosses here |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-20 | Information Disclosure | Cross-space link enumeration via the new endpoint | mitigate | DocLinkService.getOutgoing/getIncoming already RBAC-check the source doc; route surface adds no new bypass |
| T-21-21 | Tampering | XSS via link label rendered in sidebar | mitigate | React escapes text content by default; render `{label}: {title}` as text nodes only; grep gate in Task 2 verify rejects raw-HTML injection |
| T-21-22 | Denial of Service | Sidebar fetches for every doc switch | accept | react-query default cache+stale-time bounds re-fetch; v1 doesn't warrant optimisation |
</threat_model>

<verification>
- Endpoint returns {outgoing, incoming}
- Controller spec covers RBAC propagation
- UI renders without TS errors
- Sidebar shows empty state on docs with no links
- Grep gate: zero raw-HTML injection props in LinkedDocsPanel.tsx
</verification>

<success_criteria>
- GET route exists and is unit-spec'd
- LinkedDocsPanel renders inside DocEditorArea
- Click-to-navigate works (manual verification in 21-07 live UAT)
- Read-only — no Create/Delete buttons (v1 scope per 21-CONTEXT)
</success_criteria>

<output>
Create `.planning/phases/21-knowledge-graph-write-linking/21-06-SUMMARY.md` when done.
</output>
