---
phase: 17.1-agent-mcp-hardening-live-test-follow-ups
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/nestjs-backend/src/features/agent/mcp/teable-mcp-tools.ts
  - apps/nestjs-backend/src/features/agent/mcp/teable-mcp-server.service.ts
  - apps/nestjs-backend/src/features/agent/mcp/teable-mcp-server.spec.ts
autonomous: true
requirements: [AGENT-01-LIVE]
must_haves:
  truths:
    - "MCP tools/list returns the 4 interface tools (get_app, get_dashboard, run_app_action, update_dashboard) in addition to the existing 12 built-in tools"
    - "MCP tools/call for an interface tool dispatches to InterfaceToolsService.executeInterfaceTool with identity {userId, baseId}"
    - "checkPermission closure runs once before either dispatch branch (interface or built-in)"
  artifacts:
    - path: "apps/nestjs-backend/src/features/agent/mcp/teable-mcp-tools.ts"
      provides: "registerTeableMcpTools extended signature with interfaceToolsService param"
      contains: "interfaceToolsService"
    - path: "apps/nestjs-backend/src/features/agent/mcp/teable-mcp-server.service.ts"
      provides: "Constructor-injected InterfaceToolsService passed into registerTeableMcpTools"
      contains: "InterfaceToolsService"
  key_links:
    - from: "teable-mcp-server.service.ts"
      to: "teable-mcp-tools.ts:registerTeableMcpTools"
      via: "constructor DI of InterfaceToolsService passed into helper"
      pattern: "registerTeableMcpTools\\([^)]*interfaceToolsService"
---

<objective>
Close bug-3 from `17-VERIFICATION.md` (`phase_17_bugs_found_by_live_test`): the 4 interface tools defined in `INTERFACE_TOOLS` are never wired to the MCP server's `tools/list` and `tools/call` handlers, so AGENT-01 fails at runtime even though source/unit tests pass.

Purpose: AGENT-01 must be live-true — a space-permitted agent calling `tools/list` over MCP must receive 16 tools (12 built-in + 4 interface), and `tools/call` for an interface tool name must route to `InterfaceToolsService.executeInterfaceTool`.
Output: Extended `registerTeableMcpTools` signature + caller update; existing unit specs stay green; a new unit case proves interface tools appear in `tools/list` output.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/17.1-agent-mcp-hardening-live-test-follow-ups/17.1-CONTEXT.md
@.planning/phases/17-agent-mcp-enhancement/17-VERIFICATION.md
@apps/nestjs-backend/src/features/agent/mcp/teable-mcp-tools.ts
@apps/nestjs-backend/src/features/agent/mcp/teable-mcp-server.service.ts
@apps/nestjs-backend/src/features/agent/mcp/interface-tools.service.ts
@apps/nestjs-backend/src/features/agent/mcp/teable-mcp-server.spec.ts

<interfaces>
From interface-tools.service.ts (verified):
- getToolDefinitions(): typeof INTERFACE_TOOLS — returns array of {name, description, inputSchema}
- async executeInterfaceTool(name: string, input: unknown, identity: {userId: string, baseId: string}): Promise<unknown>
- Class is @Injectable(); use VALUE import — never `import type`

From teable-mcp-server.service.ts (current, line 87):
- registerTeableMcpTools(server, registry, execution, agentCtx, checkPermission)
- checkPermission closure at line 73 calls permissionService.getRoleByBaseId(agent.baseId)

From teable-mcp-tools.ts (current, line 46):
- export function registerTeableMcpTools(server, registry, execution, agentCtx, checkPermission): void
- tools/list handler (~line 60) returns registry.getBuiltInTools()
- tools/call handler dispatches to execution.executeTool(toolName, args, agentCtx)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend registerTeableMcpTools to merge interface tools (D-17.1-01)</name>
  <read_first>
    - apps/nestjs-backend/src/features/agent/mcp/teable-mcp-tools.ts (entire file, ~107 lines)
    - apps/nestjs-backend/src/features/agent/mcp/interface-tools.service.ts (verify getToolDefinitions + executeInterfaceTool signatures)
    - apps/nestjs-backend/src/features/agent/mcp/teable-mcp-server.spec.ts (current 5 tests — pattern for the new case)
  </read_first>
  <files>apps/nestjs-backend/src/features/agent/mcp/teable-mcp-tools.ts, apps/nestjs-backend/src/features/agent/mcp/teable-mcp-server.spec.ts</files>
  <behavior>
    - tools/list returns built-in tools concatenated with interfaceToolsService.getToolDefinitions() — total length is registry.getBuiltInTools().length + 4
    - tools/list output includes a tool named `get_app` and one named `get_dashboard`
    - tools/call for toolName `get_app` invokes interfaceToolsService.executeInterfaceTool('get_app', input, {userId: agentCtx.userId, baseId: agentCtx.baseId}) — verified via vi.fn() spy
    - tools/call for toolName `search_records` still routes to execution.executeTool (built-in branch preserved)
    - checkPermission is awaited once before either dispatch branch (verified via mock call count)
  </behavior>
  <action>
    Add an `interfaceToolsService: InterfaceToolsService` parameter to `registerTeableMcpTools` (place it after `execution`, before `agentCtx`). Use a VALUE import — `import { InterfaceToolsService } from './interface-tools.service';` — never `import type`, per Phase 17 bug-1 lesson.

    In the `tools/list` handler, return `{ tools: [...registry.getBuiltInTools(), ...interfaceToolsService.getToolDefinitions()] }`.

    In the `tools/call` handler, after the existing `await checkPermission()` line, branch on `toolName`: if it is one of the 4 interface tool names (`get_app`, `get_dashboard`, `run_app_action`, `update_dashboard` — derive the set from `interfaceToolsService.getToolDefinitions().map(t => t.name)` so the list stays in sync), call `interfaceToolsService.executeInterfaceTool(toolName, args, { userId: agentCtx.userId, baseId: agentCtx.baseId })` and return its result; otherwise fall through to the existing `execution.executeTool(toolName, args, agentCtx)` path.

    Add ONE new vitest case to `teable-mcp-server.spec.ts` (or co-located spec — use the same pattern as the existing 5 tests) titled `registers interface tools alongside built-in tools and dispatches by name`. Mock InterfaceToolsService with vi.fn() returning `[{name:'get_app',...},{name:'get_dashboard',...},{name:'run_app_action',...},{name:'update_dashboard',...}]` from getToolDefinitions and a spy on executeInterfaceTool. Invoke the registered handlers (use the SDK Server mock pattern already in the file) and assert: (a) tools/list includes get_app, (b) tools/call for get_app calls executeInterfaceTool with the expected identity, (c) tools/call for search_records calls execution.executeTool.

    Do NOT touch the `parsedBody` arg in `teable-mcp-server.service.ts:99` (bug-2 regression risk) — only the registerTeableMcpTools call site changes in this task.
  </action>
  <acceptance_criteria>
    - `pnpm --filter @teable/nestjs-backend vitest run apps/nestjs-backend/src/features/agent/mcp/teable-mcp-server.spec.ts` passes — all prior 5 tests + new test green
    - `grep -n "import type.*InterfaceToolsService" apps/nestjs-backend/src/features/agent/mcp/teable-mcp-tools.ts` returns nothing (value import only)
    - `grep -nE "registerTeableMcpTools\\s*\\(" apps/nestjs-backend/src/features/agent/mcp/teable-mcp-tools.ts` shows the new signature with `interfaceToolsService`
    - The 4 interface tool names (`get_app`, `get_dashboard`, `run_app_action`, `update_dashboard`) all appear in the tools/list branch (grep `interfaceToolsService.getToolDefinitions` returns at least one match in teable-mcp-tools.ts)
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @teable/nestjs-backend vitest run apps/nestjs-backend/src/features/agent/mcp/teable-mcp-server.spec.ts</automated>
  </verify>
  <done>tools/list returns 16 tools; tools/call dispatches interface tool names to executeInterfaceTool; unit spec proves both branches.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire InterfaceToolsService into TeableMcpServerService DI + call site</name>
  <read_first>
    - apps/nestjs-backend/src/features/agent/mcp/teable-mcp-server.service.ts (entire file, ~108 lines)
    - apps/nestjs-backend/src/features/agent/mcp/teable-mcp-tools.ts (after Task 1 — confirm new signature)
    - apps/nestjs-backend/src/features/agent/agent.module.ts (or wherever the module providers list lives — confirm InterfaceToolsService is already a provider; if not, add it)
    - apps/nestjs-backend/src/features/agent/mcp/agent-mcp-flow.integration.spec.ts (must stay green)
  </read_first>
  <files>apps/nestjs-backend/src/features/agent/mcp/teable-mcp-server.service.ts</files>
  <behavior>
    - TeableMcpServerService constructor receives InterfaceToolsService via Nest DI (value import)
    - The injected instance is passed positionally to registerTeableMcpTools per the new signature from Task 1
    - The `transport.handleRequest(req, res, parsedBody)` call at line 99 retains its third argument (no bug-2 regression)
    - Existing 5 unit tests + 5 integration tests stay green
  </behavior>
  <action>
    Add `import { InterfaceToolsService } from './interface-tools.service';` (VALUE import). Add `private readonly interfaceToolsService: InterfaceToolsService` to the constructor parameter list of `TeableMcpServerService`. Update the `registerTeableMcpTools(...)` call (currently at ~line 87) to pass `this.interfaceToolsService` in the position defined by Task 1's new signature.

    Verify line 99 still reads `await transport.handleRequest(req, res, parsedBody);` — do not change. Verify line 73 (checkPermission closure) is untouched — bug-4 work happens in plan 02, do not duplicate.

    If `InterfaceToolsService` is not in the providers array of `agent.module.ts` (or wherever the module wiring lives), add it. Use a value import there as well.
  </action>
  <acceptance_criteria>
    - `pnpm --filter @teable/nestjs-backend vitest run apps/nestjs-backend/src/features/agent/mcp/teable-mcp-server.spec.ts apps/nestjs-backend/src/features/agent/mcp/agent-mcp-flow.integration.spec.ts` passes (all 10 tests green)
    - `grep -n "import type.*InterfaceToolsService" apps/nestjs-backend/src/features/agent/mcp/teable-mcp-server.service.ts` returns nothing
    - `grep -nE "transport\\.handleRequest\\(req,\\s*res,\\s*parsedBody\\)" apps/nestjs-backend/src/features/agent/mcp/teable-mcp-server.service.ts` returns exactly one match (bug-2 not regressed)
    - `grep -nE "this\\.interfaceToolsService" apps/nestjs-backend/src/features/agent/mcp/teable-mcp-server.service.ts` shows the service is referenced in the registerTeableMcpTools call
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @teable/nestjs-backend vitest run apps/nestjs-backend/src/features/agent/mcp/</automated>
  </verify>
  <done>TeableMcpServerService constructs with InterfaceToolsService; call site wired; parsedBody preserved; all mcp/ specs green.</done>
</task>

</tasks>

<verification>
- All vitest specs under `apps/nestjs-backend/src/features/agent/mcp/` pass.
- No `import type` for any DI-injected service in modified files.
- `parsedBody` arg from commit `00450e8b5` is preserved at `teable-mcp-server.service.ts:99`.
- tools/list now contains 16 tool names (12 built-in + 4 interface).
</verification>

<success_criteria>
Bug-3 from `phase_17_bugs_found_by_live_test` frontmatter is closeable: the registration path exists in source, the unit spec proves the dispatch, and the call site injects the service. Live-UAT smoke (plan 04) will confirm `tools/list` shows 16 tools end-to-end.
</success_criteria>

<output>
Create `.planning/phases/17.1-agent-mcp-hardening-live-test-follow-ups/17.1-01-SUMMARY.md` on completion.
</output>
