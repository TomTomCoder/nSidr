---
phase: 17.1-agent-mcp-hardening-live-test-follow-ups
plan: 04
type: execute
wave: 2
depends_on: ["17.1-01", "17.1-02", "17.1-03"]
files_modified:
  - .planning/phases/17-agent-mcp-enhancement/17-VERIFICATION.md
  - .planning/REQUIREMENTS.md
  - scripts/uat/17.1-smoke.sh
autonomous: false
requirements: [AGENT-01-LIVE, AGENT-05-LIVE-rbac, AGENT-05-LIVE-scope]
must_haves:
  truths:
    - "Live `tools/list` returns 16 tool names (12 built-in + 4 interface) against booted backend"
    - "A space owner without an explicit base-collaborator row reaches MCP tools/call without -32600"
    - "17-VERIFICATION.md frontmatter shows bugs 3, 4, scope-test as FIXED"
    - "REQUIREMENTS.md lists AGENT-01-LIVE, AGENT-05-LIVE-rbac, AGENT-05-LIVE-scope"
  artifacts:
    - path: "scripts/uat/17.1-smoke.sh"
      provides: "Re-runnable curl/jq smoke script for the 3 live checks"
      min_lines: 30
    - path: ".planning/phases/17-agent-mcp-enhancement/17-VERIFICATION.md"
      provides: "Updated frontmatter — bugs 3, 4, scope-test marked FIXED"
      contains: "status: FIXED"
  key_links:
    - from: "scripts/uat/17.1-smoke.sh"
      to: "/api/agent/mcp/:agentId"
      via: "JSON-RPC POST with cookie auth"
      pattern: "agent/mcp"
---

<objective>
Close phase 17.1 by re-running the same live UAT pattern that found the original 6 bugs (2026-06-06), updating the VERIFICATION frontmatter, and adding the 3 new requirement IDs to REQUIREMENTS.md. This plan is the human-verified gate — Claude scripts everything but the human runs the smoke against a booted backend.

Purpose: Convert source-fixes from plans 01-03 into live-true status.
Output: Committed smoke script + updated VERIFICATION.md + REQUIREMENTS.md entries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/17-agent-mcp-enhancement/17-VERIFICATION.md
@.planning/phases/17.1-agent-mcp-hardening-live-test-follow-ups/17.1-01-SUMMARY.md
@.planning/phases/17.1-agent-mcp-hardening-live-test-follow-ups/17.1-02-SUMMARY.md
@.planning/phases/17.1-agent-mcp-hardening-live-test-follow-ups/17.1-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add requirement IDs to REQUIREMENTS.md</name>
  <read_first>
    - .planning/REQUIREMENTS.md (entire file — find the AGENT section around line 19-24)
    - .planning/ROADMAP.md (confirm Phase 17.1 row lists AGENT-01-LIVE, AGENT-05-LIVE-rbac, AGENT-05-LIVE-scope)
  </read_first>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    Append 3 lines to the `### Agent System (AGENT)` section of REQUIREMENTS.md (after AGENT-05):

    - AGENT-01-LIVE: An agent's interface tools (get_app, get_dashboard, run_app_action, update_dashboard) are reachable via MCP `tools/list` and `tools/call` at runtime (live-verified, not just unit-tested).
    - AGENT-05-LIVE-rbac: MCP RBAC gate honours space-inherited roles — consistent with REST AgentPermissionGuard.
    - AGENT-05-LIVE-scope: search_knowledge_base scope wiring (docIds/folderIds/null) has deterministic regression coverage.

    Mark each as `[x]` only if plans 01-03 are complete (check by reading their SUMMARYs in `read_first`). Otherwise leave `[ ]` and let the smoke task flip them.
  </action>
  <acceptance_criteria>
    - `grep -nE "AGENT-01-LIVE|AGENT-05-LIVE-rbac|AGENT-05-LIVE-scope" .planning/REQUIREMENTS.md` returns 3 matches
    - File still parses as valid markdown (no broken sections above/below)
  </acceptance_criteria>
  <verify>
    <automated>grep -cE "AGENT-(01-LIVE|05-LIVE-rbac|05-LIVE-scope)" .planning/REQUIREMENTS.md | grep -q "^3$"</automated>
  </verify>
  <done>3 new requirement IDs present.</done>
</task>

<task type="auto">
  <name>Task 2: Write 17.1-smoke.sh re-runnable smoke script</name>
  <read_first>
    - .planning/phases/17-agent-mcp-enhancement/17-VERIFICATION.md (Live Test Findings section — read the exact request/response shapes used on 2026-06-06)
    - scripts/ directory listing to confirm where to place it (create scripts/uat/ if missing)
  </read_first>
  <files>scripts/uat/17.1-smoke.sh</files>
  <action>
    Create an executable bash script at `scripts/uat/17.1-smoke.sh` that runs 3 checks against a booted dev backend (assumed on `http://localhost:3000`). The script reads `AGENT_ID`, `SESSION_COOKIE`, `BASE_URL` from env (with defaults) and prints PASS/FAIL per check:

    1. **tools/list returns 16 tools** — POST JSON-RPC `{"jsonrpc":"2.0","id":1,"method":"tools/list"}` to `${BASE_URL}/api/agent/mcp/${AGENT_ID}` with cookie. Parse with `jq` and assert `.result.tools | length == 16` AND `[.result.tools[].name] | contains(["get_app","get_dashboard","run_app_action","update_dashboard"])`.
    2. **Space-owner inheritance** — call tools/list with a session whose user has space-owner-only collaborator role for the agent's base. Assert HTTP 200 and no `-32600 Access denied` error in the response body.
    3. **Scope wiring (proxy for Test 3)** — POST `tools/call` for `search_knowledge_base` with the agent that has knowledgeSources set to a known docId. Assert response is HTTP 200 and `.result.scoped == true`.

    Set `set -euo pipefail` at top. Make executable (`chmod +x`). Use `curl` for the HTTP calls. Exit code 0 only if all 3 PASS.

    Note: `curl` is normally redirected by context-mode rules, but THIS script is a committed artifact for a human to run against a booted backend — it never runs in the planning sandbox. Reference the script from VERIFICATION.md.
  </action>
  <acceptance_criteria>
    - `test -x scripts/uat/17.1-smoke.sh` (executable bit set)
    - `bash -n scripts/uat/17.1-smoke.sh` exits 0 (valid bash syntax)
    - Script contains the 3 named checks (`grep -cE "tools/list|space-owner|scope" scripts/uat/17.1-smoke.sh` >= 3)
    - First line is `#!/usr/bin/env bash` and second is `set -euo pipefail`
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/uat/17.1-smoke.sh && test -x scripts/uat/17.1-smoke.sh</automated>
  </verify>
  <done>Script committed and syntax-valid; ready for human to run against booted backend.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human runs 17.1-smoke.sh against booted backend</name>
  <what-built>3 source fixes (plans 01-03) and a re-runnable smoke script. This checkpoint asks the human to boot the backend and run the script.</what-built>
  <how-to-verify>
    1. Boot the dev backend (`pnpm dev` or the project's standard boot command — see CLAUDE.md / project_launch_architecture memory: port 3000 for combined dev, 3001 for Next, 3002/3001 for NestJS).
    2. Ensure DB is migrated (`pnpm db:migrate` or equivalent) and at least one agent exists with a non-null `knowledgeSources` referencing a real indexed doc.
    3. Export `AGENT_ID=<id>`, `SESSION_COOKIE=<your auth cookie>`, then run `bash scripts/uat/17.1-smoke.sh`.
    4. Expected: all 3 checks print PASS, script exits 0.
    5. If any check fails: paste the failing output here. Likely cause for each:
       - Check 1 fail (count != 16): plan 01 wiring incomplete — re-read SUMMARY 17.1-01.
       - Check 2 fail (-32600): plan 02 gate swap incomplete — verify `getEffectiveBaseRole` is actually called.
       - Check 3 fail (.scoped == false): plan 03 spec is green but executeTool hydration regressed; check `agent-execution.service.ts:344` for the bug-5 fix.
  </how-to-verify>
  <resume-signal>Type "smoke pass" to proceed to task 4 (frontmatter update), or paste failing output to triage.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Update 17-VERIFICATION.md frontmatter — mark bugs FIXED</name>
  <read_first>
    - .planning/phases/17-agent-mcp-enhancement/17-VERIFICATION.md (entire `phase_17_bugs_found_by_live_test` frontmatter block)
  </read_first>
  <files>.planning/phases/17-agent-mcp-enhancement/17-VERIFICATION.md</files>
  <action>
    Edit the frontmatter:
    - bug-3-interface-tools-not-registered: change `status: OPEN — requires extending registerTeableMcpTools signature...` to `status: FIXED — plan 17.1-01 (commit <sha>) extended registerTeableMcpTools to merge InterfaceToolsService.getToolDefinitions() and dispatch to executeInterfaceTool`
    - bug-4-rbac-no-space-inheritance: change to `status: FIXED — plan 17.1-02 (commit <sha>) added permissionService.getEffectiveBaseRole and swapped the MCP gate to use it`
    - Add a new entry `bug-5-scope-test-coverage` (or annotate the existing bug-5 closure with the new spec): `status: FIXED — plan 17.1-03 (commit <sha>) adds agent-knowledge-scope.integration.spec.ts with 5 deterministic cases`

    Leave bugs 1, 2, 5 (executeTool hydration), 6 (SQL column) untouched — they were already FIXED in commit `00450e8b5`.

    Add a closing note at the bottom of the file referencing this phase and the smoke script:
    `Phase 17.1 closure: all 6 phase-17 live-UAT bugs now FIXED. Re-runnable smoke at scripts/uat/17.1-smoke.sh. Closed: 2026-06-06.`

    Use `<sha>` literally if commits haven't landed yet; otherwise substitute the actual short SHA from `git log --oneline -5`.
  </action>
  <acceptance_criteria>
    - `grep -cE "status: OPEN" .planning/phases/17-agent-mcp-enhancement/17-VERIFICATION.md` returns 0 (no remaining OPEN bugs)
    - `grep -E "Phase 17\\.1 closure" .planning/phases/17-agent-mcp-enhancement/17-VERIFICATION.md` returns 1 match
    - YAML frontmatter still parses (run `node -e "require('js-yaml').load(require('fs').readFileSync('.planning/phases/17-agent-mcp-enhancement/17-VERIFICATION.md','utf8').split('---')[1])"` exits 0)
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -c 'status: OPEN' .planning/phases/17-agent-mcp-enhancement/17-VERIFICATION.md)" = "0" && grep -q "Phase 17.1 closure" .planning/phases/17-agent-mcp-enhancement/17-VERIFICATION.md</automated>
  </verify>
  <done>Frontmatter shows zero OPEN bugs; closure note present.</done>
</task>

</tasks>

<verification>
- Human smoke run PASSES all 3 checks.
- REQUIREMENTS.md lists the 3 new IDs.
- 17-VERIFICATION.md frontmatter shows 0 OPEN bugs.
- scripts/uat/17.1-smoke.sh exists, is executable, syntax-valid.
</verification>

<success_criteria>
Phase 17.1 is closed. Phase 17 live-UAT bugs (3, 4, scope-test) are FIXED with both unit + live coverage. AGENT-01 is live-true. Build order proceeds to Phase 16.
</success_criteria>

<output>
Create `.planning/phases/17.1-agent-mcp-hardening-live-test-follow-ups/17.1-04-SUMMARY.md` and `.planning/phases/17.1-agent-mcp-hardening-live-test-follow-ups/17.1-VERIFICATION.md` on completion.
</output>
