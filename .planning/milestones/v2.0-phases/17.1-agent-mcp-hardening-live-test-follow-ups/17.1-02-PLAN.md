---
phase: 17.1-agent-mcp-hardening-live-test-follow-ups
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/nestjs-backend/src/features/auth/permission.service.ts
  - apps/nestjs-backend/src/features/auth/permission.service.spec.ts
  - apps/nestjs-backend/src/features/agent/mcp/teable-mcp-server.service.ts
  - apps/nestjs-backend/src/features/agent/mcp/rbac-gating.spec.ts
autonomous: true
requirements: [AGENT-05-LIVE-rbac]
must_haves:
  truths:
    - "A user with a space-owner collaborator row (but no base-collaborator row) on a base in that space resolves to a non-null base role via getEffectiveBaseRole"
    - "MCP checkPermission closure uses getEffectiveBaseRole and admits space-inherited owners"
    - "Existing getRoleByBaseId behavior is byte-for-byte unchanged (no REST regression)"
  artifacts:
    - path: "apps/nestjs-backend/src/features/auth/permission.service.ts"
      provides: "getEffectiveBaseRole(baseId) method"
      contains: "async getEffectiveBaseRole"
    - path: "apps/nestjs-backend/src/features/agent/mcp/teable-mcp-server.service.ts"
      provides: "MCP gate uses getEffectiveBaseRole"
      contains: "getEffectiveBaseRole"
  key_links:
    - from: "teable-mcp-server.service.ts:checkPermission"
      to: "permission.service.ts:getEffectiveBaseRole"
      via: "single method swap in closure"
      pattern: "permissionService\\.getEffectiveBaseRole\\(agent\\.baseId\\)"
---

<objective>
Close bug-4 from `17-VERIFICATION.md` (`phase_17_bugs_found_by_live_test`): the MCP RBAC gate uses `permissionService.getRoleByBaseId` which only consults base-level collaborator rows, while REST's `AgentPermissionGuard` inherits space role. A space owner gets `-32600 Access denied` from MCP but reaches the same agent via REST — inconsistent semantics.

Purpose: Make MCP RBAC honour space-inherited roles, surgically. Add a NEW method `getEffectiveBaseRole(baseId)` rather than mutating `getRoleByBaseId` (which has many REST callers — see permission.service.ts:371). Swap one line in the MCP closure.
Output: New permission method + unit spec + RBAC integration coverage demonstrating space-owner admittance.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/17.1-agent-mcp-hardening-live-test-follow-ups/17.1-CONTEXT.md
@.planning/phases/17-agent-mcp-enhancement/17-VERIFICATION.md
@apps/nestjs-backend/src/features/auth/permission.service.ts
@apps/nestjs-backend/src/features/agent/mcp/teable-mcp-server.service.ts
@apps/nestjs-backend/src/features/agent/mcp/rbac-gating.spec.ts

<interfaces>
From permission.service.ts (verified):
- async getRoleByBaseId(baseId: string): Promise<IBaseRole | null>  // line 110 — base-collaborator rows only
- async getRoleBySpaceId(spaceId: string, includeInactiveResource?: boolean): Promise<ISpaceRole | null>  // line 73 — space-collaborator rows
- import { getMaxLevelRole } from '../../utils/get-max-level-role'  // line 18
- Existing line 371-372 already shows the inheritance pattern: getRoleByBaseId + getRoleBySpaceId combined

From teable-mcp-server.service.ts (current line 73-74):
- const checkPermission = async (): Promise<void> => {
-   const role = await this.permissionService.getRoleByBaseId(agent.baseId);
- The closure throws -32600 when role is null
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add getEffectiveBaseRole(baseId) to PermissionService (D-17.1-02)</name>
  <read_first>
    - apps/nestjs-backend/src/features/auth/permission.service.ts (lines 70-130 for existing role lookups, lines 340-380 for the existing combine pattern)
    - apps/nestjs-backend/src/features/auth/permission.service.spec.ts (existing unit test pattern for prisma stubs)
    - apps/nestjs-backend/prisma/schema.prisma (confirm `base.spaceId` field name)
    - apps/nestjs-backend/src/utils/get-max-level-role.ts (signature of getMaxLevelRole)
  </read_first>
  <files>apps/nestjs-backend/src/features/auth/permission.service.ts, apps/nestjs-backend/src/features/auth/permission.service.spec.ts</files>
  <behavior>
    - getEffectiveBaseRole(baseId) returns the base role from getRoleByBaseId(baseId) when non-null (existing semantics preserved)
    - When getRoleByBaseId returns null, the method resolves base → spaceId via prisma.base.findUnique({where:{id:baseId}, select:{spaceId:true}}) and returns the space role from getRoleBySpaceId(spaceId)
    - When both lookups return null, the method returns null
    - When the base does not exist (findUnique returns null), the method returns null without throwing
    - getRoleByBaseId behavior is BIT-FOR-BIT unchanged — no edits to that method, only a new sibling method added
  </behavior>
  <action>
    Add a new public async method `getEffectiveBaseRole(baseId: string): Promise<IBaseRole | null>` to `PermissionService`. Place it directly below `getRoleByBaseId` (after line ~120). Logic:

    1. `const baseRole = await this.getRoleByBaseId(baseId);` — if non-null, return it.
    2. `const base = await this.prismaService.txClient().base.findUnique({ where: { id: baseId }, select: { spaceId: true } });` (use the same prisma access pattern already used inside the class — search for `this.prismaService` to copy the exact style).
    3. If `base?.spaceId` is undefined/null, return null.
    4. `const spaceRole = await this.getRoleBySpaceId(base.spaceId);` — return it as `IBaseRole | null` (space roles are a superset compatible with base RBAC — see the existing combine at line 371-372 for precedent).

    Do NOT use `getMaxLevelRole` unless step 4 needs to combine multiple roles — for a single-space-role case a direct return mirrors existing patterns. Do not modify `getRoleByBaseId`. Do not change `getRoleBySpaceId`.

    Add a unit spec block in `permission.service.spec.ts` titled `getEffectiveBaseRole` with 4 cases mirroring the behavior list above. Reuse the prisma stub pattern already in the file (search for `mockPrisma` or `prismaService` mock setup).
  </action>
  <acceptance_criteria>
    - `pnpm --filter @teable/nestjs-backend vitest run apps/nestjs-backend/src/features/auth/permission.service.spec.ts` passes including 4 new cases
    - `grep -nE "async getRoleByBaseId\\b" apps/nestjs-backend/src/features/auth/permission.service.ts` shows the SAME line content as before this plan (no mutation) — verify via `git diff apps/nestjs-backend/src/features/auth/permission.service.ts` shows only added lines, no modifications to existing getRoleByBaseId body
    - `grep -nE "async getEffectiveBaseRole\\(baseId: string\\)" apps/nestjs-backend/src/features/auth/permission.service.ts` returns one match
    - No `import type` introduced for any DI-injected dependency
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @teable/nestjs-backend vitest run apps/nestjs-backend/src/features/auth/permission.service.spec.ts</automated>
  </verify>
  <done>New method added; 4 unit cases green; existing getRoleByBaseId untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Swap MCP gate to use getEffectiveBaseRole + extend rbac-gating spec</name>
  <read_first>
    - apps/nestjs-backend/src/features/agent/mcp/teable-mcp-server.service.ts (lines 70-100 — the checkPermission closure)
    - apps/nestjs-backend/src/features/agent/mcp/rbac-gating.spec.ts (current 13 tests — extend the pattern)
    - apps/nestjs-backend/src/features/auth/permission.service.ts (confirm Task 1 landed)
  </read_first>
  <files>apps/nestjs-backend/src/features/agent/mcp/teable-mcp-server.service.ts, apps/nestjs-backend/src/features/agent/mcp/rbac-gating.spec.ts</files>
  <behavior>
    - The `checkPermission` closure (currently line 73-74) calls `permissionService.getEffectiveBaseRole(agent.baseId)` instead of `getRoleByBaseId`
    - A new rbac-gating test case proves: when getRoleByBaseId returns null but getRoleBySpaceId returns 'owner', the MCP gate admits the call (does not throw -32600)
    - All 13 existing rbac-gating tests stay green
    - `transport.handleRequest(req, res, parsedBody)` at line 99 is NOT regressed (third argument preserved)
  </behavior>
  <action>
    In `teable-mcp-server.service.ts`, change the single line inside the `checkPermission` closure from `await this.permissionService.getRoleByBaseId(agent.baseId)` to `await this.permissionService.getEffectiveBaseRole(agent.baseId)`. No other changes to the closure or surrounding code. Do not touch line 99.

    In `rbac-gating.spec.ts`, add ONE new test case `admits a space-owner who has no explicit base-collaborator row (space role inherited via getEffectiveBaseRole)`. Reuse the existing PermissionService mock pattern: mock `getEffectiveBaseRole` (the new method) to return `'owner'`. Assert the MCP call resolves (does not throw the -32600 access-denied JSON-RPC error). Add a complementary case if the existing 13 mock `getRoleByBaseId`: those existing mocks now need to migrate to mock `getEffectiveBaseRole` instead — update all 13 in a single find/replace pass.
  </action>
  <acceptance_criteria>
    - `pnpm --filter @teable/nestjs-backend vitest run apps/nestjs-backend/src/features/agent/mcp/rbac-gating.spec.ts` passes — 13 existing tests + at least 1 new test green
    - `pnpm --filter @teable/nestjs-backend vitest run apps/nestjs-backend/src/features/agent/mcp/teable-mcp-server.spec.ts` still passes (no regression)
    - `grep -nE "getRoleByBaseId\\(agent\\.baseId\\)" apps/nestjs-backend/src/features/agent/mcp/teable-mcp-server.service.ts` returns zero matches
    - `grep -nE "getEffectiveBaseRole\\(agent\\.baseId\\)" apps/nestjs-backend/src/features/agent/mcp/teable-mcp-server.service.ts` returns exactly one match
    - `grep -nE "transport\\.handleRequest\\(req,\\s*res,\\s*parsedBody\\)" apps/nestjs-backend/src/features/agent/mcp/teable-mcp-server.service.ts` returns exactly one match (bug-2 not regressed)
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @teable/nestjs-backend vitest run apps/nestjs-backend/src/features/agent/mcp/</automated>
  </verify>
  <done>MCP gate uses effective role; space owners pass; all rbac-gating tests green; parsedBody preserved.</done>
</task>

</tasks>

<verification>
- permission.service.spec.ts adds 4 cases for getEffectiveBaseRole, all green.
- rbac-gating.spec.ts: 13 existing tests + 1+ new space-inherit case all green.
- No mutation to getRoleByBaseId body (git diff confirms additive only).
- parsedBody arg preserved at teable-mcp-server.service.ts:99.
</verification>

<success_criteria>
Bug-4 is source-closeable: MCP gate honours space inheritance via the new surgical method; REST callers of getRoleByBaseId see zero behavior change. Live-UAT smoke (plan 04) will confirm a space owner reaches MCP without an explicit base-collaborator row.
</success_criteria>

<output>
Create `.planning/phases/17.1-agent-mcp-hardening-live-test-follow-ups/17.1-02-SUMMARY.md` on completion.
</output>
