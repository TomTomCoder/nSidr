---
phase: 17.1-agent-mcp-hardening-live-test-follow-ups
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified:
  - apps/nestjs-backend/src/features/agent/mcp/agent-knowledge-scope.integration.spec.ts
autonomous: true
requirements: [AGENT-05-LIVE-scope]
must_haves:
  truths:
    - "Agent with knowledgeSources={docIds:[A]} calls hybridSearch with scope.docIds=[A]"
    - "Agent with knowledgeSources={folderIds:[F]} calls hybridSearch with scope.folderIds=[F]"
    - "Agent with knowledgeSources={docIds:[A], folderIds:[F]} passes both fields"
    - "Agent with knowledgeSources=null calls hybridSearch with scope=undefined (whole-space, backward-compat)"
    - "Out-of-scope doc B is never returned in cases 1-3"
  artifacts:
    - path: "apps/nestjs-backend/src/features/agent/mcp/agent-knowledge-scope.integration.spec.ts"
      provides: "5 vitest cases proving search_knowledge_base scope wiring"
      min_lines: 120
  key_links:
    - from: "agent-knowledge-scope.integration.spec.ts"
      to: "DocSearchService.hybridSearch"
      via: "vi.fn() spy on the service method, in-memory Prisma stub for imported_doc + doc_chunk"
      pattern: "hybridSearch.*scope"
---

<objective>
Close bug-5 / scope-test from `17-VERIFICATION.md`: live e2e (Test 3) could not prove `search_knowledge_base` semantic discrimination because only one indexed doc exists in the dev DB. The actual wiring bug (`executeTool` stripping `knowledgeSources`) was fixed in commit `00450e8b5`. This phase adds the deterministic regression test that catches future occurrences of that class of bug.

Purpose: Deterministic, in-process integration spec that asserts the SCOPE was passed correctly through `executeTool → executeToolCall → DocSearchService.hybridSearch`. Test the wiring, not the embedding ranking (which is non-deterministic and burns credits).
Output: New vitest spec with 5 cases mirroring the pattern in `agent-mcp-flow.integration.spec.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/17.1-agent-mcp-hardening-live-test-follow-ups/17.1-CONTEXT.md
@.planning/phases/17-agent-mcp-enhancement/17-VERIFICATION.md
@apps/nestjs-backend/src/features/agent/mcp/agent-mcp-flow.integration.spec.ts
@apps/nestjs-backend/src/features/agent/agent-execution.service.ts
@apps/nestjs-backend/src/features/doc-search/search.service.ts

<interfaces>
From agent-mcp-flow.integration.spec.ts (verified pattern at lines 27, 39-108, 145-280):
- Uses vitest (describe/it/expect/beforeEach), in-memory Prisma builder buildInMemoryPrisma()
- AgentMemoryService instantiated with prisma stub: new AgentMemoryService(prisma as never)
- Tests assert behavior end-to-end without booting Nest

From agent-execution.service.ts (post bug-5 fix):
- executeTool(toolName, args, ctx): hydrates full agent record (including knowledgeSources) before delegating to executeToolCall

From search.service.ts:
- hybridSearch(query, options): options.scope?: { docIds?: string[]; folderIds?: string[] }
- Scope filter from search.service.ts:46 now uses `d.id IN (...)` (bug-6 fix)
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — write 5 failing scope-wiring cases (D-17.1-03)</name>
  <read_first>
    - apps/nestjs-backend/src/features/agent/mcp/agent-mcp-flow.integration.spec.ts (entire file — copy structure)
    - apps/nestjs-backend/src/features/agent/agent-execution.service.ts (lines 320-380 — executeTool + executeToolCall search_knowledge_base branch)
    - apps/nestjs-backend/src/features/doc-search/search.service.ts (hybridSearch signature + scope option shape)
    - apps/nestjs-backend/prisma/schema.prisma (imported_doc + doc_chunk shapes if scope filter requires them)
  </read_first>
  <files>apps/nestjs-backend/src/features/agent/mcp/agent-knowledge-scope.integration.spec.ts</files>
  <behavior>
    Create the spec with describe block `Agent knowledge scope — search_knowledge_base wiring`. 5 it() cases:

    1. `passes scope.docIds when agent.knowledgeSources = { docIds: [A] }` — assert hybridSearch spy called with options.scope.docIds === ['docA']
    2. `passes scope.folderIds when agent.knowledgeSources = { folderIds: [F] }` — assert options.scope.folderIds === ['folderF']
    3. `passes both docIds and folderIds when both set` — assert both fields present
    4. `passes scope=undefined when knowledgeSources = null` — assert options.scope is undefined (NOT empty object)
    5. `out-of-scope doc B is never in returned results` — fixtures include doc A (in-scope) and doc B (out-of-scope) chunks; the test fakes hybridSearch to filter by scope, then asserts the agent-tool result excludes doc-B chunkIds for cases 1/2/3
  </behavior>
  <action>
    Create `apps/nestjs-backend/src/features/agent/mcp/agent-knowledge-scope.integration.spec.ts`. Mirror the structure of `agent-mcp-flow.integration.spec.ts`:

    1. Copy the `buildInMemoryPrisma` helper (or import it if exported — check first; if not, duplicate the relevant slice scoped to `imported_doc` and `doc_chunk` rows).
    2. Seed fixtures: imported_doc rows {id:'docA', spaceId:'spc1'}, {id:'docB', spaceId:'spc1'}, {id:'docInFolder', spaceId:'spc1', folderId:'folderF'}; doc_chunk rows for each doc (3 chunks per doc).
    3. Build a vi.fn() spy `hybridSearchSpy` whose implementation filters in-memory chunks by the `options.scope` argument (so case 5 holds). Inject this into a stubbed DocSearchService passed to AgentExecutionService (or use the public AgentExecutionService constructor — read the existing service to determine what dependencies are needed; stub only what's required and use the SAME `as never` pattern the reference spec uses).
    4. For each it() case: build an agent stub with the relevant `knowledgeSources`, call `agentExecutionService.executeTool('search_knowledge_base', {query:'anything', limit:5}, {agentId:'a1', userId:'u1', baseId:'b1'})`, assert against `hybridSearchSpy.mock.calls[0][1].scope`.

    Use VALUE imports for any class type-checked by Nest DI (AgentMemoryService, AgentExecutionService, DocSearchService, PermissionService) — never `import type` per Phase 17 bug-1 lesson.

    No fenced implementation code in this <action>. The executor reads the reference spec and emits an idiomatic equivalent.
  </action>
  <acceptance_criteria>
    - `pnpm --filter @teable/nestjs-backend vitest run apps/nestjs-backend/src/features/agent/mcp/agent-knowledge-scope.integration.spec.ts` passes all 5 cases
    - The spec contains exactly 5 `it(` blocks
    - `grep -nE "import type.*(AgentExecutionService|DocSearchService|AgentMemoryService|PermissionService)" apps/nestjs-backend/src/features/agent/mcp/agent-knowledge-scope.integration.spec.ts` returns zero matches (value imports only)
    - The case "passes scope=undefined when knowledgeSources = null" explicitly asserts `expect(call[1].scope).toBeUndefined()` (NOT `toEqual({})` — backward-compat requires undefined, not empty)
    - `grep -nE "docB|out-of-scope" apps/nestjs-backend/src/features/agent/mcp/agent-knowledge-scope.integration.spec.ts` returns at least one match (case 5 proven)
    - The existing 5 tests in `agent-mcp-flow.integration.spec.ts` still pass
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @teable/nestjs-backend vitest run apps/nestjs-backend/src/features/agent/mcp/agent-knowledge-scope.integration.spec.ts apps/nestjs-backend/src/features/agent/mcp/agent-mcp-flow.integration.spec.ts</automated>
  </verify>
  <done>5 deterministic scope-wiring cases green; reference flow spec unregressed; the next bug like bug-5 (executeTool stripping fields) has automated detection.</done>
</task>

</tasks>

<verification>
- New spec has 5 it() cases, all green.
- No `import type` for DI-injected services.
- Reference spec (agent-mcp-flow.integration.spec.ts, 5 tests) stays green.
- Case 4 explicitly asserts scope === undefined (not empty object) for backward-compat.
</verification>

<success_criteria>
The class of bug that bug-5 represented (silent field stripping on the public executeTool surface) now has automated regression coverage. Live-UAT semantic discrimination (Test 3 in the original VERIFICATION) remains a data-bound limitation, not a code gap.
</success_criteria>

<output>
Create `.planning/phases/17.1-agent-mcp-hardening-live-test-follow-ups/17.1-03-SUMMARY.md` on completion.
</output>
