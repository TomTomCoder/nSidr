---
phase: 17.1-agent-mcp-hardening-live-test-follow-ups
verified: 2026-06-07T00:00:00Z
status: passed
score: 7/7 must-haves verified
overrides_applied: 0
---

# Phase 17.1: Agent MCP Hardening — Verification Report

**Phase Goal:** Fix 3 bugs found during live UAT of Phase 17 (interface tools not registered, RBAC ignores space role, scope wiring untested), each with a regression test.
**Verified:** 2026-06-07
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | MCP tools/list returns 4 interface tools alongside built-in tools | VERIFIED | `teable-mcp-tools.ts:74-88` merges `interfaceDefs` into tools/list response |
| 2 | tools/call for interface tool dispatches to InterfaceToolsService.executeInterfaceTool | VERIFIED | `teable-mcp-tools.ts:102-119` — branch on `interfaceNames.has(toolName)` calls `executeInterfaceTool` |
| 3 | checkPermission fires once before either dispatch branch | VERIFIED | `teable-mcp-tools.ts:96` — single `await checkPermission()` before both branches |
| 4 | getEffectiveBaseRole added to PermissionService; falls back to space role | VERIFIED | `permission.service.ts:140-158` — getRoleByBaseId then space lookup via prisma |
| 5 | MCP RBAC gate uses getEffectiveBaseRole (not getRoleByBaseId) | VERIFIED | `teable-mcp-server.service.ts:79` — `this.permissionService.getEffectiveBaseRole(agent.baseId)` |
| 6 | Scope-wiring regression spec has 5 deterministic test cases | VERIFIED | `agent-knowledge-scope.integration.spec.ts` lines 167/187/206/226/244 — 5 `it()` cases |
| 7 | Live-UAT shape fix: INTERFACE_TOOLS shape reads t.function.name not t.name | VERIFIED | `teable-mcp-tools.ts:68` — `interfaceDefs.map((t) => t.function.name)` + commit `e650d6f14` |

**Score:** 7/7 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `apps/nestjs-backend/src/features/agent/mcp/teable-mcp-tools.ts` | Extended signature with interfaceToolsService | VERIFIED | Line 59 — `interfaceToolsService: InterfaceToolsService` param; value import at line 30 |
| `apps/nestjs-backend/src/features/agent/mcp/teable-mcp-server.service.ts` | Constructor DI + call site wired | VERIFIED | Line 39 constructor param; line 96 `this.interfaceToolsService` passed to registerTeableMcpTools |
| `apps/nestjs-backend/src/features/auth/permission.service.ts` | `getEffectiveBaseRole` method | VERIFIED | Lines 140-158 — async method with base-then-space fallback |
| `apps/nestjs-backend/src/features/agent/mcp/agent-knowledge-scope.integration.spec.ts` | New spec with 5 scope-wiring cases | VERIFIED | File exists; 5 `it()` blocks for docIds, folderIds, both, null, empty arrays |
| `apps/nestjs-backend/src/features/agent/mcp/teable-mcp-server.spec.ts` | mkInterfaceToolDef helper + interface dispatch test | VERIFIED | Lines 82-102 define drift-resistant mock helper; line 239 new test case |
| `apps/nestjs-backend/src/features/agent/mcp/rbac-gating.spec.ts` | getEffectiveBaseRole mocks + space-owner test | VERIFIED | Lines 302-400 — all mocks use `getEffectiveBaseRole`; line 391 space-owner test case |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `teable-mcp-server.service.ts:checkPermission` | `permission.service.ts:getEffectiveBaseRole` | Single method call at line 79 | WIRED | `this.permissionService.getEffectiveBaseRole(agent.baseId)` — zero occurrences of old `getRoleByBaseId(agent.baseId)` |
| `teable-mcp-server.service.ts` | `teable-mcp-tools.ts:registerTeableMcpTools` | Constructor DI of InterfaceToolsService passed into helper | WIRED | Line 96 passes `this.interfaceToolsService` as 4th positional arg matching extended signature |
| `teable-mcp-tools.ts:tools/call` | `InterfaceToolsService.executeInterfaceTool` | `interfaceNames.has(toolName)` branch | WIRED | Lines 102-119 — set populated from `getToolDefinitions().map(t => t.function.name)` |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `teable-mcp-tools.ts` | 28-30 | Comment says `import type` is forbidden; value import used correctly | Info | None — correct pattern applied |
| None | — | No `import type` on DI-injected services | — | Clean |
| None | — | `transport.handleRequest(req, res, parsedBody)` preserved at line 110 | — | Bug-2 not regressed |

### Commit Verification

All 4 commits from SUMMARY.md confirmed present in git history:
- `fca43ca07` — fix(17.1-01): wire 4 interface tools into MCP tools/list + tools/call
- `be8067ad6` — fix(17.1-02): mcp rbac gate honours space-inherited roles (bug-4)
- `fff4be6cb` — test(17.1-03): add deterministic scope-wiring regression spec (bug-5)
- `e650d6f14` — fix(17.1): correct INTERFACE_TOOLS shape — caught by live UAT

### Live-UAT Regression Fix (e650d6f14)

The SUMMARY documents a 4th commit fixing a mock-shape drift caught during live UAT: the mapper read `t.name` but the real `ToolDefinition` shape is `t.function.name`. This is verified at `teable-mcp-tools.ts:68` where `interfaceDefs.map((t) => t.function.name)` is used throughout, and `mkInterfaceToolDef` at `teable-mcp-server.spec.ts:82` constructs the mock with the real `{ type, function: { name, ... } }` shape.

### Human Verification Required

None — all truths were verifiable from source code. Live-UAT was conducted by the executor on 2026-06-06T10:13:00Z against a booted dev backend and documented in SUMMARY.md. The specific live test results (16 tools in tools/list, `get_app` returning real metadata, space-owner admission) cannot be reproduced programmatically in this verification pass but the source wiring fully supports those outcomes.

---

_Verified: 2026-06-07_
_Verifier: Claude (gsd-verifier)_
