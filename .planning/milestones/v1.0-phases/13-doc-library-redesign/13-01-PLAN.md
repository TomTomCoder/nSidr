---
phase: 13-doc-library-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db-main-prisma/prisma/postgres/schema.prisma
  - packages/db-main-prisma/prisma/postgres/migrations
autonomous: true
requirements: [D-11]
must_haves:
  truths:
    - "DocFolder table exists in the database with self-referential parent FK"
    - "ImportedDoc has folderId and order columns"
    - "Prisma client types include DocFolder and the new ImportedDoc fields"
  artifacts:
    - path: packages/db-main-prisma/prisma/postgres/schema.prisma
      provides: "DocFolder model + ImportedDoc folderId/order fields"
      contains: "model DocFolder"
    - path: packages/db-main-prisma/prisma/postgres/migrations
      provides: "Hand-written SQL migration for DocFolder + ImportedDoc columns"
  key_links:
    - from: ImportedDoc.folderId
      to: DocFolder.id
      via: foreign key ON DELETE SET NULL
      pattern: "ImportedDoc_folderId_fkey"
---

<objective>
Add the `DocFolder` Prisma model (nested folder hierarchy) and the `folderId` + `order` fields on `ImportedDoc`, then apply a hand-written SQL migration and regenerate the Prisma client. This is the [BLOCKING] foundation — no backend service or frontend work can start until the schema is live and the client is regenerated.

Purpose: Establish the folder hierarchy data layer (D-11) that the entire phase builds on.
Output: Updated schema.prisma, a new migration directory with hand-crafted SQL, regenerated Prisma client.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-doc-library-redesign/13-CONTEXT.md
@.planning/phases/13-doc-library-redesign/13-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1 [BLOCKING]: Add DocFolder model + ImportedDoc fields to schema.prisma</name>
  <read_first>
    - packages/db-main-prisma/prisma/postgres/schema.prisma (the file being modified — find the existing ImportedDoc, DocChunk, DocLink, and Space models)
    - .planning/phases/13-doc-library-redesign/13-PATTERNS.md (schema.prisma section, lines 30-81 — exact Prisma additions)
  </read_first>
  <action>
    Add a new `DocFolder` model after the existing `ImportedDoc` model: fields `id` (String @id @default(cuid())), `spaceId` (String), `parentFolderId` (String?), `name` (String), `order` (Float @default(0)), `createdAt` (DateTime @default(now())). Relations: `space` to Space (onDelete Cascade), self-relation `parent`/`children` named "FolderChildren" (onDelete SetNull), and `docs` ImportedDoc[]. Map to table "DocFolder".

    Add to the existing `ImportedDoc` model: `folderId String?`, `order Float @default(0)`, and `folder DocFolder? @relation(fields: [folderId], references: [id], onDelete: SetNull)`.

    Add a back-relation on the `Space` model: `docFolders DocFolder[]` (find the Space model and add this line alongside its other doc relations if any exist).

    Follow exact field types from 13-PATTERNS.md. Do NOT run `prisma migrate dev` — schema edit only here; migration is Task 2.
  </action>
  <verify>
    <automated>grep -q "model DocFolder" packages/db-main-prisma/prisma/postgres/schema.prisma &amp;&amp; grep -A40 "model ImportedDoc" packages/db-main-prisma/prisma/postgres/schema.prisma | grep -q "folderId"</automated>
  </verify>
  <acceptance_criteria>
    - `model DocFolder` block exists in schema.prisma with all 6 scalar fields and the FolderChildren self-relation.
    - `ImportedDoc` model contains `folderId String?` and `order Float @default(0)`.
    - `Space` model has a `docFolders DocFolder[]` back-relation.
    - No `prisma migrate dev` was run.
  </acceptance_criteria>
  <done>schema.prisma defines DocFolder + ImportedDoc folderId/order, validated by grep.</done>
</task>

<task type="auto">
  <name>Task 2 [BLOCKING]: Write hand-crafted migration SQL + deploy + prisma generate</name>
  <read_first>
    - packages/db-main-prisma/prisma/postgres/schema.prisma (just-modified file)
    - .planning/phases/13-doc-library-redesign/13-PATTERNS.md (Migration SQL pattern, lines 36-57)
    - packages/db-main-prisma/prisma/postgres/migrations (inspect the most recent existing migration directory to copy its naming convention and migration_lock.toml format)
  </read_first>
  <action>
    Create a new timestamped migration directory under `packages/db-main-prisma/prisma/postgres/migrations/` (match the timestamp_name convention of existing migrations, e.g. `20260601000000_add_doc_folder_model`). Inside it write `migration.sql` containing exactly: `CREATE EXTENSION IF NOT EXISTS vector;`, the `CREATE TABLE "DocFolder"` DDL with PK + spaceId FK (Cascade) + parentFolderId self-FK (SET NULL), then the three `ALTER TABLE "ImportedDoc"` statements (ADD COLUMN folderId, ADD COLUMN order DOUBLE PRECISION NOT NULL DEFAULT 0, ADD CONSTRAINT folderId_fkey SET NULL). Use the exact SQL from 13-PATTERNS.md lines 38-56.

    Do NOT use `prisma migrate dev` (Pitfall 3 — pgvector Unsupported type breaks auto-generation). Apply with:
    `pnpm -F @teable/db-main-prisma prisma-migrate deploy --schema ./prisma/postgres/schema.prisma`
    Then regenerate the client:
    `pnpm -F @teable/db-main-prisma prisma-generate`
  </action>
  <verify>
    <automated>ls packages/db-main-prisma/prisma/postgres/migrations/*add_doc_folder*/migration.sql &amp;&amp; grep -q "CREATE TABLE \"DocFolder\"" packages/db-main-prisma/prisma/postgres/migrations/*add_doc_folder*/migration.sql</automated>
  </verify>
  <acceptance_criteria>
    - A new migration directory exists containing `migration.sql` with `CREATE TABLE "DocFolder"` and the 3 ImportedDoc ALTER statements.
    - `prisma-migrate deploy` completed without error (migration applied to DB).
    - `prisma-generate` completed — the generated client exports a `DocFolder` delegate (verify: generated `@teable/db-main-prisma` types reference `docFolder`).
  </acceptance_criteria>
  <done>Migration applied, Prisma client regenerated with DocFolder delegate available.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → DB | DDL executes as DB superuser at deploy time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | hand-written migration SQL | mitigate | SQL is reviewed verbatim against 13-PATTERNS.md; uses CREATE EXTENSION IF NOT EXISTS (idempotent) and ON DELETE SET NULL/CASCADE to preserve referential integrity |
| T-13-SC | Tampering | no package installs in this plan | accept | No npm/pip/cargo installs in Wave 1 |
</threat_model>

<verification>
- `model DocFolder` present in schema.prisma.
- Migration directory with migration.sql applied via prisma-migrate deploy.
- Prisma client regenerated; `prisma.docFolder` delegate available to downstream services.
</verification>

<success_criteria>
DocFolder table live in DB, ImportedDoc has folderId/order columns, Prisma client regenerated. All downstream waves can now reference `prisma.docFolder` and `ImportedDoc.folderId`.
</success_criteria>

<output>
Create `.planning/phases/13-doc-library-redesign/13-01-SUMMARY.md` when done
</output>
