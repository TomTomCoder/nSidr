---
phase: 13-doc-library-redesign
plan: 04
type: execute
wave: 3
depends_on: ["13-02", "13-03"]
files_modified:
  - packages/openapi/src/doc-search/doc.ts
  - packages/openapi/src/doc-search/doc-folder.ts
  - packages/openapi/src/doc-search/index.ts
  - apps/nextjs-app/src/features/app/blocks/doc-search/hooks.ts
autonomous: true
requirements: [D-08]
must_haves:
  truths:
    - "Doc + folder CRUD endpoints are callable via typed @teable/openapi SDK functions"
    - "hooks.ts uses SDK functions instead of raw fetch()"
    - "IImportedDoc carries optional folderId, order, rawContent fields"
  artifacts:
    - path: packages/openapi/src/doc-search/doc.ts
      provides: "createDoc/updateDoc/listDocs/deleteDoc + Zod schemas"
      exports: ["createDoc", "updateDoc", "CreateDocSchema", "UpdateDocSchema"]
    - path: packages/openapi/src/doc-search/doc-folder.ts
      provides: "folder CRUD SDK functions + schemas"
      exports: ["CreateDocFolderSchema", "UpdateDocFolderSchema", "IDocFolder"]
  key_links:
    - from: hooks.ts
      to: "@teable/openapi"
      via: SDK function imports replacing fetch()
      pattern: "from '@teable/openapi'"
---

<objective>
Add typed SDK definitions for the new doc + folder endpoints to `@teable/openapi` (Zod schemas, axios wrappers), extend `IImportedDoc` with optional `folderId`/`order`/`rawContent`, and replace every raw `fetch()` in the frontend `hooks.ts` with SDK functions using the React Query keys pattern (D-08).

Purpose: Type-safe, consistent API access matching the rest of the app (D-08).
Output: New doc.ts + doc-folder.ts SDK modules, extended index.ts, refactored hooks.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-doc-library-redesign/13-PATTERNS.md

<interfaces>
From 13-PATTERNS.md:
- doc.ts: CreateDocSchema { title?, folderId?, content?(max 512_000), order? }, UpdateDocSchema (all optional), ICreateDoc/IUpdateDoc types, axios wrappers createDoc/updateDoc/listDocs/getDoc/deleteDoc (lines 306-334)
- doc-folder.ts: CreateDocFolderSchema { name(1-200), parentFolderId?, order? }, UpdateDocFolderSchema = partial, IDocFolder interface; wrappers listDocFolders/createDocFolder/updateDocFolder/deleteDocFolder (lines 338-363)
- index.ts: add `export * from './doc'` + `export * from './doc-folder'`; extend IImportedDoc with folderId?, order?, rawContent? (lines 367-386)
- hooks.ts current endpoints to replace (CONTEXT lines 121-127): GET docs, GET doc, GET links, POST import/markdown, POST import/pdf, DELETE doc. NEW: createDoc, updateDoc, folder CRUD.
- docKeys structure (13-PATTERNS lines 689-694): list/doc/links + NEW folders(spaceId)

Pattern source: packages/openapi/src/ai/agent.ts (registerRoute + urlBuilder + axios)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define doc.ts + doc-folder.ts SDK modules + extend index.ts</name>
  <read_first>
    - packages/openapi/src/doc-search/index.ts (the file being modified — existing IImportedDoc, IDocSearchQuery, IDocLinkGraph + barrel exports)
    - packages/openapi/src/ai/agent.ts (analog — z.object schema + registerRoute + urlBuilder + axios wrapper pattern)
    - packages/openapi/src/axios.ts (axios instance import path)
    - .planning/phases/13-doc-library-redesign/13-PATTERNS.md (doc.ts/doc-folder.ts/index.ts sections, lines 306-386)
  </read_first>
  <action>
    Create `packages/openapi/src/doc-search/doc.ts`: route constants, `CreateDocSchema`/`UpdateDocSchema` (Zod, content `.max(512_000)` per T-13-05 DoS mitigation), inferred `ICreateDoc`/`IUpdateDoc` types, and axios wrapper functions `createDoc(spaceId, data)`, `updateDoc(spaceId, docId, data)`, `listDocs(spaceId)`, `getDoc(spaceId, docId)`, `deleteDoc(spaceId, docId)` — using `urlBuilder` + the shared `axios` instance, mirroring agent.ts. Use 13-PATTERNS.md lines 306-334.

    Create `packages/openapi/src/doc-search/doc-folder.ts`: `CreateDocFolderSchema` (name 1-200), `UpdateDocFolderSchema = CreateDocFolderSchema.partial()`, `IDocFolder` interface, and wrappers `listDocFolders(spaceId)`, `createDocFolder(spaceId, data)`, `updateDocFolder(spaceId, folderId, data)`, `deleteDocFolder(spaceId, folderId)`. Use 13-PATTERNS.md lines 338-363.

    Modify `index.ts`: add `export * from './doc'` and `export * from './doc-folder'`; extend the `IImportedDoc` interface with `folderId?: string | null`, `order?: number`, `rawContent?: string` (optional — Pitfall 5).
  </action>
  <verify>
    <automated>pnpm -F @teable/openapi exec tsc --noEmit 2>&amp;1 | grep -v '^#' | grep -c "doc-search" | grep -q '^0$' &amp;&amp; grep -q "from './doc-folder'" packages/openapi/src/doc-search/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - doc.ts exports CreateDocSchema/UpdateDocSchema + createDoc/updateDoc/listDocs/getDoc/deleteDoc.
    - doc-folder.ts exports the folder schemas + IDocFolder + 4 wrapper functions.
    - index.ts re-exports both modules and IImportedDoc has folderId/order/rawContent optional fields.
    - CreateDocSchema content has `.max(512_000)`.
    - @teable/openapi typechecks with no doc-search errors.
  </acceptance_criteria>
  <done>SDK modules defined, index extended, openapi typechecks.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor hooks.ts to use SDK functions + React Query keys</name>
  <read_first>
    - apps/nextjs-app/src/features/app/blocks/doc-search/hooks.ts (the file being modified — every raw fetch() call + existing docKeys + mutation onSuccess invalidation)
    - .planning/phases/13-doc-library-redesign/13-PATTERNS.md (hooks.ts section, lines 673-698)
  </read_first>
  <action>
    Replace all raw `fetch()` calls in hooks.ts with the new `@teable/openapi` SDK functions (`listDocs`, `getDoc`, `deleteDoc`, `listDocFolders`, `createDocFolder`, `updateDocFolder`, `deleteDocFolder`, `createDoc`, `updateDoc`) — read `.data` off the axios response. Extend the `docKeys` object with `folders: (spaceId) => ['doc-folders', spaceId]`. Add new hooks: `useCreateDoc`, `useUpdateDoc`, `useDocFolders`, `useCreateDocFolder`, `useUpdateDocFolder`, `useDeleteDocFolder`, each a useQuery/useMutation that invalidates the relevant docKeys on success (mirror the existing onSuccess invalidation pattern, 13-PATTERNS lines 696-697). Keep existing import (markdown/pdf) hooks but route them through SDK functions if equivalents exist; otherwise leave the import endpoints as-is (they are out of scope per CONTEXT — DocImportPanel survives).
  </action>
  <verify>
    <automated>grep -v '^//' apps/nextjs-app/src/features/app/blocks/doc-search/hooks.ts | grep -c "fetch(" | grep -q '^0$' &amp;&amp; grep -q "from '@teable/openapi'" apps/nextjs-app/src/features/app/blocks/doc-search/hooks.ts</automated>
  </verify>
  <acceptance_criteria>
    - No raw `fetch(` call remains in hooks.ts (excluding comments).
    - hooks.ts imports doc/folder SDK functions from `@teable/openapi`.
    - docKeys includes a `folders` key.
    - New hooks (useCreateDoc, useUpdateDoc, folder CRUD hooks) exist and invalidate query keys on mutation success.
  </acceptance_criteria>
  <done>hooks.ts fully on SDK; folder + doc CRUD hooks available to UI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API | request bodies validated by Zod schemas at the SDK boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05 | DoS | CreateDocSchema/UpdateDocSchema content | mitigate | content `.max(512_000)` Zod constraint (V5 Input Validation) |
| T-13-07 | Tampering | IImportedDoc shape change | mitigate | New fields added as optional (Pitfall 5) — non-breaking for existing callers |
| T-13-SC | Tampering | no package installs | accept | No installs in this plan |
</threat_model>

<verification>
- @teable/openapi typechecks (no doc-search errors).
- hooks.ts contains zero raw fetch() and imports SDK functions.
</verification>

<success_criteria>
All doc + folder operations are reachable through typed SDK functions, hooks.ts is fully migrated off raw fetch, and IImportedDoc exposes folderId/order/rawContent for the UI.
</success_criteria>

<output>
Create `.planning/phases/13-doc-library-redesign/13-04-SUMMARY.md` when done
</output>
