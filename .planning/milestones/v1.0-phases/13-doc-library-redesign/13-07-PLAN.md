---
phase: 13-doc-library-redesign
plan: 07
type: execute
wave: 6
depends_on: ["13-05", "13-06"]
files_modified:
  - apps/nextjs-app/src/features/app/blocks/doc-search/DocLibrary.tsx
  - apps/nextjs-app/src/features/app/blocks/doc-search/DocViewer.tsx
  - apps/nextjs-app/src/features/app/blocks/doc-search/index.ts
autonomous: true
requirements: [D-01, D-07]
must_haves:
  truths:
    - "DocLibrary renders a re-resizable DocFolderTree (240px) beside a flex-1 DocEditorArea"
    - "DocViewer.tsx no longer exists and is not referenced anywhere"
    - "The doc-library page route still mounts DocLibrary inside SpaceInnerLayout"
  artifacts:
    - path: apps/nextjs-app/src/features/app/blocks/doc-search/DocLibrary.tsx
      provides: "Inner split layout (tree + editor)"
      contains: "DocFolderTree"
  key_links:
    - from: DocLibrary.tsx
      to: DocFolderTree + DocEditorArea
      via: Resizable split panel
      pattern: "Resizable"
---

<objective>
Refactor `DocLibrary.tsx` into the inner split layout (D-01) — a `re-resizable` `DocFolderTree` (240px, minWidth 160, maxWidth 400) beside a `flex-1` `DocEditorArea` — staying inside `SpaceInnerLayout`. Delete `DocViewer.tsx` entirely (D-07) and remove all references to it.

Purpose: Compose the two new UI surfaces into the page and retire the fullscreen modal viewer (D-01, D-07).
Output: Rewritten DocLibrary.tsx, deleted DocViewer.tsx, cleaned index.ts/imports.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-doc-library-redesign/13-PATTERNS.md
@.planning/phases/13-doc-library-redesign/13-UI-SPEC.md

<interfaces>
From 13-PATTERNS.md DocLibrary.tsx section (lines 390-422):
- Replace flex-col body with: a `flex h-full overflow-hidden` container holding a `Resizable` (defaultSize width 240, minWidth 160, maxWidth 400, enable right) wrapping `DocFolderTree spaceId=...`, then a `flex-1 overflow-hidden` div holding `DocEditorArea spaceId=...`
- Remove selectedDoc/showSearch local state — selection now lives in useDocEditorStore
- Resizable handle pattern from Sidebar.tsx (13-PATTERNS lines 714-726)

Layout contract: 13-UI-SPEC.md Layout Contract (lines 31-48).
D-07: DocViewer.tsx deleted entirely — selecting a doc renders inline in DocEditorArea.
Keep unchanged: DocImportPanel, GlobalDocSearchPanel, DocSearchPanel.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor DocLibrary.tsx to inner split layout</name>
  <read_first>
    - apps/nextjs-app/src/features/app/blocks/doc-search/DocLibrary.tsx (the file being modified — current flex-col body, selectedDoc/showSearch state, DocViewer usage)
    - apps/nextjs-app/src/features/app/components/sidebar/Sidebar.tsx (Resizable handle pattern, lines 118-153)
    - apps/nextjs-app/src/features/app/blocks/doc-search/DocFolderTree.tsx (from 13-05 — props)
    - apps/nextjs-app/src/features/app/blocks/doc-search/DocEditorArea.tsx (from 13-06 — props)
    - .planning/phases/13-doc-library-redesign/13-PATTERNS.md (DocLibrary.tsx section, lines 390-422)
  </read_first>
  <action>
    Rewrite the `DocLibrary` body to the inner split: a `flex h-full overflow-hidden` container with a `Resizable` (from `re-resizable`, defaultSize width 240, minWidth 160, maxWidth 400, enable right, handle styled per Sidebar.tsx) wrapping `DocFolderTree` (spaceId prop), followed by a `flex-1 overflow-hidden` div wrapping `DocEditorArea` (spaceId prop). Remove the old `selectedDoc`/`showSearch` local state and any DocViewer usage — selection lives in `useDocEditorStore` now. Preserve the DocImportPanel trigger entry point (opened from the tree header per 13-05). Keep DocLibrary on `SpaceInnerLayout` — do NOT create a new layout file (D-01).
  </action>
  <verify>
    <automated>grep -q "Resizable" apps/nextjs-app/src/features/app/blocks/doc-search/DocLibrary.tsx &amp;&amp; grep -q "DocFolderTree" apps/nextjs-app/src/features/app/blocks/doc-search/DocLibrary.tsx &amp;&amp; grep -q "DocEditorArea" apps/nextjs-app/src/features/app/blocks/doc-search/DocLibrary.tsx</automated>
  </verify>
  <acceptance_criteria>
    - DocLibrary.tsx body is a flex split: Resizable(DocFolderTree) + flex-1(DocEditorArea).
    - selectedDoc/showSearch local state removed.
    - No new layout file created; DocLibrary still renders inside SpaceInnerLayout's children.
  </acceptance_criteria>
  <done>DocLibrary composes tree + editor as a re-resizable inner split.</done>
</task>

<task type="auto">
  <name>Task 2: Delete DocViewer.tsx + remove all references</name>
  <read_first>
    - apps/nextjs-app/src/features/app/blocks/doc-search/DocViewer.tsx (the file being deleted)
    - apps/nextjs-app/src/features/app/blocks/doc-search/index.ts (the file being modified — barrel exports referencing DocViewer)
  </read_first>
  <action>
    Delete `apps/nextjs-app/src/features/app/blocks/doc-search/DocViewer.tsx` (D-07). Remove any `DocViewer` export from `index.ts` and any import of DocViewer across the doc-search feature (and anywhere else in nextjs-app). Use a repo-wide search to find references before deleting; fix or remove each.
  </action>
  <verify>
    <automated>test ! -f apps/nextjs-app/src/features/app/blocks/doc-search/DocViewer.tsx &amp;&amp; ! grep -rn "DocViewer" apps/nextjs-app/src 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - DocViewer.tsx no longer exists.
    - No reference to `DocViewer` remains anywhere under apps/nextjs-app/src (grep returns nothing).
    - nextjs-app typechecks with no errors referencing DocViewer or DocLibrary.
  </acceptance_criteria>
  <done>DocViewer fully removed; no dangling references.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | composition-only plan; no new input boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-10 | Tampering | DocViewer removal | accept | Pure deletion + recomposition; threats covered by upstream component plans |
| T-13-SC | Tampering | no package installs | accept | No installs in this plan |
</threat_model>

<verification>
- DocLibrary renders Resizable(DocFolderTree) + DocEditorArea.
- DocViewer.tsx deleted; zero references remain.
- nextjs-app typechecks.
</verification>

<success_criteria>
The doc-library page presents the folder tree beside the inline editor with no fullscreen viewer; DocViewer is gone.
</success_criteria>

<output>
Create `.planning/phases/13-doc-library-redesign/13-07-SUMMARY.md` when done
</output>
