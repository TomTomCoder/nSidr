---
phase: 13-doc-library-redesign
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - apps/nestjs-backend/src/features/doc-search/doc-folder.service.ts
  - apps/nestjs-backend/src/features/doc-search/doc-folder.controller.ts
  - apps/nestjs-backend/src/features/doc-search/doc-folder.service.spec.ts
  - apps/nestjs-backend/src/features/doc-search/doc-search.module.ts
autonomous: true
requirements: [D-11]
must_haves:
  truths:
    - "A folder can be created, renamed, reparented, reordered, and deleted via REST"
    - "Deleting a folder moves its child folders and docs to the parent (not orphaned/deleted)"
    - "Folders are scoped to the authenticated user's space"
  artifacts:
    - path: apps/nestjs-backend/src/features/doc-search/doc-folder.service.ts
      provides: "Folder CRUD with child-move-on-delete"
      exports: ["DocFolderService"]
    - path: apps/nestjs-backend/src/features/doc-search/doc-folder.controller.ts
      provides: "6 folder REST endpoints"
      exports: ["DocFolderController"]
  key_links:
    - from: doc-folder.controller.ts
      to: DocFolderService
      via: constructor injection
      pattern: "private readonly.*DocFolderService"
    - from: doc-search.module.ts
      to: DocFolderController
      via: controllers array registration
      pattern: "DocFolderController"
---

<objective>
Build the folder data layer: `DocFolderService` (list/create/update/delete with child-move-on-delete logic), `DocFolderController` (6 REST endpoints under `/api/spaces/:spaceId/doc-folders`), and register both in `DocSearchModule`.

Purpose: Provide the backend CRUD surface the folder tree consumes (D-11 specifics, lines 132-136 of CONTEXT).
Output: New service + controller + spec, module wiring updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-doc-library-redesign/13-PATTERNS.md

<interfaces>
From 13-PATTERNS.md doc-folder.service.ts section (lines 171-209):
- listFolders(spaceId): prisma.docFolder.findMany({ where: { spaceId }, orderBy: { order: 'asc' } })
- createFolder(spaceId, { name, parentFolderId?, order? })
- updateFolder(folderId, { name?, parentFolderId?, order? })  // rename / reparent / reorder
- deleteFolder(folderId): move child folders + docs to folder.parentFolderId, then delete

Endpoints (CONTEXT lines 132-136):
- GET    /api/spaces/:spaceId/doc-folders
- POST   /api/spaces/:spaceId/doc-folders
- PATCH  /api/spaces/:spaceId/doc-folders/:folderId
- DELETE /api/spaces/:spaceId/doc-folders/:folderId
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DocFolderService with child-move-on-delete</name>
  <read_first>
    - apps/nestjs-backend/src/features/doc-search/doc-ingest.controller.ts (analog for PrismaService injection + spaceId controller pattern)
    - apps/nestjs-backend/src/features/doc-search/ingestion.service.ts (analog for @Injectable service + PrismaService usage)
    - .planning/phases/13-doc-library-redesign/13-PATTERNS.md (doc-folder.service.ts section, lines 171-209)
  </read_first>
  <behavior>
    - createFolder persists a row with spaceId, name, parentFolderId (default null), order (default 0)
    - listFolders returns only rows matching spaceId, ordered by order asc
    - updateFolder applies provided fields (name/parentFolderId/order) only
    - deleteFolder reparents child folders (parentFolderId → folder.parentFolderId) AND docs (folderId → folder.parentFolderId) before deleting the folder row
  </behavior>
  <action>
    Create `doc-folder.service.ts` exporting `@Injectable() DocFolderService` with constructor-injected `PrismaService`. Implement `listFolders`, `createFolder`, `updateFolder`, `deleteFolder` per the interfaces block. `deleteFolder` MUST: load the folder via findUniqueOrThrow, `updateMany` child folders' parentFolderId to the deleted folder's parentFolderId, `updateMany` docs' folderId to the same, then delete the folder — exactly as 13-PATTERNS.md lines 195-207.

    Write `doc-folder.service.spec.ts` (Wave 0 gap) covering the four behaviors above with a mocked PrismaService — assert deleteFolder calls updateMany on both docFolder and importedDoc before delete.
  </action>
  <verify>
    <automated>pnpm test --filter=nestjs-backend -- --testPathPattern=doc-folder.service.spec</automated>
  </verify>
  <acceptance_criteria>
    - `doc-folder.service.spec.ts` passes.
    - deleteFolder spec asserts both `docFolder.updateMany` and `importedDoc.updateMany` are invoked with `{ parentFolderId/folderId: folder.parentFolderId }` before `docFolder.delete`.
    - createFolder defaults parentFolderId to null and order to 0 when omitted.
  </acceptance_criteria>
  <done>DocFolderService implemented; spec green covering CRUD + child reparenting.</done>
</task>

<task type="auto">
  <name>Task 2: DocFolderController (4 endpoints) + module registration</name>
  <read_first>
    - apps/nestjs-backend/src/features/doc-search/doc-ingest.controller.ts (controller decorator + @Param('spaceId') + @Req user pattern)
    - apps/nestjs-backend/src/features/doc-search/doc-search.module.ts (the file being modified — controllers/providers arrays)
    - apps/nestjs-backend/src/features/doc-search/doc-folder.service.ts (just created)
    - .planning/phases/13-doc-library-redesign/13-PATTERNS.md (NestJS spaceId ownership guard, lines 745-751)
  </read_first>
  <action>
    Create `doc-folder.controller.ts` with `@Controller('api/spaces/:spaceId/doc-folders')` exporting `DocFolderController`, constructor-injecting `DocFolderService`. Implement: GET `/` → listFolders(spaceId); POST `/` → createFolder(spaceId, body); PATCH `/:folderId` → updateFolder(folderId, body); DELETE `/:folderId` → deleteFolder(folderId). Each method validates spaceId ownership using the same guard/membership pattern as existing `/api/spaces/:spaceId` controllers in the codebase (do NOT invent a new mechanism — mirror doc-ingest.controller.ts's auth surface).

    Register `DocFolderController` in `doc-search.module.ts` controllers array and `DocFolderService` in providers array. Preserve all existing entries.
  </action>
  <verify>
    <automated>grep -q "DocFolderController" apps/nestjs-backend/src/features/doc-search/doc-search.module.ts &amp;&amp; grep -q "DocFolderService" apps/nestjs-backend/src/features/doc-search/doc-search.module.ts &amp;&amp; pnpm -F @teable/nestjs-backend exec tsc --noEmit -p tsconfig.json 2>&amp;1 | grep -v '^#' | grep -c "doc-folder" | grep -q '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - `doc-folder.controller.ts` declares 4 routes (GET/POST/PATCH/DELETE) under the doc-folders path.
    - `doc-search.module.ts` lists both DocFolderController (controllers) and DocFolderService (providers).
    - TypeScript compiles with no errors referencing doc-folder files.
  </acceptance_criteria>
  <done>Folder controller wired into DocSearchModule; compiles clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API | spaceId path param is attacker-controllable; must be validated against the authenticated user's spaces |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02 | Spoofing | DocFolderController routes | mitigate | Validate spaceId ownership before any DB op, mirroring existing /api/spaces controller guards (V4 Access Control) |
| T-13-03 | Information Disclosure | listFolders | mitigate | findMany always scoped by `where: { spaceId }` — no cross-space leakage |
| T-13-SC | Tampering | no package installs | accept | No installs in this plan |
</threat_model>

<verification>
- doc-folder.service.spec passes (CRUD + reparent-on-delete).
- DocFolderController + DocFolderService registered in DocSearchModule.
- Backend compiles.
</verification>

<success_criteria>
Folders can be created, listed, renamed, reparented, reordered, and deleted via REST, scoped to the user's space, with children safely reparented on delete.
</success_criteria>

<output>
Create `.planning/phases/13-doc-library-redesign/13-02-SUMMARY.md` when done
</output>
