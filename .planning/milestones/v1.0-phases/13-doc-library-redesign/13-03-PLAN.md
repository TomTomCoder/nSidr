---
phase: 13-doc-library-redesign
plan: 03
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - apps/nestjs-backend/src/features/doc-search/doc-crud.controller.ts
  - apps/nestjs-backend/src/features/doc-search/doc-crud.controller.spec.ts
  - apps/nestjs-backend/src/features/doc-search/doc-ingest.processor.ts
  - apps/nestjs-backend/src/features/doc-search/ingestion.service.ts
  - apps/nestjs-backend/src/features/doc-search/embedding.service.ts
  - apps/nestjs-backend/src/features/ai/unified-ai.service.ts
  - apps/nestjs-backend/src/features/doc-search/doc-search.module.ts
autonomous: true
requirements: [D-05, D-06, D-09, D-10]
must_haves:
  truths:
    - "An empty doc can be created via POST and content updated via PATCH"
    - "Updating doc content sets isIndexed=false and queues a reindex job"
    - "The ingest processor handles a 'reindex' job by re-chunking and re-embedding the doc"
    - "EmbeddingService routes embeddings through UnifiedAiService"
    - "Missing pgvector extension logs a clear error without crashing the app"
  artifacts:
    - path: apps/nestjs-backend/src/features/doc-search/doc-crud.controller.ts
      provides: "POST create empty doc + PATCH update doc"
      exports: ["DocCrudController"]
    - path: apps/nestjs-backend/src/features/ai/unified-ai.service.ts
      provides: "generateEmbeddings(texts) method"
      contains: "generateEmbeddings"
  key_links:
    - from: doc-crud.controller.ts PATCH handler
      to: DOC_INGEST_QUEUE
      via: queue.add('reindex', ...)
      pattern: "queue\\.add\\('reindex'"
    - from: embedding.service.ts
      to: UnifiedAiService.generateEmbeddings
      via: delegation
      pattern: "unifiedAiService\\.generateEmbeddings"
---

<objective>
Build the doc CRUD + re-indexation backend: `DocCrudController` (POST create empty doc, PATCH update title/content/folderId/order), a `reindex` branch in `DocIngestProcessor` + `reindexDoc` in `IngestionService`, route `EmbeddingService` through `UnifiedAiService.generateEmbeddings` (D-09), and a pgvector startup guard in `DocSearchModule` (D-10).

Purpose: Wire in-app doc creation + auto-save re-indexation (D-05/D-06) and decouple embeddings from hardcoded OpenAI (D-09), with a non-fatal pgvector guard (D-10).
Output: New controller + spec, processor/ingestion/embedding/unified-ai modifications, module guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-doc-library-redesign/13-PATTERNS.md

<interfaces>
From 13-PATTERNS.md:
- DocCrudController @Controller('api/spaces/:spaceId/docs'): POST createDoc(spaceId, body, req), PATCH updateDoc(spaceId, docId, body) (lines 99-154)
- DocIngestJobData extended with type 'reindex' + optional docId (lines 219-229)
- process() adds `else if (type === 'reindex' && job.data.docId)` → ingestionService.reindexDoc(spaceId, docId) (lines 234-244)
- UnifiedAiService.generateEmbeddings(texts: string[]): Promise<number[][]> (lines 256-276)
- EmbeddingService delegates generateBatchEmbeddings → unifiedAiService.generateEmbeddings (lines 287-301)
- BullMQ inject: @InjectQueue(DOC_INGEST_QUEUE) private readonly queue: Queue (lines 707-711)

NOTE (Open Question 2): The processor uses a single process() method handling ALL jobs (no @Process('ingest') decorator) — add an `if (job.name === 'reindex')` / type branch, do not assume name-specific decorator.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: UnifiedAiService.generateEmbeddings + EmbeddingService delegation + spec</name>
  <read_first>
    - apps/nestjs-backend/src/features/ai/unified-ai.service.ts (the file being modified — class structure, existing chat() method, ServiceUnavailableException usage)
    - apps/nestjs-backend/src/features/doc-search/embedding.service.ts (the file being modified — current direct OpenAI call to replace)
    - apps/nestjs-backend/src/features/ai/unified-ai.service.spec.ts (existing spec — pattern for the new spec)
    - .planning/phases/13-doc-library-redesign/13-PATTERNS.md (unified-ai.service.ts + embedding.service.ts sections, lines 249-301)
  </read_first>
  <action>
    Add `generateEmbeddings(texts: string[]): Promise<number[][]>` to `UnifiedAiService` (D-09). Per RESEARCH Open Question 3 the configured provider may lack embedding support — implement the batched OpenAI embeddings call (model `text-embedding-3-small`, batchSize 20) as the concrete fallback, throwing `ServiceUnavailableException` when `OPENAI_API_KEY` is absent or the API returns non-OK. Use 13-PATTERNS.md lines 256-276 exactly.

    Refactor `EmbeddingService`: inject `UnifiedAiService`, make `generateBatchEmbeddings(texts)` delegate to `unifiedAiService.generateEmbeddings(texts)`, and `generateEmbedding(text)` call the batch with a single element. Remove the direct `https://api.openai.com/v1/embeddings` call from EmbeddingService.

    Create `embedding.service.spec.ts` (Wave 0 gap) asserting generateBatchEmbeddings delegates to a mocked UnifiedAiService.generateEmbeddings (covers D-09).
  </action>
  <verify>
    <automated>pnpm test --filter=nestjs-backend -- --testPathPattern="embedding.service.spec"</automated>
  </verify>
  <acceptance_criteria>
    - `UnifiedAiService` exports `generateEmbeddings` returning `number[][]`.
    - `EmbeddingService.generateBatchEmbeddings` calls `unifiedAiService.generateEmbeddings` (verified by spec mock assertion).
    - No `api.openai.com/v1/embeddings` literal remains in embedding.service.ts (grep returns 0 non-comment matches).
    - embedding.service.spec passes.
  </acceptance_criteria>
  <done>Embeddings routed through UnifiedAiService; spec green.</done>
</task>

<task type="auto">
  <name>Task 2: DocCrudController (POST/PATCH) + reindex job branch + module wiring</name>
  <read_first>
    - apps/nestjs-backend/src/features/doc-search/doc-ingest.controller.ts (analog — InjectQueue, DOC_INGEST_QUEUE, DocIngestJobData, @Param/@Body/@Req)
    - apps/nestjs-backend/src/features/doc-search/doc-ingest.processor.ts (the file being modified — process() method + DocIngestJobData interface)
    - apps/nestjs-backend/src/features/doc-search/ingestion.service.ts (the file being modified — existing ingestMarkdown; add reindexDoc)
    - apps/nestjs-backend/src/features/doc-search/doc-search.module.ts (the file being modified — register DocCrudController)
    - .planning/phases/13-doc-library-redesign/13-PATTERNS.md (doc-crud.controller.ts + processor sections, lines 85-245)
  </read_first>
  <action>
    Create `doc-crud.controller.ts`: `@Controller('api/spaces/:spaceId/docs')` `DocCrudController` injecting `@InjectQueue(DOC_INGEST_QUEUE)` Queue + PrismaService. POST `/` creates an ImportedDoc (title default 'Untitled', sourceType 'markdown', rawContent from body.content ?? '', folderId, order, isIndexed false, createdBy req.user.id) and queues a `'reindex'` job only when rawContent is non-empty. PATCH `/:docId` applies provided title/content/folderId/order; when content is provided set `isIndexed: false` and queue a `'reindex'` job. Use 13-PATTERNS.md lines 99-154 verbatim. Validate spaceId ownership using the existing /api/spaces guard pattern.

    Extend `DocIngestJobData` in doc-ingest.processor.ts: add `'reindex'` to the type union, make `title`/`userId` optional, add optional `docId`. In `process()` add a branch `else if (type === 'reindex' && job.data.docId)` calling `ingestionService.reindexDoc(spaceId, docId)` — branch on `job.data.type`, not a decorator (Open Question 2).

    Add `reindexDoc(spaceId, docId)` to `IngestionService`: load the doc's rawContent, delete its existing DocChunk rows, re-chunk + re-embed using existing ingestion helpers, then set `isIndexed: true`. Reuse the existing chunk/embed code path used by ingestMarkdown.

    Register `DocCrudController` in doc-search.module.ts controllers array.
  </action>
  <verify>
    <automated>pnpm test --filter=nestjs-backend -- --testPathPattern="doc-crud.controller.spec"</automated>
  </verify>
  <acceptance_criteria>
    - `doc-crud.controller.spec.ts` (Wave 0) passes, asserting: PATCH with content sets isIndexed=false AND calls queue.add('reindex', ...); POST creates doc with isIndexed=false.
    - `DocIngestJobData.type` union includes `'reindex'`.
    - process() has a reindex branch calling `ingestionService.reindexDoc`.
    - `IngestionService.reindexDoc` exists and deletes old chunks before re-embedding then sets isIndexed=true.
    - DocCrudController registered in doc-search.module.ts.
  </acceptance_criteria>
  <done>Doc create/update endpoints live; content updates queue reindex; processor + ingestion handle reindex; spec green.</done>
</task>

<task type="auto">
  <name>Task 3: pgvector startup guard in DocSearchModule (D-10)</name>
  <read_first>
    - apps/nestjs-backend/src/features/doc-search/doc-search.module.ts (the file being modified)
    - .planning/phases/13-doc-library-redesign/13-PATTERNS.md (No Analog Found section, lines 757-759 — onModuleInit try/catch pattern)
  </read_first>
  <action>
    Make `DocSearchModule` implement `OnModuleInit`. In `onModuleInit()`, run `CREATE EXTENSION IF NOT EXISTS vector` via the Prisma raw executor inside a try/catch. On failure, log via NestJS `Logger` at error level: "pgvector extension not installed — DocSearch and embedding features unavailable." Do NOT rethrow — the app must continue booting (D-10). Inject PrismaService into the module class for the raw query (or use a small init provider if module-level injection is awkward — follow the codebase's existing module-init convention if one exists).
  </action>
  <verify>
    <automated>grep -q "OnModuleInit" apps/nestjs-backend/src/features/doc-search/doc-search.module.ts &amp;&amp; grep -q "pgvector extension not installed" apps/nestjs-backend/src/features/doc-search/doc-search.module.ts</automated>
  </verify>
  <acceptance_criteria>
    - DocSearchModule implements OnModuleInit and runs `CREATE EXTENSION IF NOT EXISTS vector` in try/catch.
    - On failure it logs the exact message and does NOT rethrow (no crash).
    - Backend compiles.
  </acceptance_criteria>
  <done>pgvector guard logs cleanly on missing extension; app boots regardless.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API | spaceId/docId path params + doc content body are attacker-controllable |
| API → OpenAI | embeddings request crosses to third-party |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04 | Spoofing | DocCrudController routes | mitigate | Validate spaceId ownership before DB ops (V4) |
| T-13-05 | DoS | PATCH/POST rawContent | mitigate | Zod maxLength 512KB on content enforced in @teable/openapi schema (Plan 13-04, V5) |
| T-13-06 | Information Disclosure | generateEmbeddings | accept | Embeddings are non-PII derived vectors; OPENAI_API_KEY read from env, never logged |
| T-13-SC | Tampering | no package installs | accept | No installs in this plan |
</threat_model>

<verification>
- embedding.service.spec + doc-crud.controller.spec green.
- DocCrudController registered; reindex branch + reindexDoc present.
- pgvector guard logs without crashing.
</verification>

<success_criteria>
In-app doc create/update works, content updates trigger re-indexation via BullMQ, embeddings flow through UnifiedAiService, and a missing pgvector extension degrades gracefully.
</success_criteria>

<output>
Create `.planning/phases/13-doc-library-redesign/13-03-SUMMARY.md` when done
</output>
