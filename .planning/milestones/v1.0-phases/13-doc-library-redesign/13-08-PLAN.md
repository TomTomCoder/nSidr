---
phase: 13-doc-library-redesign
plan: 08
type: execute
wave: 7
depends_on: ["13-07", "13-03"]
files_modified: []
autonomous: false
requirements: [D-05, D-06]
must_haves:
  truths:
    - "A user can create a folder, create a doc in it, edit content, and see it auto-save"
    - "Auto-save triggers re-indexation and the status dot flips to indexed"
    - "Split and Preview modes render the markdown"
    - "The search_knowledge_base agent tool returns content from the newly created doc"
  artifacts: []
  key_links:
    - from: editor auto-save
      to: search_knowledge_base agent tool
      via: re-indexation pipeline (DOC_INGEST_QUEUE → pgvector)
      pattern: "search_knowledge_base"
---

<objective>
End-to-end smoke verification of the redesigned doc library against the running app: folder creation, in-app doc creation + editing, auto-save → re-index, mode switching, and the full loop into the `search_knowledge_base` agent tool returning the new doc's content.

Purpose: Prove the goal-backward truths hold against the real stack (D-05, D-06 + the search loop).
Output: Verified phase; any defects surfaced for gap closure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-doc-library-redesign/13-CONTEXT.md
@.planning/phases/13-doc-library-redesign/13-UI-SPEC.md

<interfaces>
- App runs on port 3000 (Next.js UI). Backend dev on 3001. (MEMORY: project_launch_architecture)
- Doc library route: /space/[spaceId]/doc-library
- Agent tool: search_knowledge_base registered in apps/nestjs-backend/src/features/agent/agent-tool-registry.service.ts, backed by apps/nestjs-backend/src/features/doc-search/search.service.ts (spaceId-scoped)
- Status dot: green=indexed (bg-success), yellow=pending (bg-warning)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend + frontend build/typecheck gate before browser run</name>
  <read_first>
    - .planning/phases/13-doc-library-redesign/13-RESEARCH.md (Validation Architecture — test commands)
  </read_first>
  <action>
    Confirm the stack is healthy before the manual browser pass: run the nestjs-backend doc test subset and a typecheck of both nextjs-app and openapi. Ensure the app is running on port 3000 (start dev if needed). Fix any compile/test failures or surface them as blockers.
  </action>
  <verify>
    <automated>pnpm test --filter=nestjs-backend -- --testPathPattern="doc-folder.service.spec|doc-crud.controller.spec|embedding.service.spec"</automated>
  </verify>
  <acceptance_criteria>
    - doc-folder.service.spec, doc-crud.controller.spec, embedding.service.spec all green.
    - nextjs-app and @teable/openapi typecheck clean.
    - App reachable on http://localhost:3000.
  </acceptance_criteria>
  <done>Stack compiles and unit specs pass; app is up for manual verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The full redesigned doc library: re-resizable folder tree + inline CodeMirror markdown editor with Edit/Split/Preview, auto-save re-indexation, and agent knowledge-base search integration.</what-built>
  <how-to-verify>
    1. Visit http://localhost:3000 and open a space, then the "Doc Library" nav item (route /space/[spaceId]/doc-library).
    2. Click "New Folder" in the tree header — confirm a folder appears; rename it inline (double-click or context menu → Rename), press Enter.
    3. Use the folder's context menu → "New Document" — confirm an empty doc opens immediately in the editor (edit mode).
    4. Type several lines of markdown (include a heading and a bullet list). Wait ~1s — confirm the "Saving..." → "Saved" indicator appears, and the doc node's status dot turns yellow (Indexing...) then green (Indexed) within a few seconds.
    5. Toggle "Split" — confirm CodeMirror left + rendered markdown right. Toggle "Preview" — confirm rendered markdown only. Toggle back to "Edit".
    6. Open the AI/agent chat and ask a question whose answer is in the doc you just wrote (use a distinctive phrase you typed). Confirm the agent's `search_knowledge_base` returns content from your new doc.
    7. Delete the document (context menu → Delete → confirm dialog "Delete Document"). Confirm it disappears and the editor shows the "Select a document" empty state.
    8. Reload the page — confirm the folder persists and the tree expand state is remembered.
  </how-to-verify>
  <resume-signal>Type "approved" if all 8 steps pass, or describe which step failed and what you observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → API → pgvector → agent | full create→index→search loop crosses every tier |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-11 | Information Disclosure | search_knowledge_base cross-space | mitigate | search.service is spaceId-scoped; verify the agent only returns the active space's docs |
| T-13-SC | Tampering | no package installs | accept | No installs in this plan |
</threat_model>

<verification>
- Unit specs green; typechecks clean; app on 3000.
- 8-step manual smoke passes (create folder/doc, edit, auto-save→index, split/preview, agent search, delete, persistence).
</verification>

<success_criteria>
The redesigned doc library works end-to-end: folder/doc creation, markdown editing, auto-save re-indexation, mode switching, and agent knowledge-base retrieval — all against the live stack.
</success_criteria>

<output>
Create `.planning/phases/13-doc-library-redesign/13-08-SUMMARY.md` when done
</output>
