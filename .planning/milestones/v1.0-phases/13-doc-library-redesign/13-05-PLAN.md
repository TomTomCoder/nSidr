---
phase: 13-doc-library-redesign
plan: 05
type: execute
wave: 4
depends_on: ["13-04"]
files_modified:
  - apps/nextjs-app/src/features/app/blocks/doc-search/useDocEditorStore.ts
  - apps/nextjs-app/src/features/app/blocks/doc-search/DocFolderTree.tsx
  - packages/sdk/src/config/local-storage-keys.ts
autonomous: true
requirements: [D-02, D-04, D-05, D-06]
must_haves:
  truths:
    - "The folder tree renders folders and docs hierarchically from the API"
    - "A folder can be created, renamed (inline), and deleted via the tree UI"
    - "A new empty doc can be created under a folder via the tree"
    - "Each doc node shows an index status dot (green=indexed, yellow=pending)"
    - "Selecting a doc node updates useDocEditorStore.selectedDocId"
  artifacts:
    - path: apps/nextjs-app/src/features/app/blocks/doc-search/useDocEditorStore.ts
      provides: "Zustand persisted store for selectedDocId/selectedFolderId/mode"
      exports: ["useDocEditorStore"]
    - path: apps/nextjs-app/src/features/app/blocks/doc-search/DocFolderTree.tsx
      provides: "headless-tree folder+doc tree with inline rename + context menu + status dot"
      exports: ["DocFolderTree"]
  key_links:
    - from: DocFolderTree.tsx
      to: useDocEditorStore
      via: setSelectedDoc on node primary action
      pattern: "setSelectedDoc"
    - from: DocFolderTree.tsx
      to: useDocFolders/useCreateDocFolder hooks
      via: react-query hooks from hooks.ts
      pattern: "useDocFolders"
---

<objective>
Create the Zustand store `useDocEditorStore` (D-04) and the `DocFolderTree` component (D-02) — a headless-tree-based hierarchy of folder + doc nodes with inline rename, context menu (Rename / New Document / Delete), index status dots (D-05), and "New Folder"/"New Document" creation (D-06), mirroring `BaseNodeTree` exactly.

Purpose: The navigation surface of the knowledge workspace.
Output: useDocEditorStore.ts, DocFolderTree.tsx, new LocalStorageKeys entries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-doc-library-redesign/13-PATTERNS.md
@.planning/phases/13-doc-library-redesign/13-UI-SPEC.md

<interfaces>
From 13-PATTERNS.md:
- useDocEditorStore: { selectedDocId, selectedFolderId, mode: 'edit'|'split'|'preview', setSelectedDoc, setSelectedFolder, setMode }, persist name LocalStorageKeys.DocEditor, partialize mode only (lines 642-668)
- DocTreeItemData union: folder { type:'folder', id, name, parentFolderId, order, children? } | doc { type:'doc', id, title, folderId, order, isIndexed } (lines 455-457)
- useTree config: rootItemId ROOT_ID, indent 20, memoized dataLoader (Pitfall 4), getItemName/isItemFolder by type, features [syncDataLoaderFeature, selectionFeature, hotkeysCoreFeature, dragAndDropFeature, keyboardDragAndDropFeature] (lines 464-485)
- expandedItems via useLocalStorage(LocalStorageKeys.DocFolderTreeExpandedItems, []) (lines 490-493)
- ItemStatus dot: bg-emerald-500 if isIndexed else bg-yellow-500 (lines 498-504; UI-SPEC uses bg-success/bg-warning)
- hooks from hooks.ts (13-04): useDocFolders, useCreateDocFolder, useUpdateDocFolder, useDeleteDocFolder, useCreateDoc, useDocs (list)

Analog: apps/nextjs-app/src/features/app/blocks/base/base-side-bar/BaseNodeTree.tsx
UI contract: 13-UI-SPEC.md (DocFolderTree Panel section, lines 156-204) — header copy "Knowledge Base", node heights h-8, status dot colors, context menu items, copywriting table.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: useDocEditorStore + LocalStorageKeys entries</name>
  <read_first>
    - apps/nextjs-app/src/features/app/components/sidebar/useSidebarStore.ts (analog — persist + LocalStorageKeys)
    - apps/nextjs-app/src/features/app/components/sidebar/useChatPanelStore.ts (analog — partialize pattern)
    - packages/sdk/src/config/local-storage-keys.ts (the file being modified — LocalStorageKeys enum)
    - .planning/phases/13-doc-library-redesign/13-PATTERNS.md (useDocEditorStore section, lines 629-668)
  </read_first>
  <action>
    Add two entries to the `LocalStorageKeys` enum in packages/sdk/src/config/local-storage-keys.ts: `DocEditor = 'ls_doc_editor'` and `DocFolderTreeExpandedItems = 'ls_doc_folder_tree_expanded'`.

    Create `useDocEditorStore.ts` exporting `useDocEditorStore` via `create<IDocEditorState>()(persist(...))` with state `{ selectedDocId, selectedFolderId, mode }` and setters `setSelectedDoc`/`setSelectedFolder`/`setMode`, persisted under `LocalStorageKeys.DocEditor`, `partialize` to persist `mode` only (D-04, 13-PATTERNS lines 642-668).
  </action>
  <verify>
    <automated>grep -q "DocEditor = 'ls_doc_editor'" packages/sdk/src/config/local-storage-keys.ts &amp;&amp; grep -q "useDocEditorStore" apps/nextjs-app/src/features/app/blocks/doc-search/useDocEditorStore.ts</automated>
  </verify>
  <acceptance_criteria>
    - LocalStorageKeys has DocEditor and DocFolderTreeExpandedItems entries.
    - useDocEditorStore exports the store with selectedDocId/selectedFolderId/mode + 3 setters.
    - persist partialize keeps only `mode`.
  </acceptance_criteria>
  <done>Store + LocalStorageKeys ready for tree and editor consumption.</done>
</task>

<task type="auto">
  <name>Task 2: DocFolderTree component (headless-tree + inline rename + context menu + status dot)</name>
  <read_first>
    - apps/nextjs-app/src/features/app/blocks/base/base-side-bar/BaseNodeTree.tsx (THE analog — copy structure: imports, useTree, inline rename, context menu, drag line, status, useLocalStorage expand)
    - apps/nextjs-app/src/features/app/blocks/doc-search/hooks.ts (data hooks from 13-04)
    - apps/nextjs-app/src/features/app/blocks/doc-search/useDocEditorStore.ts (just created — Task 1)
    - .planning/phases/13-doc-library-redesign/13-PATTERNS.md (DocFolderTree.tsx section, lines 427-533)
    - .planning/phases/13-doc-library-redesign/13-UI-SPEC.md (DocFolderTree Panel + Copywriting + Color + Interaction States sections)
  </read_first>
  <action>
    Create `DocFolderTree.tsx` mirroring `BaseNodeTree.tsx`. Build the `DocTreeItemData` union (folder | doc). Fetch folders via `useDocFolders(spaceId)` and docs via the docs list hook; assemble a memoized treeItems map keyed by id with `children` arrays (folders' children = subfolders + docs by folderId; ROOT_ID children = top-level folders + folderless docs). Memoize the dataLoader (Pitfall 4 — getItem/getChildren read from a stable ref). Configure `useTree` with the features list from the interfaces block; `getItemName`/`isItemFolder` branch on `type`; `canDrag: () => !editingId`.

    Render the panel header per UI-SPEC: Folder icon + "Knowledge Base" + ghost icon buttons "New Folder" (calls useCreateDocFolder) and "Import" (opens existing DocImportPanel trigger — leave import wiring intact). Render tree rows at `h-8`: folders show ChevronRight (rotate-90 expanded) + Folder icon; docs show File icon + name + `ItemStatus` dot (`bg-success` indexed / `bg-warning` pending, `size-1.5 rounded-full`, with aria-label). Add a `MoreHorizontal` context menu (DropdownMenu) per node: Rename, New Document (folders only → useCreateDoc with folderId then setSelectedDoc to the new id), separator, Delete (text-destructive → AlertDialog confirm with the UI-SPEC delete copy). Inline rename input replaces the label (Enter save → useUpdateDocFolder/updateDoc, Escape cancel, auto-select). On node primary action: if doc, `setSelectedDoc(id)`; if folder, `setSelectedFolder(id)`. Persist expand state with `useLocalStorage(LocalStorageKeys.DocFolderTreeExpandedItems, [])`.

    Use exact copy strings and color tokens from 13-UI-SPEC.md. Drag-drop reorder uses createOnDropHandler (drag between folders is nice-to-have/deferred — wire reorder within same parent; cross-folder may be left as no-op).
  </action>
  <verify>
    <automated>pnpm -F @teable/nextjs-app exec tsc --noEmit 2>&amp;1 | grep -v '^#' | grep -c "DocFolderTree" | grep -q '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - DocFolderTree.tsx exports `DocFolderTree`, uses `useTree` with syncDataLoaderFeature + selectionFeature + dragAndDropFeature + hotkeysCoreFeature + keyboardDragAndDropFeature.
    - dataLoader is memoized (does not re-create per render).
    - Header renders "Knowledge Base" + New Folder + Import controls.
    - Doc nodes render an ItemStatus dot colored by isIndexed; status span has an aria-label.
    - Context menu has Rename / New Document (folders) / Delete; Delete opens an AlertDialog with the UI-SPEC copy.
    - Selecting a doc calls setSelectedDoc; creating a New Document calls useCreateDoc with folderId.
    - nextjs-app typechecks with no DocFolderTree errors.
  </acceptance_criteria>
  <done>Folder tree renders hierarchy with create/rename/delete + status dots, wired to store + SDK hooks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → API | folder/doc mutations go through the SDK + React Query hooks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-08 | Tampering | inline rename / create inputs | mitigate | Lengths bounded server-side via Zod (Plan 13-04); UI is presentational only |
| T-13-SC | Tampering | no package installs | accept | No installs in this plan |
</threat_model>

<verification>
- nextjs-app typechecks (no DocFolderTree errors).
- Store + LocalStorageKeys entries present.
- Tree renders folders/docs with status dots, context menu, inline rename (browser-verified in Plan 13-08).
</verification>

<success_criteria>
The folder tree lists the space's folders and docs, supports create/rename/delete of folders and new-doc creation, shows index status, and drives selection through useDocEditorStore.
</success_criteria>

<output>
Create `.planning/phases/13-doc-library-redesign/13-05-SUMMARY.md` when done
</output>
