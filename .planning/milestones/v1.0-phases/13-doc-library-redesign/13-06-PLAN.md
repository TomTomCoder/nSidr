---
phase: 13-doc-library-redesign
plan: 06
type: execute
wave: 5
depends_on: ["13-05"]
files_modified:
  - apps/nextjs-app/src/features/app/blocks/doc-search/DocEditorArea.tsx
  - apps/nextjs-app/package.json
autonomous: true
requirements: [D-03, D-04, D-05]
must_haves:
  truths:
    - "Selecting a doc loads its content into a CodeMirror markdown editor"
    - "Typing triggers an 800ms-debounced auto-save (PATCH) that re-indexes"
    - "Edit / Split / Preview modes toggle via the toolbar"
    - "Split and Preview render markdown via react-markdown"
    - "The toolbar shows a breadcrumb + index status badge + transient save indicator"
  artifacts:
    - path: apps/nextjs-app/src/features/app/blocks/doc-search/DocEditorArea.tsx
      provides: "CodeMirror markdown editor + split/preview + auto-save"
      exports: ["DocEditorArea"]
  key_links:
    - from: DocEditorArea.tsx
      to: useUpdateDoc (PATCH)
      via: lodash/debounce 800ms on content change
      pattern: "debounce\\("
    - from: DocEditorArea.tsx
      to: useDocEditorStore
      via: mode + selectedDocId
      pattern: "useDocEditorStore"
---

<objective>
Create `DocEditorArea` — a CodeMirror `lang-markdown` editor (D-03, the only new package) with Edit/Split/Preview modes driven by `useDocEditorStore` (D-04), 800ms debounced auto-save that PATCHes content and triggers re-indexation (D-05), a toolbar (breadcrumb + mode ToggleGroup + index status badge + transient "Saving/Saved" indicator), a title input, and a no-doc-selected empty state.

Purpose: The editing surface of the knowledge workspace.
Output: DocEditorArea.tsx, `@codemirror/lang-markdown` added to nextjs-app, react-markdown availability confirmed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-doc-library-redesign/13-PATTERNS.md
@.planning/phases/13-doc-library-redesign/13-UI-SPEC.md

<interfaces>
From 13-PATTERNS.md DocEditorArea section (lines 537-625):
- Two separate useEffects (Pitfall 2): init effect deps [theme, readOnly] only; value-sync effect deps [value] using view.dispatch
- Extensions: history, drawSelection, dropCursor, markdown(), EditorView.lineWrapping, syntaxHighlighting(defaultHighlightStyle), oneDark when dark, keymap [defaultKeymap, historyKeymap]
- Auto-save: stable debounce(800) over a saveRef; handleChange sets local content + debouncedSave
- Modes: edit = CodeMirror only; split = CodeMirror w-1/2 + ReactMarkdown w-1/2; preview = ReactMarkdown only
- hooks: useUpdateDoc (PATCH), useDoc/getDoc (load selected doc content + isIndexed)

Analog: apps/nextjs-app/src/features/app/base-node/CodeEditor.tsx (CodeMirror direct EditorView pattern)
UI contract: 13-UI-SPEC.md DocEditorArea section (lines 207-256) — toolbar h-10, ToggleGroup Edit/Split/Preview, Badge statuses, title input text-lg, prose prose-sm preview, FolderOpen empty state, save indicator.
Pitfall 1: react-markdown is in packages/sdk/package.json (v9.0.1) but maybe not nextjs-app — verify/add.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @codemirror/lang-markdown + confirm react-markdown availability</name>
  <read_first>
    - apps/nextjs-app/package.json (the file being modified — confirm existing @codemirror/* versions + whether react-markdown is present)
    - packages/sdk/package.json (confirm react-markdown@9.0.1)
    - .planning/phases/13-doc-library-redesign/13-RESEARCH.md (Package Legitimacy Audit + Pitfall 1)
  </read_first>
  <action>
    In `apps/nextjs-app`, install the only new package: `@codemirror/lang-markdown@6.5.0` (approved in the Package Legitimacy Audit). Verify `react-markdown` is importable from nextjs-app — if it is NOT already a direct/transitive dependency resolvable from nextjs-app, add `react-markdown@9.0.1` to apps/nextjs-app (Pitfall 1). Run the workspace install.
  </action>
  <verify>
    <automated>grep -q "@codemirror/lang-markdown" apps/nextjs-app/package.json &amp;&amp; node -e "require.resolve('react-markdown', { paths: ['apps/nextjs-app'] })" 2>/dev/null &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `@codemirror/lang-markdown` at ^6.5.0 present in apps/nextjs-app/package.json.
    - `react-markdown` resolvable from apps/nextjs-app.
  </acceptance_criteria>
  <done>lang-markdown installed; react-markdown confirmed importable.</done>
</task>

<task type="auto">
  <name>Task 2: DocEditorArea — CodeMirror markdown + modes + auto-save + toolbar</name>
  <read_first>
    - apps/nextjs-app/src/features/app/base-node/CodeEditor.tsx (THE analog — two-useEffect EditorView lifecycle, theme handling, value sync via dispatch)
    - apps/nextjs-app/src/features/app/blocks/doc-search/useDocEditorStore.ts (mode + selectedDocId — from 13-05)
    - apps/nextjs-app/src/features/app/blocks/doc-search/hooks.ts (useUpdateDoc + doc load hook — from 13-04)
    - .planning/phases/13-doc-library-redesign/13-PATTERNS.md (DocEditorArea section, lines 537-625)
    - .planning/phases/13-doc-library-redesign/13-UI-SPEC.md (DocEditorArea section, lines 207-256 + Copywriting + Loading States)
  </read_first>
  <action>
    Create `DocEditorArea.tsx`. Read `selectedDocId` + `mode` from `useDocEditorStore`. When no doc is selected, render the centered "Select a document" empty state (FolderOpen 48px, UI-SPEC copy). When a doc is selected, load it (getDoc/useDoc) into local content state.

    Editor: instantiate CodeMirror `EditorView` exactly as CodeEditor.tsx — init `useEffect` deps `[theme, readOnly]` ONLY, extensions per the interfaces block with `markdown()` swapped in and `EditorView.lineWrapping` added; a SECOND `useEffect` deps `[value]` syncs external content via `view.dispatch` (Pitfall 2 — never recreate the view on keystroke). 16px content padding via EditorView.theme.

    Auto-save: a stable `lodash/debounce` (800ms) wrapping a `saveRef` that calls `useUpdateDoc` PATCH with `{ content }` (D-05). `handleChange` sets local content + fires the debounce. Show a transient "Saving..."→"Saved" indicator (text-xs text-muted-foreground, fade after ~2s).

    Toolbar (h-10, border-b, px-4): breadcrumb (folder name > doc title, text-xs muted with ChevronRight separators); a shadcn `ToggleGroup` with Edit/Split/Preview calling `setMode`; an index-status `Badge` reflecting the loaded doc's isIndexed ("Indexed"/"Indexing..."/"Index failed" — poll isIndexed via the doc query refetch interval while pending, D-05). Title input below toolbar: full-width borderless `text-lg font-semibold`, placeholder "Untitled", PATCHes title on change (debounced or onBlur).

    Modes: edit = CodeMirror container only; split = `flex h-full` CodeMirror w-1/2 + ReactMarkdown preview w-1/2 (`prose prose-sm dark:prose-invert`); preview = ReactMarkdown only full width. Render preview through the react-markdown component only — never inject a raw HTML string into the DOM (XSS mitigation).

    Use exact copy + tokens from 13-UI-SPEC.md.
  </action>
  <verify>
    <automated>pnpm -F @teable/nextjs-app exec tsc --noEmit 2>&amp;1 | grep -v '^#' | grep -c "DocEditorArea" | grep -q '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - DocEditorArea.tsx exports `DocEditorArea` with two separate useEffects (init deps [theme, readOnly]; sync deps [value]).
    - `markdown()` from @codemirror/lang-markdown is used; EditorView.lineWrapping enabled.
    - Auto-save uses lodash/debounce(800) and calls the PATCH update hook with content.
    - Toolbar has a 3-item ToggleGroup (Edit/Split/Preview) bound to setMode and an index status Badge.
    - Split + Preview render through the react-markdown component (no raw HTML injection).
    - No-doc-selected empty state matches UI-SPEC copy.
    - nextjs-app typechecks with no DocEditorArea errors.
  </acceptance_criteria>
  <done>Editor loads/edits markdown with auto-save re-index, mode toggle, breadcrumb, and status badge.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| markdown content → preview DOM | user-authored markdown rendered in preview |
| npm install | @codemirror/lang-markdown added |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-09 | Tampering | markdown preview pane | mitigate | Render via react-markdown component (safe by default); never pass a raw HTML string to the DOM |
| T-13-SC | Tampering | @codemirror/lang-markdown install | mitigate | Approved [OK] in Package Legitimacy Audit (13-RESEARCH.md) — millions/wk, official codemirror repo; pinned 6.5.0 |
</threat_model>

<verification>
- @codemirror/lang-markdown installed; react-markdown resolvable.
- nextjs-app typechecks (no DocEditorArea errors).
- Editor + modes + auto-save behavior browser-verified in Plan 13-08.
</verification>

<success_criteria>
A selected doc opens in a markdown editor; edits auto-save and re-index; Edit/Split/Preview modes work; toolbar shows breadcrumb, status badge, and save indicator.
</success_criteria>

<output>
Create `.planning/phases/13-doc-library-redesign/13-06-SUMMARY.md` when done
</output>
