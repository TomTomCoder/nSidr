---
phase: 01-authority-matrix
plan: 04
type: execute
wave: 3
depends_on: [01-02, 01-03]
files_modified:
  - packages/sdk/src/config/react-query-keys.ts
  - apps/nextjs-app/src/features/app/blocks/AuthorityMatrix.tsx
autonomous: false
requirements:
  - PHASE01-FRONTEND-QUERY-KEYS
  - PHASE01-FRONTEND-UI
  - PHASE01-FRONTEND-PERMISSION-GUARD
must_haves:
  truths:
    - "User with `base|authority_matrix_config` permission sees the matrix management UI at /base/{baseId}/authority-matrix"
    - "User without that permission sees a redirect or locked state"
    - "User can create a new matrix via the '+ Nouvelle matrice' button → dialog → save → matrix appears"
    - "User can add a role to a matrix and toggle action checkboxes → role persists to backend"
    - "User can delete a matrix (confirmation dialog) and a role (immediate)"
    - "List refetches after each mutation"
  artifacts:
    - path: "packages/sdk/src/config/react-query-keys.ts"
      provides: "authorityMatrixList(baseId) and authorityMatrix(baseId, matrixId) keys"
      contains: "authorityMatrix"
    - path: "apps/nextjs-app/src/features/app/blocks/AuthorityMatrix.tsx"
      provides: "Working management UI replacing the stub"
      min_lines: 200
  key_links:
    - from: "AuthorityMatrix.tsx"
      to: "@teable/openapi axios wrappers"
      via: "useQuery + useMutation"
      pattern: "getAuthorityMatrixList|createAuthorityMatrix"
    - from: "AuthorityMatrix.tsx"
      to: "ReactQueryKeys.authorityMatrixList"
      via: "queryKey + invalidateQueries"
      pattern: "ReactQueryKeys\\.authorityMatrix"
---

<objective>
Replace the existing `AuthorityMatrix.tsx` stub with a fully functional management UI matching the UI-SPEC contract: list of matrix cards with role rows, action checkbox grid grouped by category, create/edit/delete via dialog, add/edit/delete roles inline. Wire all interactions to the backend via @teable/openapi axios wrappers and React Query.

Purpose: Delivers the user-facing feature. End-to-end working: user can configure named permission matrices and roles for their base.
Output: Updated react-query-keys.ts + replaced AuthorityMatrix.tsx (the page route, layout, sidebar nav, and SSR are already wired — DO NOT modify those files).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-authority-matrix/01-CONTEXT.md
@.planning/phases/01-authority-matrix/01-UI-SPEC.md
@.planning/phases/01-authority-matrix/01-02-SUMMARY.md
@.planning/phases/01-authority-matrix/01-03-SUMMARY.md
@apps/nextjs-app/src/features/app/blocks/AuthorityMatrix.tsx
@packages/sdk/src/config/react-query-keys.ts

<interfaces>
Available imports (verified):

- @teable/openapi (from Plan 01-02):
    getAuthorityMatrixList, getAuthorityMatrix,
    createAuthorityMatrix, updateAuthorityMatrix, deleteAuthorityMatrix,
    createAuthorityMatrixRole, updateAuthorityMatrixRole, deleteAuthorityMatrixRole,
    type IAuthorityMatrixVo, type IAuthorityMatrixRoleVo,
    type ICreateAuthorityMatrixRo, type IUpdateAuthorityMatrixRoleRo

- @teable/core: `Action` enum (used to render checkbox grid grouped by category prefix)

- @teable/sdk: `useBase()` (current base context — alternative to router.query.baseId), `usePermission()` if available, `ReactQueryKeys` (after Task 1 adds new keys)

- @teable/ui-lib/shadcn (verified via UI-SPEC):
    Button, Input, Checkbox, Card, CardContent, Dialog, DialogContent, DialogHeader,
    DialogTitle, DialogFooter, AlertDialog (and AlertDialogContent, etc.), Badge

- @teable/icons: Plus, Trash2, Pencil, Lock from lucide-react via the icons package

- @tanstack/react-query: useQuery, useMutation, useQueryClient

- next/router: useRouter — `useRouter().query.baseId as string`

UI-SPEC reference (.planning/phases/01-authority-matrix/01-UI-SPEC.md):
- Page shell: `<div className="h-full flex-col md:flex">` with header `border-b px-8 py-4` (h2 "Matrice d'autorité" + Plus button "Nouvelle matrice") and content `flex-1 overflow-y-auto px-8 py-4 space-y-4`
- MatrixCard: `Card` with `shadow-none border`, `CardContent className="p-4"`, header row with name+description+edit+delete icons, role section under `border-t mt-4 pt-4`
- Role row: inline-editable name `Input className="h-7 text-xs w-32"` saving on blur, action checkbox grid `flex flex-wrap gap-x-6 gap-y-3` overflow-x-auto, ghost destructive Trash2 button
- Create/Edit dialog: `sm:max-w-md`, name + description inputs, Annuler + Enregistrer buttons
- Delete matrix: AlertDialog confirmation. Delete role: immediate, no dialog.
- Empty state (no matrices): centered text stack — heading "Aucune matrice configurée", body "Créez votre première matrice pour définir des rôles personnalisés.", CTA reuses "Nouvelle matrice" button
- Loading: 2 skeleton cards `<div className="h-40 rounded-lg bg-muted animate-pulse" />`
- Action category labels (French): Tables (table:*), Champs (field:*), Vues (view:*), Enregistrements (record:*), Collaboration (collaborator:*), Base (base:*), Automatisations (automation:*), Autres (rest)
- Permission guard: redirect or locked state if `base|authority_matrix_config` is false (NOT 404)
- Optimistic updates: NOT used. Show loading state on mutation, then refetch via invalidateQueries.

Existing react-query-keys.ts pattern (verify exact shape on read): each entry like `tableList: (baseId: string) => ['table-list', baseId] as const,` — follow same format for new entries.

Stub to replace: apps/nextjs-app/src/features/app/blocks/AuthorityMatrix.tsx (current content shows an "Enterprise upgrade" placeholder — replace entirely while keeping the same default export name `AuthorityMatrixPage` that the page file imports).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add authority matrix React Query keys</name>
  <files>packages/sdk/src/config/react-query-keys.ts</files>
  <read_first>
    - packages/sdk/src/config/react-query-keys.ts (FULL FILE — find the export structure and a logical place to add the two new keys)
  </read_first>
  <action>
    Edit `packages/sdk/src/config/react-query-keys.ts`. Add these two key factories to the `ReactQueryKeys` object literal (or whatever export structure the file currently uses — match it exactly):

      authorityMatrixList: (baseId: string) => ['authority-matrix-list', baseId] as const,
      authorityMatrix: (baseId: string, matrixId: string) => ['authority-matrix', baseId, matrixId] as const,

    Insert them in alphabetical order if the file is alphabetized; otherwise group them near base-related keys. Preserve all existing keys.
  </action>
  <verify>
    <automated>grep -q "authorityMatrixList:" packages/sdk/src/config/react-query-keys.ts && grep -q "authorityMatrix:" packages/sdk/src/config/react-query-keys.ts && grep -q "'authority-matrix-list'" packages/sdk/src/config/react-query-keys.ts && grep -q "'authority-matrix'" packages/sdk/src/config/react-query-keys.ts && cd packages/sdk && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep "authorityMatrixList: (baseId: string)" packages/sdk/src/config/react-query-keys.ts` matches exactly once
    - `grep "authorityMatrix: (baseId: string, matrixId: string)" packages/sdk/src/config/react-query-keys.ts` matches exactly once
    - Both new keys use `as const` for tuple typing
    - `cd packages/sdk && pnpm tsc --noEmit` exits 0
    - No existing keys removed
  </acceptance_criteria>
  <done>Both keys added, sdk package typechecks clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Replace AuthorityMatrix.tsx with full management UI</name>
  <files>apps/nextjs-app/src/features/app/blocks/AuthorityMatrix.tsx</files>
  <read_first>
    - apps/nextjs-app/src/features/app/blocks/AuthorityMatrix.tsx (current stub — preserve the export name)
    - apps/nextjs-app/src/pages/base/[baseId]/authority-matrix.tsx (verify expected import shape — likely `import { AuthorityMatrixPage } from '...'` or default import)
    - .planning/phases/01-authority-matrix/01-UI-SPEC.md (FULL FILE — visual contract)
    - packages/core/src/auth/actions.ts (Action enum values for the checkbox grid)
    - apps/nextjs-app/src/features/app/blocks/space/SpaceCollaborators.tsx OR similar existing react-query feature block (pattern reference for useQuery + useMutation + invalidateQueries)
    - packages/sdk/src/config/react-query-keys.ts (verify ReactQueryKeys export name and import path)
  </read_first>
  <action>
    Replace the entire content of `apps/nextjs-app/src/features/app/blocks/AuthorityMatrix.tsx`. Preserve the export signature: the page file at `apps/nextjs-app/src/pages/base/[baseId]/authority-matrix.tsx` imports it (read that file first to confirm the exact import — likely `export const AuthorityMatrixPage` or `export default`). Match it exactly.

    All user-supplied strings (matrix.name, matrix.description, role.name) MUST be rendered via standard JSX text interpolation — React's default escaping is sufficient. Do NOT use raw HTML injection patterns.

    Implement these components in this single file (or extracted as local components within the same file — do NOT create new files):

    A) Top-level component `AuthorityMatrixPage`:
       - Get baseId: `const router = useRouter(); const baseId = router.query.baseId as string;`
       - Permission guard: import `usePermission` from @teable/sdk if available — if `permission?.['base|authority_matrix_config'] === false`, render a centered "Locked" state with `Lock` icon + text "Vous n'avez pas accès à la configuration de la matrice d'autorité." (do NOT call notFound() / render 404).
       - Fetch list: `useQuery({ queryKey: ReactQueryKeys.authorityMatrixList(baseId), queryFn: () => getAuthorityMatrixList(baseId).then(r => r.data), enabled: !!baseId })`
       - Loading: render 2 skeleton cards `<div className="h-40 rounded-lg bg-muted animate-pulse" />`
       - Error: render error text "Impossible de charger les matrices. Réessayez ou rechargez la page." in `text-sm text-destructive`
       - Empty state: centered stack — h3 "Aucune matrice configurée" + p "Créez votre première matrice pour définir des rôles personnalisés." + same `<CreateMatrixButton />`
       - Page shell exactly as UI-SPEC: `<div className="h-full flex-col md:flex">` with header (h2 "Matrice d'autorité" text-2xl font-semibold tracking-tight + `<CreateMatrixButton size="sm" />`) and content `flex-1 overflow-y-auto px-8 py-4 space-y-4` rendering `data.list.map(matrix => <MatrixCard key={matrix.id} matrix={matrix} baseId={baseId} />)`

    B) `<CreateMatrixButton size?>`: opens `<MatrixDialog mode="create" baseId={baseId} />` on click. Renders `<Button size={size} ...><Plus className="size-4 mr-2" />Nouvelle matrice</Button>`.

    C) `<MatrixDialog mode: 'create' | 'edit', baseId, matrix?, open, onOpenChange>`:
       - Controlled dialog `<Dialog>` with `<DialogContent className="sm:max-w-md">`
       - Title: "Nouvelle matrice" / "Modifier la matrice"
       - Local state: `name`, `description` initialized from matrix prop in edit mode
       - Footer: Annuler (variant outline, onClick close) + Enregistrer (default, disabled when name is empty or mutation pending)
       - On submit: createAuthorityMatrix(baseId, { name, description }) or updateAuthorityMatrix(baseId, matrix.id, { name, description })
       - On success: queryClient.invalidateQueries({ queryKey: ReactQueryKeys.authorityMatrixList(baseId) }); close dialog

    D) `<MatrixCard matrix: IAuthorityMatrixVo, baseId>`:
       - `<Card className="shadow-none border"><CardContent className="p-4">`
       - Header row (flex justify-between items-start): name (text-base font-semibold) + description below (text-sm text-muted-foreground) on left; on right: edit icon Button (variant ghost, size icon h-7 w-7) opens MatrixDialog mode=edit, delete icon Button (variant ghost, size icon h-7 w-7, text-destructive) opens AlertDialog
       - Delete confirmation `AlertDialog` with title "Supprimer la matrice", body "Cette action est irréversible. Tous les rôles associés seront supprimés.", actions: Annuler + Supprimer (variant destructive). On confirm: deleteAuthorityMatrix(baseId, matrix.id) → invalidate list.
       - Roles section: `<div className="border-t mt-4 pt-4">` with header "Rôles" (text-xs font-semibold text-muted-foreground mb-2)
       - matrix.roles.map(role => <RoleRow key={role.id} role={role} baseId={baseId} matrixId={matrix.id} />)
       - Add role footer: `<Button size="xs" variant="outline" onClick={handleAddRole}><Plus className="size-3.5 mr-1" />Ajouter un rôle</Button>`
       - handleAddRole: createAuthorityMatrixRole(baseId, matrix.id, { name: 'Nouveau rôle', actions: [] }) → invalidate list

    E) `<RoleRow role, baseId, matrixId>`:
       - flex layout with: inline-editable name `<Input defaultValue={role.name} className="h-7 text-xs w-32" onBlur={handleNameBlur} />`
       - handleNameBlur: if value changed, updateAuthorityMatrixRole(baseId, matrixId, role.id, { name: newValue }) → invalidate list
       - Action checkbox grid: see (F)
       - Delete: `<Button size="xs" variant="ghost" className="text-destructive hover:text-destructive"><Trash2 className="size-3.5" /></Button>` onClick → deleteAuthorityMatrixRole(baseId, matrixId, role.id) → invalidate list. NO confirmation dialog (per UI-SPEC).

    F) `<ActionCheckboxGrid actions: string[], onChange>`:
       - Group all values from `Action` enum (from @teable/core) by prefix before the first `|` or `:`. Map prefixes to French labels per UI-SPEC table:
         - `table` → Tables, `field` → Champs, `view` → Vues, `record` → Enregistrements, `collaborator` → Collaboration, `base` → Base, `automation` → Automatisations, fallback → Autres
       - Container: `<div className="flex flex-wrap gap-x-6 gap-y-3 overflow-x-auto">`
       - Per group: `<div className="min-w-fit"><p className="text-xs font-semibold text-muted-foreground mb-1">{label}</p><div className="space-y-1">{actions in group}</div></div>`
       - Per action: `<label className="flex items-center gap-1 text-xs"><Checkbox checked={actions.includes(actionValue)} onCheckedChange={(c) => onChange(c ? [...actions, actionValue] : actions.filter(a => a !== actionValue))} />{actionLabel}</label>`
       - actionLabel: take portion after the prefix and humanize it; e.g. `field|create` → "create", `record|update` → "update". Keep concise.
       - onChange wires to: updateAuthorityMatrixRole(baseId, matrixId, role.id, { actions: newActions }) → invalidate list

    Imports needed:
      import { useState } from 'react';
      import { useRouter } from 'next/router';
      import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
      import { Plus, Trash2, Pencil, Lock } from '@teable/icons';
      import { Action } from '@teable/core';
      import {
        getAuthorityMatrixList, createAuthorityMatrix, updateAuthorityMatrix, deleteAuthorityMatrix,
        createAuthorityMatrixRole, updateAuthorityMatrixRole, deleteAuthorityMatrixRole,
        type IAuthorityMatrixVo, type IAuthorityMatrixRoleVo,
      } from '@teable/openapi';
      import { ReactQueryKeys, usePermission } from '@teable/sdk'; // verify exact path
      import {
        Button, Input, Checkbox, Card, CardContent,
        Dialog, DialogContent, DialogHeader, DialogTitle, DialogFooter,
        AlertDialog, AlertDialogContent, AlertDialogHeader, AlertDialogTitle, AlertDialogDescription, AlertDialogFooter, AlertDialogCancel, AlertDialogAction, AlertDialogTrigger,
      } from '@teable/ui-lib/shadcn';

    Copywriting: use the exact French strings from UI-SPEC § Copywriting Contract — verify each one matches.
  </action>
  <verify>
    <automated>test -f apps/nextjs-app/src/features/app/blocks/AuthorityMatrix.tsx && [ "$(wc -l < apps/nextjs-app/src/features/app/blocks/AuthorityMatrix.tsx)" -ge "200" ] && grep -q "Matrice d'autorité" apps/nextjs-app/src/features/app/blocks/AuthorityMatrix.tsx && grep -q "Nouvelle matrice" apps/nextjs-app/src/features/app/blocks/AuthorityMatrix.tsx && grep -q "Aucune matrice configurée" apps/nextjs-app/src/features/app/blocks/AuthorityMatrix.tsx && grep -q "getAuthorityMatrixList" apps/nextjs-app/src/features/app/blocks/AuthorityMatrix.tsx && grep -q "createAuthorityMatrix" apps/nextjs-app/src/features/app/blocks/AuthorityMatrix.tsx && grep -q "ReactQueryKeys.authorityMatrixList" apps/nextjs-app/src/features/app/blocks/AuthorityMatrix.tsx && grep -q "base|authority_matrix_config" apps/nextjs-app/src/features/app/blocks/AuthorityMatrix.tsx && cd apps/nextjs-app && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File length ≥ 200 lines (real implementation, not stub)
    - Contains exact French strings: "Matrice d'autorité", "Nouvelle matrice", "Ajouter un rôle", "Enregistrer", "Annuler", "Aucune matrice configurée", "Créez votre première matrice pour définir des rôles personnalisés.", "Supprimer la matrice", "Cette action est irréversible. Tous les rôles associés seront supprimés.", "Modifier la matrice", "Nom de la matrice", "Description (optionnel)"
    - All 7 mutation/query axios wrappers from @teable/openapi imported and referenced (getAuthorityMatrixList, createAuthorityMatrix, updateAuthorityMatrix, deleteAuthorityMatrix, createAuthorityMatrixRole, updateAuthorityMatrixRole, deleteAuthorityMatrixRole)
    - `ReactQueryKeys.authorityMatrixList` referenced both as queryKey AND in invalidateQueries (≥ 2 occurrences)
    - Permission check references `'base|authority_matrix_config'`
    - `Action` from @teable/core imported and iterated (`Object.values(Action)` or similar)
    - `cd apps/nextjs-app && pnpm tsc --noEmit` exits 0
    - No `notFound()` call (must be locked-state, not 404 — per UI-SPEC)
    - `<Dialog>` and `<AlertDialog>` both used (matrix dialog + delete confirmation)
    - File contains NO unsafe HTML injection — verifiable by `grep -c "InnerHTML" apps/nextjs-app/src/features/app/blocks/AuthorityMatrix.tsx` returning 0
  </acceptance_criteria>
  <done>UI fully implemented per UI-SPEC, type-clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual UAT — full Authority Matrix flow</name>
  <what-built>
    Full Authority Matrix feature: DB tables, REST API, and UI. All previous tasks (Plans 01-01, 01-02, 01-03 and Plan 04 Tasks 1-2) have been completed and the dev server is ready.
  </what-built>
  <how-to-verify>
    1) Start dev: `pnpm dev` (or the relevant monorepo dev command — likely separate processes for nestjs-backend and nextjs-app)
    2) Log in as a base Owner or Creator
    3) Navigate to a base, click the "Authority Matrix" / Lock icon in the sidebar — page should load at `/base/{baseId}/authority-matrix`
    4) Verify empty state: heading "Aucune matrice configurée" + body text + "+ Nouvelle matrice" button visible
    5) Click "+ Nouvelle matrice" — dialog opens with Name + Description fields and Annuler/Enregistrer buttons. Title is "Nouvelle matrice".
    6) Enter name "Test Matrix" + description "test" → click Enregistrer. Dialog closes; new matrix card appears.
    7) Click the Pencil edit icon on the card — dialog reopens pre-filled, title "Modifier la matrice". Change name to "Test Matrix v2", click Enregistrer. Card updates.
    8) Click "+ Ajouter un rôle" on the card — a role row appears with "Nouveau rôle" name and empty action grid.
    9) Click the role name input, change to "Editor", blur — name persists (refresh page to confirm).
    10) Toggle several checkboxes across multiple groups (Tables, Champs, etc.) — each toggle should fire a PATCH and the checkbox state persists after refresh.
    11) Click the role's Trash2 icon — row disappears immediately, no dialog. Refresh confirms it's gone.
    12) Click the matrix card's Trash2 icon — AlertDialog appears with "Supprimer la matrice" + body text + Annuler/Supprimer (red). Click Supprimer. Card disappears.
    13) Permission test: log in as a non-Owner/Creator (Editor role) — page should show locked state, NOT 404.
    14) Backend test: `curl` POST to /api/base/{baseId}/authority-matrix without auth → 401/403. With non-Owner auth → 403.
  </how-to-verify>
  <resume-signal>Type "approved" if all 14 steps pass, or describe failures with step numbers.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → frontend code | DOM/React state |
| frontend → backend API | HTTP/JSON, authenticated session cookie |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | E (Elevation of Privilege) | Page access | mitigate | Permission guard checks `base|authority_matrix_config` via `usePermission`; renders locked state when false. Backend (Plan 03) is the source of truth — UI guard is UX only. |
| T-04-02 | T (Tampering) | role.actions client-side | mitigate | Client sends `actions: string[]`; backend validates against `Action` enum (Plan 03 T-03-05). Client-side checkbox UI restricts the set to known actions but cannot be trusted alone. |
| T-04-03 | I (Info Disclosure) | matrix data in browser cache | accept | React Query cache contains only matrix names, descriptions, role names, and action lists — no PII. Acceptable. |
| T-04-04 | T (XSS) | matrix.name / role.name / matrix.description rendering | mitigate | All user-provided strings rendered via standard JSX interpolation — React escapes by default. Acceptance criterion forbids unsafe HTML injection patterns in this file. |
| T-04-05 | S (Spoofing) | session | accept | Existing app session/CSRF protections apply; not specific to this feature. |
</threat_model>

<verification>
- packages/sdk types check
- apps/nextjs-app types check for the modified file
- Manual UAT (Task 3) confirms end-to-end flow works
</verification>

<success_criteria>
A user with `base|authority_matrix_config` permission can create, edit, and delete named permission matrices for their base, add roles to those matrices, configure each role's allowed Action set via grouped checkboxes, and have all changes persist to PostgreSQL via the NestJS API. A user without that permission sees a locked state, not 404. All 14 UAT steps pass.
</success_criteria>

<output>
After completion, create `.planning/phases/01-authority-matrix/01-04-SUMMARY.md` documenting the React Query keys added, the AuthorityMatrix.tsx implementation summary (component breakdown), UAT result, and any deviations from UI-SPEC.
</output>
