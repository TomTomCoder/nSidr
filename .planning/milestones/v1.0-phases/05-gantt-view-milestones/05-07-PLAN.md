---
phase: 05-gantt-view-milestones
plan: 07
type: execute
wave: 2
depends_on:
  - "05-05"
files_modified:
  - apps/nextjs-app/src/features/app/blocks/view/View.tsx
  - apps/nextjs-app/src/features/app/blocks/view/constant.ts
  - apps/nextjs-app/src/features/app/blocks/table/table-header/AddView.tsx
  - packages/icons/src/components/GanttChart.tsx
  - packages/icons/src/index.ts
autonomous: true
gap_closure: true
requirements:
  - PHASE05-VIEW-REGISTRATION
must_haves:
  truths:
    - "View.tsx getViewComponent switch handles ViewType.Gantt and renders GanttView"
    - "constant.ts VIEW_ICON_MAP includes ViewType.Gantt entry"
    - "AddView.tsx viewInfoList includes a Gantt entry with correct type and icon"
    - "GanttChart icon component exists in packages/icons"
    - "pnpm build --filter @teable/nextjs-app exits 0"
  artifacts:
    - path: "apps/nextjs-app/src/features/app/blocks/view/View.tsx"
      provides: "ViewType.Gantt case in getViewComponent switch"
      contains: "ViewType.Gantt"
    - path: "apps/nextjs-app/src/features/app/blocks/view/constant.ts"
      provides: "GanttChart icon in VIEW_ICON_MAP"
      contains: "ViewType.Gantt"
    - path: "apps/nextjs-app/src/features/app/blocks/table/table-header/AddView.tsx"
      provides: "Gantt entry in viewInfoList"
      contains: "ViewType.Gantt"
    - path: "packages/icons/src/components/GanttChart.tsx"
      provides: "GanttChart SVG icon component"
      contains: "GanttChart"
  key_links:
    - from: "View.tsx"
      to: "GanttView component"
      via: "case ViewType.Gantt: return <GanttView />"
      pattern: "GanttView"
    - from: "constant.ts"
      to: "GanttChart icon"
      via: "VIEW_ICON_MAP[ViewType.Gantt]"
      pattern: "GanttChart"
---

<objective>
Wire the Gantt view into the frontend routing (View.tsx switch), the "Add View" picker (AddView.tsx), and the view icon map (constant.ts). Create the GanttChart icon component in the icons package. Without these, the Gantt view is invisible to users — they cannot create or navigate to it.

Purpose: The Gantt UI components exist but are unreachable. This plan makes them accessible through the standard view switching mechanisms.
Output: Users can see "Gantt" in the view type picker, create Gantt views, and navigate to them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-gantt-view-milestones/05-CONTEXT.md

<interfaces>
View.tsx current switch (apps/nextjs-app/src/features/app/blocks/view/View.tsx):
```typescript
import { CalendarView } from './calendar/CalendarView';
// ... other view imports ...
const getViewComponent = () => {
  switch (viewType) {
    case ViewType.Grid: return <GridView {...props} />;
    case ViewType.Form: return <FormView />;
    case ViewType.Kanban: return <KanbanView />;
    case ViewType.Gallery: return <GalleryView />;
    case ViewType.Calendar: return <CalendarView />;
    case ViewType.Plugin: return <PluginView />;
    default: return null;
  }
};
```

constant.ts current VIEW_ICON_MAP (apps/nextjs-app/src/features/app/blocks/view/constant.ts):
```typescript
import { ViewType } from '@teable/core';
import { Sheet, ClipboardList as Form, LayoutGrid as Gallery, Kanban, Component, Calendar } from '@teable/icons';
export const VIEW_ICON_MAP = {
  [ViewType.Grid]: Sheet,
  [ViewType.Kanban]: Kanban,
  [ViewType.Gallery]: Gallery,
  [ViewType.Calendar]: Calendar,
  [ViewType.Form]: Form,
  [ViewType.Plugin]: Component,
};
```

AddView.tsx viewInfoList (apps/nextjs-app/src/features/app/blocks/table/table-header/AddView.tsx):
```typescript
const viewInfoList = [
  { name: t('view.category.table'), type: ViewType.Grid, Icon: VIEW_ICON_MAP[ViewType.Grid] },
  { name: t('view.category.gallery'), type: ViewType.Gallery, Icon: VIEW_ICON_MAP[ViewType.Gallery] },
  { name: t('view.category.kanban'), type: ViewType.Kanban, Icon: VIEW_ICON_MAP[ViewType.Kanban] },
  { name: t('view.category.calendar'), type: ViewType.Calendar, Icon: VIEW_ICON_MAP[ViewType.Calendar] },
  { name: t('view.category.form'), type: ViewType.Form, Icon: VIEW_ICON_MAP[ViewType.Form] },
];
```

Icons pattern: packages/icons/src/index.ts exports from ./components/ directory.
Each icon is a separate .tsx file in packages/icons/src/components/ exporting a default React SVG component.
Example: `export { default as Calendar } from './components/Calendar';`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create GanttChart icon component + export from icons index</name>
  <files>
    packages/icons/src/components/GanttChart.tsx
    packages/icons/src/index.ts
  </files>
  <read_first>
    - packages/icons/src/index.ts (first 10 lines — confirm the export pattern)
    - packages/icons/src/components/Calendar.tsx (reference for icon component structure — props, SVG format)
    - packages/icons/src/components/BarChart2.tsx (another reference icon)
  </read_first>
  <action>
    Step 1 — Read an existing icon component (Calendar.tsx or BarChart2.tsx) to understand the exact component structure: props type, SVG viewBox, className handling, forwardRef pattern, etc.

    Step 2 — Create packages/icons/src/components/GanttChart.tsx:
    Use the EXACT same component structure as the reference icon. The SVG content should be a Gantt chart icon (horizontal bars of different lengths stacked vertically — a standard Gantt chart icon). Here is the SVG path data for a Gantt chart icon (24x24 viewBox):
    ```
    <rect x="3" y="4" width="8" height="3" rx="1" />
    <rect x="7" y="9" width="10" height="3" rx="1" />
    <rect x="5" y="14" width="6" height="3" rx="1" />
    <rect x="9" y="19" width="8" height="3" rx="1" />
    ```
    These represent 4 horizontal bars at different x-offsets and widths, stacked vertically — the classic Gantt chart icon.

    Mirror the EXACT component signature from the reference icon (forwardRef, props spreading, className, etc.). Do NOT guess the pattern — read the reference first.

    Step 3 — Edit packages/icons/src/index.ts:
    Add in alphabetical order (after the "G" section):
    ```typescript
    export { default as GanttChart } from './components/GanttChart';
    ```
  </action>
  <acceptance_criteria>
    - File packages/icons/src/components/GanttChart.tsx exists
    - grep "GanttChart" packages/icons/src/components/GanttChart.tsx returns a match
    - grep "GanttChart" packages/icons/src/index.ts returns a match
    - The icon component follows the same structure as other icons in the package (forwardRef, SVG, etc.)
  </acceptance_criteria>
  <verify>
    <automated>test -f packages/icons/src/components/GanttChart.tsx && grep -q "GanttChart" packages/icons/src/index.ts && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>GanttChart icon component created and exported from icons index.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire Gantt into View.tsx switch + constant.ts icon map + AddView.tsx picker + build nextjs-app</name>
  <files>
    apps/nextjs-app/src/features/app/blocks/view/View.tsx
    apps/nextjs-app/src/features/app/blocks/view/constant.ts
    apps/nextjs-app/src/features/app/blocks/table/table-header/AddView.tsx
  </files>
  <read_first>
    - apps/nextjs-app/src/features/app/blocks/view/View.tsx (full file — find switch, imports)
    - apps/nextjs-app/src/features/app/blocks/view/constant.ts (full file)
    - apps/nextjs-app/src/features/app/blocks/table/table-header/AddView.tsx (full file)
  </read_first>
  <action>
    Step 1 — Edit apps/nextjs-app/src/features/app/blocks/view/constant.ts:
    Add import: `GanttChart` from `@teable/icons` (add to the existing destructured import from '@teable/icons').
    Add to VIEW_ICON_MAP:
    ```typescript
    [ViewType.Gantt]: GanttChart,
    ```

    Step 2 — Edit apps/nextjs-app/src/features/app/blocks/view/View.tsx:
    Add import at top: `import { GanttView } from './gantt/GanttView';`
    Add case in the getViewComponent switch before `default: return null;`:
    ```typescript
    case ViewType.Gantt:
      return <GanttView />;
    ```

    Step 3 — Edit apps/nextjs-app/src/features/app/blocks/table/table-header/AddView.tsx:
    Add Gantt entry to viewInfoList AFTER the Calendar entry:
    ```typescript
    {
      name: t('view.category.gantt'),
      type: ViewType.Gantt,
      Icon: VIEW_ICON_MAP[ViewType.Gantt],
    },
    ```
    NOTE: The i18n key `view.category.gantt` may not exist yet. Check the i18n locale files. If it does not exist, add it to the relevant locale files (look at where `view.category.calendar` is defined and add `gantt` next to it). If you cannot find the locale files or adding them would touch too many files, use a hardcoded string `'Gantt'` as a fallback — it can be localized later.

    Step 4 — Build to verify everything compiles:
    ```bash
    cd /Users/tommylambert/Documents/Claude_Folder/teable && pnpm build --filter @teable/icons --filter @teable/nextjs-app
    ```
    If build fails, read errors and fix. Common issues:
    - If `ViewType.Gantt` is not recognized, Plan 05 may not be built yet — this plan depends on 05-05.
    - If `GanttChart` is not found in @teable/icons, Task 1 was incomplete.
    - If i18n key is missing, use hardcoded 'Gantt' string.
  </action>
  <acceptance_criteria>
    - grep "ViewType.Gantt" apps/nextjs-app/src/features/app/blocks/view/View.tsx returns a match
    - grep "GanttView" apps/nextjs-app/src/features/app/blocks/view/View.tsx returns a match
    - grep "GanttChart" apps/nextjs-app/src/features/app/blocks/view/constant.ts returns a match
    - grep "ViewType.Gantt" apps/nextjs-app/src/features/app/blocks/view/constant.ts returns a match
    - grep "ViewType.Gantt" apps/nextjs-app/src/features/app/blocks/table/table-header/AddView.tsx returns a match
    - pnpm build --filter @teable/nextjs-app exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -q "ViewType.Gantt" apps/nextjs-app/src/features/app/blocks/view/View.tsx && grep -q "GanttChart" apps/nextjs-app/src/features/app/blocks/view/constant.ts && grep -q "ViewType.Gantt" apps/nextjs-app/src/features/app/blocks/table/table-header/AddView.tsx && cd /Users/tommylambert/Documents/Claude_Folder/teable && pnpm build --filter @teable/nextjs-app 2>&1 | tail -5</automated>
  </verify>
  <done>Gantt view appears in View.tsx router, icon map, and AddView picker. Frontend app builds without errors. Users can create and navigate to Gantt views.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| view type switcher -> component router | User selects view type, router loads corresponding component |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-15 | S (Spoofing) | View.tsx Gantt case | accept | No new auth surface — view access is already gated by table permissions. Adding a switch case does not bypass existing access control |
</threat_model>

<verification>
- `grep -q "ViewType.Gantt" apps/nextjs-app/src/features/app/blocks/view/View.tsx`
- `grep -q "GanttChart" apps/nextjs-app/src/features/app/blocks/view/constant.ts`
- `grep -q "ViewType.Gantt" apps/nextjs-app/src/features/app/blocks/table/table-header/AddView.tsx`
- `grep -q "GanttChart" packages/icons/src/index.ts`
- `pnpm build --filter @teable/nextjs-app` exits 0
</verification>

<success_criteria>
Users can see "Gantt" in the Add View picker with the correct icon. Clicking it creates a Gantt view. Navigating to a Gantt view renders the GanttView component. The app builds without TypeScript errors.
</success_criteria>

<output>
After completion, create `.planning/phases/05-gantt-view-milestones/05-07-SUMMARY.md`
</output>
