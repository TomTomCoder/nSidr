---
phase: 14-process-separation
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - apps/nextjs-app/next.config.js
  - package.json
autonomous: true
requirements: [AC1, AC4]

must_haves:
  truths:
    - "Next.js rewrites /api/* to NestJS at 3001 in dev (non-prod) mode"
    - "`dev:separated` script starts both processes concurrently"
    - "`start:separated` script starts both processes in production mode"
    - "Combined mode scripts are unchanged"
  artifacts:
    - path: "apps/nextjs-app/next.config.js"
      provides: "/api/* dev proxy rewrite rule"
      contains: "apiProxy"
    - path: "package.json"
      provides: "dev:separated and start:separated root scripts"
      contains: "dev:separated"
  key_links:
    - from: "apps/nextjs-app/next.config.js rewrites()"
      to: "http://localhost:3001/api/:path*"
      via: "Next.js server-side rewrite"
      pattern: "destination.*3001"
---

<objective>
Add /api/* proxy rewrite in next.config.js and add `dev:separated` / `start:separated` root scripts using concurrently.

Purpose: In separated mode the browser sends /api/* requests to Next.js (port 3000). Next.js must proxy them to NestJS (port 3001) server-side so cookies are forwarded and there is no CORS issue. The root scripts make it a one-command startup.

Output: Updated next.config.js with apiProxy rewrite, updated package.json with two new root scripts, and concurrently added to root devDependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-process-separation/14-CONTEXT.md
@.planning/phases/14-process-separation/14-01-SUMMARY.md
</context>

<interfaces>
From apps/nextjs-app/next.config.js (lines 48, 220-227 — existing socket rewrite pattern):

```javascript
const NEXTJS_SOCKET_PORT = process.env.SOCKET_PORT || '3001';

async rewrites() {
  const socketProxy = {
    source: '/socket/:path*',
    destination: `http://localhost:${NEXTJS_SOCKET_PORT}/socket/:path*`,
  };
  return isProd ? [] : [socketProxy];
},
```

The API proxy should follow the same pattern, appended alongside socketProxy in the non-prod array.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 2.1: Add /api/* rewrite proxy to next.config.js</name>
  <files>apps/nextjs-app/next.config.js</files>
  <action>
    Edit the `rewrites()` function at lines 220-227 of apps/nextjs-app/next.config.js.

    Add a second rewrite object alongside the existing `socketProxy` constant, using the already-declared `NEXTJS_SOCKET_PORT`:
      const apiProxy = {
        source: '/api/:path*',
        destination: `http://localhost:${NEXTJS_SOCKET_PORT}/api/:path*`,
      };

    Update the return statement to include both in the non-prod array:
      return isProd ? [] : [socketProxy, apiProxy];

    Do not declare any new port constant — `NEXTJS_SOCKET_PORT` is already declared above and covers the same NestJS port (3001). Do not modify isProd, NEXTJS_SOCKET_PORT, or any other part of the file.

    Final rewrites() should look like:
      async rewrites() {
        const socketProxy = {
          source: '/socket/:path*',
          destination: `http://localhost:${NEXTJS_SOCKET_PORT}/socket/:path*`,
        };
        const apiProxy = {
          source: '/api/:path*',
          destination: `http://localhost:${NEXTJS_SOCKET_PORT}/api/:path*`,
        };
        return isProd ? [] : [socketProxy, apiProxy];
      },
  </action>
  <verify>
    <automated>grep -c "apiProxy" apps/nextjs-app/next.config.js</automated>
  </verify>
  <done>next.config.js rewrites() returns [socketProxy, apiProxy] in dev and [] in prod. Both proxy entries point to the same NestJS port via NEXTJS_SOCKET_PORT.</done>
</task>

<task type="auto">
  <name>Task 2.2: Add concurrently to root devDependencies and add separated-mode scripts</name>
  <files>package.json</files>
  <action>
    Two changes to the root package.json:

    1. In `"devDependencies"`, add:
         "concurrently": "^9.1.2"

       Insert alphabetically (after "changesets" or similar, before "cross-env" / "eslint" — insert at the correct alphabetical position). Confirm `concurrently` is not already present before adding.

    2. In `"scripts"`, add two new entries after the existing `"dev:v2"` line (around line 30):
         "dev:separated": "concurrently --kill-others-on-fail --names \"api,web\" --prefix-colors \"blue,green\" \"PORT=3001 BACKEND_SKIP_NEXT_START=true pnpm --filter @teable/backend dev\" \"BACKEND_URL=http://localhost:3001 PORT=3000 pnpm --filter @teable/nextjs-app dev\"",
         "start:separated": "concurrently --kill-others-on-fail --names \"api,web\" --prefix-colors \"blue,green\" \"PORT=3001 BACKEND_SKIP_NEXT_START=true pnpm --filter @teable/backend start\" \"BACKEND_URL=http://localhost:3001 PORT=3000 pnpm --filter @teable/nextjs-app start\""

    Do not modify any existing scripts. Combined mode (`pnpm --filter @teable/backend start`) stays unchanged.

    After editing, run `pnpm install` from the repo root to install concurrently.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); console.log(p.scripts['dev:separated'] ? 'FOUND' : 'MISSING')"</automated>
  </verify>
  <done>package.json has dev:separated and start:separated scripts; concurrently is in devDependencies; pnpm install succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Next.js /api/* rewrite → NestJS | Server-side proxy; request headers (including cookies) forwarded by Next.js |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02 | Elevation of Privilege | /api/* rewrite rule | mitigate | Rewrite only active when `isProd=false`; production uses direct NestJS routing; no new attack surface in prod |
| T-14-03 | Spoofing | Cookie forwarding through Next.js proxy | accept | Both processes on localhost; no cross-origin cookie leakage; rewrite is server-side (not redirect) so cookies are forwarded correctly |
| T-14-SC | Tampering | concurrently npm package install | mitigate | Verify package at npmjs.com/package/concurrently before install; well-known package (>20M weekly downloads) |
</threat_model>

<verification>
1. `grep -c "apiProxy" apps/nextjs-app/next.config.js` — must be >= 2 (declaration + return array)
2. `node -e "const p=require('./package.json'); console.log(p.scripts['dev:separated'])"` — must print script string
3. `node -e "const p=require('./package.json'); console.log(p.devDependencies['concurrently'])"` — must print version string
4. Start with `pnpm dev:separated`; visit http://localhost:3000 — browser loads app
5. `curl -s http://localhost:3001/api/health` — returns 200 (NestJS running independently)
</verification>

<success_criteria>
- next.config.js proxies /api/* to NestJS port in dev mode only
- package.json has dev:separated and start:separated scripts
- concurrently in root devDependencies and installed
- Combined mode (`pnpm --filter @teable/backend start`) unchanged and still works
</success_criteria>

<output>
Create `.planning/phases/14-process-separation/14-02-SUMMARY.md` when done
</output>
