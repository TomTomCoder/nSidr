---
phase: 14-process-separation
plan: 03
type: execute
wave: 3
depends_on: ["14-01", "14-02"]
files_modified:
  - apps/nextjs-app/e2e/doc-library-separated.spec.ts
autonomous: false
requirements: [AC1, AC2, AC3, AC4, AC5]

must_haves:
  truths:
    - "doc-library save completes in < 3 seconds under separated mode"
    - "NestJS RSS stays below 500MB after 10 minutes of normal use"
    - "Combined mode still passes existing doc-library.spec.ts"
    - "Browser at localhost:3000 loads the app in separated mode"
  artifacts:
    - path: "apps/nextjs-app/e2e/doc-library-separated.spec.ts"
      provides: "Playwright regression for save latency under separated mode"
      contains: "dev:separated"
  key_links:
    - from: "apps/nextjs-app/e2e/doc-library-separated.spec.ts"
      to: "http://localhost:3000"
      via: "Playwright browser"
      pattern: "localhost:3000"
---

<objective>
Write a Playwright regression spec that boots the app in separated mode and asserts doc-library save stability, then run both the new spec and the existing doc-library.spec.ts to confirm no regressions.

Purpose: Prove that process separation eliminates the 30–60 second GC stalls. The test suite must pass under separated mode before Phase 14 is marked complete.

Output: New spec file doc-library-separated.spec.ts; human verification checkpoint for RSS monitoring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-process-separation/14-CONTEXT.md
@.planning/phases/14-process-separation/14-01-SUMMARY.md
@.planning/phases/14-process-separation/14-02-SUMMARY.md
@apps/nextjs-app/e2e/doc-library.spec.ts
@apps/nextjs-app/playwright.config.ts
</context>

<interfaces>
From apps/nextjs-app/playwright.config.ts (lines 9-46):
- `E2E_WEBSERVER_MODE` controls which server command runs ('DEV' | 'START' | 'BUILD_AND_START')
- `PLAYWRIGHT_PORT` defaults to 3000
- webServer.command is constructed from the mode; the new spec needs its own config or must be run with the existing config after servers are already running externally.

From apps/nextjs-app/e2e/doc-library.spec.ts (line 67):
- Existing save timeout is 5_000ms (5 seconds). The new test must assert completion in < 3_000ms.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 3.1: Write doc-library-separated.spec.ts regression spec</name>
  <files>apps/nextjs-app/e2e/doc-library-separated.spec.ts</files>
  <action>
    Create apps/nextjs-app/e2e/doc-library-separated.spec.ts. This spec is designed to run against servers already started externally by `pnpm dev:separated` (or `start:separated`). It does NOT use a webServer config — it connects directly to http://localhost:3000.

    The spec must:

    1. Connect to the running app at http://localhost:3000 (no webServer block — external servers assumed running).

    2. Test: "doc-library save completes in < 3 seconds in separated mode"
       - Navigate to the doc-library page (copy the navigation pattern from doc-library.spec.ts, typically /space/:id/doc or similar route)
       - Type unique content into the editor
       - Assert the save indicator appears within 3000ms using this exact assertion:
           await expect(page.getByText('Saved')).toBeVisible({ timeout: 3000 });
         The save indicator is a <span> rendered in DocEditorArea when saveStatus transitions from 'idle', showing "Saving..." then "Saved". Use getByText('Saved') — do not use data-testid selectors or inspect other selectors.
       - Assert no "GC stall" symptom: no pending PATCH /api/*/docs/* request takes more than 3000ms (intercept via `page.waitForResponse(r => r.url().includes('/docs/') && r.request().method() === 'PATCH', { timeout: 3000 })`)

    3. Test: "NestJS health endpoint responds at port 3001"
       - `await page.request.get('http://localhost:3001/api/health')` — assert status 200
       - This confirms NestJS is running as a separate process

    4. Test: "combined mode pnpm start still works"
       - This test is marked with `test.skip` and a comment: "Run separately: pnpm --filter @teable/backend start — see AC4"
       - Do not try to start combined mode inside the spec; just document it

    Import `{ test, expect }` from `@playwright/test`. Use `test.describe('doc-library — separated mode')`.

    Add `test.use({ baseURL: 'http://localhost:3000' })` at the top of the describe block.

    Do not import from playwright.config.ts — this spec is standalone.
  </action>
  <verify>
    <automated>node -e "require('fs').existsSync('apps/nextjs-app/e2e/doc-library-separated.spec.ts') ? process.exit(0) : process.exit(1)" && echo "FILE_EXISTS"</automated>
  </verify>
  <done>doc-library-separated.spec.ts exists with at least 2 non-skipped tests covering save latency and NestJS health endpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All three plans' changes are in place:
    - axios.ts uses BACKEND_URL
    - next.config.js proxies /api/* in dev
    - package.json has dev:separated / start:separated
    - doc-library-separated.spec.ts is written

    Start the separated stack and run the new spec:
      pnpm dev:separated
      (wait 30 seconds for both processes to start)
      cd apps/nextjs-app && npx playwright test e2e/doc-library-separated.spec.ts --headed
  </what-built>
  <how-to-verify>
    1. Run `pnpm dev:separated` from repo root — both NestJS (port 3001) and Next.js (port 3000) start with coloured prefixes [api] / [web]
    2. Visit http://localhost:3000 — app loads and shows the UI
    3. Visit http://localhost:3001/api/health — returns {"status":"ok"} or similar 200 JSON
    4. Open a doc-library document and type new content
    5. Confirm "Saved" indicator appears in under 3 seconds (no 30-60s stall)
    6. Run Playwright spec: `npx playwright test e2e/doc-library-separated.spec.ts` — all tests pass
    7. Check NestJS process RSS: `ps -o pid,rss,pmem -p $(lsof -ti:3001) | head -3` — RSS column below 500000 (500MB) after 10 minutes
    8. Confirm combined mode still works: `pnpm --filter @teable/backend start` — starts without error (AC4)
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any failures (save latency, process errors, test failures)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright test runner → localhost:3000/3001 | Test-only; no production trust boundary changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04 | Information Disclosure | Playwright test connecting to localhost:3001 directly | accept | Test-only; never runs in production; NestJS on 3001 is localhost-only in dev |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | No new packages; @playwright/test already in devDependencies |
</threat_model>

<verification>
1. `npx playwright test e2e/doc-library-separated.spec.ts` — all non-skipped tests pass
2. Save latency test asserts PATCH /api/*/docs/* response arrives in < 3000ms
3. Health endpoint test returns 200 from localhost:3001
4. Combined mode: `pnpm --filter @teable/backend start` exits cleanly (or starts without error for CI)
5. NestJS RSS < 500MB after 10 minutes (human-verified via Activity Monitor / `ps -o pid,rss,pmem -p $(lsof -ti:3001) | head -3`)
</verification>

<success_criteria>
- AC1: `dev:separated` boots two processes; browser at localhost:3000 loads the app; /api/health returns 200 from port 3001
- AC2: Doc-library save PATCH resolves in < 3 seconds (no GC stalls)
- AC3: NestJS RSS < 500MB after 10 minutes
- AC4: Combined mode unchanged and working
- AC5: SSR pages render without "Cannot connect to API" errors
</success_criteria>

<output>
Create `.planning/phases/14-process-separation/14-03-SUMMARY.md` when done
</output>
