---
phase: 11-super-agent-hardening
plan: 05
type: execute
wave: 4
depends_on: ["11-01", "11-03", "11-04"]
files_modified:
  - apps/nestjs-backend/src/features/agent/agent-trigger.service.spec.ts
  - apps/nestjs-backend/src/features/agent/agent-event.listener.spec.ts
  - apps/nestjs-backend/src/features/agent/agent.controller.spec.ts
  - apps/nestjs-backend/src/features/agent/agent-execution.service.spec.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Trigger CRUD service behavior is covered by a passing spec"
    - "The record.created/record.updated → agent.* projection/listener path is covered"
    - "Webhook secret auth returns 200 with the correct secret and 401 with a wrong one"
    - "An execution-loop happy path runs with a mocked model and saves the conversation"
    - "All new specs run green in the existing vitest setup"
  artifacts:
    - path: "apps/nestjs-backend/src/features/agent/agent-event.listener.spec.ts"
      provides: "record.* → agent.* projection/listener coverage"
      contains: "record.created"
    - path: "apps/nestjs-backend/src/features/agent/agent-execution.service.spec.ts"
      provides: "execution-loop happy path with mocked model"
      contains: "run"
  key_links:
    - from: "agent-event.listener.spec.ts"
      to: "AgentTriggerService.handleRecordCreated"
      via: "OnEvent('record.created') dispatch"
      pattern: "record\\.created"
    - from: "agent-execution.service.spec.ts"
      to: "mocked AiService.getModelInstance"
      via: "vi.mock / stub returning canned text"
      pattern: "getModelInstance"
---

<objective>
First regression tests for the agent system (AC6): trigger CRUD service, the record.created/record.updated → agent.* projection/listener path, webhook secret auth (200 vs 401), and one execution-loop happy path with a mocked model. Match existing vitest conventions (`describe/it/expect` from 'vitest', `vi.mock` for deps).

Purpose: Lock in the behaviors fixed in plans 11-01..11-04 against regression.
Output: Four passing spec files in the existing vitest unit setup.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-super-agent-hardening/CONTEXT.md

<interfaces>
vitest config: apps/nestjs-backend/vitest.config.ts; run unit specs with `pnpm --filter @teable/nestjs-backend test-unit` (vitest run).
Reference spec style: apps/nestjs-backend/src/features/v2/v2-action-trigger.service.spec.ts — `import { describe, expect, it } from 'vitest'`, plain object fixtures, no Nest TestingModule required.
AgentTriggerService: createTrigger/list/toggle/delete via prisma.agentTrigger; handleRecordCreated/handleRecordUpdated (~116-140); handleDm (~105).
AgentEventListener: @OnEvent('record.created') / ('record.updated') / ('agent.dm') / ('agent.mention').
AgentController.agentWebhook: looks up active webhook trigger, checks X-Agent-Secret header, throws UnauthorizedException('Invalid webhook secret') on mismatch.
AgentExecutionService.run(ctx): async generator; depends on AiService.getModelInstance (mock to return canned generateText result) + AgentConversationService (mock createConversation/saveMessage/getConversationHistory/markConversationComplete).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trigger CRUD + record-projection listener specs</name>
  <read_first>
    - apps/nestjs-backend/src/features/agent/agent-trigger.service.ts (CRUD methods + handleRecordCreated/handleRecordUpdated)
    - apps/nestjs-backend/src/features/agent/agent-event.listener.ts (@OnEvent handlers)
    - apps/nestjs-backend/src/features/v2/v2-action-trigger.service.spec.ts (vitest style reference)
  </read_first>
  <action>
    Create `agent-trigger.service.spec.ts`: instantiate `AgentTriggerService` with a mocked PrismaService (vi.fn() stubs for `agentTrigger.create/findMany/update/delete`) and any other injected deps; assert createTrigger persists with the expected shape, list returns rows, toggle updates isActive, delete removes. Create `agent-event.listener.spec.ts`: instantiate `AgentEventListener` with a mocked `AgentTriggerService`; emit/dispatch the `record.created` and `record.updated` handlers and assert they forward to `handleRecordCreated`/`handleRecordUpdated` with the projected payload (record → agent.* mapping). Use `import { describe, it, expect, vi, beforeEach } from 'vitest'`. Do not require a running DB or Nest TestingModule — construct services directly with stubs, mirroring the reference spec.
  </action>
  <verify>
    <automated>cd apps/nestjs-backend && pnpm vitest run src/features/agent/agent-trigger.service.spec.ts src/features/agent/agent-event.listener.spec.ts --silent</automated>
  </verify>
  <acceptance_criteria>
    - Both spec files exist and pass under `vitest run`.
    - Trigger create/list/toggle/delete behaviors asserted against mocked prisma.
    - record.created and record.updated handlers shown to invoke the trigger service projection path.
  </acceptance_criteria>
  <done>Trigger CRUD and the record→agent projection path are covered and green.</done>
</task>

<task type="auto">
  <name>Task 2: Webhook secret auth + execution-loop happy-path specs</name>
  <read_first>
    - apps/nestjs-backend/src/features/agent/agent.controller.ts (agentWebhook ~152-176; X-Agent-Secret check)
    - apps/nestjs-backend/src/features/agent/agent-execution.service.ts (run() ~67-175; streamLlmIteration model resolution)
    - apps/nestjs-backend/src/features/agent/agent-conversation.service.ts (methods to mock)
  </read_first>
  <action>
    Create `agent.controller.spec.ts`: construct `AgentController` with mocked deps; for `agentWebhook`, stub the prisma trigger lookup to return an active webhook trigger whose secret matches a known value, assert a valid `X-Agent-Secret` resolves successfully (200-equivalent: no throw / returns ack) and a wrong secret throws `UnauthorizedException` (401-equivalent). Create `agent-execution.service.spec.ts`: construct `AgentExecutionService` with mocked AiService (`getModelInstance` returns a stub model; mock the `generateText`/model call to return canned `{ text: 'final answer', steps: [] }` with no tool calls), mocked AgentConversationService (createConversation→id, getConversationHistory→empty, saveMessage/markConversationComplete as vi.fn()), and other stubbed deps; iterate the `run()` async generator for a manual trigger and assert it yields a text/done sequence ending in the final answer AND that `saveMessage` (assistant) + `markConversationComplete` were called. Add a second happy-path assertion is optional. Keep mocks minimal; if a dependency is hard to stub, inject a partial mock typed via `as unknown as <Type>`.
  </action>
  <verify>
    <automated>cd apps/nestjs-backend && pnpm vitest run src/features/agent/agent.controller.spec.ts src/features/agent/agent-execution.service.spec.ts --silent</automated>
  </verify>
  <acceptance_criteria>
    - Webhook spec: correct secret succeeds; wrong secret throws UnauthorizedException.
    - Execution spec: `run()` with a mocked model yields a final text and saves+completes the conversation.
    - Both files pass under `vitest run`; no real DB/model network calls.
  </acceptance_criteria>
  <done>Webhook auth and one execution happy path are covered and green.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @teable/nestjs-backend test-unit` includes the four new specs and passes.
</verification>

<success_criteria>
- Four spec files (trigger CRUD, record-projection listener, webhook secret, execution happy path) run green in the existing vitest setup.
</success_criteria>

<output>
Create `.planning/phases/11-super-agent-hardening/11-05-SUMMARY.md` when done.
</output>
