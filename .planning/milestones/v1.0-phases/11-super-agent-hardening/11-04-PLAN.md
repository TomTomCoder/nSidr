---
phase: 11-super-agent-hardening
plan: 04
type: execute
wave: 3
depends_on: ["11-01", "11-03"]
files_modified:
  - apps/nestjs-backend/src/features/agent/agent.controller.ts
  - apps/nestjs-backend/src/features/agent/agent-trigger.service.ts
autonomous: false
requirements: []
must_haves:
  truths:
    - "agent.dm is emitted from a real direct-message path so AgentEventListener.handleAgentDm → handleDm actually fires"
    - "POST :id/message accepts a user message addressed to an agent and emits agent.dm"
    - "A human-verified end-to-end run completes against a configured model"
  artifacts:
    - path: "apps/nestjs-backend/src/features/agent/agent.controller.ts"
      provides: "POST :id/message endpoint emitting agent.dm"
      contains: "message"
  key_links:
    - from: "AgentController.message"
      to: "agent.dm event"
      via: "EventEmitter2.emit('agent.dm', payload)"
      pattern: "emit\\('agent.dm'"
    - from: "agent.dm"
      to: "AgentEventListener.handleAgentDm"
      via: "@OnEvent('agent.dm')"
      pattern: "@OnEvent\\('agent.dm'\\)"
---

<objective>
Give the DM trigger a real source (AC4) and human-verify end-to-end execution (AC1 checkpoint). Today `AgentEventListener.handleAgentDm` is wired to `@OnEvent('agent.dm')` and calls `AgentTriggerService.handleDm`, but nothing emits `agent.dm` (only `agent.mention` is emitted, from comment-open-api.service.ts). Add a minimal `POST :id/message` endpoint that emits `agent.dm`, making the wired handler reachable. Then a human verifies a real agent run completes against a configured model.

Purpose: Close AC4 and gate AC1 on operator-configured AI.
Output: DM emitter endpoint + human-verified live run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-super-agent-hardening/CONTEXT.md

<interfaces>
AgentEventListener (agent-event.listener.ts): @OnEvent('agent.dm') handleAgentDm(payload: { agentId; message; fromUserId; ... }) → triggerService.handleDm(agentId, { message, fromUserId }).
AgentTriggerService.handleDm(agentId, { message, fromUserId }): void (agent-trigger.service.ts ~105).
comment-open-api.service.ts is the REFERENCE emitter: it injects EventEmitter2 and calls `emit('agent.mention', payload)`.
AgentController uses ClsService<IClsStore> for the user id; needs EventEmitter2 injected.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add POST :id/message endpoint that emits agent.dm</name>
  <read_first>
    - apps/nestjs-backend/src/features/agent/agent.controller.ts (constructor injections; existing routes; ClsService usage)
    - apps/nestjs-backend/src/features/agent/agent-event.listener.ts (@OnEvent('agent.dm') handleAgentDm payload shape ~32-48)
    - apps/nestjs-backend/src/features/comment/comment-open-api.service.ts (reference: EventEmitter2 injection + emit('agent.mention', payload))
    - apps/nestjs-backend/src/features/agent/agent-trigger.service.ts (handleDm signature ~105)
  </read_first>
  <action>
    Inject `EventEmitter2` (from '@nestjs/event-emitter') into `AgentController`. Add `@Post(':id/message')` `sendMessage(@Param('id') id, @Body() body)` decorated `@Permissions('base|create')` (covered by the AgentPermissionGuard from plan 11-01). Resolve `fromUserId` from `this.cls.get('user')?.id`. Build the payload to EXACTLY match `handleAgentDm`'s expected shape (`{ agentId: id, message: body.message, fromUserId }` plus any conversationId the listener forwards) and call `this.eventEmitter.emit('agent.dm', payload)`. Return a lightweight ack (e.g. `{ accepted: true }`); the actual run happens via the listener → handleDm path. Do not duplicate the run loop here. Ensure `EventModule`/`EventEmitterModule` is available to AgentModule (it already powers agent.mention — confirm import).
  </action>
  <verify>
    <automated>cd apps/nestjs-backend && grep -q "emit('agent.dm'" src/features/agent/agent.controller.ts && grep -q "@Post(':id/message')" src/features/agent/agent.controller.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `POST :id/message` exists, guarded `base|create`, resolves `fromUserId` from ClsService.
    - It emits `agent.dm` with a payload matching `handleAgentDm`'s expected shape.
    - No duplicate execution loop; the run flows through the existing listener → handleDm path.
    - EventEmitter2 wiring present and module imports resolve.
  </acceptance_criteria>
  <done>agent.dm is emitted from a real endpoint and the wired handler fires.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Backend: authorized agent routes (11-01), persistent tool state (11-02), conversationId-aware run loop with graceful AI-config error handling (11-03), and a POST :id/message DM emitter (this plan). All wired to the existing AgentExecutionService run loop and AgentEventListener.
  </what-built>
  <how-to-verify>
    Prerequisite (operator action): configure a real AI model for the test base — set the base AI / AI Gateway model + provider/API key in base settings, since AiService.getAIConfig throws when unconfigured. The agent under test must have `modelKey` set or the base chatModel.lg configured.
    1. Start the stack (NestJS embeds Next.js on port 3001 per project notes; ensure Postgres + Redis are running).
    2. Open an agent's chat (UnifiedChatContainer) and send a message.
    3. Confirm the run streams progress/think/text events and produces a final assistant message; verify the conversation is saved (appears in the conversations list / GET :id/conversations).
    4. Send a follow-up referencing the prior turn; confirm the agent remembers it (AC3 — same conversationId reused).
    5. Negative path: point the agent at a base with NO AI config (or temporarily clear it) and run; confirm a single structured error event with actionable copy appears and the server does NOT crash / no unhandled rejection in logs (AC1 graceful failure).
    6. DM path: POST /api/agent/:id/message with a body `{ "message": "hi" }` (authenticated) and confirm in logs that handleAgentDm → handleDm fires.
  </how-to-verify>
  <resume-signal>Type "approved" once a live run completes, follow-up memory works, the unconfigured-base error is graceful, and agent.dm fires — or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → POST :id/message | Authenticated, base-scoped (guard from 11-01); emits internal agent.dm event |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-06 | Spoofing | fromUserId on :id/message | mitigate | fromUserId taken from authenticated ClsService user, not from client body |
| T-11-07 | Elevation of Privilege | :id/message route | mitigate | @Permissions('base\|create') + AgentPermissionGuard scope to owning base |
</threat_model>

<verification>
- Human checkpoint above gates AC1 (live run + graceful failure) and confirms AC4 (agent.dm fires).
</verification>

<success_criteria>
- POST :id/message emits agent.dm and the wired handler runs.
- A human confirms a live end-to-end run, follow-up memory, and graceful AI-config failure.
</success_criteria>

<output>
Create `.planning/phases/11-super-agent-hardening/11-04-SUMMARY.md` when done.
</output>
