---
phase: 11-super-agent-hardening
plan: 06
type: execute
wave: 4
depends_on: ["11-05"]
files_modified:
  - apps/nestjs-backend/src/features/agent/agent-execution.service.ts
autonomous: true
optional: true
requirements: []
must_haves:
  truths:
    - "Record CRUD tools resolve real field→column mappings instead of raw $queryRawUnsafe JSON access"
    - "create_record/update_record/delete_record operate via the field→column model and remain test-covered"
  artifacts:
    - path: "apps/nestjs-backend/src/features/agent/agent-execution.service.ts"
      provides: "record tools using the real field→column model"
      contains: "create_record"
  key_links:
    - from: "executeToolCall create_record/update_record"
      to: "field→column resolution service"
      via: "replace $queryRawUnsafe with mapped column access"
      pattern: "create_record|update_record"
---

<objective>
STRETCH / OPTIONAL (AC7). The built-in record tools (`create_record`, `update_record`, `delete_record`) in `AgentExecutionService.executeToolCall` currently use raw `$queryRawUnsafe` JSON access against the data table. Replace that with the real field→column model so records are written through Teable's field mapping rather than ad-hoc raw SQL. This plan is optional — execute only if it does not bloat the phase; defer otherwise.

Purpose: Harden record tools (AC7 stretch).
Output: record tools backed by the field→column model.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-super-agent-hardening/CONTEXT.md

<interfaces>
agent-execution.service.ts executeToolCall handles 'create_record' (~505-525), 'update_record' (~541-558), 'delete_record' (~573-582) using $queryRawUnsafe / direct column writes.
Teable exposes record-create/update services that map fields → db columns (locate the canonical RecordService/RecordOpenApiService used elsewhere in the backend; prefer reusing it over raw SQL).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route record tools through the field→column model</name>
  <read_first>
    - apps/nestjs-backend/src/features/agent/agent-execution.service.ts (create_record/update_record/delete_record cases ~505-590)
    - The canonical record service used elsewhere (grep the backend for the service that maps field ids → column names and performs create/update/delete; reuse it rather than raw SQL)
  </read_first>
  <action>
    Replace the `$queryRawUnsafe`-based JSON column access in `create_record`, `update_record`, and `delete_record` with calls to Teable's existing record service that resolves field ids → columns (inject it via the module). Keep the tool input contract stable (tableId + field-keyed values), translating field names/ids to the service's expected shape. Preserve the existing structured `{ success, error }` return values and error logging. If the canonical service cannot be cleanly injected here without large refactor, scope this to `create_record` first and note the rest as a follow-up in the SUMMARY (do not expand the phase). Do NOT add a Prisma migration.
  </action>
  <verify>
    <automated>cd apps/nestjs-backend && ! grep -A8 "case 'create_record'" src/features/agent/agent-execution.service.ts | grep -q "queryRawUnsafe" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - At least `create_record` no longer uses `$queryRawUnsafe`; it writes via the field→column record service.
    - Tool input/return contract unchanged (`{ success, error }`).
    - Existing execution spec (plan 11-05) still passes; no DB migration introduced.
  </acceptance_criteria>
  <done>Record tools write through the real field→column model.</done>
</task>

</tasks>

<verification>
- Backend typechecks; plan 11-05 execution spec still green.
</verification>

<success_criteria>
- Record tools use the field→column model rather than raw $queryRawUnsafe JSON access.
</success_criteria>

<output>
Create `.planning/phases/11-super-agent-hardening/11-06-SUMMARY.md` when done.
</output>
