---
phase: "10-unified-ai-assistant"
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db-main-prisma/prisma/postgres/schema.prisma
autonomous: true
requirements:
  - D-05
  - D-07
must_haves:
  truths:
    - "WorkspaceConversation records are persisted scoped to spaceId"
    - "WorkspaceConversationMessage records link to a conversation via conversationId"
    - "WorkspaceConversationMessage has a unique proposalId column for O(1) proposal lookup"
    - "Prisma client is regenerated and types are available after db push"
  artifacts:
    - path: "packages/db-main-prisma/prisma/postgres/schema.prisma"
      provides: "WorkspaceConversation + WorkspaceConversationMessage models"
      contains: "model WorkspaceConversation"
  key_links:
    - from: "WorkspaceConversation"
      to: "Space"
      via: "spaceId FK"
      pattern: "relation.*Space.*fields.*spaceId"
    - from: "WorkspaceConversationMessage"
      to: "WorkspaceConversation"
      via: "conversationId FK with onDelete Cascade"
      pattern: "onDelete: Cascade"
    - from: "ActionProposalService.acceptProposal()"
      to: "WorkspaceConversationMessage"
      via: "proposalId @unique column — O(1) lookup without in-memory filter"
      pattern: "proposalId.*@unique"
---

<objective>
Add the `WorkspaceConversation` and `WorkspaceConversationMessage` Prisma models to the schema and push to the database.

Purpose: Unified AI chat requires conversations scoped to spaceId. The existing `AgentConversation` model has a required `agentId` FK to the Agent model — it cannot be reused without corruption. A new parallel model with `spaceId` FK is the correct approach per D-05 and research finding A1.
Output: Two new Prisma models in schema.prisma + db push complete + Prisma client regenerated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-unified-ai-assistant/10-CONTEXT.md
@.planning/phases/10-unified-ai-assistant/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add WorkspaceConversation + WorkspaceConversationMessage to schema.prisma [BLOCKING]</name>
  <files>packages/db-main-prisma/prisma/postgres/schema.prisma</files>
  <read_first>
    - packages/db-main-prisma/prisma/postgres/schema.prisma — read lines 20-36 (Space model) and lines 1215-1251 (AgentConversation pattern to mirror)
  </read_first>
  <action>
    Append the following two models at the end of schema.prisma, after the last model definition. Also add `workspaceConversations WorkspaceConversation[]` to the Space model relation list (after the existing `importedDocs ImportedDoc[]` line).

    Model 1 — WorkspaceConversation:
    - id: String @id @default(cuid())
    - spaceId: String @map("space_id")
    - title: String? (auto-generated from first message or user-editable)
    - status: String @default("in_progress") — values: 'in_progress' | 'completed'
    - createdTime: DateTime @default(now()) @map("created_time")
    - updatedTime: DateTime @updatedAt @map("updated_time")
    - createdBy: String @map("created_by")
    - Relations: space Space @relation(fields: [spaceId], references: [id], onDelete: Cascade); messages WorkspaceConversationMessage[]
    - Indexes: @@index([spaceId]); @@index([createdTime])
    - @@map("workspace_conversation")

    Model 2 — WorkspaceConversationMessage:
    - id: String @id @default(cuid())
    - conversationId: String @map("conversation_id")
    - role: String — values: 'user' | 'assistant'
    - type: String — values: 'text' | 'proposal' | 'tool_result' | 'error'
    - content: String @db.Text
    - proposalId: String? @unique @map("proposal_id") — set to the UUID proposalId when type='proposal'; null for all other message types. This column enables O(1) lookup via findUnique in acceptProposal() without in-memory filtering.
    - metadata: Json? — stores proposal JSON when type='proposal' (proposalId, action, args, accepted, acceptedAt)
    - createdTime: DateTime @default(now()) @map("created_time")
    - Relations: conversation WorkspaceConversation @relation(fields: [conversationId], references: [id], onDelete: Cascade)
    - Indexes: @@index([conversationId])
    - @@map("workspace_conversation_message")

    Per D-05 and research Pattern 3. Do NOT add toolName/toolInput/toolOutput fields — the metadata Json column covers all proposal and tool data for this model.
  </action>
  <verify>
    <automated>cd /Users/tommylambert/Documents/Claude_Folder/teable && grep -c "model WorkspaceConversation" packages/db-main-prisma/prisma/postgres/schema.prisma</automated>
  </verify>
  <done>grep returns 2 (one for each model: WorkspaceConversation and WorkspaceConversationMessage). Space model has the workspaceConversations relation line. WorkspaceConversationMessage has the proposalId String? @unique column.</done>
</task>

<task type="auto">
  <name>Task 2: Push schema to DB and regenerate Prisma client</name>
  <files>packages/db-main-prisma/prisma/postgres/schema.prisma</files>
  <read_first>
    - packages/db-main-prisma/prisma/postgres/schema.prisma — confirm Task 1 edits are present before pushing
  </read_first>
  <action>
    Run the following commands in sequence from the repo root:
    1. `pnpm --filter @teable/db-main-prisma db:push` — pushes schema changes to the running PostgreSQL instance (creates workspace_conversation and workspace_conversation_message tables, including the unique index on proposal_id)
    2. `pnpm --filter @teable/db-main-prisma generate` — regenerates the Prisma client so TypeScript types for WorkspaceConversation and WorkspaceConversationMessage are available to all packages

    If db:push fails with "Environment variable not found: DATABASE_URL", check that the local .env has DATABASE_URL set. Do not modify .env — surface the error in the summary.
  </action>
  <verify>
    <automated>cd /Users/tommylambert/Documents/Claude_Folder/teable && pnpm --filter @teable/db-main-prisma generate 2>&1 | tail -5</automated>
  </verify>
  <done>Prisma generate completes without errors. The generated client directory contains types for WorkspaceConversation (confirm via: grep -r "WorkspaceConversation" node_modules/.pnpm/ 2>/dev/null | head -1 OR grep "WorkspaceConversation" packages/db-main-prisma/generated/client/index.d.ts | head -1).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB migration | Schema changes are applied to the production-equivalent DB; irreversible without a rollback migration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | schema.prisma migration | mitigate | Verify model definitions match research spec before push; review diff before applying |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this plan — Prisma generate uses existing installed binary |
</threat_model>

<verification>
After both tasks complete:
- `grep "workspace_conversation" packages/db-main-prisma/prisma/postgres/schema.prisma` returns 2+ lines
- `grep "WorkspaceConversation" packages/db-main-prisma/prisma/postgres/schema.prisma` returns 4+ lines (model declarations + relation refs)
- `grep "proposalId.*@unique\|proposal_id.*unique" packages/db-main-prisma/prisma/postgres/schema.prisma` returns 1 line
- Prisma generate exits 0
</verification>

<success_criteria>
Two tables — workspace_conversation and workspace_conversation_message — exist in PostgreSQL. workspace_conversation_message has a unique index on proposal_id. Prisma client TypeScript types are regenerated and importable from @teable/db-main-prisma.
</success_criteria>

<output>
Create `.planning/phases/10-unified-ai-assistant/10-01-SUMMARY.md` when done
</output>
