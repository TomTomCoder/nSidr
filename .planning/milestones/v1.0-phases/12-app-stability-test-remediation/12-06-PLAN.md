---
phase: 12-app-stability-test-remediation
plan: 06
type: execute
wave: 3
depends_on: [12-03, 12-05]
files_modified:
  - .planning/TESTING-PLAN.md
autonomous: false
requirements: [12-UI]
must_haves:
  truths:
    - "TESTING-PLAN.md sections 5-8 (Gantt, Authority Matrix, Prompt System, Super Agent) each have every row marked [P] or [F]"
    - "Super Agent DM trigger (8.11) is verified now that POST :id/message is implemented"
    - "Every bug discovered in sections 5-8 is fixed in source or recorded in Bug Tracking"
    - "No row in sections 5-8 remains [ ] after this pass"
  artifacts:
    - path: ".planning/TESTING-PLAN.md"
      provides: "Status column updated for sections 5-8"
      contains: "[P]"
  key_links:
    - from: "Super Agent DM test (8.11)"
      to: "POST :id/message → agent.dm"
      via: "browser-verified DM send"
      pattern: "\\[P\\]|\\[F\\]"
---

<objective>
Progressive UI testing pass 2. Run TESTING-PLAN.md sections 5-8 (Gantt View, Authority Matrix, Prompt System, Super Agent System) in the browser, fix every bug found, and mark each row [P]/[F]. Section 8.11 (Agent DM trigger) is newly testable after 12-01/11-04.

Purpose: These are the feature-heavy Phase 1/2/4/5 surfaces. Verifying them in-browser catches integration bugs that unit/E2E specs miss.
Output: TESTING-PLAN.md sections 5-8 fully marked; source fixes for discovered bugs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/TESTING-PLAN.md
@.planning/phases/12-app-stability-test-remediation/12-RESEARCH.md
@.planning/codebase/ARCHITECTURE.md

<interfaces>
App on http://localhost:3001. Login: test@e2e.com / 12345678.
Section 8.11 (Agent DM): POST :id/message is now implemented (12-01/commit bbb916bac); the agent chat UI sends conversationId and emits agent.dm.
Existing supporting E2E: e2e/pages/features/gantt-view*, authority-matrix*, e2e/agent-chat.spec.ts.
Status legend: [ ] untested, [P] pass, [F] fail (fix applied). Bugs → "## Bug Tracking" table.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>Pre-flight: app running for browser testing.</what-built>
  <how-to-verify>
    1. Confirm http://localhost:3001 is reachable and login works.
    2. For Super Agent tests, confirm a base AI model config exists (or be ready to verify the graceful missing-config error path instead).
  </how-to-verify>
  <resume-signal>Type "running" once the app is reachable and AI config status is known</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Sections 5-8 of TESTING-PLAN.md, browser-driven with source fixes for failures.</what-built>
  <how-to-verify>
    Walk through each row in order:
    - Section 5 Gantt View (Phase 5): create gantt view, set start/end date fields, render bars, milestones, dependency arrows, critical path, drag-to-reschedule.
    - Section 6 Authority Matrix (Phase 1): open matrix, toggle role/permission cells, save, confirm persistence.
    - Section 7 Prompt System (Phase 2): open admin prompt overrides panel, edit an override, save, confirm the feature uses the override.
    - Section 8 Super Agent System (Phase 4): 3-step creation wizard, agent config tabs, run an agent, tool dispatch, and specifically 8.11 — send a DM to an agent (POST :id/message) and confirm the agent.dm trigger fires / agent responds (or surfaces a graceful error if no AI config).
    For each row: [P] if it passes; if it fails, fix the source bug, re-run, mark [F] with a note, and add to "## Bug Tracking". Use existing E2E specs as supporting evidence where available. Do not mark [P] without exercising the row; do not leave any section 5-8 row as [ ].
  </how-to-verify>
  <resume-signal>Type "approved" when sections 5-8 are fully marked, or describe blockers</resume-signal>
</task>

<task type="auto">
  <name>Task: Record pass-2 results in TESTING-PLAN.md</name>
  <files>.planning/TESTING-PLAN.md</files>
  <read_first>
    - .planning/TESTING-PLAN.md
  </read_first>
  <action>
    Persist the pass-2 outcomes: mark every status cell in sections 5-8 [P]/[F], add discovered bugs (file + fix summary) to "## Bug Tracking", and ensure no [ ] remains in sections 5-8. List source fixes in this plan's SUMMARY. Update section 8.11's status (previously a known stub) now that the DM trigger is implemented. Scope cap: if a single bug fix touches more than 3 files not already in files_modified, mark it [F] with a note and create a follow-up task rather than expanding this plan's scope.
  </action>
  <verify>
    <automated>awk '/^## 5\./{f=1} /^## 9\./{f=0} f && /\| `\[ \]` \|/{c++} END{exit (c>0)}' .planning/TESTING-PLAN.md && echo "no-untested-in-5-8"</automated>
  </verify>
  <acceptance_criteria>
    - Every row in TESTING-PLAN.md sections 5-8 is [P] or [F] (zero [ ])
    - Section 8.11 (DM trigger) is marked based on a browser test
    - Bug Tracking table updated; fixes listed in 12-06-SUMMARY.md
  </acceptance_criteria>
  <done>Sections 5-8 fully marked; 8.11 verified; no untested rows remain in that range.</done>
</task>

</tasks>

<verification>
- No `[ ]` status remains in TESTING-PLAN.md sections 5-8
- Section 8.11 DM trigger verified in browser
</verification>

<success_criteria>
- TESTING-PLAN.md sections 5-8 fully verified and marked [P]/[F]
- All section 5-8 bugs fixed or tracked
</success_criteria>

<output>
Create `.planning/phases/12-app-stability-test-remediation/12-06-SUMMARY.md` when done
</output>
