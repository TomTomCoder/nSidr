---
phase: 12-app-stability-test-remediation
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - apps/nestjs-backend/src/features/agent/agent-trigger.service.spec.ts
  - apps/nestjs-backend/src/features/agent/agent-event.listener.spec.ts
  - apps/nestjs-backend/src/features/agent/agent.controller.spec.ts
  - apps/nestjs-backend/src/features/agent/agent-execution.service.spec.ts
autonomous: true
requirements: [11-05]
must_haves:
  truths:
    - "Four agent module spec files exist and pass under Vitest"
    - "Trigger CRUD (create/list/delete) is covered by passing tests"
    - "agent.dm dispatch from listener to handleDm is covered by a passing test"
    - "POST :id/message emits agent.dm and is covered by a passing controller test"
    - "Execution loop happy path and missing-AI-config graceful error are both covered"
  artifacts:
    - path: "apps/nestjs-backend/src/features/agent/agent-trigger.service.spec.ts"
      provides: "Trigger CRUD + record-projection coverage"
      min_lines: 40
    - path: "apps/nestjs-backend/src/features/agent/agent-event.listener.spec.ts"
      provides: "agent.dm dispatch coverage"
      min_lines: 25
    - path: "apps/nestjs-backend/src/features/agent/agent.controller.spec.ts"
      provides: "POST :id/message emit + webhook auth coverage"
      min_lines: 40
    - path: "apps/nestjs-backend/src/features/agent/agent-execution.service.spec.ts"
      provides: "Execution loop happy path + missing-config error coverage"
      min_lines: 40
  key_links:
    - from: "agent-execution.service.spec.ts"
      to: "AgentExecutionService.run async generator"
      via: "for await collection of yielded events"
      pattern: "for await"
---

<objective>
Write the first regression test coverage for the agent module: four Vitest spec files covering trigger CRUD, the agent.dm event dispatch, the DM/webhook controller surface, and the execution loop (happy path + graceful missing-config error). All must pass via `pnpm --filter @teable/nestjs-backend test-unit`.

Purpose: Phase 11 AC6 requires regression tests. The agent module currently has zero spec coverage, so any refactor (e.g. 12-03 record tools) is unguarded.
Output: 4 `*.spec.ts` files under `apps/nestjs-backend/src/features/agent/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-app-stability-test-remediation/12-RESEARCH.md
@.planning/phases/11-super-agent-hardening/11-05-PLAN.md

<interfaces>
Reference spec style (DO read before writing): apps/nestjs-backend/src/features/v2/v2-action-trigger.service.spec.ts
- Imports from 'vitest': describe, it, expect, vi, beforeEach
- No Nest TestingModule — construct services directly with stubbed deps
- vi.fn() for individual methods; plain objects cast `as unknown as <Type>` for injected deps

Services under test (READ their constructors + public methods before writing tests):
- apps/nestjs-backend/src/features/agent/agent-trigger.service.ts
- apps/nestjs-backend/src/features/agent/agent-event.listener.ts
- apps/nestjs-backend/src/features/agent/agent.controller.ts
- apps/nestjs-backend/src/features/agent/agent-execution.service.ts

Run command: pnpm --filter @teable/nestjs-backend test-unit
Run a single file: pnpm --filter @teable/nestjs-backend test-unit <spec-filename>
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Spec for AgentTriggerService CRUD + AgentEventListener dispatch</name>
  <files>apps/nestjs-backend/src/features/agent/agent-trigger.service.spec.ts, apps/nestjs-backend/src/features/agent/agent-event.listener.spec.ts</files>
  <read_first>
    - apps/nestjs-backend/src/features/v2/v2-action-trigger.service.spec.ts
    - apps/nestjs-backend/src/features/agent/agent-trigger.service.ts
    - apps/nestjs-backend/src/features/agent/agent-event.listener.ts
  </read_first>
  <behavior>
    agent-trigger.service.spec.ts:
    - Test 1: createTrigger persists a trigger via prisma and returns it.
    - Test 2: list returns triggers for a given agentId.
    - Test 3: delete removes a trigger by id.
    - Test 4 (edge): handleDm (or handleRecordCreated, whichever exists) is invoked and routes to execution with the right agentId.
    agent-event.listener.spec.ts:
    - Test 1: handleAgentDm receives an `agent.dm` payload and calls the trigger service's handleDm with the same agentId/message/fromUserId/conversationId.
    - Test 2 (edge): missing optional conversationId is tolerated (no throw).
    - Test 3: handleRecordCreated receives a `record.created` event and dispatches to AgentTriggerService's record-projection handler (e.g. handleRecordCreated or equivalent) with the correct tableId and record payload.
    - Test 4: handleRecordUpdated receives a `record.updated` event and dispatches to AgentTriggerService's record-projection handler with the correct tableId and updated record payload.
  </behavior>
  <action>
    Construct AgentTriggerService directly with a stubbed PrismaService (vi.fn() per method actually called by the methods under test — read the source to know which). Construct AgentEventListener with a stubbed AgentTriggerService whose handleDm is a vi.fn(); assert it is called with the emitted payload shape. For record event tests, stub AgentTriggerService's projection methods (handleRecordCreated / handleRecordUpdated — read the source to confirm exact names) as vi.fn() and assert they are called with the correct arguments when the listener dispatches the event. Mirror the import and mocking style of v2-action-trigger.service.spec.ts exactly. Do not invent methods — only test methods that exist on the real services.
  </action>
  <verify>
    <automated>pnpm --filter @teable/nestjs-backend test-unit agent-trigger.service.spec.ts agent-event.listener.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both spec files exist and import from 'vitest'
    - `pnpm --filter @teable/nestjs-backend test-unit agent-trigger.service.spec.ts agent-event.listener.spec.ts` exits 0
    - Trigger create/list/delete each have a passing test
    - Listener test asserts handleDm called with the agent.dm payload
    - Listener tests assert record.created and record.updated events dispatch to the projection handler
  </acceptance_criteria>
  <done>Both spec files pass under Vitest with the behaviors above covered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Spec for AgentController (POST :id/message emit + webhook auth)</name>
  <files>apps/nestjs-backend/src/features/agent/agent.controller.spec.ts</files>
  <read_first>
    - apps/nestjs-backend/src/features/agent/agent.controller.ts
    - apps/nestjs-backend/src/features/v2/v2-action-trigger.service.spec.ts
  </read_first>
  <behavior>
    - Test 1: POST :id/message handler calls eventEmitter.emit('agent.dm', payload) with { agentId, message, fromUserId, conversationId } and returns the expected response (201/ok shape per the handler).
    - Test 2: agentWebhook with a matching X-Agent-Secret resolves successfully.
    - Test 3 (edge): agentWebhook with a wrong secret throws UnauthorizedException('Invalid webhook secret').
  </behavior>
  <action>
    Construct AgentController directly, injecting stubbed dependencies (EventEmitter2 with emit: vi.fn(), the cls service returning a fixed user id, and whatever services the message/webhook handlers call — read the constructor). For the emit test, call the message handler and assert eventEmitter.emit was called with 'agent.dm' and the correct payload. For webhook auth, stub the trigger lookup to return a trigger with a known secret and assert the secret-mismatch path throws UnauthorizedException. Use the cls/user mock pattern; if `cls.get('user')` is used, stub cls.get to return `{ id: 'usr1' }`.
  </action>
  <verify>
    <automated>pnpm --filter @teable/nestjs-backend test-unit agent.controller.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - agent.controller.spec.ts exists and imports from 'vitest'
    - Test asserts emit('agent.dm', ...) called with agentId/message/fromUserId/conversationId
    - Test asserts UnauthorizedException on wrong webhook secret
    - `pnpm --filter @teable/nestjs-backend test-unit agent.controller.spec.ts` exits 0
  </acceptance_criteria>
  <done>agent.controller.spec.ts passes under Vitest covering emit + webhook auth.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Spec for AgentExecutionService run loop (happy path + missing config)</name>
  <files>apps/nestjs-backend/src/features/agent/agent-execution.service.spec.ts</files>
  <read_first>
    - apps/nestjs-backend/src/features/agent/agent-execution.service.ts
    - apps/nestjs-backend/src/features/agent/agent-conversation.service.ts
  </read_first>
  <behavior>
    - Test 1 (happy path): run(ctx) is an async generator; mock the AI service's model-instance getter to yield a final `{ text: 'final', steps: [] }`; collect events via `for await` and assert a terminal/text event is produced and the conversation save methods are called.
    - Test 2 (graceful missing-config): when the AI/base config is absent, run(ctx) surfaces a clear error event (or throws a typed error) rather than crashing with an unhandled raw config error — assert the error path is exercised and is graceful.
  </behavior>
  <action>
    Construct AgentExecutionService directly with stubbed AiService (mock getModelInstance/getModelInstance equivalent to return a stub that yields the final step), stubbed AgentConversationService (createConversation, getConversationHistory, saveMessage, markConversationComplete as vi.fn()), and stubbed prisma/data prisma as needed. CRITICAL: iterate the generator with `for await (const event of service.run(ctx)) { events.push(event); }` then assert on the collected array — never call run synchronously. For the missing-config test, make the config lookup return null/undefined and assert run emits/raises a graceful error (match the actual error-surfacing mechanism in the source — read it first; it was hardened in 11-03).
  </action>
  <verify>
    <automated>pnpm --filter @teable/nestjs-backend test-unit agent-execution.service.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - agent-execution.service.spec.ts exists and uses `for await` to drain the generator
    - Happy-path test asserts a text/terminal event and conversation save calls
    - Missing-config test asserts a graceful error path (not an unhandled throw)
    - `pnpm --filter @teable/nestjs-backend test-unit agent-execution.service.spec.ts` exits 0
  </acceptance_criteria>
  <done>agent-execution.service.spec.ts passes covering happy path and graceful missing-config.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @teable/nestjs-backend test-unit` runs all four new specs and exits 0
- All four spec files present under apps/nestjs-backend/src/features/agent/
</verification>

<success_criteria>
- 4 agent spec files exist and pass
- Trigger CRUD, agent.dm dispatch, record.created/updated dispatch, POST :id/message emit, webhook auth, execution happy path, and graceful missing-config all covered
</success_criteria>

<output>
Create `.planning/phases/12-app-stability-test-remediation/12-02-SUMMARY.md` when done
</output>
