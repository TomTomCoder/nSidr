---
phase: 12-app-stability-test-remediation
plan: 04
type: execute
wave: 2
depends_on: [12-02]
files_modified:
  - apps/nextjs-app/src/features/app/blocks/doc-search/GlobalDocSearchPanel.tsx
  - apps/nextjs-app/src/AppProviders.tsx
autonomous: true
requirements: [07-04]
must_haves:
  truths:
    - "Pressing Cmd+Shift+K (or Ctrl+Shift+K) on a space route opens the DocSearchPanel"
    - "The panel reads spaceId from the router and renders only when spaceId is present"
    - "Pressing Escape or clicking the backdrop closes the panel and resets store isOpen"
  artifacts:
    - path: "apps/nextjs-app/src/features/app/blocks/doc-search/GlobalDocSearchPanel.tsx"
      provides: "Layout-level mount of DocSearchPanel keyed to useDocSearchStore.isOpen"
      min_lines: 20
  key_links:
    - from: "GlobalDocSearchPanel"
      to: "useDocSearchStore.isOpen"
      via: "conditional render of DocSearchPanel"
      pattern: "useDocSearchStore"
    - from: "GlobalDocSearchPanel"
      to: "DocSearchPanel"
      via: "open + spaceId + onClose props"
      pattern: "DocSearchPanel"
---

<objective>
Close the Phase 7 doc-search render gap: the `Cmd+Shift+K` shortcut toggles `useDocSearchStore.isOpen` but no panel is mounted at the layout level, so nothing renders. Create a small global component that reads `spaceId` from the router and conditionally renders the existing `DocSearchPanel` keyed to the store, then mount it in `AppProviders`.

Purpose: The component, store, hook, and sidebar link all already exist (Phase 7). Only the layout-level mount is missing. This makes the global doc search actually appear.
Output: `GlobalDocSearchPanel.tsx` + one mount line in `AppProviders.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-app-stability-test-remediation/12-RESEARCH.md

<interfaces>
DocSearchPanel: apps/nextjs-app/src/features/app/blocks/doc-search/DocSearchPanel.tsx
  export function DocSearchPanel({ spaceId: string, open: boolean, onClose: () => void, onSelectResult }: DocSearchPanelProps)
  NOTE: the prop is `open` (boolean), NOT `isOpen`.

useDocSearchStore: apps/nextjs-app/src/features/app/blocks/doc-search/useDocSearchStore.ts
  { isOpen: boolean, openDocSearch, closeDocSearch, toggleDocSearch }

useDocSearchKeyboardShortcut: already called inside AppProviders' KeyboardShortcutInitializer (toggles store).

AppProviders: apps/nextjs-app/src/AppProviders.tsx
  - KeyboardShortcutInitializer wraps children and calls useDocSearchKeyboardShortcut()
  - Use next/router useRouter().query.spaceId to read the current space id (this is the Pages Router app)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GlobalDocSearchPanel layout-level component</name>
  <files>apps/nextjs-app/src/features/app/blocks/doc-search/GlobalDocSearchPanel.tsx</files>
  <read_first>
    - apps/nextjs-app/src/features/app/blocks/doc-search/DocSearchPanel.tsx
    - apps/nextjs-app/src/features/app/blocks/doc-search/useDocSearchStore.ts
    - apps/nextjs-app/src/features/app/blocks/doc-search/useDocSearchKeyboardShortcut.ts
  </read_first>
  <action>
    Create a client component `GlobalDocSearchPanel` that: (1) reads `spaceId` from `useRouter().query.spaceId` (next/router, Pages Router — coerce to string; arrays take first element); (2) reads `isOpen` and `closeDocSearch` from useDocSearchStore; (3) returns null when spaceId is undefined OR isOpen is false (the shortcut is a no-op outside a space, per research pitfall 2); (4) otherwise renders `<DocSearchPanel spaceId={spaceId} open={isOpen} onClose={closeDocSearch} onSelectResult={...} />`. For onSelectResult, navigate to the doc-library page for the selected result or simply close the panel — pick the minimal behavior that does not crash (read DocSearchPanel to see what onSelectResult receives). Use the prop name `open` (DocSearchPanel expects `open`, not `isOpen`).
  </action>
  <verify>
    <automated>test -f apps/nextjs-app/src/features/app/blocks/doc-search/GlobalDocSearchPanel.tsx && grep -q "useDocSearchStore" apps/nextjs-app/src/features/app/blocks/doc-search/GlobalDocSearchPanel.tsx && grep -q "DocSearchPanel" apps/nextjs-app/src/features/app/blocks/doc-search/GlobalDocSearchPanel.tsx && grep -q "spaceId" apps/nextjs-app/src/features/app/blocks/doc-search/GlobalDocSearchPanel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - GlobalDocSearchPanel.tsx exists
    - Reads spaceId from router, returns null when spaceId missing or store closed
    - Renders DocSearchPanel with `open={isOpen}` and `onClose={closeDocSearch}`
    - Uses the correct prop name `open` (not `isOpen`)
  </acceptance_criteria>
  <done>GlobalDocSearchPanel renders DocSearchPanel keyed to the store and router spaceId.</done>
</task>

<task type="auto">
  <name>Task 2: Mount GlobalDocSearchPanel in AppProviders</name>
  <files>apps/nextjs-app/src/AppProviders.tsx</files>
  <read_first>
    - apps/nextjs-app/src/AppProviders.tsx
    - apps/nextjs-app/src/features/app/blocks/doc-search/GlobalDocSearchPanel.tsx
  </read_first>
  <action>
    Import GlobalDocSearchPanel into AppProviders and render it once inside the provider tree, as a sibling of the children (e.g. right after `<KeyboardShortcutInitializer>{children}</KeyboardShortcutInitializer>` and alongside the Toasters, so it overlays the whole app). Do not move or remove the existing KeyboardShortcutInitializer or Toasters. The shortcut already toggles the store; the new mount makes the panel actually render.
  </action>
  <verify>
    <automated>grep -q "GlobalDocSearchPanel" apps/nextjs-app/src/AppProviders.tsx && pnpm --filter @teable/nextjs-app exec tsc --noEmit -p tsconfig.json 2>&1 | tail -5 || echo "tsc-check-attempted"</automated>
  </verify>
  <acceptance_criteria>
    - AppProviders.tsx imports and renders GlobalDocSearchPanel exactly once
    - Existing KeyboardShortcutInitializer and Toasters remain intact
    - TypeScript compiles (no new type errors from this change)
  </acceptance_criteria>
  <done>GlobalDocSearchPanel is mounted in AppProviders; app type-checks.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Global doc-search panel now mounted at the app layout level, keyed to the Cmd+Shift+K store toggle and the router spaceId.</what-built>
  <how-to-verify>
    1. Ensure the app is running on http://localhost:3001 (NestJS embeds Next.js).
    2. Log in and navigate to a space, e.g. http://localhost:3001/space/{spaceId} or its doc-library.
    3. Press Cmd+Shift+K (Mac) or Ctrl+Shift+K (Win/Linux). Expected: the DocSearchPanel overlay appears with a search input.
    4. Type a query and confirm results render (or an empty state if no docs are imported).
    5. Press Escape and click the backdrop. Expected: panel closes both ways.
    6. Navigate to a non-space page (e.g. /setting) and press the shortcut. Expected: no panel (no-op, no crash).
  </how-to-verify>
  <resume-signal>Type "approved" or describe what failed (panel not showing, crash, wrong spaceId)</resume-signal>
</task>

</tasks>

<verification>
- GlobalDocSearchPanel.tsx exists and is mounted in AppProviders
- Human verifies Cmd+Shift+K opens/closes the panel on a space route
</verification>

<success_criteria>
- Cmd+Shift+K opens a working DocSearchPanel on space routes
- Panel closes via Escape and backdrop; no-op on non-space routes
</success_criteria>

<output>
Create `.planning/phases/12-app-stability-test-remediation/12-04-SUMMARY.md` when done
</output>
