---
phase: 12-app-stability-test-remediation
plan: 05
type: execute
wave: 3
depends_on: [12-01, 12-02, 12-04]
files_modified:
  - .planning/TESTING-PLAN.md
autonomous: false
requirements: [12-UI]
must_haves:
  truths:
    - "TESTING-PLAN.md sections 1-4 (Auth, Space/Base Nav, Grid View, View Switching) each have every row marked [P] or [F]"
    - "Every bug discovered in sections 1-4 is either fixed in source or recorded in the Bug Tracking table"
    - "No row in sections 1-4 remains [ ] after this pass"
  artifacts:
    - path: ".planning/TESTING-PLAN.md"
      provides: "Status column updated for sections 1-4"
      contains: "[P]"
  key_links:
    - from: "browser test pass"
      to: "TESTING-PLAN.md status column"
      via: "manual verification updating [ ] to [P]/[F]"
      pattern: "\\[P\\]|\\[F\\]"
---

<objective>
Progressive UI testing pass 1. Run TESTING-PLAN.md sections 1-4 in the browser, fix every bug found in source, and update each row's status to `[P]` (pass) or `[F]` (fail+fix-applied). This is the first of three progressive passes; later passes cover sections 5-15.

Purpose: Phase 12 goal requires every one of the 90 test cases verified in a real browser. Sections 1-4 cover the foundational flows (auth, navigation, grid, view switching) everything else depends on.
Output: TESTING-PLAN.md sections 1-4 fully marked; any source fixes for discovered bugs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/TESTING-PLAN.md
@.planning/phases/12-app-stability-test-remediation/12-RESEARCH.md
@.planning/codebase/ARCHITECTURE.md

<interfaces>
App runs on http://localhost:3001 (NestJS embeds Next.js; PostgreSQL + Redis required).
E2E auth fixture: apps/nextjs-app/e2e/fixtures/auth.ts (test@e2e.com / 12345678, storageState .auth.json, targets :3001).
Existing E2E specs to reuse where they cover a section: apps/nextjs-app/e2e/full-sweep.spec.ts, e2e/pages/features/grid-view*, view-switching specs.
Status legend (per TESTING-PLAN.md): [ ] untested, [P] pass, [F] fail (fix applied). Record bugs in the "## Bug Tracking" table.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>Pre-flight: the app must be running for browser testing.</what-built>
  <how-to-verify>
    1. Confirm PostgreSQL and Redis are up (project notes: stale PID fix may be needed for PG).
    2. Start the app and confirm it serves on http://localhost:3001.
    3. Confirm you can log in (test@e2e.com / 12345678 or your dev account).
  </how-to-verify>
  <resume-signal>Type "running" once http://localhost:3001 is reachable and login works</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Sections 1-4 of TESTING-PLAN.md, driven in the browser with source fixes applied for any failures.</what-built>
  <how-to-verify>
    Walk through TESTING-PLAN.md sections in order, performing each row's "Test step" and checking the "Expected" result in the browser:
    - Section 1 Authentication (rows 1.1-1.8): login, invalid creds, signup, forgot-password, OAuth redirect, JWT-invalid redirect.
    - Section 2 Space & Base Navigation (2.1-2.8): space list, create/open base, create/rename/delete table, duplicate base, trash+restore.
    - Section 3 Grid View (all rows): cell edit, add/delete field, sort, filter, row add/delete, etc.
    - Section 4 View Type Switching (all rows): switch grid↔form↔gallery↔kanban↔calendar↔gantt and back.
    For each row: if it passes, set status to [P]. If it fails, fix the source bug (frontend or backend), re-run the step, and set status to [F] with a one-line note; add the bug to the "## Bug Tracking" table with the file touched. Where an existing E2E spec already exercises the flow, you may run it (`cd apps/nextjs-app && pnpm playwright test <spec>`) as supporting evidence, but the browser walk is authoritative for [P]/[F].
    Do NOT mark a row [P] without actually exercising it. Do NOT leave any section 1-4 row as [ ].
  </how-to-verify>
  <resume-signal>Type "approved" when sections 1-4 are fully marked, or describe blockers</resume-signal>
</task>

<task type="auto">
  <name>Task: Record pass-1 results in TESTING-PLAN.md</name>
  <files>.planning/TESTING-PLAN.md</files>
  <read_first>
    - .planning/TESTING-PLAN.md
  </read_first>
  <action>
    Persist the outcomes from the human-verify checkpoint: update every status cell in sections 1-4 to [P] or [F], add discovered bugs (with file + fix summary) to the "## Bug Tracking" table, and ensure no [ ] remains in sections 1-4. If source files were changed to fix bugs, list them in this plan's SUMMARY. Scope cap: if a single bug fix touches more than 3 files not already in files_modified, mark it [F] with a note and create a follow-up task rather than expanding this plan's scope.
  </action>
  <verify>
    <automated>awk '/^## 1\./{f=1} /^## 5\./{f=0} f && /\| `\[ \]` \|/{c++} END{exit (c>0)}' .planning/TESTING-PLAN.md && echo "no-untested-in-1-4"</automated>
  </verify>
  <acceptance_criteria>
    - Every row in TESTING-PLAN.md sections 1-4 is [P] or [F] (zero [ ])
    - Bug Tracking table lists each discovered bug with the file touched
    - Any source fixes are listed in 12-05-SUMMARY.md
  </acceptance_criteria>
  <done>Sections 1-4 fully marked; bugs recorded; no untested rows remain in that range.</done>
</task>

</tasks>

<verification>
- No `[ ]` status remains in TESTING-PLAN.md sections 1-4
- Discovered bugs recorded in Bug Tracking; fixes listed in SUMMARY
</verification>

<success_criteria>
- TESTING-PLAN.md sections 1-4 fully verified in browser and marked [P]/[F]
- All section 1-4 bugs fixed in source or tracked
</success_criteria>

<output>
Create `.planning/phases/12-app-stability-test-remediation/12-05-SUMMARY.md` when done
</output>
