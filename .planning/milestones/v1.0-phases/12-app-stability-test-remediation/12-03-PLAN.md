---
phase: 12-app-stability-test-remediation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/nestjs-backend/src/features/agent/agent-execution.service.ts
  - apps/nestjs-backend/src/features/agent/agent.module.ts
autonomous: true
requirements: [11-06]
must_haves:
  truths:
    - "create_record agent tool creates records via RecordOpenApiService, not raw SQL"
    - "RecordOpenApiService is injectable into AgentModule without a circular dependency"
    - "Existing agent execution tests still pass after the swap"
  artifacts:
    - path: "apps/nestjs-backend/src/features/agent/agent-execution.service.ts"
      provides: "create_record tool backed by RecordOpenApiService"
      contains: "RecordOpenApiService"
  key_links:
    - from: "AgentExecutionService.create_record tool"
      to: "RecordOpenApiService.createRecords"
      via: "field-id → value record creation"
      pattern: "recordOpenApiService\\.(createRecords|multipleCreateRecords)"
---

<objective>
Replace the raw `$queryRawUnsafe` implementation of the agent's `create_record` tool with `RecordOpenApiService`, which resolves field IDs to columns via Teable's real record model. STRETCH / OPTIONAL: scope strictly to `create_record`. If `RecordOpenApiService` cannot be injected into `AgentModule` without a circular dependency, STOP and document the blocker — do NOT expand the phase or hand-roll an alternative.

Purpose: Phase 11 AC7. Raw SQL JSON-column access bypasses field typecasting/validation and is fragile. This is the only stretch item; correctness and non-expansion matter more than completeness.
Output: `create_record` backed by `RecordOpenApiService`; `agent.module.ts` imports the record open-api module if needed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-app-stability-test-remediation/12-RESEARCH.md
@.planning/phases/11-super-agent-hardening/11-06-PLAN.md

<interfaces>
RecordOpenApiService: apps/nestjs-backend/src/features/record/open-api/record-open-api.service.ts
- async createRecords(...) and async multipleCreateRecords(tableId, createRecordsRo: ICreateRecordsRo)
- Module: apps/nestjs-backend/src/features/record/open-api/record-open-api.module.ts (RecordOpenApiModule)

Current raw SQL in agent-execution.service.ts at lines ~347, ~374, ~400 (search_records / others) and the create_record tool branch.
AgentModule current imports (agent.module.ts): PrismaModule, AiModule, DocSearchModule, HttpModule, BullModule.
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <decision>Can RecordOpenApiService be injected into AgentModule without a circular dependency?</decision>
  <context>
    11-06 is explicitly optional. The executor must first determine whether RecordOpenApiModule (which exports RecordOpenApiService) can be imported into AgentModule cleanly. Check whether RecordOpenApiModule or its transitive deps import anything from AgentModule (directly or via app.module wiring). Build the backend (`pnpm --filter @teable/nestjs-backend build` or `tsc`) after adding the import to detect circular-dep / Nest DI errors at startup.
  </context>
  <options>
    <option id="proceed">
      <name>No circular dep — proceed with the swap</name>
      <pros>Removes fragile raw SQL; uses the canonical record path</pros>
      <cons>Touches DI wiring</cons>
    </option>
    <option id="abort">
      <name>Circular dep detected — abort and document</name>
      <pros>Keeps the phase from expanding; honors the optional/stretch scope</pros>
      <cons>create_record stays on raw SQL (acceptable — it is stretch)</cons>
    </option>
  </options>
  <resume-signal>Select: proceed or abort</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Swap create_record to RecordOpenApiService (only if "proceed")</name>
  <files>apps/nestjs-backend/src/features/agent/agent-execution.service.ts, apps/nestjs-backend/src/features/agent/agent.module.ts</files>
  <read_first>
    - apps/nestjs-backend/src/features/agent/agent-execution.service.ts
    - apps/nestjs-backend/src/features/agent/agent.module.ts
    - apps/nestjs-backend/src/features/record/open-api/record-open-api.service.ts
    - apps/nestjs-backend/src/features/record/open-api/record-open-api.module.ts
  </read_first>
  <action>
    Only execute if the prior checkpoint resolved to "proceed". Add RecordOpenApiModule to AgentModule imports and inject RecordOpenApiService into AgentExecutionService's constructor. In the `create_record` tool branch (the one currently using $queryRawUnsafe to insert), replace the raw insert with a call to `recordOpenApiService.createRecords(tableId, { records: [{ fields: <fieldId→value map> }], fieldKeyType: FieldKeyType.Id })` (confirm the exact RO shape from ICreateRecordsRo). Map the tool-call arguments (which use field IDs) to the records RO. Do NOT touch search_records or the other raw-SQL branches — they are out of scope for this minimum-viable stretch. Keep the tool's return shape (success/record id) identical to before so the LLM loop is unaffected.
  </action>
  <verify>
    <automated>grep -q "RecordOpenApiService" apps/nestjs-backend/src/features/agent/agent-execution.service.ts && grep -q "RecordOpenApiModule" apps/nestjs-backend/src/features/agent/agent.module.ts && pnpm --filter @teable/nestjs-backend build</automated>
  </verify>
  <acceptance_criteria>
    - agent-execution.service.ts references RecordOpenApiService and the create_record branch no longer uses $queryRawUnsafe
    - agent.module.ts imports RecordOpenApiModule
    - Backend compiles with no circular-dependency/DI error
  </acceptance_criteria>
  <done>create_record creates via RecordOpenApiService; build clean.</done>
</task>

</tasks>

<verification>
- If proceed: grep confirms RecordOpenApiService wired in agent-execution.service.ts AND RecordOpenApiModule wired in agent.module.ts; backend build exits 0
- If abort: 12-03-SUMMARY.md documents the circular-dependency blocker and create_record remains on raw SQL
</verification>

<success_criteria>
- create_record uses RecordOpenApiService OR the stretch is cleanly aborted with a documented DI blocker
- No new circular dependencies introduced
</success_criteria>

<output>
Create `.planning/phases/12-app-stability-test-remediation/12-03-SUMMARY.md` when done
</output>
