---
phase: 12-app-stability-test-remediation
plan: 07
type: execute
wave: 3
depends_on: [12-06]
files_modified:
  - .planning/TESTING-PLAN.md
autonomous: false
requirements: [12-UI]
must_haves:
  truths:
    - "TESTING-PLAN.md sections 9-15 (Unified AI Chat, Integrations, Doc Search, Share/API, Admin, Trash, Database View) each have every row marked [P] or [F]"
    - "Doc Search section (11) is verified now that the global panel is mounted (12-04)"
    - "Every bug discovered in sections 9-15 is fixed in source or recorded in Bug Tracking"
    - "No row in sections 9-15 remains [ ] after this pass"
  artifacts:
    - path: ".planning/TESTING-PLAN.md"
      provides: "Status column updated for sections 9-15"
      contains: "[P]"
  key_links:
    - from: "Doc Search test (section 11)"
      to: "GlobalDocSearchPanel + Cmd+Shift+K"
      via: "browser-verified panel open + search"
      pattern: "\\[P\\]|\\[F\\]"
---

<objective>
Progressive UI testing pass 3. Run TESTING-PLAN.md sections 9-15 (Unified AI Chat, Google & Slack Integrations, Document Import & Vector Search, Share Base & API Access, Admin Panel, Trash & Undo/Redo, Database View) in the browser, fix every bug, and mark each row [P]/[F]. Section 11 (Doc Search) is newly testable after 12-04.

Purpose: Final feature surfaces. After this pass, all 90 cases across all 15 sections are exercised.
Output: TESTING-PLAN.md sections 9-15 fully marked; source fixes for discovered bugs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/TESTING-PLAN.md
@.planning/phases/12-app-stability-test-remediation/12-RESEARCH.md
@.planning/codebase/ARCHITECTURE.md

<interfaces>
App on http://localhost:3001. Login: test@e2e.com / 12345678.
Section 11 Doc Search: global panel now mounted (12-04); Cmd+Shift+K opens DocSearchPanel on space routes; doc-library page at /space/{spaceId}/doc-library.
Integrations may require OAuth credentials (Phase 6) — token exchange is partial; verify the connect-flow redirect and graceful handling rather than full third-party round-trip if creds are absent.
Supporting E2E: e2e/doc-library.spec.ts, e2e/pages/features/integrations*, e2e/full-sweep.spec.ts (trash, admin).
Status legend: [ ] untested, [P] pass, [F] fail (fix applied). Bugs → "## Bug Tracking" table.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>Pre-flight: app running for browser testing.</what-built>
  <how-to-verify>
    1. Confirm http://localhost:3001 is reachable and login works.
    2. Note which integration OAuth credentials are configured (affects how far section 10 can go).
    3. Confirm at least one document can be imported for section 11 (or accept empty-state verification).
  </how-to-verify>
  <resume-signal>Type "running" once the app is reachable and integration/doc prerequisites are known</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Sections 9-15 of TESTING-PLAN.md, browser-driven with source fixes for failures.</what-built>
  <how-to-verify>
    Walk through each row in order:
    - Section 9 Unified AI Workspace Chat (Phase 10): open chat sidebar, send a message, see workspace-aware response, action proposal card, accept proposal, conversation history persists.
    - Section 10 Integrations (Phase 6): open integrations panel, start an OAuth connect flow for a provider, confirm redirect; verify graceful handling if creds absent.
    - Section 11 Doc Search (Phase 7): press Cmd+Shift+K on a space route → panel opens; import a markdown/PDF doc; search (semantic/keyword/hybrid); open a doc in the doc-library.
    - Section 12 Share Base & API Access: create a share link, open it, generate an API token, call an API endpoint.
    - Section 13 Admin Panel: open admin, view users/settings, queues (/admin/queues), performance (/admin/performance).
    - Section 14 Trash & Undo/Redo: delete + restore from trash; undo/redo a record edit.
    - Section 15 Database View: open database view, verify rows render.
    For each row: [P] if it passes; if it fails, fix the source bug, re-run, mark [F] with a note, and add to "## Bug Tracking". Do not mark [P] without exercising the row; do not leave any section 9-15 row as [ ].
  </how-to-verify>
  <resume-signal>Type "approved" when sections 9-15 are fully marked, or describe blockers</resume-signal>
</task>

<task type="auto">
  <name>Task: Record pass-3 results in TESTING-PLAN.md</name>
  <files>.planning/TESTING-PLAN.md</files>
  <read_first>
    - .planning/TESTING-PLAN.md
  </read_first>
  <action>
    Persist the pass-3 outcomes: mark every status cell in sections 9-15 [P]/[F], add discovered bugs (file + fix summary) to "## Bug Tracking", and ensure no [ ] remains anywhere in sections 9-15. Update section 11's status now that the global doc-search panel is mounted. List source fixes in this plan's SUMMARY. Scope cap: if a single bug fix touches more than 3 files not already in files_modified, mark it [F] with a note and create a follow-up task rather than expanding this plan's scope.
  </action>
  <verify>
    <automated>awk '/^## 9\./{f=1} /^## Bug Tracking/{f=0} f && /\| `\[ \]` \|/{c++} END{exit (c>0)}' .planning/TESTING-PLAN.md && echo "no-untested-in-9-15"</automated>
  </verify>
  <acceptance_criteria>
    - Every row in TESTING-PLAN.md sections 9-15 is [P] or [F] (zero [ ])
    - Section 11 (Doc Search) marked based on a browser test of the global panel
    - Bug Tracking table updated; fixes listed in 12-07-SUMMARY.md
  </acceptance_criteria>
  <done>Sections 9-15 fully marked; doc-search verified; no untested rows remain in that range.</done>
</task>

</tasks>

<verification>
- No `[ ]` status remains in TESTING-PLAN.md sections 9-15
- Doc Search (section 11) verified in browser via the global panel
</verification>

<success_criteria>
- TESTING-PLAN.md sections 9-15 fully verified and marked [P]/[F]
- All section 9-15 bugs fixed or tracked
</success_criteria>

<output>
Create `.planning/phases/12-app-stability-test-remediation/12-07-SUMMARY.md` when done
</output>
