---
phase: 12-app-stability-test-remediation
plan: 08
type: execute
wave: 4
depends_on: [12-02, 12-03, 12-05, 12-06, 12-07]
files_modified:
  - .planning/phases/12-app-stability-test-remediation/12-SUMMARY.md
autonomous: true
requirements: [12-UI, 11-05]
must_haves:
  truths:
    - "All backend unit tests pass via pnpm --filter @teable/nestjs-backend test-unit"
    - "The Playwright smoke suite passes (or every failure is documented as a known, tracked issue)"
    - "TESTING-PLAN.md has zero untested [ ] entries across all 15 sections"
    - "12-SUMMARY.md exists summarizing the phase outcome"
  artifacts:
    - path: ".planning/phases/12-app-stability-test-remediation/12-SUMMARY.md"
      provides: "Phase 12 completion summary"
      min_lines: 25
  key_links:
    - from: "final smoke pass"
      to: "TESTING-PLAN.md fully green"
      via: "grep confirms no [ ] remain"
      pattern: "| `\[ \]` |"
---

<objective>
Final smoke pass and phase wrap-up. Run the full backend unit suite, run the Playwright smoke suite, confirm TESTING-PLAN.md has no untested entries anywhere, and write the phase summary.

Purpose: Gate the phase as complete. Aggregates the evidence from the three progressive passes and the regression specs into a single verifiable end state.
Output: `12-SUMMARY.md`; confirmed green test state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/TESTING-PLAN.md
@.planning/phases/12-app-stability-test-remediation/12-RESEARCH.md
@.planning/ROADMAP.md

<interfaces>
Backend unit run: pnpm --filter @teable/nestjs-backend test-unit
Playwright smoke: cd apps/nextjs-app && pnpm playwright test --grep @smoke  (app must be running on :3001; full-sweep.spec.ts is the smoke entry)
Note: full-sweep.spec.ts may not be tagged @smoke — if --grep @smoke matches nothing, run `pnpm playwright test full-sweep.spec.ts` instead.
TESTING-PLAN.md status legend: [ ] untested, [P] pass, [F] fail (fix applied).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full backend unit suite</name>
  <files>(none — verification only)</files>
  <read_first>
    - apps/nestjs-backend/package.json
  </read_first>
  <action>
    Run `pnpm --filter @teable/nestjs-backend test-unit` and confirm it exits 0, including the four agent specs added in 12-02. If any test fails, fix the failing spec or the source it covers (within Phase 12 scope) and re-run until green. Capture the pass/fail counts for the summary.
  </action>
  <verify>
    <automated>pnpm --filter @teable/nestjs-backend test-unit</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @teable/nestjs-backend test-unit` exits 0
    - The four 12-02 agent specs are included in the run and pass
  </acceptance_criteria>
  <done>Full backend unit suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Run Playwright smoke suite</name>
  <files>(none — verification only)</files>
  <read_first>
    - apps/nextjs-app/e2e/full-sweep.spec.ts
    - apps/nextjs-app/playwright.config.ts
  </read_first>
  <action>
    Ensure the app is running on http://localhost:3001 (set PLAYWRIGHT_PORT=3001 if config defaults to 3000, per research pitfall 3). Run the smoke suite: `cd apps/nextjs-app && PLAYWRIGHT_PORT=3001 pnpm playwright test --grep @smoke`. If --grep @smoke matches no tests, run `pnpm playwright test full-sweep.spec.ts` instead. Record results. If a smoke test fails due to a real regression, fix it or document it as a tracked known issue in the summary — do not silently pass over a failure.
  </action>
  <verify>
    <automated>cd apps/nextjs-app && PLAYWRIGHT_PORT=3001 pnpm playwright test --grep @smoke --reporter=line 2>&1 | tail -15 || (echo "smoke-grep-empty-trying-full-sweep" && PLAYWRIGHT_PORT=3001 pnpm playwright test full-sweep.spec.ts --reporter=line 2>&1 | tail -15)</automated>
  </verify>
  <acceptance_criteria>
    - Playwright smoke (or full-sweep) suite runs against :3001
    - Result recorded; any failure either fixed or documented as a tracked known issue
  </acceptance_criteria>
  <done>Smoke suite executed; outcome captured.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm TESTING-PLAN green and write 12-SUMMARY.md</name>
  <files>.planning/phases/12-app-stability-test-remediation/12-SUMMARY.md</files>
  <read_first>
    - .planning/TESTING-PLAN.md
    - @$HOME/.claude/get-shit-done/templates/summary.md
  </read_first>
  <action>
    Confirm TESTING-PLAN.md has zero `[ ]` status cells across all 15 sections (every row is [P] or [F]). If any [ ] remain, STOP and report which section — the phase is not complete. Then write 12-SUMMARY.md following the template: summarize the four work streams (11-04 backfill, regression specs, optional record-tools, doc-search panel fix), the three progressive testing passes, the final smoke results (unit pass/fail counts + Playwright outcome), the list of bugs found and fixed (from the Bug Tracking table), and any remaining tracked known issues.
  </action>
  <verify>
    <automated>grep -c '| `\[ \]` |' .planning/TESTING-PLAN.md | grep -qx 0 && test -f .planning/phases/12-app-stability-test-remediation/12-SUMMARY.md && echo COMPLETE</automated>
  </verify>
  <acceptance_criteria>
    - TESTING-PLAN.md contains zero `| `[ ]` |` status cells
    - 12-SUMMARY.md exists and summarizes all four work streams + test results + bugs fixed
  </acceptance_criteria>
  <done>TESTING-PLAN.md fully green; 12-SUMMARY.md written.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @teable/nestjs-backend test-unit` exits 0
- `grep -c '| `[ ]` |' .planning/TESTING-PLAN.md` equals 0
- 12-SUMMARY.md exists
</verification>

<success_criteria>
- Backend unit suite green; Playwright smoke executed with outcome documented
- TESTING-PLAN.md has no untested entries
- Phase 12 summary written
</success_criteria>

<output>
Create `.planning/phases/12-app-stability-test-remediation/12-SUMMARY.md` (this plan's deliverable; no separate plan summary needed)
</output>
