---
phase: 09-ui-feature-testing
plan: 10
type: execute
wave: 2
depends_on:
  - 09-00
files_modified:
  - apps/nextjs-app/e2e/pages/features/prompt-system.spec.ts
autonomous: true
requirements:
  - PHASE9-PROMPT
must_haves:
  truths:
    - /admin/prompts loads with 5 default prompt entries
    - prompt override edit/delete cycle completes without error
---

# Plan 09-10: Prompt System Admin UI Tests (Phase 2)

## Objective
Test the Prompt System admin panel (Phase 2). Navigate to /admin/prompts, verify 5 default entries are listed, test editing and deleting a prompt override.

## Tasks

<task id="09-10-01" type="execute">
  <title>Create prompt-system Playwright spec</title>
  <read_first>
    - apps/nextjs-app/e2e/fixtures/auth.ts
    - apps/nextjs-app/playwright.config.ts
    - apps/nextjs-app/src/features/app/blocks/admin (scan for prompt panel component)
  </read_first>
  <action>
    Create apps/nextjs-app/e2e/pages/features/prompt-system.spec.ts:
    1. Use storageState from auth fixture (admin user)
    2. Navigate to /admin or find Admin Panel link in UI
    3. Find "Prompt" or "IA" section in admin navigation
    4. Assert: list of ≥5 prompt entries is visible (table or card list)
    5. Click edit on one prompt → fill in override text → save → assert success toast or 200 response
    6. Click delete/reset on the same prompt → assert it reverts to default label
  </action>
  <acceptance_criteria>
    - prompt-system.spec.ts exists
    - Spec tests: page load, ≥5 entries visible, edit saves, delete reverts
    - `grep "admin\|prompt\|Prompt" apps/nextjs-app/e2e/pages/features/prompt-system.spec.ts` exits 0
  </acceptance_criteria>
</task>

<task id="09-10-02" type="execute">
  <title>Run spec and fix prompt system bugs</title>
  <read_first>
    - apps/nextjs-app/e2e/pages/features/prompt-system.spec.ts
    - apps/nestjs-backend/src/features/ai/prompt.service.ts
  </read_first>
  <action>
    Run: `cd apps/nextjs-app && pnpm exec playwright test e2e/pages/features/prompt-system.spec.ts --reporter=line`
    If tests fail:
    - Admin page not found → check Next.js admin route in apps/nextjs-app/src/pages/admin/
    - PUT/DELETE 500 → check prompt.service.ts for the override CRUD methods
    Fix source, re-run until green
  </action>
  <acceptance_criteria>
    - `cd apps/nextjs-app && pnpm exec playwright test e2e/pages/features/prompt-system.spec.ts` exits 0
  </acceptance_criteria>
</task>

## Verification
Prompt system admin panel loads. 5 prompt entries visible. Edit and delete cycle completes without server errors.
