---
phase: 09-ui-feature-testing
plan: 11
type: execute
wave: 3
depends_on:
  - 09-00
files_modified:
  - apps/nextjs-app/e2e/pages/features/integrations.spec.ts
autonomous: true
requirements:
  - PHASE9-INTEGRATIONS
must_haves:
  truths:
    - integrations panel loads with 6 provider cards
    - OAuth connect flow initiates without 500 error
---

# Plan 09-11: Integrations Panel UI Tests (Phase 6)

## Objective
Test the Integrations Panel (Phase 6). Verify 6 OAuth provider cards are shown, connect flow initiates without error.

## Tasks

<task id="09-11-01" type="execute">
  <title>Create integrations Playwright spec</title>
  <read_first>
    - apps/nextjs-app/e2e/fixtures/auth.ts
    - apps/nextjs-app/playwright.config.ts
    - apps/nestjs-backend/src/features/setting/open-api/domain-verification.controller.ts
  </read_first>
  <action>
    Create apps/nextjs-app/e2e/pages/features/integrations.spec.ts:
    1. Use storageState from auth fixture
    2. Navigate to admin panel or space settings → find "Integrations" link
    3. Assert: 6 provider cards visible with names: Gmail, Google Calendar, Google Drive, Google Chat, Google Meet, Slack
    4. Click "Connecter" or "Connect" on Gmail card
    5. Assert: either OAuth redirect initiated (URL changes to accounts.google.com or a popup opens) OR a modal appears — in both cases assert no 500 error from the server
    6. Close/cancel the OAuth flow and return to integrations page
  </action>
  <acceptance_criteria>
    - integrations.spec.ts exists
    - Tests: page load with 6 providers, connect flow initiates, no 500 errors
    - `grep -c "Gmail\|Slack\|provider" apps/nextjs-app/e2e/pages/features/integrations.spec.ts` outputs ≥3
  </acceptance_criteria>
</task>

<task id="09-11-02" type="execute">
  <title>Run spec and fix integrations bugs</title>
  <read_first>
    - apps/nextjs-app/e2e/pages/features/integrations.spec.ts
    - apps/nestjs-backend/src/features (grep for IntegrationController)
  </read_first>
  <action>
    Run: `cd apps/nextjs-app && pnpm exec playwright test e2e/pages/features/integrations.spec.ts --reporter=line`
    If tests fail:
    - Page not found → check admin routes or space settings navigation
    - Provider cards not rendering → check IntegrationsPanel component
    - 500 on connect → check IntegrationController OAuth endpoint
    Fix source, re-run until green
  </action>
  <acceptance_criteria>
    - `cd apps/nextjs-app && pnpm exec playwright test e2e/pages/features/integrations.spec.ts` exits 0
  </acceptance_criteria>
</task>

## Verification
Integrations panel loads. 6 provider cards visible. Connect flow initiates without server error.
