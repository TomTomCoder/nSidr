---
phase: 09-ui-feature-testing
plan: 08
type: execute
wave: 4
depends_on:
  - 09-05
  - 09-06
  - 09-07
files_modified:
  - apps/nextjs-app/e2e/full-sweep.spec.ts
autonomous: true
requirements:
  - PHASE9-FULL-SWEEP
must_haves:
  truths:
    - space can be created, renamed, and deleted without errors
    - base can be created in Hoostn space, a table added, and the base deleted
    - a deleted record appears in Corbeille and can be restored
    - /admin route loads without 500 and admin panels render
    - Authority Matrix table renders with role columns in space settings
  artifacts:
    - path: apps/nextjs-app/e2e/full-sweep.spec.ts
      provides: Playwright spec covering 5 management sub-flows
      contains: "full-sweep"
  key_links:
    - from: apps/nextjs-app/e2e/full-sweep.spec.ts
      to: apps/nestjs-backend/src/features/
      via: space/base CRUD + trash + admin + authority matrix API endpoints
      pattern: "space|base|trash|admin|authority"
---

<objective>
Full sweep test covering 5 management sub-flows: space CRUD, base CRUD, trash/restore, admin panel, and authority matrix. Each sub-flow is a separate Playwright test within the same spec file.

Purpose: Provide broad regression coverage of core management features before Phase 9 is marked complete.

Output: apps/nextjs-app/e2e/full-sweep.spec.ts — five passing Playwright tests.
</objective>

<execution_context>
@/Users/tommylambert/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tommylambert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-ui-feature-testing/09-00-PLAN.md
@apps/nextjs-app/e2e/fixtures/auth.ts
</context>

<tasks>

<task id="09-08-01" type="auto">
  <name>Task 1: Write full-sweep spec — space management and base management</name>
  <files>apps/nextjs-app/e2e/full-sweep.spec.ts</files>
  <action>
    Create apps/nextjs-app/e2e/full-sweep.spec.ts. Add `test.use({ storageState: authFile })` at the top (import authFile from './fixtures/auth').

    Set up a shared API error collector in a beforeEach:
      const apiErrors: { url: string; status: number }[] = [];
      page.on('response', r => { if (r.status() >= 500) apiErrors.push({ url: r.url(), status: r.status() }); });

    Write the following two tests inside describe("Full Sweep"):

    ——— Test 1: "Space management — create, rename, delete" ———
    1. Navigate to http://localhost:3001.
    2. Create a new space: look for "New space" button or a + icon near the spaces list (getByRole('button', { name: /new space|nouvel espace/i })). Click it. Type "E2E-Sweep-Space" as the name. Confirm.
    3. Wait for the space to appear in the sidebar: expect(page.getByText('E2E-Sweep-Space')).toBeVisible({ timeout: 8000 }).
    4. Rename it: right-click (or click the kebab menu) on "E2E-Sweep-Space" > choose Rename. Type "E2E-Sweep-Space-Renamed". Confirm. Assert the renamed label is visible.
    5. Delete it: right-click (or kebab) > Delete. Confirm in the dialog. Assert "E2E-Sweep-Space-Renamed" is no longer in the sidebar.
    6. Assert: expect(apiErrors.filter(e => e.url.includes('space'))).toHaveLength(0).

    ——— Test 2: "Base management — create base, add table, delete base" ———
    1. Navigate to http://localhost:3001.
    2. Click the "Hoostn" space in the sidebar. If not found, use the first available space.
    3. Create a new base: look for "New base" or "Add base" button. Click it. Name it "E2E-Sweep-Base". Confirm.
    4. Wait for "E2E-Sweep-Base" to appear: expect(page.getByText('E2E-Sweep-Base')).toBeVisible({ timeout: 8000 }).
    5. Click "E2E-Sweep-Base" to open it.
    6. Add a table: look for "+ Add table" or "New table" button. Click it. Accept the default name or type "SweepTable". Confirm.
    7. Assert the table tab is visible: expect(page.getByRole('tab', { name: /SweepTable|Table/i }).first()).toBeVisible({ timeout: 5000 }).
    8. Delete the base: navigate back to the space. Right-click "E2E-Sweep-Base" > Delete. Confirm. Assert it is gone.
    9. Assert: expect(apiErrors.filter(e => e.url.includes('base'))).toHaveLength(0).
  </action>
  <verify>
    <automated>test -f /Users/tommylambert/Documents/Claude_Folder/teable/apps/nextjs-app/e2e/full-sweep.spec.ts && echo "spec exists"</automated>
  </verify>
  <done>full-sweep.spec.ts exists with tests 1 and 2 (space management and base management)</done>
</task>

<task id="09-08-02" type="auto">
  <name>Task 2: Add trash, admin panel, and authority matrix tests to full-sweep spec</name>
  <files>apps/nextjs-app/e2e/full-sweep.spec.ts</files>
  <action>
    Append three additional tests inside the same describe("Full Sweep") block in full-sweep.spec.ts (open the file first to confirm the closing `})`  location, then insert before it).

    ——— Test 3: "Trash — delete record, verify in Corbeille, restore" ———
    1. Navigate to http://localhost:3001.
    2. Open the "nXtFlow" base > Contacts table. If not found, use nXtFlow > first available table.
    3. Right-click the first data row or click the row actions menu. Choose "Delete" or "Supprimer".
    4. Navigate to Corbeille: look for a "Corbeille" or "Trash" link in the left sidebar or base navigation. Click it.
    5. Wait for the trash list to appear: expect(page.getByRole('table, list').first()).toBeVisible({ timeout: 8000 }). Alternatively look for any deleted-record entry.
    6. Click "Restore" (or "Restaurer") on the first item in trash. Confirm.
    7. Assert the trash list no longer shows that item (or is empty).
    8. Assert: expect(apiErrors.filter(e => e.url.includes('trash') || e.url.includes('corbeille'))).toHaveLength(0).

    ——— Test 4: "Admin panel — /admin loads without 500" ———
    1. Navigate to http://localhost:3001/admin.
    2. Wait for the page to load (waitForLoadState('networkidle')).
    3. Assert the page does not show a "500" error message: expect(page.getByText('500')).not.toBeVisible().
    4. Assert at least one admin panel section header is visible. Try: page.getByRole('heading').first() is visible, or page.locator('main, [data-testid="admin"]').isVisible().
    5. Assert: expect(apiErrors.filter(e => e.url.includes('admin'))).toHaveLength(0).

    ——— Test 5: "Authority Matrix — space settings table renders with role columns" ———
    1. Navigate to http://localhost:3001.
    2. Open space settings for the first space: look for a settings/gear icon near the space name in the sidebar. Click it.
    3. In the settings panel, navigate to "Matrice d'autorité" or "Authority Matrix" tab/link.
    4. Wait for the matrix table to appear: timeout 8 000 ms.
    5. Assert that at least two column headers matching role names are visible: look for getByRole('columnheader') and assert count >= 2.
    6. Assert: expect(apiErrors.filter(e => e.url.includes('authority') || e.url.includes('permission'))).toHaveLength(0).

    After appending, run a quick syntax check:
      npx tsc --noEmit --project apps/nextjs-app/tsconfig.json apps/nextjs-app/e2e/full-sweep.spec.ts 2>&1 | tail -5
    Fix any TypeScript errors before finishing.
  </action>
  <verify>
    <automated>grep -c "Authority Matrix\|Corbeille\|admin" /Users/tommylambert/Documents/Claude_Folder/teable/apps/nextjs-app/e2e/full-sweep.spec.ts</automated>
  </verify>
  <done>full-sweep.spec.ts contains all 5 tests — space, base, trash, admin, authority matrix — and passes tsc --noEmit</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test browser → NestJS management APIs | REST CRUD; localhost only |
| /admin route | Admin-only; storageState carries admin session |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-08-01 | Elevation of Privilege | /admin access in test | accept | Test uses authenticated admin session from storageState; local dev only |
| T-09-08-02 | Information Disclosure | admin panel content in test logs | accept | Local dev only; no secrets in admin panel content |
| T-09-08-03 | Tampering | space/base deletion in test | accept | Test creates its own ephemeral space/base and deletes only those; risk of polluting shared dev data is low |
| T-09-08-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
Run after both tasks (requires dev server at :3001):

```bash
cd /Users/tommylambert/Documents/Claude_Folder/teable/apps/nextjs-app
E2E_WEBSERVER_MODE=DEV pnpm exec playwright test e2e/full-sweep.spec.ts --project="Desktop Chrome" --reporter=list
```

Run all Phase 9 tests together:
```bash
E2E_WEBSERVER_MODE=DEV pnpm exec playwright test e2e/ --project="Desktop Chrome" --reporter=list
```
</verification>

<success_criteria>
- full-sweep.spec.ts exists and passes tsc --noEmit
- Test 1: space created, renamed, and deleted without console errors or 500s
- Test 2: base created in Hoostn space, table added, base deleted without errors
- Test 3: record deleted, found in Corbeille, restored without 500s
- Test 4: /admin loads, no 500 error text visible, at least one heading renders
- Test 5: Authority Matrix table renders with >= 2 role column headers
- Zero 500-status API responses across all 5 flows
</success_criteria>

<output>
Create `.planning/phases/09-ui-feature-testing/09-08-SUMMARY.md` when done
</output>
