---
phase: 09-ui-feature-testing
plan: 04
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - apps/nextjs-app/e2e/pages/features/gantt-view.spec.ts
  - apps/nestjs-backend/src/features/view/constant.ts
autonomous: true
requirements:
  - PHASE9-GANTT
must_haves:
  truths:
    - gantt test creates a table with startDate and endDate fields
    - gantt view is created via the Add View UI
    - timeline bars render for records with valid date ranges
    - milestone markers are visible for the milestone threshold record
    - GanttOptionsPanel opens and all 9 options fields are present
    - any source bugs in view/constant.ts or Gantt components are fixed
  artifacts:
    - path: apps/nextjs-app/e2e/pages/features/gantt-view.spec.ts
      provides: Gantt view E2E test suite (Phase 5 feature)
  key_links:
    - from: apps/nextjs-app/e2e/pages/features/gantt-view.spec.ts
      to: apps/nextjs-app/e2e/fixtures/auth.ts
      via: storageState import
      pattern: "authFile"
    - from: apps/nextjs-app/e2e/pages/features/gantt-view.spec.ts
      to: apps/nestjs-backend/src/features/view/constant.ts
      via: ViewType.Gantt registration check
      pattern: "Gantt"
---

<objective>
Write and pass a Playwright E2E test for the Phase 5 Gantt view: create a dedicated table with start/end date fields, create the Gantt view, verify timeline bars and milestone markers render, and test the GanttOptionsPanel.

Purpose: Gantt is the most complex custom view type (8 gap-closure plans built it). The refactor/architecture-deep-fix branch modified view/constant.ts and the query-builder files — all of which affect Gantt rendering. This plan validates the full Gantt stack end-to-end and fixes any regressions.

Output: gantt-view.spec.ts with passing assertions, plus any source fixes needed in view/constant.ts or Gantt frontend components.
</objective>

<execution_context>
@/Users/tommylambert/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tommylambert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-ui-feature-testing/09-RESEARCH.md
@apps/nextjs-app/e2e/fixtures/auth.ts
@apps/nextjs-app/e2e/fixtures/testBase.ts

Risk files to read before writing assertions (modified on refactor branch):
- apps/nestjs-backend/src/features/view/constant.ts
- apps/nestjs-backend/src/features/record/query-builder/field-select-visitor.ts
- apps/nestjs-backend/src/features/record/query-builder/field-cte-visitor.ts
</context>

<tasks>

<task id="09-04-01" type="auto">
  <name>Task 1: Write gantt-view.spec.ts with timeline and options tests</name>
  <files>apps/nextjs-app/e2e/pages/features/gantt-view.spec.ts</files>
  <action>
    Create apps/nextjs-app/e2e/pages/features/gantt-view.spec.ts.

    Use `test.use({ storageState: authFile })`.

    test.beforeAll — Gantt requires a dedicated table (the research doc notes: "Pitfall 3: Gantt requires pre-configured date fields"):
    - Navigate to E2E-Test-Base
    - Create a new table named "GanttTest-Table" (if it does not already exist)
    - Add a date field named "StartDate" (field type: Date)
    - Add a date field named "EndDate" (field type: Date)
    - Create 3 records with valid date ranges:
      - Record 1: "Task Alpha" — StartDate: today, EndDate: today + 7 days
      - Record 2: "Task Beta" — StartDate: today + 3 days, EndDate: today + 10 days
      - Record 3: "Milestone Task" — StartDate: today + 5 days, EndDate: today + 5 days (single-day = milestone)
    - Use JavaScript Date arithmetic to compute the actual ISO date strings, not hardcoded values

    Test "create gantt view":
    - Navigate to GanttTest-Table
    - Click "Add view" → select "Gantt" (assert it appears in the list — if absent, the ViewType.Gantt registration is broken)
    - When prompted to select start/end fields: select "StartDate" and "EndDate"
    - Name the view "Test Gantt View" and confirm
    - Assert the Gantt timeline canvas/container is visible (look for the gantt chart container — check the Gantt component source for data-testid or a distinctive class)

    Test "timeline bars render":
    - Assert at least 2 timeline bars are visible in the chart area (CSS or SVG elements representing date ranges)
    - Assert "Task Alpha" label is visible in the row sidebar

    Test "milestone marker renders":
    - Assert a milestone marker (diamond shape, distinct CSS class, or different bar style) is visible for "Milestone Task"
    - The milestone threshold behavior is: when EndDate equals StartDate (or within the milestoneThreshold days), the record renders as a milestone instead of a bar

    Test "GanttOptionsPanel opens":
    - Click the settings/options icon in the Gantt toolbar
    - Assert the GanttOptionsPanel is visible
    - Assert all of these 9 option fields are present (use getByLabel or getByText):
      startField, endField, milestoneThreshold, dependencyField, criticalPathEnabled,
      colorField, rowHeight, showWeekends, zoomLevel
    - Close the panel

    Test "error state when no fields set":
    - Create a second Gantt view on "GanttTest-Table" WITHOUT configuring start/end fields (dismiss the field selector without choosing)
    - Assert an error/empty state message is visible ("Please configure start and end date fields" or similar)

    Do NOT test drag-to-reschedule in this plan — that is a complex interaction that can be added as a follow-up if the basic rendering is confirmed working.
  </action>
  <verify>
    <automated>test -f /Users/tommylambert/Documents/Claude_Folder/teable/apps/nextjs-app/e2e/pages/features/gantt-view.spec.ts && echo "gantt spec exists"</automated>
  </verify>
  <done>gantt-view.spec.ts exists with 5 test cases covering view creation, bars, milestone, options panel, and error state</done>
</task>

<task id="09-04-02" type="auto">
  <name>Task 2: Run spec and fix Gantt source bugs until green</name>
  <files>
    apps/nextjs-app/e2e/pages/features/gantt-view.spec.ts
    apps/nestjs-backend/src/features/view/constant.ts
    apps/nestjs-backend/src/features/record/query-builder/field-select-visitor.ts
    apps/nestjs-backend/src/features/record/query-builder/field-cte-visitor.ts
    apps/nestjs-backend/src/features/record/query-builder/field-formatting-visitor.ts
  </files>
  <action>
    Run the spec:
    ```
    E2E_WEBSERVER_MODE=DEV pnpm --filter @teable/nextjs-app exec playwright test e2e/pages/features/gantt-view.spec.ts --project="Desktop Chrome"
    ```

    Most likely failure modes for Gantt (from research known issues):

    1. "Gantt" option missing from Add View dropdown:
       - Read apps/nestjs-backend/src/features/view/constant.ts
       - Check that ViewType.Gantt is in the allowed view types array
       - If missing: add it (the enum value was defined in Phase 5, plan 05-01)
       - Also check apps/nextjs-app/src/features/app/blocks/view/ for the frontend view registration (the constant.ts in that directory was modified)

    2. Gantt timeline container not found:
       - Read the Gantt component file (apps/nextjs-app/src/features/app/blocks/view/gantt/ — check this path from ROADMAP plan 05-03)
       - Find the root container's data-testid or class
       - Update the spec selector

    3. Query builder returns wrong field data:
       - Read field-select-visitor.ts and field-cte-visitor.ts
       - Check that Date field type is still handled correctly (not accidentally filtered out by the refactor)
       - Fix the type guard or field-type condition if it was broken

    4. GanttOptionsPanel missing options:
       - If fewer than 9 options appear, read the GanttOptionsPanel component source
       - Add any missing option fields that were accidentally removed

    Re-run after each fix until exit 0. Use test.skip() with a comment for any blocker that would require a DB migration or a separate plan.

    After fixing: verify the broader view infrastructure is not broken by running:
    ```
    E2E_WEBSERVER_MODE=DEV pnpm --filter @teable/nextjs-app exec playwright test e2e/pages/features/ --project="Desktop Chrome"
    ```
    (This runs all feature specs. If newly introduced fixes break earlier specs, fix the regression.)
  </action>
  <verify>
    <automated>E2E_WEBSERVER_MODE=DEV pnpm --filter @teable/nextjs-app exec playwright test e2e/pages/features/gantt-view.spec.ts --project="Desktop Chrome" --reporter=list</automated>
  </verify>
  <done>All gantt-view.spec.ts tests pass (or skipped with justification). Source bugs in view/constant.ts and query-builder files are resolved. No regressions in earlier spec files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → view creation API | POST /view requires admin auth; Gantt view options stored as JSON in DB |
| test → record update API | Date field updates go through NestJS record service |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04-01 | Tampering | Gantt view options JSON | accept | Admin-only endpoint; test environment |
| T-09-04-02 | Information Disclosure | date field values in timeline | accept | Test data only; no PII |
| T-09-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
```bash
E2E_WEBSERVER_MODE=DEV pnpm --filter @teable/nextjs-app exec playwright test e2e/pages/features/gantt-view.spec.ts --project="Desktop Chrome"
```
Expected: all tests pass, exit code 0.

Regression check:
```bash
E2E_WEBSERVER_MODE=DEV pnpm --filter @teable/nextjs-app exec playwright test e2e/pages/features/ --project="Desktop Chrome"
```
Expected: no regressions in plans 09-01, 09-02, 09-03 test files.
</verification>

<success_criteria>
- gantt-view.spec.ts: Gantt view created, timeline bars visible, milestone marker visible, GanttOptionsPanel has 9 options, error state shown when fields not configured
- view/constant.ts has ViewType.Gantt registered (if it was missing)
- query-builder files handle Date field type correctly
- All previous feature spec files still pass
- gantt-view.spec.ts exits 0 against live dev server
</success_criteria>

<output>
Create `.planning/phases/09-ui-feature-testing/09-04-SUMMARY.md` when done
</output>
