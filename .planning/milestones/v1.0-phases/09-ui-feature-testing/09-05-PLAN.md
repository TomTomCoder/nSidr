---
phase: 09-ui-feature-testing
plan: 05
type: execute
wave: 3
depends_on:
  - 09-00
files_modified:
  - apps/nextjs-app/e2e/agent-chat.spec.ts
autonomous: true
requirements:
  - PHASE9-AGENT-CHAT
must_haves:
  truths:
    - agent chat panel sends message and receives response
    - streaming response text appears in the panel within 15 seconds
    - suggestion chips render and clicking one does not produce a JS error
  artifacts:
    - path: apps/nextjs-app/e2e/agent-chat.spec.ts
      provides: Playwright spec for Agent Chat panel end-to-end flow
      contains: "Nouveau Chat"
  key_links:
    - from: apps/nextjs-app/e2e/agent-chat.spec.ts
      to: apps/nestjs-backend/src/features/agent/agent-execution.service.ts
      via: POST /api/agent/chat streaming SSE endpoint
      pattern: "agent.*chat|Nouveau Chat"
---

<objective>
Test the Agent Chat panel introduced in Phase 4. Opens the nXtFlow base, navigates to the Contacts table, sends a chat message, verifies a streaming response appears, and clicks a suggestion chip.

Purpose: Confirm the Phase 4 agent chat feature is wired end-to-end from the UI through the NestJS agent execution service without JS errors or silent failures.

Output: apps/nextjs-app/e2e/agent-chat.spec.ts — a passing Playwright spec.
</objective>

<execution_context>
@/Users/tommylambert/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tommylambert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-ui-feature-testing/09-00-PLAN.md
@apps/nextjs-app/e2e/fixtures/auth.ts
@apps/nestjs-backend/src/features/agent/agent-execution.service.ts
@apps/nestjs-backend/src/features/agent/agent.module.ts
</context>

<tasks>

<task id="09-05-01" type="auto">
  <name>Task 1: Investigate Agent Chat source and fix blocking bugs</name>
  <files>apps/nestjs-backend/src/features/agent/agent-execution.service.ts, apps/nestjs-backend/src/features/agent/agent.module.ts</files>
  <action>
    Read agent-execution.service.ts and agent.module.ts to identify any startup errors or missing provider registrations that would prevent the agent chat API from responding.

    Common issues to look for and fix:
    - Missing `AgentModule` import in a parent module (AppModule or AiModule) — if absent, add it.
    - Unresolved injection tokens (e.g., missing @InjectRepository or missing provider in the module providers array).
    - Any TODO/FIXME comments left in streaming logic that would cause the SSE stream to close immediately without sending data.
    - If the agent endpoint is behind a feature flag env var (e.g., ENABLE_AGENT), confirm it is set to true in apps/nestjs-backend/.env or add a fallback default of true.

    Also read apps/nextjs-app/src/features/app/blocks/ to locate the AgentChat or ChatPanel component path. Note the exact button label used to open the chat panel (may be "Nouveau Chat", "New Chat", or an icon button with aria-label).

    Apply minimal fixes only — do not refactor. If a fix would require more than 10 lines of change, note it in a comment and skip it; the test will assert the known failure instead.
  </action>
  <verify>
    <automated>grep -r "AgentModule\|AgentExecutionService" /Users/tommylambert/Documents/Claude_Folder/teable/apps/nestjs-backend/src --include="*.ts" -l | head -10</automated>
  </verify>
  <done>AgentModule is registered in its parent module, no unresolved injection tokens visible, streaming handler exists in agent-execution.service.ts</done>
</task>

<task id="09-05-02" type="auto">
  <name>Task 2: Write agent chat Playwright spec</name>
  <files>apps/nextjs-app/e2e/agent-chat.spec.ts</files>
  <action>
    Create apps/nextjs-app/e2e/agent-chat.spec.ts using storageState from e2e/fixtures/auth.ts (import authFile from './fixtures/auth').

    The spec must contain one top-level describe block "Agent Chat panel" with the following test "sends a message and receives a streaming response":

    1. Navigate to http://localhost:3001 — the app should land on the spaces/home page.
    2. Click the "nXtFlow" base in the left sidebar. If not found, skip with test.skip('nXtFlow base not found') rather than failing hard.
    3. Click the "Contacts" table tab or entry in the base sidebar.
    4. Open the agent chat panel: look for a button with text "Nouveau Chat" or aria-label matching /chat|agent/i in the right panel area. Click it.
    5. Wait for the chat input to appear (role="textbox" or textarea within the chat panel).
    6. Type the message "Liste tous les contacts" into the chat input.
    7. Submit: press Enter or click the Send button (role="button" name=/send|envoyer/i).
    8. Wait up to 15 000 ms for a non-empty response element to appear. Use:
       `await expect(page.locator('[data-testid="agent-response"], .agent-response, .chat-message').last()).not.toBeEmpty({ timeout: 15000 })`.
       If no data-testid is found, fall back to any element that appears after the user message bubble.
    9. Capture any suggestion chips (elements with role="button" inside the response area). If at least one chip exists, click the first one and wait 3 000 ms for a reaction (no hard assertion — just verify no unhandled JS exception was thrown by checking page.on('pageerror') count remains 0).

    Add a `test.use({ storageState: authFile })` at the top of the file.
    Collect JS errors with: `const jsErrors: string[] = []; page.on('pageerror', e => jsErrors.push(e.message));`
    Final assertion: `expect(jsErrors.filter(e => !e.includes('ResizeObserver'))).toHaveLength(0)`.
  </action>
  <verify>
    <automated>test -f /Users/tommylambert/Documents/Claude_Folder/teable/apps/nextjs-app/e2e/agent-chat.spec.ts && echo "spec exists"</automated>
  </verify>
  <done>agent-chat.spec.ts exists, imports authFile, tests message send + response + chip click, asserts zero JS errors</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test browser → NestJS agent API | SSE streaming; runs only over localhost |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05-01 | Information Disclosure | agent chat response in test logs | accept | Test runs locally only; logs not shipped |
| T-09-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
Run after both tasks complete (requires dev server at :3001):

```bash
cd /Users/tommylambert/Documents/Claude_Folder/teable/apps/nextjs-app
E2E_WEBSERVER_MODE=DEV pnpm exec playwright test e2e/agent-chat.spec.ts --project="Desktop Chrome" --reporter=list
```
</verification>

<success_criteria>
- agent-chat.spec.ts exists and is syntactically valid TypeScript
- Test navigates to nXtFlow > Contacts > opens chat panel > sends message
- Response element becomes non-empty within 15 s timeout
- Zero unfiltered JS errors on the page during the flow
- AgentModule is registered in its parent module with no obvious injection errors
</success_criteria>

<output>
Create `.planning/phases/09-ui-feature-testing/09-05-SUMMARY.md` when done
</output>
