---
phase: 09-ui-feature-testing
plan: 06
type: execute
wave: 3
depends_on:
  - 09-00
files_modified:
  - apps/nextjs-app/e2e/automations.spec.ts
autonomous: true
requirements:
  - PHASE9-AUTOMATIONS
must_haves:
  truths:
    - automation list loads without a 404 or 500 API error
    - automation panel opens when an automation is clicked
    - automation panel shows a trigger configuration section and a Run button
  artifacts:
    - path: apps/nextjs-app/e2e/automations.spec.ts
      provides: Playwright spec for Automations panel flow
      contains: "Automations"
  key_links:
    - from: apps/nextjs-app/e2e/automations.spec.ts
      to: apps/nestjs-backend/src/features/
      via: GET /api/base/:baseId/automation endpoint
      pattern: "automation"
---

<objective>
Test the Automations feature introduced in Phase 4. Navigates to the nXtFlow base, opens the Automations section, opens the "Test" automation, and verifies the panel renders with a trigger and a Run button.

Purpose: Confirm the automation list API and panel UI are wired correctly without 404/500 errors.

Output: apps/nextjs-app/e2e/automations.spec.ts — a passing Playwright spec.
</objective>

<execution_context>
@/Users/tommylambert/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tommylambert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-ui-feature-testing/09-00-PLAN.md
@apps/nextjs-app/e2e/fixtures/auth.ts
</context>

<tasks>

<task id="09-06-01" type="auto">
  <name>Task 1: Locate automation routes and fix 404-producing bugs</name>
  <files>apps/nestjs-backend/src/features/</files>
  <action>
    Search for the automation controller/router in apps/nestjs-backend/src/features/ by running:
      grep -r "automation\|Automation" apps/nestjs-backend/src/features --include="*.ts" -l

    Read the controller file(s) found. Check for:
    - Route path mismatches (e.g., route registered as /automations but client calls /automation).
    - Missing module registration — if an AutomationModule exists but is not imported in AppModule or a feature module, add the import.
    - Missing @ApiTags or guard that blocks unauthenticated access — if a guard is blocking the test user, note it; the storageState session should carry the cookie so this should not be an issue.

    If no automation controller exists at all, create a stub controller that returns `{ data: [], total: 0 }` for GET /api/base/:baseId/automation so the UI does not crash with a 404. Add it to the nearest feature module.

    Apply only minimal fixes. Document any deeper issues as inline comments rather than attempting large refactors.
  </action>
  <verify>
    <automated>grep -r "AutomationModule\|AutomationController\|automation" /Users/tommylambert/Documents/Claude_Folder/teable/apps/nestjs-backend/src/features --include="*.ts" -l 2>/dev/null | head -10</automated>
  </verify>
  <done>Automation controller exists (real or stub) and is registered in a NestJS module; no obvious 404-producing route mismatch</done>
</task>

<task id="09-06-02" type="auto">
  <name>Task 2: Write automations Playwright spec</name>
  <files>apps/nextjs-app/e2e/automations.spec.ts</files>
  <action>
    Create apps/nextjs-app/e2e/automations.spec.ts using storageState from e2e/fixtures/auth.ts.

    Add `test.use({ storageState: authFile })` at the top.

    Intercept API errors: collect 4xx/5xx responses with:
      const apiErrors: { url: string; status: number }[] = [];
      page.on('response', r => { if (r.status() >= 400) apiErrors.push({ url: r.url(), status: r.status() }); });

    The spec must contain one describe block "Automations" with the test "automation list loads and panel opens":

    1. Navigate to http://localhost:3001.
    2. Click the "nXtFlow" base in the left sidebar. Skip with test.skip if not found.
    3. Look for "Automations" in the base top navigation or left sidebar (try: page.getByRole('tab', { name: /automations/i }) or page.getByText('Automations').first()). Click it.
    4. Wait for the automation list panel to appear (timeout 8 000 ms). Assert that no API response had status 404 or 500 for a URL containing "automation".
    5. Click the "Test" automation entry in the list (page.getByText('Test').first() scoped to the automation list container). If not found, click the first automation in the list instead.
    6. Wait for the automation detail panel to open. Assert:
       - A trigger section is visible: page.getByText(/trigger|déclencheur/i) is visible within 5 000 ms.
       - A Run button is visible: page.getByRole('button', { name: /run|exécuter/i }) is visible within 5 000 ms.

    Final assertions:
      expect(apiErrors.filter(e => e.url.includes('automation'))).toHaveLength(0)
  </action>
  <verify>
    <automated>test -f /Users/tommylambert/Documents/Claude_Folder/teable/apps/nextjs-app/e2e/automations.spec.ts && echo "spec exists"</automated>
  </verify>
  <done>automations.spec.ts exists, intercepts API errors, verifies automation list + panel + trigger + Run button without 404/500 errors</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test browser → NestJS automation API | REST; runs only over localhost |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06-01 | Information Disclosure | automation list response in test logs | accept | Local dev only |
| T-09-06-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
Run after both tasks (requires dev server at :3001):

```bash
cd /Users/tommylambert/Documents/Claude_Folder/teable/apps/nextjs-app
E2E_WEBSERVER_MODE=DEV pnpm exec playwright test e2e/automations.spec.ts --project="Desktop Chrome" --reporter=list
```
</verification>

<success_criteria>
- automations.spec.ts exists and is syntactically valid TypeScript
- Test navigates to nXtFlow > Automations > clicks "Test" automation
- Trigger section and Run button are visible in the panel
- No 404 or 500 responses for automation API calls
</success_criteria>

<output>
Create `.planning/phases/09-ui-feature-testing/09-06-SUMMARY.md` when done
</output>
