---
phase: 09-ui-feature-testing
plan: 01
type: execute
wave: 1
depends_on:
  - 09-00
files_modified:
  - apps/nextjs-app/e2e/pages/features/grid-view.spec.ts
autonomous: true
requirements:
  - PHASE9-GRID
must_haves:
  truths:
    - test opens E2E-Test-Base and creates a table
    - test adds text, number, single-select, and date fields
    - test creates 3 records, edits one, deletes one
    - all Playwright assertions pass with no console errors
    - any source bugs discovered are fixed before plan completes
  artifacts:
    - path: apps/nextjs-app/e2e/pages/features/grid-view.spec.ts
      provides: full grid view E2E test suite
  key_links:
    - from: apps/nextjs-app/e2e/pages/features/grid-view.spec.ts
      to: apps/nextjs-app/e2e/fixtures/auth.ts
      via: storageState import
      pattern: "authFile"
    - from: apps/nextjs-app/e2e/pages/features/grid-view.spec.ts
      to: apps/nextjs-app/e2e/fixtures/testBase.ts
      via: getOrCreateTestBase helper
      pattern: "getOrCreateTestBase"
---

<objective>
Write and pass a Playwright E2E test for Grid view: create a table, add four field types, manipulate records, and fix any bugs found in the source.

Purpose: Grid is the core data-entry surface — if it is broken, every other view test is meaningless. This plan validates the entire field-creation and record-CRUD pipeline (frontend + NestJS API).

Output: apps/nextjs-app/e2e/pages/features/grid-view.spec.ts with passing assertions.
</objective>

<execution_context>
@/Users/tommylambert/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tommylambert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-ui-feature-testing/09-RESEARCH.md
@apps/nextjs-app/e2e/fixtures/auth.ts
@apps/nextjs-app/e2e/fixtures/testBase.ts

Known risk files (modified on this branch — read before writing assertions):
- apps/nestjs-backend/src/features/record/query-builder/field-select-visitor.ts
- apps/nestjs-backend/src/features/record/query-builder/field-cte-visitor.ts
- apps/nextjs-app/src/features/app/blocks/share/view/component/grid/GridViewBase.tsx
- apps/nestjs-backend/src/features/table/open-api/table-open-api-v2.mapper.ts
</context>

<tasks>

<task id="09-01-01" type="auto">
  <name>Task 1: Write grid-view.spec.ts with field and record tests</name>
  <files>apps/nextjs-app/e2e/pages/features/grid-view.spec.ts</files>
  <action>
    Create apps/nextjs-app/e2e/pages/features/grid-view.spec.ts. Use `storageState: authFile` (from e2e/fixtures/auth) on the test.use() call at the top of the file.

    Structure with a single `test.describe('Grid View', () => { ... })` block.

    test.beforeAll: call getOrCreateTestBase(page) to ensure E2E-Test-Base exists.

    Test "create a table": 
    - Navigate into E2E-Test-Base 
    - Click the "Add table" button (getByRole or data-testid)
    - Type a table name "GridTest-Table" and confirm
    - Assert the table tab/title "GridTest-Table" is visible

    Test "add fields — text, number, single-select, date":
    - For each field type: click the "+" column header button to add a field, select the field type from the dropdown, enter a name ("TextField", "NumberField", "StatusField", "DueDateField"), save
    - Assert each field header is visible in the grid

    Test "create 3 records":
    - Click the "+" row or "Add record" button three times (or use keyboard shortcut if available)
    - For record 1: click the text cell, type "Alpha record", press Tab/Enter to confirm
    - For record 2: type "Beta record"
    - For record 3: type "Gamma record"
    - Assert all three row values are visible

    Test "edit a record":
    - Double-click or click-to-expand the first record ("Alpha record")
    - Change the text value to "Alpha record — edited"
    - Close the expand panel or press Enter
    - Assert "Alpha record — edited" is visible in the grid

    Test "delete a record":
    - Right-click "Beta record" row OR select it via checkbox and click the delete option
    - Confirm the deletion dialog if one appears
    - Assert "Beta record" is NO LONGER visible
    - Assert 2 rows remain (Alpha edited + Gamma)

    Console error guard: attach a page.on('console', ...) listener in beforeAll that collects error-level messages, and assert in afterAll that no console.error messages were logged (exclude known-harmless React HMR messages).

    Selectors guidance: prefer data-testid attributes if present in the codebase; fall back to role-based selectors. If a selector is not obvious, read the relevant component file to find the testid.

    Do NOT use arbitrary sleep() calls. Use page.waitForSelector(), page.waitForURL(), or expect(locator).toBeVisible() with an explicit timeout.
  </action>
  <verify>
    <automated>test -f /Users/tommylambert/Documents/Claude_Folder/teable/apps/nextjs-app/e2e/pages/features/grid-view.spec.ts && echo "spec exists"</automated>
  </verify>
  <done>grid-view.spec.ts exists with describe block containing all 5 test cases</done>
</task>

<task id="09-01-02" type="auto">
  <name>Task 2: Run the spec and fix source bugs until green</name>
  <files>
    apps/nextjs-app/e2e/pages/features/grid-view.spec.ts
    apps/nestjs-backend/src/features/record/query-builder/field-select-visitor.ts
    apps/nestjs-backend/src/features/record/query-builder/field-cte-visitor.ts
    apps/nestjs-backend/src/features/record/query-builder/field-formatting-visitor.ts
    apps/nextjs-app/src/features/app/blocks/share/view/component/grid/GridViewBase.tsx
    apps/nestjs-backend/src/features/table/open-api/table-open-api-v2.mapper.ts
  </files>
  <action>
    Run the spec against the live dev server:

    ```
    E2E_WEBSERVER_MODE=DEV pnpm --filter @teable/nextjs-app exec playwright test e2e/pages/features/grid-view.spec.ts --project="Desktop Chrome"
    ```

    For each failure:
    1. Read the Playwright error trace (HTML report at e2e/.out/reports/html) and/or the console output
    2. Identify whether the failure is in the test selector/logic OR in the source code
    3. If the test selector is wrong: fix the selector in grid-view.spec.ts
    4. If the source code is broken: read the failing backend/frontend file, identify the regression introduced by the refactor/architecture-deep-fix branch, and fix it

    Priority source files to check on failures (from research known-risks):
    - field-select-visitor.ts, field-cte-visitor.ts, field-formatting-visitor.ts: check that field type handling is still correct after refactor
    - GridViewBase.tsx: check that column/row rendering is not broken by the shared view change
    - table-open-api-v2.mapper.ts: check that table creation response shape matches what the UI expects

    Re-run after each fix until `playwright test e2e/pages/features/grid-view.spec.ts` exits 0.

    If a bug cannot be fixed within the scope of this plan (e.g., requires a database migration or affects multiple unrelated subsystems), document it in a BUGS.md section of the plan SUMMARY and skip that specific assertion with `test.skip()` and a comment explaining the blocker.
  </action>
  <verify>
    <automated>E2E_WEBSERVER_MODE=DEV pnpm --filter @teable/nextjs-app exec playwright test e2e/pages/features/grid-view.spec.ts --project="Desktop Chrome" --reporter=list</automated>
  </verify>
  <done>All grid-view.spec.ts tests pass (or skipped with documented reason). No unhandled source regressions remain in the files listed above.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → NestJS API | Record CRUD calls go through the NestJS REST API; test uses admin session |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-01 | Tampering | record delete endpoint | accept | Test-only environment; admin auth required |
| T-09-01-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
```bash
E2E_WEBSERVER_MODE=DEV pnpm --filter @teable/nextjs-app exec playwright test e2e/pages/features/grid-view.spec.ts --project="Desktop Chrome"
```
Expected: 5 tests pass, exit code 0.
</verification>

<success_criteria>
- grid-view.spec.ts exists with 5 test cases covering table creation, 4 field types, record create/edit/delete
- All tests pass (or skipped with written justification) against live dev server at :3001
- No console.error messages during test run
- Any source file fixes committed with descriptive messages
</success_criteria>

<output>
Create `.planning/phases/09-ui-feature-testing/09-01-SUMMARY.md` when done
</output>
