---
phase: 09-ui-feature-testing
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - apps/nextjs-app/e2e/pages/features/form-view.spec.ts
  - apps/nextjs-app/e2e/pages/features/gallery-view.spec.ts
autonomous: true
requirements:
  - PHASE9-FORM
  - PHASE9-GALLERY
must_haves:
  truths:
    - form view test submits a form and verifies the record appears in grid view
    - gallery view test creates the view and verifies cards render for existing records
    - both spec files exit 0 on playwright test run
  artifacts:
    - path: apps/nextjs-app/e2e/pages/features/form-view.spec.ts
      provides: form view E2E test suite
    - path: apps/nextjs-app/e2e/pages/features/gallery-view.spec.ts
      provides: gallery view E2E test suite
  key_links:
    - from: apps/nextjs-app/e2e/pages/features/form-view.spec.ts
      to: apps/nextjs-app/e2e/fixtures/auth.ts
      via: storageState import
      pattern: "authFile"
    - from: apps/nextjs-app/e2e/pages/features/gallery-view.spec.ts
      to: apps/nextjs-app/e2e/fixtures/auth.ts
      via: storageState import
      pattern: "authFile"
---

<objective>
Write and pass Playwright E2E tests for Form view (create, fill, submit, verify record in grid) and Gallery view (create view, verify card rendering).

Purpose: Form and Gallery are alternative view types built on the same NestJS view infrastructure. Testing both in the same wave (parallel to Grid) exercises view-type registration and the record-creation path from a non-grid surface.

Output: form-view.spec.ts and gallery-view.spec.ts with passing assertions.
</objective>

<execution_context>
@/Users/tommylambert/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tommylambert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-ui-feature-testing/09-RESEARCH.md
@apps/nextjs-app/e2e/fixtures/auth.ts
@apps/nextjs-app/e2e/fixtures/testBase.ts
</context>

<tasks>

<task id="09-02-01" type="auto">
  <name>Task 1: Write and pass form-view.spec.ts</name>
  <files>apps/nextjs-app/e2e/pages/features/form-view.spec.ts</files>
  <action>
    Create apps/nextjs-app/e2e/pages/features/form-view.spec.ts.

    Use `test.use({ storageState: authFile })` at the top (import authFile from '../../fixtures/auth').

    test.beforeAll: call getOrCreateTestBase to ensure E2E-Test-Base exists. Reuse the "GridTest-Table" created by Plan 09-01, or create a fresh table "FormTest-Table" with at least a text field "Title" if it does not exist.

    Test "create form view":
    - Navigate to E2E-Test-Base and open the target table
    - Click the "Add view" button (the + icon in the view switcher sidebar or tab bar)
    - Select "Form" from the view type list
    - Name the view "Test Form View" and confirm
    - Assert the form builder UI is visible (form layout with draggable fields, not a grid)

    Test "fill and submit form":
    - In the form view, click "Share form" or navigate to the public form URL (check for a share button or preview button that opens the form in submission mode)
    - If a separate share URL is generated: navigate to it in a new tab using `page.goto(shareUrl)` or `context.newPage()`
    - If the form is submitted directly in the builder: switch to preview mode
    - Fill in the Title field with "Form-Submitted-Record"
    - Click the Submit button
    - Assert a success message appears ("Record submitted", "Thank you", or similar)

    Test "submitted record appears in grid":
    - Navigate back to the table's default Grid view
    - Assert "Form-Submitted-Record" is visible in the record list
    - Assert the record count increased by 1 compared to before the form submission

    After writing the spec, run it:
    ```
    E2E_WEBSERVER_MODE=DEV pnpm --filter @teable/nextjs-app exec playwright test e2e/pages/features/form-view.spec.ts --project="Desktop Chrome"
    ```
    Fix any selector issues or source bugs discovered. Re-run until exit 0. Document skipped tests with test.skip() + comment if a blocker is found.
  </action>
  <verify>
    <automated>test -f /Users/tommylambert/Documents/Claude_Folder/teable/apps/nextjs-app/e2e/pages/features/form-view.spec.ts && echo "form spec exists"</automated>
  </verify>
  <done>form-view.spec.ts passes all assertions: form view created, form submitted, record visible in grid view</done>
</task>

<task id="09-02-02" type="auto">
  <name>Task 2: Write and pass gallery-view.spec.ts</name>
  <files>apps/nextjs-app/e2e/pages/features/gallery-view.spec.ts</files>
  <action>
    Create apps/nextjs-app/e2e/pages/features/gallery-view.spec.ts.

    Use `test.use({ storageState: authFile })` at the top.

    Reuse E2E-Test-Base and "GridTest-Table" (which should have 2 records from Plan 09-01 after the delete test: "Alpha record — edited" and "Gamma record"). If the table has fewer than 2 records, add records in beforeAll before switching views.

    Test "create gallery view":
    - Navigate to E2E-Test-Base and open "GridTest-Table"
    - Click the "Add view" button
    - Select "Gallery" from the view type list
    - Name it "Test Gallery View" and confirm
    - Assert the gallery view is active (URL changes or view label is selected)

    Test "gallery cards render":
    - Assert at least 2 gallery cards are visible (use a CSS selector for the gallery card container — check the app source for the component's class or data-testid)
    - Assert that the text content "Alpha record" (or the edited version) is visible within a card
    - Assert no JavaScript error appears in the console (use the console error listener pattern from 09-01)

    Test "gallery card shows field values":
    - Click one card to expand it (if expand is supported)
    - OR assert that at least the primary field value is rendered on the card face without needing expansion
    - Assert the record data matches what was entered in the grid test

    After writing the spec, run it:
    ```
    E2E_WEBSERVER_MODE=DEV pnpm --filter @teable/nextjs-app exec playwright test e2e/pages/features/gallery-view.spec.ts --project="Desktop Chrome"
    ```
    Fix any selector issues or source bugs. Re-run until exit 0.
  </action>
  <verify>
    <automated>test -f /Users/tommylambert/Documents/Claude_Folder/teable/apps/nextjs-app/e2e/pages/features/gallery-view.spec.ts && echo "gallery spec exists"</automated>
  </verify>
  <done>gallery-view.spec.ts passes: gallery view created, cards visible, primary field values rendered</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| form submission → API | Public form submission may not require auth; test validates record creation path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-01 | Elevation of Privilege | public form endpoint | accept | Test environment only; form is linked to a test base under admin control |
| T-09-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
```bash
E2E_WEBSERVER_MODE=DEV pnpm --filter @teable/nextjs-app exec playwright test e2e/pages/features/form-view.spec.ts e2e/pages/features/gallery-view.spec.ts --project="Desktop Chrome"
```
Expected: all tests pass, exit code 0.
</verification>

<success_criteria>
- form-view.spec.ts: form view created, form submitted, record visible in grid
- gallery-view.spec.ts: gallery view created, at least 2 cards visible, primary field values rendered
- Both files exit 0 on test run against live dev server at :3001
</success_criteria>

<output>
Create `.planning/phases/09-ui-feature-testing/09-02-SUMMARY.md` when done
</output>
