---
phase: 09-ui-feature-testing
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/nextjs-app/playwright.config.ts
  - apps/nextjs-app/e2e/fixtures/auth.ts
  - apps/nextjs-app/e2e/fixtures/testBase.ts
autonomous: true
requirements:
  - PHASE9-SETUP
must_haves:
  truths:
    - playwright baseURL points to localhost:3001
    - auth fixture logs in as admin and saves storageState to e2e/fixtures/.auth.json
    - testBase fixture creates a base named E2E-Test-Base via UI
    - pnpm --filter @teable/nextjs-app exec playwright test e2e/fixtures/ exits 0
  artifacts:
    - path: apps/nextjs-app/playwright.config.ts
      provides: corrected baseURL and webServerPort pointing to 3001
      contains: "localhost:3001"
    - path: apps/nextjs-app/e2e/fixtures/auth.ts
      provides: admin storageState fixture
    - path: apps/nextjs-app/e2e/fixtures/testBase.ts
      provides: E2E-Test-Base creation fixture
  key_links:
    - from: apps/nextjs-app/e2e/fixtures/auth.ts
      to: apps/nextjs-app/e2e/fixtures/.auth.json
      via: page.context().storageState()
      pattern: "storageState"
    - from: apps/nextjs-app/e2e/fixtures/testBase.ts
      to: apps/nextjs-app/e2e/fixtures/auth.ts
      via: storageState option in browser context
      pattern: "storageState.*auth"
---

<objective>
Fix the Playwright port mismatch and create the shared auth + test-base fixtures that every downstream plan depends on.

Purpose: All feature tests in Waves 1–2 require (1) a correct baseURL and (2) an authenticated browser session. Without these two prerequisites every test file will fail immediately on a login redirect or a 404.

Output: Corrected playwright.config.ts, auth.ts fixture that saves admin storageState, testBase.ts fixture that creates "E2E-Test-Base" via UI.
</objective>

<execution_context>
@/Users/tommylambert/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tommylambert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-ui-feature-testing/09-RESEARCH.md
@apps/nextjs-app/playwright.config.ts
</context>

<tasks>

<task id="09-00-01" type="auto">
  <name>Task 1: Fix Playwright baseURL and webServerPort</name>
  <files>apps/nextjs-app/playwright.config.ts</files>
  <action>
    Two changes are required in playwright.config.ts:

    1. Change the const `webServerPort = 3000` to `webServerPort = 3001`. This affects webServer.port, the DEV/START/BUILD_AND_START commands, and indirectly the baseURL.

    2. Add `baseURL: 'http://localhost:3001'` to the top-level `use:` block (the one that already contains `trace: 'retry-with-trace'` and `contextOptions`). This ensures page.goto('/some-path') resolves against the correct origin regardless of E2E_WEBSERVER_MODE.

    Do not change anything else — reporter paths, timeout values, project definitions, and env loading logic are correct as-is.
  </action>
  <verify>
    <automated>grep -c 'localhost:3001' /Users/tommylambert/Documents/Claude_Folder/teable/apps/nextjs-app/playwright.config.ts</automated>
  </verify>
  <done>playwright.config.ts contains at least one reference to localhost:3001 and the webServerPort const equals 3001</done>
</task>

<task id="09-00-02" type="auto">
  <name>Task 2: Create admin auth storageState fixture</name>
  <files>apps/nextjs-app/e2e/fixtures/auth.ts</files>
  <action>
    Create apps/nextjs-app/e2e/fixtures/auth.ts as a standalone Playwright setup script (not a test spec — it uses the base `chromium` from `@playwright/test` directly).

    The script must:
    - Launch a browser in headless mode with baseURL http://localhost:3001
    - Navigate to /auth/login (or the app's actual login route — check by navigating to / and following the redirect)
    - Fill the email field with tommy.lambert@converteo.com and the password field with the value from process.env.E2E_ADMIN_PASSWORD (default fallback: 'admin123' for local dev)
    - Click the login/submit button
    - Wait for navigation away from the login page (waitForURL that does NOT match /auth/login or /login)
    - Save the context's storageState to apps/nextjs-app/e2e/fixtures/.auth.json via `await context.storageState({ path: authFile })`
    - Close the browser

    Export a const `authFile = path.join(__dirname, '.auth.json')` so other fixtures can import it.

    Use selectors that are robust: prefer `page.getByLabel('Email')`, `page.getByLabel('Password')`, `page.getByRole('button', { name: /sign in|login/i })`. Fall back to `input[type=email]`, `input[type=password]`, `button[type=submit]` if labels are not present.

    This file runs via `playwright test e2e/fixtures/auth.ts` as a setup project — it is also importable as a module for the authFile path constant.
  </action>
  <verify>
    <automated>test -f /Users/tommylambert/Documents/Claude_Folder/teable/apps/nextjs-app/e2e/fixtures/auth.ts && echo "file exists"</automated>
  </verify>
  <done>auth.ts exists, exports authFile const, contains storageState save logic, and uses tommy.lambert@converteo.com as the email</done>
</task>

<task id="09-00-03" type="auto">
  <name>Task 3: Create E2E-Test-Base fixture</name>
  <files>apps/nextjs-app/e2e/fixtures/testBase.ts</files>
  <action>
    Create apps/nextjs-app/e2e/fixtures/testBase.ts as a Playwright test file that:

    1. Uses `storageState: authFile` (imported from ./auth) so it runs as the authenticated admin.
    2. Navigates to the spaces/home page (likely / or /space or /dashboard — follow the app structure after login).
    3. Creates a new base named "E2E-Test-Base":
       - Click the "New base" or "Create base" button (prefer getByRole('button', { name: /new base|create base/i }) or look for a + icon in the sidebar)
       - Fill in the base name field with "E2E-Test-Base"
       - Confirm/submit
       - Wait for the base to appear in the sidebar or navigate into it
    4. Stores the created base's ID or URL for downstream test files. Export a const `TEST_BASE_NAME = 'E2E-Test-Base'` and a helper function `getOrCreateTestBase(page)` that checks if "E2E-Test-Base" already exists (to avoid duplicates on re-runs) and creates it only if absent.

    The file must also export a `test` fixture extension that other spec files can import:
    ```
    export { test, expect } from '@playwright/test';
    ```
    (This is a thin re-export so Wave 1 specs can import from './fixtures/testBase' and get both the base setup and the Playwright test harness.)

    Important: wrap the base-creation logic in a `test.beforeAll` or export it as a callable async function — do NOT create a free-standing `test()` block that would be picked up by the test runner as a real test case, unless it is named clearly (e.g., "Setup: create E2E-Test-Base") and skipped when the base already exists.
  </action>
  <verify>
    <automated>test -f /Users/tommylambert/Documents/Claude_Folder/teable/apps/nextjs-app/e2e/fixtures/testBase.ts && echo "file exists"</automated>
  </verify>
  <done>testBase.ts exists, imports authFile, exports TEST_BASE_NAME and getOrCreateTestBase helper, re-exports test/expect from @playwright/test</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test env → app | Playwright sends credentials over localhost HTTP; no external exposure |
| storageState file | .auth.json written to disk contains session tokens — must not be committed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-00-01 | Information Disclosure | e2e/fixtures/.auth.json | mitigate | Add .auth.json to .gitignore before committing |
| T-09-00-02 | Spoofing | admin credentials in env | accept | Local dev only; no secrets hardcoded in source — uses E2E_ADMIN_PASSWORD env var |
| T-09-00-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
Run after all three tasks:

```bash
cd /Users/tommylambert/Documents/Claude_Folder/teable/apps/nextjs-app
grep 'localhost:3001' playwright.config.ts
test -f e2e/fixtures/auth.ts && echo "auth.ts OK"
test -f e2e/fixtures/testBase.ts && echo "testBase.ts OK"
```

Full fixture run (requires dev server running at :3001):
```bash
E2E_WEBSERVER_MODE=DEV pnpm --filter @teable/nextjs-app exec playwright test e2e/fixtures/ --project="Desktop Chrome"
```
</verification>

<success_criteria>
- playwright.config.ts has webServerPort = 3001 and baseURL: 'http://localhost:3001' in the use block
- e2e/fixtures/auth.ts exists, logs in as tommy.lambert@converteo.com, saves storageState to .auth.json
- e2e/fixtures/testBase.ts exists, uses stored auth, creates E2E-Test-Base, exports test/expect
- .auth.json added to .gitignore
- pnpm --filter @teable/nextjs-app exec playwright test e2e/fixtures/ exits 0 (with live server)
</success_criteria>

<output>
Create `.planning/phases/09-ui-feature-testing/09-00-SUMMARY.md` when done
</output>
