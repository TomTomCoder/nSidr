---
phase: 09-ui-feature-testing
plan: 09
type: execute
wave: 2
depends_on:
  - 09-00
files_modified:
  - apps/nextjs-app/e2e/pages/features/authority-matrix.spec.ts
autonomous: true
requirements:
  - PHASE9-AUTHORITY-MATRIX
must_haves:
  truths:
    - authority matrix page renders with role columns
    - permission toggle saves without error
    - unauthorized access is blocked
---

# Plan 09-09: Authority Matrix UI Tests (Phase 1)

## Objective
Test the Authority Matrix feature (Phase 1) via Playwright. Navigate to space settings, verify the matrix table renders with role columns, test permission toggles, and verify unauthorized access is blocked.

## Tasks

<task id="09-09-01" type="execute">
  <title>Create authority-matrix Playwright spec</title>
  <read_first>
    - apps/nextjs-app/e2e/fixtures/auth.ts
    - apps/nextjs-app/playwright.config.ts
    - apps/nextjs-app/src/features/app/blocks/space/space-side-bar/SpaceInnerSideBar.tsx
  </read_first>
  <action>
    Create apps/nextjs-app/e2e/pages/features/authority-matrix.spec.ts:
    1. Use storageState from auth fixture
    2. Navigate to /space/{spaceId} — extract spaceId from URL after login
    3. Click "Paramètres de l'espace" in sidebar
    4. Find "Matrice d'autorité" section or link and click it
    5. Assert: table element with ≥4 columns (Owner/Creator/Editor/Commenter/Viewer) is visible
    6. Click one permission checkbox and assert no network error (check response.status() !== 500)
    7. Test unauthorized access: use a fresh browser context with no auth, navigate to the authority matrix URL, assert redirect to login or 403 response
  </action>
  <acceptance_criteria>
    - authority-matrix.spec.ts exists at the path above
    - Spec contains ≥3 test cases (renders, toggle, unauthorized)
    - `grep "Matrice\|authority\|matrix" apps/nextjs-app/e2e/pages/features/authority-matrix.spec.ts` exits 0
  </acceptance_criteria>
</task>

<task id="09-09-02" type="execute">
  <title>Run spec and fix any authority matrix bugs</title>
  <read_first>
    - apps/nextjs-app/e2e/pages/features/authority-matrix.spec.ts
    - apps/nestjs-backend/src/features/base-node/base-node.service.ts
  </read_first>
  <action>
    Run: `cd apps/nextjs-app && pnpm exec playwright test e2e/pages/features/authority-matrix.spec.ts --reporter=line`
    If tests fail:
    - 404 on settings page → check sidebar navigation selector in SpaceInnerSideBar.tsx
    - 500 on toggle → check authority matrix API endpoint in base-node.service.ts
    Fix source files, re-run until spec exits 0
  </action>
  <acceptance_criteria>
    - `cd apps/nextjs-app && pnpm exec playwright test e2e/pages/features/authority-matrix.spec.ts` exits 0
    - No test.skip() masking failures
  </acceptance_criteria>
</task>

## Verification
All authority matrix Playwright tests pass. Matrix table renders with role columns. Permission toggle completes without server error. Unauthorized access is blocked.
