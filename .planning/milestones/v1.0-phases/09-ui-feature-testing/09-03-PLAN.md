---
phase: 09-ui-feature-testing
plan: 03
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - apps/nextjs-app/e2e/pages/features/kanban-view.spec.ts
  - apps/nextjs-app/e2e/pages/features/calendar-view.spec.ts
autonomous: true
requirements:
  - PHASE9-KANBAN
  - PHASE9-CALENDAR
must_haves:
  truths:
    - kanban test creates view on a single-select field, drags a card to another column, verifies state persists after reload
    - calendar test creates view on a date field, verifies events render in the calendar grid
    - both spec files exit 0 on playwright test run
  artifacts:
    - path: apps/nextjs-app/e2e/pages/features/kanban-view.spec.ts
      provides: kanban view E2E test suite including drag-and-drop
    - path: apps/nextjs-app/e2e/pages/features/calendar-view.spec.ts
      provides: calendar view E2E test suite
  key_links:
    - from: apps/nextjs-app/e2e/pages/features/kanban-view.spec.ts
      to: apps/nextjs-app/e2e/fixtures/auth.ts
      via: storageState import
      pattern: "authFile"
    - from: apps/nextjs-app/e2e/pages/features/calendar-view.spec.ts
      to: apps/nextjs-app/e2e/fixtures/auth.ts
      via: storageState import
      pattern: "authFile"
---

<objective>
Write and pass Playwright E2E tests for Kanban view (drag card between columns, persist state) and Calendar view (events render on date field).

Purpose: Kanban and Calendar depend on field-type configuration (single-select and date respectively) that was set up in Plan 09-01. Wave 2 is the right time to test these view types after the Grid baseline is confirmed green.

Output: kanban-view.spec.ts and calendar-view.spec.ts with passing assertions.
</objective>

<execution_context>
@/Users/tommylambert/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tommylambert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-ui-feature-testing/09-RESEARCH.md
@apps/nextjs-app/e2e/fixtures/auth.ts
@apps/nextjs-app/e2e/fixtures/testBase.ts
</context>

<tasks>

<task id="09-03-01" type="auto">
  <name>Task 1: Write and pass kanban-view.spec.ts</name>
  <files>apps/nextjs-app/e2e/pages/features/kanban-view.spec.ts</files>
  <action>
    Create apps/nextjs-app/e2e/pages/features/kanban-view.spec.ts.

    Use `test.use({ storageState: authFile })`.

    test.beforeAll:
    - Ensure E2E-Test-Base and "GridTest-Table" exist (via getOrCreateTestBase).
    - Ensure "StatusField" (the single-select field added in Plan 09-01) exists with at least 2 options. If options are missing, add them: open the field settings for "StatusField" and create options "Todo" and "Done".
    - Ensure at least 2 records exist. Set record 1's StatusField to "Todo" and record 2's to "Todo" (so both are in the same initial column and we can drag one to "Done").

    Test "create kanban view":
    - Navigate to E2E-Test-Base / GridTest-Table
    - Click "Add view" → select "Kanban"
    - When prompted to select a grouping field, select "StatusField"
    - Name the view "Test Kanban View" and confirm
    - Assert the Kanban board is visible with at least one column labeled "Todo"

    Test "drag card to another column":
    - Find the first card in the "Todo" column (locator for card title text)
    - Find the "Done" column drop target
    - Use Playwright drag-and-drop: `page.dragAndDrop(sourceSelector, targetSelector)`
      OR use mouse events: `page.mouse.move(x,y)`, `page.mouse.down()`, `page.mouse.move(targetX,targetY)`, `page.mouse.up()`
    - Wait for the drag animation to settle (waitForResponse on the record update API call or waitForTimeout(500) max)
    - Assert the card now appears in the "Done" column
    - Assert the "Todo" column has one fewer card

    Test "state persists after page reload":
    - Reload the page (`page.reload()`)
    - Assert the card that was dragged is still in the "Done" column (the record's StatusField value was persisted)

    Run the spec and fix bugs:
    ```
    E2E_WEBSERVER_MODE=DEV pnpm --filter @teable/nextjs-app exec playwright test e2e/pages/features/kanban-view.spec.ts --project="Desktop Chrome"
    ```
    Fix selector issues or source bugs discovered. Document any skips with test.skip() + comment.

    Known risk: view constants may have been changed on the refactor branch (src/features/view/constant.ts is modified). If Kanban is not available in the Add View dropdown, read that file and fix the missing ViewType registration.
  </action>
  <verify>
    <automated>test -f /Users/tommylambert/Documents/Claude_Folder/teable/apps/nextjs-app/e2e/pages/features/kanban-view.spec.ts && echo "kanban spec exists"</automated>
  </verify>
  <done>kanban-view.spec.ts passes: view created, card dragged to Done column, state persists on reload</done>
</task>

<task id="09-03-02" type="auto">
  <name>Task 2: Write and pass calendar-view.spec.ts</name>
  <files>apps/nextjs-app/e2e/pages/features/calendar-view.spec.ts</files>
  <action>
    Create apps/nextjs-app/e2e/pages/features/calendar-view.spec.ts.

    Use `test.use({ storageState: authFile })`.

    test.beforeAll:
    - Ensure E2E-Test-Base and "GridTest-Table" exist.
    - Ensure "DueDateField" (the date field added in Plan 09-01) exists.
    - Ensure at least 1 record has a value set for DueDateField. If no records have a date, set one: today's date formatted as the app expects (likely YYYY-MM-DD). Set it on record 1 ("Alpha record — edited").

    Test "create calendar view":
    - Navigate to E2E-Test-Base / GridTest-Table
    - Click "Add view" → select "Calendar"
    - When prompted to select a date field, select "DueDateField"
    - Name the view "Test Calendar View" and confirm
    - Assert the calendar grid is visible (month/week grid with date cells)

    Test "events render":
    - Navigate to the current month in the calendar (it should be the default view)
    - Assert that at least one event/card is visible in the calendar grid for the date that was set
    - Assert the event displays the record title ("Alpha record — edited" or the primary field text)

    Test "calendar navigation":
    - Click the "next month" or ">" navigation button
    - Assert the calendar header updates to show the next month
    - Click the "previous month" or "<" button to return
    - Assert the event is visible again in the original month

    Run and fix:
    ```
    E2E_WEBSERVER_MODE=DEV pnpm --filter @teable/nextjs-app exec playwright test e2e/pages/features/calendar-view.spec.ts --project="Desktop Chrome"
    ```
    Fix any issues. Re-run until exit 0.
  </action>
  <verify>
    <automated>test -f /Users/tommylambert/Documents/Claude_Folder/teable/apps/nextjs-app/e2e/pages/features/calendar-view.spec.ts && echo "calendar spec exists"</automated>
  </verify>
  <done>calendar-view.spec.ts passes: view created, events visible on correct date, navigation works</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → record update API | Drag-and-drop triggers a PATCH/PUT to NestJS — test uses admin session |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03-01 | Tampering | record field update on drag | accept | Test environment; admin auth required for all mutations |
| T-09-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
```bash
E2E_WEBSERVER_MODE=DEV pnpm --filter @teable/nextjs-app exec playwright test e2e/pages/features/kanban-view.spec.ts e2e/pages/features/calendar-view.spec.ts --project="Desktop Chrome"
```
Expected: all tests pass, exit code 0.
</verification>

<success_criteria>
- kanban-view.spec.ts: view created, card dragged to Done, state persists on reload
- calendar-view.spec.ts: view created, events visible, month navigation works
- Both files exit 0 against live dev server
- Any view/constant source fixes committed
</success_criteria>

<output>
Create `.planning/phases/09-ui-feature-testing/09-03-SUMMARY.md` when done
</output>
